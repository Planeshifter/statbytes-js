(function e(t,n,r){function s(o,u){if(!n[o]){if(!t[o]){var a=typeof require=="function"&&require;if(!u&&a)return a(o,!0);if(i)return i(o,!0);var f=new Error("Cannot find module '"+o+"'");throw f.code="MODULE_NOT_FOUND",f}var l=n[o]={exports:{}};t[o][0].call(l.exports,function(e){var n=t[o][1][e];return s(n?n:e)},l,l.exports,e,t,n,r)}return n[o].exports}var i=typeof require=="function"&&require;for(var o=0;o<r.length;o++)s(r[o]);return s})({1:[function(require,module,exports){
'use strict';

// FLOW //

/**
* FUNCTION: Flow()
*	Flow constructor.
*
* @constructor
* @returns {Flow} Flow instance
*/
function Flow() {
	if ( !(this instanceof Flow) ) {
		return new Flow();
	}
	this._value = null;
	return this;
} // end FUNCTION Flow()

/**
* METHOD: value( [value] )
*	Flow value setter and getter. If no `value` is provided, returns the flow `value`. If a `value` is provided, sets the flow `value`.
*
* @param {*} [value] - flow value
* @returns {Flow|*} Flow instance or value
*/
Flow.prototype.value = function( value ) {
	if ( !arguments.length ) {
		return this._value;
	}
	this._value = value;
	return this;
}; // end METHOD value()

/**
* METHOD: inspect()
*	Logs the current flow value to the console.
*
* @returns {Flow} Flow instance
*/
Flow.prototype.inspect = function() {
	console.log( this._value );
	return this;
}; // end METHOD inspect()


// CREATE INTERFACE //

/**
* FUNCTION: createMethod( name, func )
*	Creates a fluent method.
*
* @param {String} name - method name
* @param {Function} func - method to bind to Flow prototype
*/
function createMethod( name, func ) {
	Flow.prototype[ name ] = function() {
		var args = Array.prototype.slice.call( arguments );
		args.unshift( this._value );
		this._value = func.apply( null, args );
		return this;
	};
} // end FUNCTION createMethod()

/**
* FUNCTION: createFlow( compute )
*	Binds a fluent interface to the compute object.
*
* @param {Object} compute - object containing compute methods
* @returns {Flow} Flow constructor
*/
function createFlow( compute ) {
	var methods = Object.keys( compute ),
		method;

	methods.filter( function filter( method ) {
		return typeof compute[ method ] === 'function';
	});

	for ( var i = 0; i < methods.length; i++ ) {
		method = methods[ i ];
		createMethod( method, compute[ method ] );
	}

	return Flow;
} // end FUNCTION createFlow()


// EXPORTS //

module.exports = createFlow;

},{}],2:[function(require,module,exports){
'use strict';

// MODULES //

var createFlow = require( './flow.js' );


// COMPUTE //

var compute = {};

/**
* Utilities.
*/
compute.polyval = require( 'compute-polynomial' );
compute.find = require( 'compute-find' );
compute.dims = require( 'compute-dims' );
compute.issorted = require( 'compute-issorted' );
compute.flatten = require( 'compute-flatten' );

/**
* Array creation.
*/
compute.linspace = require( 'compute-linspace' );
compute.incrspace = require( 'compute-incrspace' );
compute.indexspace = require( 'compute-indexspace' );
compute.logspace = require( 'compute-logspace' );
compute.datespace = require( 'compute-datespace' );
compute.incrdatespace = require( 'compute-incrdatespace' );
compute.zip = require( 'compute-zip' );
compute.unzip = require( 'compute-unzip' );

/**
* Sorting and reshaping arrays.
*/
compute.reverse = require( 'compute-reverse' );
compute.shuffle = require( 'compute-shuffle' );
compute.circshift = require( 'compute-circshift' );

/**
* Special functions.
*/
compute.abs = require( 'compute-abs' );
compute.sqrt = require( 'compute-sqrt' );
compute.signum = require( 'compute-signum' );
compute.erf = require( 'compute-erf' );
compute.erfc = require( 'compute-erfc' );
compute.erfinv = require( 'compute-erfinv' );
compute.erfcinv = require( 'compute-erfcinv' );

/**
* Arithmetic.
*/
compute.add = require( 'compute-add' );
compute.subtract = require( 'compute-subtract' );
compute.multiply = require( 'compute-multiply' );
compute.divide = require( 'compute-divide' );
compute.roundn = require( 'compute-roundn' );
compute.diff = require( 'compute-diff' );

/**
* Relational Operations.
*/
compute.eq = require( 'compute-eq' );
compute.neq = require( 'compute-neq' );
compute.gt = require( 'compute-gt' );
compute.geq = require( 'compute-geq' );
compute.lt = require( 'compute-lt' );
compute.leq = require( 'compute-leq' );

/**
* Logical Operations.
*/
compute.isnumeric = require( 'compute-isnumeric' );
compute.isnan = require( 'compute-isnan' );
compute.isfinite = require( 'compute-isfinite' );
compute.isinteger = require( 'compute-isinteger' );
compute.isinf = require( 'compute-isinf' );

/**
* Trigonometry.
*/
compute.deg2rad = require( 'compute-deg2rad' );
compute.rad2deg = require( 'compute-rad2deg' );

/**
* Geometry.
*/
compute.hypot = require( 'compute-hypot' );

/**
* Sets.
*/
compute.unique = require( 'compute-unique' );

/**
* Discrete Mathematics.
*/
compute.gcd = require( 'compute-gcd' );
compute.lcm = require( 'compute-lcm' );

/**
* Linear Algebra.
*/
compute.l1norm = require( 'compute-l1norm' );
compute.l2norm = require( 'compute-l2norm' );
compute.linfnorm = require( 'compute-linfnorm' );
compute.lpnorm = require( 'compute-lpnorm' );
compute.dot = require( 'compute-dot' );
compute.cross = require( 'compute-cross' );

/**
* Statistics.
*/
compute.min = require( 'compute-min' );
compute.argmin = require( 'compute-argmin' );
compute.nanmin = require( 'compute-nanmin' );
compute.argnanmin = require( 'compute-argnanmin' );
compute.incrmin = require( 'compute-incrmin' );
compute.mmin = require( 'compute-mmin' );
compute.cmin = require( 'compute-cmin' );
compute.max = require( 'compute-max' );
compute.argmax = require( 'compute-argmax' );
compute.nanmax = require( 'compute-nanmax' );
compute.argnanmax = require( 'compute-argnanmax' );
compute.incrmax = require( 'compute-incrmax' );
compute.mmax = require( 'compute-mmax' );
compute.cmax = require( 'compute-cmax' );
compute.range = require( 'compute-range' );
compute.nanrange = require( 'compute-nanrange' );
compute.sum = require( 'compute-sum' );
compute.nansum = require( 'compute-nansum' );
compute.incrsum = require( 'compute-incrsum' );
compute.msum = require( 'compute-msum' );
compute.incrmsum = require( 'compute-incrmsum' );
compute.csum = require( 'compute-csum' );
compute.prod = require( 'compute-prod' );
compute.nanprod = require( 'compute-nanprod' );
compute.mprod = require( 'compute-mprod' );
compute.cprod = require( 'compute-cprod' );
compute.mean = require( 'compute-mean' );
compute.nanmean = require( 'compute-nanmean' );
compute.incrmean = require( 'compute-incrmean' );
compute.mmean = require( 'compute-mmean' );
compute.incrmmean = require( 'compute-incrmmean' );
compute.wmean = require( 'compute-wmean' );
compute.gmean = require( 'compute-gmean' );
compute.nangmean = require( 'compute-nangmean' );
compute.hmean = require( 'compute-hmean' );
compute.nanhmean = require( 'compute-nanhmean' );
compute.qmean = require( 'compute-qmean' );
compute.nanqmean = require( 'compute-nanqmean' );
compute.variance = require( 'compute-variance' );
compute.nanvariance = require( 'compute-nanvariance' );
compute.incrvariance = require( 'compute-incrvariance' );
compute.mvariance = require( 'compute-mvariance' );
compute.incrmvariance = require( 'compute-incrmvariance' );
compute.stdev = require( 'compute-stdev' );
compute.nanstdev = require( 'compute-nanstdev' );
compute.incrstdev = require( 'compute-incrstdev' );
compute.mstdev = require( 'compute-mstdev' );
compute.incrmstdev = require( 'compute-incrmstdev' );
compute.mode = require( 'compute-mode' );
compute.median = require( 'compute-median' );
compute.nanmedian = require( 'compute-nanmedian' );
compute.quantile = require( 'compute-quantile' );
compute.quantiles = require( 'compute-quantiles' );
compute.nanquantiles = require( 'compute-nanquantiles' );
compute.iqr = require( 'compute-iqr' );
compute.idr = require( 'compute-idr' );
compute.midrange = require( 'compute-midrange' );
compute.midhinge = require( 'compute-midhinge' );
compute.midsummary = require( 'compute-midsummary' );
compute.midmean = require( 'compute-midmean' );
compute.lmidmean = require( 'compute-lmidmean' );
compute.umidmean = require( 'compute-umidmean' );
compute.truncmean = require( 'compute-truncmean' );
compute.trimean = require( 'compute-trimean' );
compute.skewness = require( 'compute-skewness' );
compute.kurtosis = require( 'compute-kurtosis' );
compute.covariance = require( 'compute-covariance' );
compute.pcorr = require( 'compute-pcorr' );

/**
* Information theory.
*/
compute.hamdist = require( 'compute-hamming' );
compute.tversky = require( 'compute-tversky-index' );

/**
* Flow.
*/
compute.flow = createFlow( compute );


// EXPORTS //

module.exports = compute;

},{"./flow.js":1,"compute-abs":6,"compute-add":84,"compute-argmax":90,"compute-argmin":91,"compute-argnanmax":92,"compute-argnanmin":93,"compute-circshift":94,"compute-cmax":97,"compute-cmin":98,"compute-covariance":99,"compute-cprod":102,"compute-cross":107,"compute-csum":108,"compute-datespace":109,"compute-deg2rad":114,"compute-diff":115,"compute-dims":116,"compute-divide":119,"compute-dot":125,"compute-eq":128,"compute-erf":134,"compute-erfc":215,"compute-erfcinv":296,"compute-erfinv":377,"compute-find":455,"compute-flatten":460,"compute-gcd":474,"compute-geq":480,"compute-gmean":487,"compute-gt":490,"compute-hamming":497,"compute-hmean":501,"compute-hypot":502,"compute-idr":504,"compute-incrdatespace":507,"compute-incrmax":510,"compute-incrmean":511,"compute-incrmin":512,"compute-incrmmean":513,"compute-incrmstdev":517,"compute-incrmsum":521,"compute-incrmvariance":525,"compute-incrspace":529,"compute-incrstdev":531,"compute-incrsum":532,"compute-incrvariance":533,"compute-indexspace":534,"compute-iqr":539,"compute-isfinite":542,"compute-isinf":543,"compute-isinteger":544,"compute-isnan":545,"compute-isnumeric":546,"compute-issorted":547,"compute-kurtosis":548,"compute-l1norm":549,"compute-l2norm":550,"compute-lcm":553,"compute-leq":559,"compute-linfnorm":566,"compute-linspace":567,"compute-lmidmean":570,"compute-logspace":571,"compute-lpnorm":574,"compute-lt":577,"compute-max":584,"compute-mean":587,"compute-median":650,"compute-midhinge":655,"compute-midmean":658,"compute-midrange":659,"compute-midsummary":660,"compute-min":663,"compute-mmax":664,"compute-mmean":665,"compute-mmin":673,"compute-mode":674,"compute-mprod":675,"compute-mstdev":680,"compute-msum":681,"compute-multiply":689,"compute-mvariance":690,"compute-nangmean":691,"compute-nanhmean":692,"compute-nanmax":693,"compute-nanmean":694,"compute-nanmedian":695,"compute-nanmin":700,"compute-nanprod":701,"compute-nanqmean":704,"compute-nanquantiles":705,"compute-nanrange":712,"compute-nanstdev":715,"compute-nansum":716,"compute-nanvariance":719,"compute-neq":720,"compute-pcorr":723,"compute-polynomial":724,"compute-prod":733,"compute-qmean":796,"compute-quantile":797,"compute-quantiles":800,"compute-rad2deg":805,"compute-range":806,"compute-reverse":809,"compute-roundn":813,"compute-shuffle":814,"compute-signum":818,"compute-skewness":888,"compute-sqrt":892,"compute-stdev":970,"compute-subtract":971,"compute-sum":979,"compute-trimean":1042,"compute-truncmean":1046,"compute-tversky-index":1055,"compute-umidmean":1063,"compute-unique":1064,"compute-unzip":1065,"compute-variance":1073,"compute-wmean":1137,"compute-zip":1138}],3:[function(require,module,exports){
'use strict';

// FUNCTIONS

var ABS = require( './number.js' );


// ABSOLUTE VALUE //

/**
* FUNCTION: abs( out, arr, accessor )
*	Computes an element-wise absolute value using an accessor.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function abs( y, x, clbk ) {
	var len = x.length,
		v, i;
	for ( i = 0; i < len; i++ ) {
		v = clbk( x[ i ], i );
		if ( typeof v === 'number' ) {
			y[ i ] = ABS( v );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION abs()


// EXPORTS //

module.exports = abs;

},{"./number.js":8}],4:[function(require,module,exports){
'use strict';

// FUNCTIONS

var ABS = require( './number.js' );


// ABSOLUTE VALUE //

/**
* FUNCTION: abs( out, arr )
*	Computes an absolute value for each array element.
*
* @private
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function abs( out, arr ) {
	var len = arr.length,
		i;
	for ( i = 0; i < len; i++ ) {
		if ( typeof arr[ i ] === 'number' ) {
			out[ i ] = ABS( arr[ i ] );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION abs()


// EXPORTS //

module.exports = abs;

},{"./number.js":8}],5:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory,
	ABS = require( './number.js' );


// ABSOLUTE VALUE //

/**
* FUNCTION: abs( arr, path[, sep] )
*	Computes an element-wise absolute value and deep sets the input array.
*
* @param {Array} arr - input array
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function abs( x, path, sep ) {
	var len = x.length,
		opts = {},
		dget,
		dset,
		v, i;

	if ( arguments.length > 2 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		for ( i = 0; i < len; i++ ) {
			v = dget( x[ i ] );
			if ( typeof v === 'number' ) {
				dset( x[ i ], ABS( v ) );
			} else {
				dset( x[ i ], NaN );
			}
		}
	}
	return x;
} // end FUNCTION abs()


// EXPORTS //

module.exports = abs;

},{"./number.js":8,"utils-deep-get":61,"utils-deep-set":67}],6:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isnan = require( 'validate.io-nan' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var abs1 = require( './number.js' ),
	abs2 = require( './array.js' ),
	abs3 = require( './accessor.js' ),
	abs4 = require( './deepset.js' ),
	abs5 = require( './matrix.js' ),
	abs6 = require( './typedarray.js' );


// ABSOLUTE VALUE //

/**
* FUNCTION: abs( x[, opts] )
*	Computes the absolute value.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} x - input value
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} absolute value(s)
*/
function abs( x, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( isNumber( x ) || isnan( x ) ) {
		return abs1( x );
	}
	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isMatrixLike( x ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'abs()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( x.length );
			out = matrix( d, x.shape, dt );
		} else {
			out = x;
		}
		return abs5( out, x );
	}
	if ( isTypedArrayLike( x ) ) {
		if ( opts.copy === false ) {
			out = x;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'abs()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( x.length );
		}
		return abs6( out, x );
	}
	if ( isArrayLike( x ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return abs4( x, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = x;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new Error( 'abs()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( x.length );
		}
		else {
			out = new Array( x.length );
		}
		if ( opts.accessor ) {
			return abs3( out, x, opts.accessor );
		}
		return abs2( out, x );
	}
	return NaN;
} // end FUNCTION abs()


// EXPORTS //

module.exports = abs;

},{"./accessor.js":3,"./array.js":4,"./deepset.js":5,"./matrix.js":7,"./number.js":8,"./typedarray.js":9,"./validate.js":10,"compute-array-constructors":12,"dstructs-matrix":21,"validate.io-array-like":70,"validate.io-matrix-like":75,"validate.io-nan":76,"validate.io-number-primitive":77,"validate.io-typed-array-like":81}],7:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var ABS = require( './number.js' );


// ABSOLUTE VALUE //

/**
* FUNCTION: abs( y, x )
*	Computes an element-wise absolute value.
*
* @param {Matrix} y - output matrix
* @param {Matrix} x - input matrix
* @returns {Matrix} output matrix
*/
function abs( y, x ) {
	var len = x.length,
		i;
	if ( y.length !== len ) {
		throw new Error( 'abs()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	for ( i = 0; i < len; i++ ) {
		y.data[ i ] = ABS( x.data[ i ] );
	}
	return y;
} // end FUNCTION abs()


// EXPORTS //

module.exports = abs;

},{"./number.js":8}],8:[function(require,module,exports){
'use strict';

/**
* FUNCTION: abs( x )
*	Computes the absolute value of an input value.
*
* @param {Number} x - input value
* @returns {Number} absolute value
*/
function abs( x ) {
	if ( x < 0 ) {
		return -x;
	} else if ( x === 0 ) {
		// Handle -0...
		return 0;
	} else {
		return x;
	}
} // end FUNCTION abs


// EXPORTS //

module.exports = abs;

},{}],9:[function(require,module,exports){
'use strict';

// FUNCTIONS

var ABS = require( './number.js' );


// ABSOLUTE VALUE //

/**
* FUNCTION: abs( out, arr )
*	Computes an absolute value for each typed-array element.
*
* @private
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} arr - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function abs( out, arr ) {
	var len = arr.length,
		i;
	for ( i = 0; i < len; i++ ) {
		out[ i ] = ABS( arr[ i ] );
	}
	return out;
} // end FUNCTION abs()


// EXPORTS //

module.exports = abs;

},{"./number.js":8}],10:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Boolean} [options.copy] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'abs()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'abs()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'abs()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'abs()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'abs()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'abs()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":73,"validate.io-function":74,"validate.io-object":78,"validate.io-string-primitive":80}],11:[function(require,module,exports){
'use strict';

var CTORS = {
	'int8': Int8Array,
	'uint8': Uint8Array,
	'uint8_clamped': Uint8ClampedArray,
	'int16': Int16Array,
	'uint16': Uint16Array,
	'int32': Int32Array,
	'uint32': Uint32Array,
	'float32': Float32Array,
	'float64': Float64Array,
	'generic': Array
};


// EXPORTS //

module.exports = CTORS;

},{}],12:[function(require,module,exports){
'use strict';

// CTORS //

var CTORS = require( './ctors.js' );


// GET CTOR //

/**
* FUNCTION: getCtor( dtype )
*	Returns an array constructor corresponding to an input data type.
*
* @param {String} dtype - data type
* @returns {Function|Null} array constructor or null
*/
function getCtor( dtype ) {
	return CTORS[ dtype ] || null;
} // end FUNCTION getCtor()


// EXPORTS //

module.exports = getCtor;

},{"./ctors.js":11}],13:[function(require,module,exports){
'use strict';

// BASE TYPES //

var BTYPES = {
	'int8': Int8Array,
	'uint8': Uint8Array,
	'uint8_clamped': Uint8ClampedArray,
	'int16': Int16Array,
	'uint16': Uint16Array,
	'int32': Int32Array,
	'uint32': Uint32Array,
	'float32': Float32Array,
	'float64': Float64Array
};


// EXPORTS //

module.exports = BTYPES;

},{}],14:[function(require,module,exports){
'use strict';

// MATRIX //

/**
* FUNCTION: Matrix( data, shape, dtype )
*	Matrix constructor.
*
* @constructor
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} data - input typed array
* @param {String} dtype - matrix data type
* @param {Number[]} shape - matrix dimensions/shape
* @param {Number} offset - matrix offset
* @param {Number[]} strides - matrix strides
* @returns {Matrix} Matrix instance
*/
function Matrix( data, dtype, shape, offset, strides ) {
	if ( !( this instanceof Matrix ) ) {
		return new Matrix( data, dtype, shape, offset, strides );
	}
	// Underlying data type:
	Object.defineProperty( this, 'dtype', {
		'value': dtype,
		'configurable': false,
		'enumerable': true,
		'writable': false
	});

	// Matrix dimensions:
	Object.defineProperty( this, 'shape', {
		'value': shape,
		'configurable': false,
		'enumerable': true,
		'writable': false
	});

	// Matrix strides:
	Object.defineProperty( this, 'strides', {
		'value': strides,
		'configurable': false,
		'enumerable': true,
		'writable': false
	});

	// Matrix offset:
	Object.defineProperty( this, 'offset', {
		'value': offset,
		'configurable': false,
		'enumerable': true,
		'writable': true
	});

	// Number of matrix dimensions:
	Object.defineProperty( this, 'ndims', {
		'value': shape.length,
		'configurable': false,
		'enumerable': true,
		'writable': false
	});

	// Matrix length:
	Object.defineProperty( this, 'length', {
		'value': data.length,
		'configurable': false,
		'enumerable': true,
		'writable': false
	});

	// Number of bytes used by the matrix elements:
	Object.defineProperty( this, 'nbytes', {
		'value': data.byteLength,
		'configurable': false,
		'enumerable': true,
		'writable': false
	});

	// Matrix data store:
	Object.defineProperty( this, 'data', {
		'value': data,
		'configurable': false,
		'enumerable': true,
		'writable': false
	});

	return this;
} // end FUNCTION Matrix()


// METHODS //

Matrix.prototype.set = require( './set.js' );
Matrix.prototype.iset = require( './iset.js' );
Matrix.prototype.mset = require( './mset.js' );
Matrix.prototype.sset = require( './sset.js' );

Matrix.prototype.get = require( './get.js' );
Matrix.prototype.iget = require( './iget.js' );
Matrix.prototype.mget = require( './mget.js' );
Matrix.prototype.sget = require( './sget.js' );

Matrix.prototype.toString = require( './toString.js' );


// EXPORTS //

module.exports = Matrix;

},{"./get.js":17,"./iget.js":19,"./iset.js":22,"./mget.js":26,"./mset.js":28,"./set.js":36,"./sget.js":38,"./sset.js":40,"./toString.js":42}],15:[function(require,module,exports){
'use strict';

// MATRIX //

/**
* FUNCTION: Matrix( data, shape, dtype )
*	Matrix constructor.
*
* @constructor
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} data - input typed array
* @param {String} dtype - matrix data type
* @param {Number[]} shape - matrix dimensions/shape
* @param {Number} offset - matrix offset
* @param {Number[]} strides - matrix strides
* @returns {Matrix} Matrix instance
*/
function Matrix( data, dtype, shape, offset, strides ) {
	if ( !( this instanceof Matrix ) ) {
		return new Matrix( data, dtype, shape, offset, strides );
	}
	this.dtype = dtype;
	this.shape = shape;
	this.strides = strides;
	this.offset = offset;
	this.ndims = shape.length;
	this.length = data.length;
	this.nbytes = data.byteLength;
	this.data = data;
	return this;
} // end FUNCTION Matrix()


// METHODS //

Matrix.prototype.set = require( './set.raw.js' );
Matrix.prototype.iset = require( './iset.raw.js' );
Matrix.prototype.mset = require( './mset.raw.js' );
Matrix.prototype.sset = require( './sset.raw.js' );

Matrix.prototype.get = require( './get.raw.js' );
Matrix.prototype.iget = require( './iget.raw.js' );
Matrix.prototype.mget = require( './mget.raw.js' );
Matrix.prototype.sget = require( './sget.raw.js' );

Matrix.prototype.toString = require( './toString.js' );


// EXPORTS //

module.exports = Matrix;

},{"./get.raw.js":18,"./iget.raw.js":20,"./iset.raw.js":23,"./mget.raw.js":27,"./mset.raw.js":29,"./set.raw.js":37,"./sget.raw.js":39,"./sset.raw.js":41,"./toString.js":42}],16:[function(require,module,exports){
'use strict';

// DATA TYPES //

var DTYPES = [
	'int8',
	'uint8',
	'uint8_clamped',
	'int16',
	'uint16',
	'int32',
	'uint32',
	'float32',
	'float64'
];


// EXPORTS //

module.exports = DTYPES;

},{}],17:[function(require,module,exports){
'use strict';

// MODULES //

var isNonNegativeInteger = require( 'validate.io-nonnegative-integer' );


// GET //

/**
* FUNCTION: get( i, j )
*	Returns a matrix element based on the provided row and column indices.
*
* @param {Number} i - row index
* @param {Number} j - column index
* @returns {Number|Undefined} matrix element
*/
function get( i, j ) {
	/*jshint validthis:true */
	if ( !isNonNegativeInteger( i ) || !isNonNegativeInteger( j ) ) {
		throw new TypeError( 'get()::invalid input argument. Indices must be nonnegative integers. Values: `[' + i + ','+ j + ']`.' );
	}
	return this.data[ this.offset + i*this.strides[0] + j*this.strides[1] ];
} // end FUNCTION get()


// EXPORTS //

module.exports = get;

},{"validate.io-nonnegative-integer":55}],18:[function(require,module,exports){
'use strict';

/**
* FUNCTION: get( i, j )
*	Returns a matrix element based on the provided row and column indices.
*
* @param {Number} i - row index
* @param {Number} j - column index
* @returns {Number|Undefined} matrix element
*/
function get( i, j ) {
	/*jshint validthis:true */
	return this.data[ this.offset + i*this.strides[0] + j*this.strides[1] ];
} // end FUNCTION get()


// EXPORTS //

module.exports = get;

},{}],19:[function(require,module,exports){
'use strict';

// MODULES //

var isInteger = require( 'validate.io-integer-primitive' );


// IGET //

/**
* FUNCTION: iget( idx )
*	Returns a matrix element located at a specified index.
*
* @param {Number} idx - linear index
* @returns {Number|Undefined} matrix element
*/
function iget( idx ) {
	/*jshint validthis:true */
	var r, j;
	if ( !isInteger( idx ) ) {
		throw new TypeError( 'iget()::invalid input argument. Must provide a integer. Value: `' + idx + '`.' );
	}
	if ( idx < 0 ) {
		idx += this.length;
		if ( idx < 0 ) {
			return;
		}
	}
	j = idx % this.strides[ 0 ];
	r = idx - j;
	if ( this.strides[ 0 ] < 0 ) {
		r = -r;
	}
	return this.data[ this.offset + r + j*this.strides[1] ];
} // end FUNCTION iget()


// EXPORTS //

module.exports = iget;

},{"validate.io-integer-primitive":53}],20:[function(require,module,exports){
'use strict';

/**
* FUNCTION: iget( idx )
*	Returns a matrix element located at a specified index.
*
* @param {Number} idx - linear index
* @returns {Number|Undefined} matrix element
*/
function iget( idx ) {
	/*jshint validthis:true */
	var r, j;
	if ( idx < 0 ) {
		idx += this.length;
		if ( idx < 0 ) {
			return;
		}
	}
	j = idx % this.strides[ 0 ];
	r = idx - j;
	if ( this.strides[ 0 ] < 0 ) {
		r = -r;
	}
	return this.data[ this.offset + r + j*this.strides[1] ];
} // end FUNCTION iget()


// EXPORTS //

module.exports = iget;

},{}],21:[function(require,module,exports){
'use strict';

// EXPORTS //

module.exports = require( './matrix.js' );
module.exports.raw = require( './matrix.raw.js' );

},{"./matrix.js":24,"./matrix.raw.js":25}],22:[function(require,module,exports){
'use strict';

// MODULES //

var isInteger = require( 'validate.io-integer-primitive' ),
	isNumber = require( 'validate.io-number-primitive' );


// ISET //

/**
* FUNCTION: iset( idx, value )
*	Sets a matrix element located at a specified index.
*
* @param {Number} idx - linear index
* @param {Number} value - value to set
* @returns {Matrix} Matrix instance
*/
function iset( idx, v ) {
	/* jshint validthis: true */
	var r, j;
	if ( !isInteger( idx ) ) {
		throw new TypeError( 'iset()::invalid input argument. An index must be an integer. Value: `' + idx + '`.' );
	}
	if ( !isNumber( v ) ) {
		throw new TypeError( 'iset()::invalid input argument. An input value must be a number primitive. Value: `' + v + '`.' );
	}
	if ( idx < 0 ) {
		idx += this.length;
		if ( idx < 0 ) {
			return this;
		}
	}
	j = idx % this.strides[ 0 ];
	r = idx - j;
	if ( this.strides[ 0 ] < 0 ) {
		r = -r;
	}
	this.data[ this.offset + r + j*this.strides[1] ] = v;
	return this;
} // end FUNCTION iset()


// EXPORTS //

module.exports = iset;

},{"validate.io-integer-primitive":53,"validate.io-number-primitive":77}],23:[function(require,module,exports){
'use strict';

/**
* FUNCTION: iset( idx, value )
*	Sets a matrix element located at a specified index.
*
* @param {Number} idx - linear index
* @param {Number} value - value to set
* @returns {Matrix} Matrix instance
*/
function iset( idx, v ) {
	/* jshint validthis: true */
	var r, j;
	if ( idx < 0 ) {
		idx += this.length;
		if ( idx < 0 ) {
			return this;
		}
	}
	j = idx % this.strides[ 0 ];
	r = idx - j;
	if ( this.strides[ 0 ] < 0 ) {
		r = -r;
	}
	this.data[ this.offset + r + j*this.strides[1] ] = v;
	return this;
} // end FUNCTION iset()


// EXPORTS //

module.exports = iset;

},{}],24:[function(require,module,exports){
'use strict';

// MODULES //

var isString = require( 'validate.io-string-primitive' ),
	isNonNegativeIntegerArray = require( 'validate.io-nonnegative-integer-array' ),
	contains = require( 'validate.io-contains' ),
	isArray = require( 'validate.io-array' ),
	cast = require( 'compute-cast-arrays' ),
	getType = require( 'compute-dtype' ),
	Matrix = require( './ctor.js' );


// VARIABLES //

var BTYPES = require( './btypes.js' ),
	DTYPES = require( './dtypes.js' );


// CREATE MATRIX //

/**
* FUNCTION: matrix( [data,] shape[, dtype] )
*	Returns a Matrix instance.
*
* @constructor
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} [data] - input typed array
* @param {Number[]} shape - matrix dimensions/shape
* @param {String} [dtype="float64"] - matrix data type
* @returns {Matrix} Matrix instance
*/
function matrix() {
	var dtype,
		ndims,
		shape,
		data,
		vFLG,
		len,
		dt,
		i;

	// Parse the input arguments (polymorphic interface)...
	if ( arguments.length === 1 ) {
		shape = arguments[ 0 ];
		vFLG = 2; // arg #s
	}
	else if ( arguments.length === 2 ) {
		if ( isString( arguments[ 1 ] ) ) {
			shape = arguments[ 0 ];
			dtype = arguments[ 1 ];
			vFLG = 23; // arg #s
		} else {
			data = arguments[ 0 ];
			shape = arguments[ 1 ];
			vFLG = 12; // arg #s
		}
	}
	else {
		data = arguments[ 0 ];
		shape = arguments[ 1 ];
		dtype = arguments[ 2 ];
		vFLG = 123; // arg #s
	}

	// Input argument validation...
	if ( !isNonNegativeIntegerArray( shape ) ) {
		throw new TypeError( 'matrix()::invalid input argument. A matrix shape must be an array of nonnegative integers. Value: `' + shape + '`.' );
	}
	ndims = shape.length;
	if ( ndims !== 2 ) {
		throw new Error( 'matrix()::invalid input argument. Shape must be a 2-element array. Value: `' + shape + '`.' );
	}
	// If a `dtype` has been provided, validate...
	if ( vFLG === 123 || vFLG === 23 ) {
		if ( !contains( DTYPES, dtype ) ) {
			throw new TypeError( 'matrix()::invalid input argument. Unrecognized/unsupported data type. Value: `' + dtype + '`.' );
		}
	} else {
		dtype = 'float64';
	}
	len = 1;
	for ( i = 0; i < ndims; i++ ) {
		len *= shape[ i ];
	}
	// If a `data` argument has been provided, validate...
	if ( vFLG === 123 || vFLG === 12 ) {
		dt = getType( data );
		if ( !contains( DTYPES, dt ) && !isArray( data ) ) {
			throw new TypeError( 'matrix()::invalid input argument. Input data must be a valid type. Consult the documentation for a list of valid data types. Value: `' + data + '`.' );
		}
		if ( len !== data.length ) {
			throw new Error( 'matrix()::invalid input argument. Matrix shape does not match the input data length.' );
		}
		// Only cast if either 1) both a `data` and `dtype` argument have been provided and they do not agree or 2) when provided a plain Array...
		if ( ( vFLG === 123 && dt !== dtype ) || dt === 'generic' ) {
			data = cast( data, dtype );
		}
	} else {
		// Initialize a zero-filled typed array:
		data = new BTYPES[ dtype ]( len );
	}
	// Return a new Matrix instance:
	return new Matrix( data, dtype, shape, 0, [shape[1],1] );
} // end FUNCTION matrix()


// EXPORTS //

module.exports = matrix;

},{"./btypes.js":13,"./ctor.js":14,"./dtypes.js":16,"compute-cast-arrays":43,"compute-dtype":46,"validate.io-array":50,"validate.io-contains":51,"validate.io-nonnegative-integer-array":54,"validate.io-string-primitive":80}],25:[function(require,module,exports){
'use strict';

// MODULES //

var isString = require( 'validate.io-string-primitive' ),
	contains = require( 'validate.io-contains' ),
	getType = require( 'compute-dtype' ),
	Matrix = require( './ctor.raw.js' );


// VARIABLES //

var BTYPES = require( './btypes.js' ),
	DTYPES = require( './dtypes.js' );


// CREATE MATRIX //

/**
* FUNCTION: matrix( [data,] shape[, dtype] )
*	Returns a Matrix instance.
*
* @constructor
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} [data] - input typed array
* @param {Number[]} shape - matrix dimensions/shape
* @param {String} [dtype="float64"] - matrix data type
* @returns {Matrix} Matrix instance
*/
function matrix() {
	var dtype,
		ndims,
		shape,
		data,
		len,
		i;

	if ( arguments.length === 1 ) {
		shape = arguments[ 0 ];
	}
	else if ( arguments.length === 2 ) {
		if ( isString( arguments[ 1 ] ) ) {
			shape = arguments[ 0 ];
			dtype = arguments[ 1 ];
		} else {
			data = arguments[ 0 ];
			shape = arguments[ 1 ];
		}
	}
	else {
		data = arguments[ 0 ];
		shape = arguments[ 1 ];
		dtype = arguments[ 2 ];
	}
	ndims = shape.length;
	if ( ndims !== 2 ) {
		throw new Error( 'matrix()::invalid input argument. Shape must be a 2-element array. Value: `' + shape + '`.' );
	}
	len = 1;
	for ( i = 0; i < ndims; i++ ) {
		len *= shape[ i ];
	}
	if ( data ) {
		if ( !dtype ) {
			dtype = getType( data );
			if ( !contains( DTYPES, dtype ) ) {
				throw new TypeError( 'matrix()::invalid input argument. Input data must be a valid type. Consult the documentation for a list of valid data types. Value: `' + data + '`.' );
			}
		}
		if ( len !== data.length ) {
			throw new Error( 'matrix()::invalid input argument. Matrix shape does not match the input data length.' );
		}
	} else {
		// Initialize a zero-filled typed array...
		if ( !dtype ) {
			dtype = 'float64';
		}
		data = new BTYPES[ dtype ]( len );
	}
	// Return a new Matrix instance:
	return new Matrix( data, dtype, shape, 0, [shape[1],1] );
} // end FUNCTION matrix()


// EXPORTS //

module.exports = matrix;

},{"./btypes.js":13,"./ctor.raw.js":15,"./dtypes.js":16,"compute-dtype":46,"validate.io-contains":51,"validate.io-string-primitive":80}],26:[function(require,module,exports){
'use strict';

// MODULES //

var isNonNegativeIntegerArray = require( 'validate.io-nonnegative-integer-array' );


// VARIABLES //

var BTYPES = require( './btypes.js' );


// MGET //

/**
* FUNCTION: mget( i[, j] )
*	Returns multiple matrix elements. If provided a single argument, `i` is treated as an array of linear indices.
*
* @param {Number[]|Null} i - linear/row indices
* @param {Number[]|Null} [j] - column indices
* @returns {Matrix} a new Matrix instance
*/
function mget( rows, cols ) {
	/*jshint validthis:true */
	var nRows,
		nCols,
		out,
		sgn,
		d,
		s0, s1, s2, s3,
		o,
		r, dr,
		i, j, m, n;

	s0 = this.strides[ 0 ];
	s1 = this.strides[ 1 ];
	o = this.offset;

	if ( arguments.length < 2 ) {
		if ( !isNonNegativeIntegerArray( rows ) ) {
			throw new TypeError( 'mget()::invalid input argument. Linear indices must be specified as a nonnegative integer array. Value: `' + rows + '`.' );
		}
		// Filter the input indices to ensure within bounds...
		i = [];
		for ( n = 0; n < rows.length; n++ ) {
			if ( rows[ n ] < this.length ) {
				i.push( rows[ n ] );
			}
		}
		m = i.length;

		// Create a row vector (matrix):
		d = new BTYPES[ this.dtype ]( m );
		out = new this.constructor( d, this.dtype, [1,m], 0, [m,1] );

		sgn = ( s0 < 0 ) ? -1 : 1;
		for ( n = 0; n < m; n++ ) {
			j = i[ n ] % s0;
			r = sgn * ( i[n] - j );
			d[ n ] = this.data[ o + r + j*s1 ];
		}
	} else {
		nRows = this.shape[ 0 ];
		if ( rows === null ) {
			i = new Array( nRows );
			for ( n = 0; n < nRows; n++ ) {
				i[ n ] = n;
			}
		}
		else if ( isNonNegativeIntegerArray( rows ) ) {
			i = [];
			for ( n = 0; n < rows.length; n++ ) {
				if ( rows[ n ] < nRows ) {
					i.push( rows[ n ] );
				}
			}
		}
		else {
			throw new TypeError( 'mget()::invalid input argument. Row indices must be specified as a nonnegative integer array. Value: `' + rows + '`.' );
		}

		nCols = this.shape[ 1 ];
		if ( cols === null ) {
			j = new Array( nCols );
			for ( n = 0; n < nCols; n++ ) {
				j[ n ] = n;
			}
		}
		else if ( isNonNegativeIntegerArray( cols ) ) {
			j = [];
			for ( n = 0; n < cols.length; n++ ) {
				if ( cols[ n ] < nCols ) {
					j.push( cols[ n ] );
				}
			}
		}
		else {
			throw new TypeError( 'mget()::invalid input argument. Column indices must be specified as a nonnegative integer array. Value: `' + cols + '`.' );
		}
		nRows = i.length;
		nCols = j.length;

		d = new BTYPES[ this.dtype ]( nRows*nCols );
		out = new this.constructor( d, this.dtype, [nRows,nCols], 0, [nCols,1]);

		s2 = out.strides[ 0 ];
		s3 = out.strides[ 1 ];
		for ( m = 0; m < nRows; m++ ) {
			r = o + i[m]*s0;
			dr = m * s2;
			for ( n = 0; n < nCols; n++ ) {
				d[ dr + n*s3 ] = this.data[ r + j[n]*s1 ];
			}
		}
	}
	return out;
} // end FUNCTION mget()


// EXPORTS //

module.exports = mget;

},{"./btypes.js":13,"validate.io-nonnegative-integer-array":54}],27:[function(require,module,exports){
'use strict';

// VARIABLES //

var BTYPES = require( './btypes.js' );


// MGET //

/**
* FUNCTION: mget( i[, j] )
*	Returns multiple matrix elements. If provided a single argument, `i` is treated as an array of linear indices.
*
* @param {Number[]|Null} i - linear/row indices
* @param {Number[]|Null} [j] - column indices
* @returns {Matrix} a new Matrix instance
*/
function mget( rows, cols ) {
	/*jshint validthis:true */
	var nRows,
		nCols,
		out,
		sgn,
		d,
		s0, s1, s2, s3,
		o,
		r, dr,
		i, j, m, n;

	s0 = this.strides[ 0 ];
	s1 = this.strides[ 1 ];
	o = this.offset;

	if ( arguments.length < 2 ) {
		i = rows;
		m = i.length;

		// Create a row vector (matrix):
		d = new BTYPES[ this.dtype ]( m );
		out = new this.constructor( d, this.dtype, [1,m], 0, [m,1] );

		sgn = ( s0 < 0 ) ? -1 : 1;
		for ( n = 0; n < m; n++ ) {
			j = i[ n ] % s0;
			r = sgn * ( i[n] - j );
			d[ n ] = this.data[ o + r + j*s1 ];
		}
	} else {
		if ( rows === null ) {
			nRows = this.shape[ 0 ];
			i = new Array( nRows );
			for ( n = 0; n < nRows; n++ ) {
				i[ n ] = n;
			}
		} else {
			nRows = rows.length;
			i = rows;
		}

		if ( cols === null ) {
			nCols = this.shape[ 1 ];
			j = new Array( nCols );
			for ( n = 0; n < nCols; n++ ) {
				j[ n ] = n;
			}
		} else {
			nCols = cols.length;
			j = cols;
		}

		d = new BTYPES[ this.dtype ]( nRows*nCols );
		out = new this.constructor( d, this.dtype, [nRows,nCols], 0, [nCols,1] );

		s2 = out.strides[ 0 ];
		s3 = out.strides[ 1 ];
		for ( m = 0; m < nRows; m++ ) {
			r = o + i[m]*s0;
			dr = m * s2;
			for ( n = 0; n < nCols; n++ ) {
				d[ dr + n*s3 ] = this.data[ r + j[n]*s1 ];
			}
		}
	}
	return out;
} // end FUNCTION mget()


// EXPORTS //

module.exports = mget;

},{"./btypes.js":13}],28:[function(require,module,exports){
'use strict';

// MODULES //

var isFunction = require( 'validate.io-function' ),
	isNumber = require( 'validate.io-number-primitive' ),
	isNonNegativeIntegerArray = require( 'validate.io-nonnegative-integer-array' );


// FUNCTIONS //

var mset1 = require( './mset1.js' ),
	mset2 = require( './mset2.js' ),
	mset3 = require( './mset3.js' ),
	mset4 = require( './mset4.js' ),
	mset5 = require( './mset5.js' ),
	mset6 = require( './mset6.js' );

/**
* FUNCTION: getIndices( idx, len )
*	Validates and returns an array of indices.
*
* @private
* @param {Number[]|Null} idx - indices
* @param {Number} len - max index
* @returns {Number[]} indices
*/
function getIndices( idx, len ) {
	var out,
		i;
	if ( idx === null ) {
		out = new Array( len );
		for ( i = 0; i < len; i++ ) {
			out[ i ] = i;
		}
	}
	else if ( isNonNegativeIntegerArray( idx ) ) {
		out = [];
		for ( i = 0; i < idx.length; i++ ) {
			if ( idx[ i ] < len ) {
				out.push( idx[ i ] );
			}
		}
	}
	else {
		throw new TypeError( 'mset()::invalid input argument. Row and column indices must be arrays of nonnegative integers. Value: `' + idx + '`.' );
	}
	return out;
} // end FUNCTION getIndices()


// MSET //

/**
* FUNCTION: mset( i[, j], value[, thisArg] )
*	Sets multiple matrix elements. If provided a single array, `i` is treated as an array of linear indices.
*
* @param {Number[]|Null} i - linear/row indices
* @param {Number[]|Null} [j] - column indices
* @param {Number|Matrix|Function} value - either a single numeric value, a matrix containing the values to set, or a function which returns a numeric value
* @returns {Matrix} Matrix instance
*/
function mset() {
	/*jshint validthis:true */
	var nargs = arguments.length,
		args,
		rows,
		cols,
		i;

	args = new Array( nargs );
	for ( i = 0; i < nargs; i++ ) {
		args[ i ] = arguments[ i ];
	}

	// 2 input arguments...
	if ( nargs < 3 ) {
		if ( !isNonNegativeIntegerArray( args[ 0 ] ) ) {
			throw new TypeError( 'mset()::invalid input argument. First argument must be an array of nonnegative integers. Value: `' + args[ 0 ] + '`.' );
		}
		// indices, clbk
		if ( isFunction( args[ 1 ] ) ) {
			mset2( this, args[ 0 ], args[ 1 ] );
		}
		// indices, number
		else if ( isNumber( args[ 1 ] ) ) {
			mset1( this, args[ 0 ], args[ 1 ] );
		}
		// indices, matrix
		else {
			// NOTE: no validation for Matrix instance.
			mset3( this, args[ 0 ], args[ 1 ] );
		}
	}
	// 3 input arguments...
	else if ( nargs === 3 ) {
		// indices, clbk, context
		if ( isFunction( args[ 1 ] ) ) {
			if ( !isNonNegativeIntegerArray( args[ 0 ] ) ) {
				throw new TypeError( 'mset()::invalid input argument. First argument must be an array of nonnegative integers. Value: `' + args[ 0 ] + '`.' );
			}
			mset2( this, args[ 0 ], args[ 1 ], args[ 2 ] );
		}
		// rows, cols, function
		else if ( isFunction( args[ 2 ] ) ) {
			rows = getIndices( args[ 0 ], this.shape[ 0 ] );
			cols = getIndices( args[ 1 ], this.shape[ 1 ] );
			mset4( this, rows, cols, args[ 2 ], this );
		}
		// rows, cols, number
		else if ( isNumber( args[ 2 ] ) ) {
			rows = getIndices( args[ 0 ], this.shape[ 0 ] );
			cols = getIndices( args[ 1 ], this.shape[ 1 ] );
			mset5( this, rows, cols, args[ 2 ] );
		}
		// rows, cols, matrix
		else {
			rows = getIndices( args[ 0 ], this.shape[ 0 ] );
			cols = getIndices( args[ 1 ], this.shape[ 1 ] );

			// NOTE: no validation for Matrix instance.
			mset6( this, rows, cols, args[ 2 ] );
		}
	}
	// 4 input arguments...
	else {
		// rows, cols, function, context
		if ( !isFunction( args[ 2 ] ) ) {
			throw new TypeError( 'mset()::invalid input argument. Callback argument must be a function. Value: `' + args[ 2 ] + '`.' );
		}
		rows = getIndices( args[ 0 ], this.shape[ 0 ] );
		cols = getIndices( args[ 1 ], this.shape[ 1 ] );
		mset4( this, rows, cols, args[ 2 ], args[ 3 ] );
	}
	return this;
} // end FUNCTION mset()


// EXPORTS //

module.exports = mset;

},{"./mset1.js":30,"./mset2.js":31,"./mset3.js":32,"./mset4.js":33,"./mset5.js":34,"./mset6.js":35,"validate.io-function":74,"validate.io-nonnegative-integer-array":54,"validate.io-number-primitive":77}],29:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var mset1 = require( './mset1.js' ),
	mset2 = require( './mset2.js' ),
	mset3 = require( './mset3.js' ),
	mset4 = require( './mset4.js' ),
	mset5 = require( './mset5.js' ),
	mset6 = require( './mset6.js' );

/**
* FUNCTION: getIndices( idx, len )
*	Returns an array of indices.
*
* @private
* @param {Number[]|Null} idx - indices
* @param {Number} len - max index
* @returns {Number[]} indices
*/
function getIndices( idx, len ) {
	var out,
		i;
	if ( idx === null ) {
		out = new Array( len );
		for ( i = 0; i < len; i++ ) {
			out[ i ] = i;
		}
	} else {
		out = idx;
	}
	return out;
} // end FUNCTION getIndices()


// MSET //

/**
* FUNCTION: mset( i[, j], value[, thisArg] )
*	Sets multiple matrix elements. If provided a single array, `i` is treated as an array of linear indices.
*
* @param {Number[]|Null} i - linear/row indices
* @param {Number[]|Null} [j] - column indices
* @param {Number|Matrix|Function} value - either a single numeric value, a matrix containing the values to set, or a function which returns a numeric value
* @returns {Matrix} Matrix instance
*/
function mset() {
	/*jshint validthis:true */
	var nargs = arguments.length,
		args,
		rows,
		cols,
		i;

	args = new Array( nargs );
	for ( i = 0; i < nargs; i++ ) {
		args[ i ] = arguments[ i ];
	}

	// 2 input arguments...
	if ( nargs < 3 ) {
		// indices, clbk
		if ( typeof args[ 1 ] === 'function' ) {
			mset2( this, args[ 0 ], args[ 1 ] );
		}
		// indices, number
		else if ( typeof args[ 1 ] === 'number' ) {
			mset1( this, args[ 0 ], args[ 1 ] );
		}
		// indices, matrix
		else {
			mset3( this, args[ 0 ], args[ 1 ] );
		}
	}
	// 3 input arguments...
	else if ( nargs === 3 ) {
		// indices, clbk, context
		if ( typeof args[ 1 ] === 'function' ) {
			mset2( this, args[ 0 ], args[ 1 ], args[ 2 ] );
		}
		// rows, cols, function
		else if ( typeof args[ 2 ] === 'function' ) {
			rows = getIndices( args[ 0 ], this.shape[ 0 ] );
			cols = getIndices( args[ 1 ], this.shape[ 1 ] );
			mset4( this, rows, cols, args[ 2 ], this );
		}
		// rows, cols, number
		else if ( typeof args[ 2 ] === 'number' ) {
			rows = getIndices( args[ 0 ], this.shape[ 0 ] );
			cols = getIndices( args[ 1 ], this.shape[ 1 ] );
			mset5( this, rows, cols, args[ 2 ] );
		}
		// rows, cols, matrix
		else {
			rows = getIndices( args[ 0 ], this.shape[ 0 ] );
			cols = getIndices( args[ 1 ], this.shape[ 1 ] );
			mset6( this, rows, cols, args[ 2 ] );
		}
	}
	// 4 input arguments...
	else {
		rows = getIndices( args[ 0 ], this.shape[ 0 ] );
		cols = getIndices( args[ 1 ], this.shape[ 1 ] );
		mset4( this, rows, cols, args[ 2 ], args[ 3 ] );
	}
	return this;
} // end FUNCTION mset()


// EXPORTS //

module.exports = mset;

},{"./mset1.js":30,"./mset2.js":31,"./mset3.js":32,"./mset4.js":33,"./mset5.js":34,"./mset6.js":35}],30:[function(require,module,exports){
'use strict';

/**
* FUNCTION: mset1( mat, idx, v )
*	Sets multiple matrix elements to a numeric value `v`.
*
* @private
* @param {Matrix} mat - Matrix instance
* @param {Number[]} idx - linear indices
* @param {Number} v - numeric value
*/
function mset1( mat, idx, v ) {
	var s0 = mat.strides[ 0 ],
		s1 = mat.strides[ 1 ],
		len = idx.length,
		o = mat.offset,
		sgn,
		r, j, n;

	sgn = ( s0 < 0 ) ? -1 : 1;
	for ( n = 0; n < len; n++ ) {
		j = idx[ n ] % s0;
		r = sgn * ( idx[n] - j );
		mat.data[ o + r + j*s1 ] = v;
	}
} // end FUNCTION mset1()


// EXPORTS //

module.exports = mset1;

},{}],31:[function(require,module,exports){
'use strict';

/**
* FUNCTION: mset2( mat, idx, clbk, ctx )
*	Sets multiple matrix elements using a callback function.
*
* @private
* @param {Matrix} mat - Matrix instance
* @param {Number[]} idx - linear indices
* @param {Function} clbk - callback function
* @param {Object} ctx - `this` context when invoking the provided callback
*/
function mset2( mat, idx, clbk, ctx ) {
	var s0 = mat.strides[ 0 ],
		s1 = mat.strides[ 1 ],
		len = idx.length,
		o = mat.offset,
		sgn,
		r, c,
		i, k, n;

	sgn = ( s0 < 0 ) ? -1 : 1;
	for ( n = 0; n < len; n++ ) {
		// Get the column number:
		c = idx[ n ] % s0;

		// Determine the row offset:
		i = sgn * ( idx[n] - c );

		// Get the row number:
		r = i / s0;

		// Calculate the index:
		k = o + i + c*s1;

		// Set the value:
		mat.data[ k ] = clbk.call( ctx, mat.data[ k ], r, c, k );
	}
} // end FUNCTION mset2()


// EXPORTS //

module.exports = mset2;

},{}],32:[function(require,module,exports){
'use strict';

/**
* FUNCTION: mset3( mat, idx, m )
*	Sets multiple matrix elements using elements from another matrix.
*
* @private
* @param {Matrix} mat - Matrix instance
* @param {Number[]} idx - linear indices
* @param {Matrix} m - Matrix instance
*/
function mset3( mat, idx, m ) {
	var s0 = mat.strides[ 0 ],
		s1 = mat.strides[ 1 ],
		s2 = m.strides[ 0 ],
		s3 = m.strides[ 1 ],
		len = idx.length,
		o0 = mat.offset,
		o1 = m.offset,
		sgn0, sgn1,
		r0, r1,
		j0, j1,
		n;

	if ( m.length !== len ) {
		throw new Error( 'mset()::invalid input argument. Number of indices does not match the number of elements in the value matrix.' );
	}
	sgn0 = ( s0 < 0 ) ? -1 : 1;
	sgn1 = ( s2 < 0 ) ? -1 : 1;
	for ( n = 0; n < len; n++ ) {
		// Get the column number and row offset for the first matrix:
		j0 = idx[ n ] % s0;
		r0 = sgn0 * ( idx[n] - j0 );

		// Get the column number and row offset for the value matrix:
		j1 = n % s2;
		r1 = sgn1 * ( n - j1 );

		mat.data[ o0 + r0 + j0*s1 ] = m.data[ o1 + r1 + j1*s3  ];
	}
} // end FUNCTION mset3()


// EXPORTS //

module.exports = mset3;

},{}],33:[function(require,module,exports){
'use strict';

/**
* FUNCTION: mset4( mat, rows, cols, clbk, ctx )
*	Sets multiple matrix elements using a callback function.
*
* @private
* @param {Matrix} mat - Matrix instance
* @param {Number[]} rows - row indices
* @param {Number[]} cols - column indices
* @param {Function} clbk - callback function
* @param {Object} ctx - `this` context when invoking the provided callback
*/
function mset4( mat, rows, cols, clbk, ctx ) {
	var s0 = mat.strides[ 0 ],
		s1 = mat.strides[ 1 ],
		nRows = rows.length,
		nCols = cols.length,
		o = mat.offset,
		r,
		i, j, k;

	for ( i = 0; i < nRows; i++ ) {
		r = o + rows[i]*s0;
		for ( j = 0; j < nCols; j++ ) {
			k = r + cols[j]*s1;
			mat.data[ k ] = clbk.call( ctx, mat.data[ k ], rows[ i ], cols[ j ], k );
		}
	}
} // end FUNCTION mset4()


// EXPORTS //

module.exports = mset4;

},{}],34:[function(require,module,exports){
'use strict';

/**
* FUNCTION: mset5( mat, rows, cols, v )
*	Sets multiple matrix elements to a numeric value `v`.
*
* @private
* @param {Matrix} mat - Matrix instance
* @param {Number[]} rows - row indices
* @param {Number[]} cols - column indices
* @param {Number} v - numeric value
*/
function mset5( mat, rows, cols, v ) {
	var s0 = mat.strides[ 0 ],
		s1 = mat.strides[ 1 ],
		nRows = rows.length,
		nCols = cols.length,
		o = mat.offset,
		r,
		i, j;

	for ( i = 0; i < nRows; i++ ) {
		r = o + rows[i]*s0;
		for ( j = 0; j < nCols; j++ ) {
			mat.data[ r + cols[j]*s1 ] = v;
		}
	}
} // end FUNCTION mset5()


// EXPORTS //

module.exports = mset5;

},{}],35:[function(require,module,exports){
'use strict';

/**
* FUNCTION: mset6( mat, rows, cols, m )
*	Sets multiple matrix elements using elements from another matrix.
*
* @private
* @param {Matrix} mat - Matrix instance
* @param {Number[]} rows - row indices
* @param {Number[]} cols - column indices
* @param {Matrix} m - Matrix instance
*/
function mset6( mat, rows, cols, m ) {
	var s0 = mat.strides[ 0 ],
		s1 = mat.strides[ 1 ],
		s2 = m.strides[ 0 ],
		s3 = m.strides[ 1 ],
		nRows = rows.length,
		nCols = cols.length,
		o0 = mat.offset,
		o1 = m.offset,
		r0, r1,
		i, j;

	if ( m.shape[ 0 ] !== nRows || m.shape[ 1 ] !== nCols ) {
		throw new Error( 'mset()::invalid input argument. The dimensions given by the row and column indices do not match the value matrix dimensions.' );
	}
	for ( i = 0; i < nRows; i++ ) {
		r0 = o0 + rows[i]*s0;
		r1 = o1 + i*s2;
		for ( j = 0; j < nCols; j++ ) {
			mat.data[ r0 + cols[j]*s1 ] = m.data[ r1 + j*s3 ];
		}
	}
} // end FUNCTION mset6()


// EXPORTS //

module.exports = mset6;

},{}],36:[function(require,module,exports){
'use strict';

// MODULES //

var isNonNegativeInteger = require( 'validate.io-nonnegative-integer' ),
	isNumber = require( 'validate.io-number-primitive' );


// SET //

/**
* FUNCTION: set( i, j, value )
*	Sets a matrix element based on the provided row and column indices.
*
* @param {Number} i - row index
* @param {Number} j - column index
* @param {Number} value - value to set
* @returns {Matrix} Matrix instance
*/
function set( i, j, v ) {
	/* jshint validthis: true */
	if ( !isNonNegativeInteger( i ) || !isNonNegativeInteger( j ) ) {
		throw new TypeError( 'set()::invalid input argument. Row and column indices must be nonnegative integers. Values: `[' + i + ',' + j + ']`.' );
	}
	if ( !isNumber( v ) ) {
		throw new TypeError( 'set()::invalid input argument. An input value must be a number primitive. Value: `' + v + '`.' );
	}
	i = this.offset + i*this.strides[0] + j*this.strides[1];
	if ( i >= 0 ) {
		this.data[ i ] = v;
	}
	return this;
} // end FUNCTION set()


// EXPORTS //

module.exports = set;

},{"validate.io-nonnegative-integer":55,"validate.io-number-primitive":77}],37:[function(require,module,exports){
'use strict';

/**
* FUNCTION: set( i, j, value )
*	Sets a matrix element based on the provided row and column indices.
*
* @param {Number} i - row index
* @param {Number} j - column index
* @param {Number} value - value to set
* @returns {Matrix} Matrix instance
*/
function set( i, j, v ) {
	/* jshint validthis: true */
	i = this.offset + i*this.strides[0] + j*this.strides[1];
	if ( i >= 0 ) {
		this.data[ i ] = v;
	}
	return this;
} // end FUNCTION set()


// EXPORTS //

module.exports = set;

},{}],38:[function(require,module,exports){
'use strict';

// MODULES //

var isString = require( 'validate.io-string-primitive' ),
	ispace = require( 'compute-indexspace' );


// VARIABLES //

var BTYPES = require( './btypes.js' );


// SUBSEQUENCE GET //

/**
* FUNCTION: sget( subsequence )
*	Returns matrix elements according to a specified subsequence.
*
* @param {String} subsequence - subsequence string
* @returns {Matrix} Matrix instance
*/
function sget( seq ) {
	/*jshint validthis:true */
	var nRows,
		nCols,
		rows,
		cols,
		seqs,
		mat,
		len,
		s0, s1,
		o,
		d,
		r, dr,
		i, j;

	if ( !isString( seq ) ) {
		throw new TypeError( 'sget()::invalid input argument. Must provide a string primitive. Value: `' + seq + '`.' );
	}
	seqs = seq.split( ',' );
	if ( seqs.length !== 2 ) {
		throw new Error( 'sget()::invalid input argument. Subsequence string must specify row and column subsequences. Value: `' + seq + '`.' );
	}
	rows = ispace( seqs[ 0 ], this.shape[ 0 ] );
	cols = ispace( seqs[ 1 ], this.shape[ 1 ] );

	nRows = rows.length;
	nCols = cols.length;
	len = nRows * nCols;

	d = new BTYPES[ this.dtype ]( len );
	mat = new this.constructor( d, this.dtype, [nRows,nCols], 0, [nCols,1] );

	if ( len ) {
		s0 = this.strides[ 0 ];
		s1 = this.strides[ 1 ];
		o = this.offset;
		for ( i = 0; i < nRows; i++ ) {
			r = o + rows[i]*s0;
			dr = i * nCols;
			for ( j = 0; j < nCols; j++ ) {
				d[ dr + j ] = this.data[ r + cols[j]*s1 ];
			}
		}
	}
	return mat;
} // end FUNCTION sget()


// EXPORTS //

module.exports = sget;

},{"./btypes.js":13,"compute-indexspace":534,"validate.io-string-primitive":80}],39:[function(require,module,exports){
'use strict';

// MODULES //

var ispace = require( 'compute-indexspace' );


// VARIABLES //

var BTYPES = require( './btypes.js' );


// SUBSEQUENCE GET //

/**
* FUNCTION: sget( subsequence )
*	Returns matrix elements according to a specified subsequence.
*
* @param {String} subsequence - subsequence string
* @returns {Matrix} Matrix instance
*/
function sget( seq ) {
	/*jshint validthis:true */
	var nRows,
		nCols,
		rows,
		cols,
		seqs,
		mat,
		len,
		s0, s1,
		o,
		d,
		r, dr,
		i, j;

	seqs = seq.split( ',' );
	rows = ispace( seqs[ 0 ], this.shape[ 0 ] );
	cols = ispace( seqs[ 1 ], this.shape[ 1 ] );

	nRows = rows.length;
	nCols = cols.length;
	len = nRows * nCols;

	d = new BTYPES[ this.dtype ]( len );
	mat = new this.constructor( d, this.dtype, [nRows,nCols], 0, [nCols,1] );

	if ( len ) {
		s0 = this.strides[ 0 ];
		s1 = this.strides[ 1 ];
		o = this.offset;
		for ( i = 0; i < nRows; i++ ) {
			r = o + rows[i]*s0;
			dr = i * nCols;
			for ( j = 0; j < nCols; j++ ) {
				d[ dr + j ] = this.data[ r + cols[j]*s1 ];
			}
		}
	}
	return mat;
} // end FUNCTION sget()


// EXPORTS //

module.exports = sget;

},{"./btypes.js":13,"compute-indexspace":534}],40:[function(require,module,exports){
'use strict';

// MODULES //

var isString = require( 'validate.io-string-primitive' ),
	isNumber = require( 'validate.io-number-primitive' ),
	isFunction = require( 'validate.io-function' ),
	ispace = require( 'compute-indexspace' );


// SUBSEQUENCE SET //

/**
* FUNCTION: sset( subsequence, value[, thisArg] )
*	Sets matrix elements according to a specified subsequence.
*
* @param {String} subsequence - subsequence string
* @param {Number|Matrix|Function} value - either a single numeric value, a matrix containing the values to set, or a function which returns a numeric value
* @param {Object} [thisArg] - `this` context when executing a callback
* @returns {Matrix} Matrix instance
*/
function sset( seq, val, thisArg ) {
	/* jshint validthis: true */
	var nRows,
		nCols,
		clbk,
		rows,
		cols,
		seqs,
		mat,
		ctx,
		s0, s1, s2, s3,
		o0, o1,
		r0, r1,
		i, j, k;

	if ( !isString( seq ) ) {
		throw new TypeError( 'sset()::invalid input argument. Must provide a string primitive. Value: `' + seq + '`.' );
	}
	seqs = seq.split( ',' );
	if ( seqs.length !== 2 ) {
		throw new Error( 'sset()::invalid input argument. Subsequence string must specify row and column subsequences. Value: `' + seq + '`.' );
	}
	if ( isFunction( val ) ) {
		clbk = val;
	}
	else if ( !isNumber( val ) ) {
		mat = val;
	}
	rows = ispace( seqs[ 0 ], this.shape[ 0 ] );
	cols = ispace( seqs[ 1 ], this.shape[ 1 ] );

	nRows = rows.length;
	nCols = cols.length;

	if ( !( nRows && nCols ) ) {
		return this;
	}
	s0 = this.strides[ 0 ];
	s1 = this.strides[ 1 ];
	o0 = this.offset;

	// Callback...
	if ( clbk ) {
		if ( arguments.length > 2 ) {
			ctx = thisArg;
		} else {
			ctx = this;
		}
		for ( i = 0; i < nRows; i++ ) {
			r0 = o0 + rows[i]*s0;
			for ( j = 0; j < nCols; j++ ) {
				k = r0 + cols[j]*s1;
				this.data[ k ] = clbk.call( ctx, this.data[ k ], rows[i], cols[j], k );
			}
		}
	}
	// Input matrix...
	else if ( mat ) {
		if ( nRows !== mat.shape[ 0 ] ) {
			throw new Error( 'sset()::invalid input arguments. Row subsequence does not match input matrix dimensions. Expected a [' + nRows + ',' + nCols + '] matrix and instead received a [' + mat.shape.join( ',' ) + '] matrix.' );
		}
		if ( nCols !== mat.shape[ 1 ] ) {
			throw new Error( 'sset()::invalid input arguments. Column subsequence does not match input matrix dimensions. Expected a [' + nRows + ',' + nCols + '] matrix and instead received a [' + mat.shape.join( ',' ) + '] matrix.' );
		}
		s2 = mat.strides[ 0 ];
		s3 = mat.strides[ 1 ];
		o1 = mat.offset;
		for ( i = 0; i < nRows; i++ ) {
			r0 = o0 + rows[i]*s0;
			r1 = o1 + i*s2;
			for ( j = 0; j < nCols; j++ ) {
				this.data[ r0 + cols[j]*s1 ] = mat.data[ r1 + j*s3 ];
			}
		}
	}
	// Single numeric value...
	else {
		for ( i = 0; i < nRows; i++ ) {
			r0 = o0 + rows[i]*s0;
			for ( j = 0; j < nCols; j++ ) {
				this.data[ r0 + cols[j]*s1 ] = val;
			}
		}
	}
	return this;
} // end FUNCTION sset()


// EXPORTS //

module.exports = sset;

},{"compute-indexspace":534,"validate.io-function":74,"validate.io-number-primitive":77,"validate.io-string-primitive":80}],41:[function(require,module,exports){
'use strict';

// MODULES //

var ispace = require( 'compute-indexspace' );


// SUBSEQUENCE SET //

/**
* FUNCTION: sset( subsequence, value[, thisArg] )
*	Sets matrix elements according to a specified subsequence.
*
* @param {String} subsequence - subsequence string
* @param {Number|Matrix|Function} value - either a single numeric value, a matrix containing the values to set, or a function which returns a numeric value
* @param {Object} [thisArg] - `this` context when executing a callback
* @returns {Matrix} Matrix instance
*/
function sset( seq, val, thisArg ) {
	/* jshint validthis: true */
	var nRows,
		nCols,
		clbk,
		rows,
		cols,
		seqs,
		mat,
		ctx,
		s0, s1, s2, s3,
		o0, o1,
		r0, r1,
		i, j, k;

	seqs = seq.split( ',' );
	if ( typeof val === 'function' ) {
		clbk = val;
	}
	else if ( typeof val !== 'number' ) {
		mat = val;
	}
	rows = ispace( seqs[ 0 ], this.shape[ 0 ] );
	cols = ispace( seqs[ 1 ], this.shape[ 1 ] );

	nRows = rows.length;
	nCols = cols.length;

	if ( !( nRows && nCols ) ) {
		return this;
	}
	s0 = this.strides[ 0 ];
	s1 = this.strides[ 1 ];
	o0 = this.offset;

	// Callback...
	if ( clbk ) {
		if ( arguments.length > 2 ) {
			ctx = thisArg;
		} else {
			ctx = this;
		}
		for ( i = 0; i < nRows; i++ ) {
			r0 = o0 + rows[i]*s0;
			for ( j = 0; j < nCols; j++ ) {
				k = r0 + cols[j]*s1;
				this.data[ k ] = clbk.call( ctx, this.data[ k ], rows[i], cols[j], k );
			}
		}
	}
	// Input matrix...
	else if ( mat ) {
		if ( nRows !== mat.shape[ 0 ] ) {
			throw new Error( 'sset()::invalid input arguments. Row subsequence does not match input matrix dimensions. Expected a [' + nRows + ',' + nCols + '] matrix and instead received a [' + mat.shape.join( ',' ) + '] matrix.' );
		}
		if ( nCols !== mat.shape[ 1 ] ) {
			throw new Error( 'sset()::invalid input arguments. Column subsequence does not match input matrix dimensions. Expected a [' + nRows + ',' + nCols + '] matrix and instead received a [' + mat.shape.join( ',' ) + '] matrix.' );
		}
		s2 = mat.strides[ 0 ];
		s3 = mat.strides[ 1 ];
		o1 = mat.offset;
		for ( i = 0; i < nRows; i++ ) {
			r0 = o0 + rows[i]*s0;
			r1 = o1 + i*s2;
			for ( j = 0; j < nCols; j++ ) {
				this.data[ r0 + cols[j]*s1 ] = mat.data[ r1 + j*s3 ];
			}
		}
	}
	// Single numeric value...
	else {
		for ( i = 0; i < nRows; i++ ) {
			r0 = o0 + rows[i]*s0;
			for ( j = 0; j < nCols; j++ ) {
				this.data[ r0 + cols[j]*s1 ] = val;
			}
		}
	}
	return this;
} // end FUNCTION sset()


// EXPORTS //

module.exports = sset;

},{"compute-indexspace":534}],42:[function(require,module,exports){
'use strict';

/**
* FUNCTION: toString()
*	Returns a string representation of Matrix elements. Rows are delineated by semicolons. Column values are comma-delimited.
*
* @returns {String} string representation
*/
function toString() {
	/* jshint validthis: true */
	var nRows = this.shape[ 0 ],
		nCols = this.shape[ 1 ],
		s0 = this.strides[ 0 ],
		s1 = this.strides[ 1 ],
		m = nRows - 1,
		n = nCols - 1,
		str = '',
		o,
		i, j;

	for ( i = 0; i < nRows; i++ ) {
		o = this.offset + i*s0;
		for ( j = 0; j < nCols; j++ ) {
			str += this.data[ o + j*s1 ];
			if ( j < n ) {
				str += ',';
			}
		}
		if ( i < m ) {
			str += ';';
		}
	}
	return str;
} // end FUNCTION toString()


// EXPORTS //

module.exports = toString;

},{}],43:[function(require,module,exports){
'use strict';

// MODULES //

var arrayLike = require( 'validate.io-array-like' ),
	typeName = require( 'type-name' );


// VARIABLES //

var DTYPES = require( 'compute-array-dtype/lib/dtypes' ),
	CTORS = require( 'compute-array-constructors/lib/ctors' );


// CAST //

/**
* FUNCTION: cast( x, type )
*	Casts an input array or array-like object to a specified type.
*
* @param {Object|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} x - value to cast
* @param {String|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} type - type to which to cast or a value from which the desired type should be inferred
* @returns {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} casted value
*/
function cast( x, type ) {
	/* jshint newcap:false */
	var ctor,
		len,
		d,
		i;

	if ( !arrayLike( x ) ) {
		throw new TypeError( 'cast()::invalid input argument. First argument must be an array-like object. Value: `' + x + '`.' );
	}
	if ( typeof type === 'string' ) {
		ctor = CTORS[ type ];
	} else {
		ctor = CTORS[ DTYPES[ typeName( type ) ] ];
	}
	if ( ctor === void 0 ) {
		throw new Error( 'cast()::invalid input argument. Unrecognized/unsupported type to which to cast. Value: `' + type + '`.' );
	}
	len = x.length;
	d = new ctor( len );
	for ( i = 0; i < len; i++ ) {
		d[ i ] = x[ i ];
	}
	return d;
} // end FUNCTION cast()


// EXPORTS //

module.exports = cast;

},{"compute-array-constructors/lib/ctors":11,"compute-array-dtype/lib/dtypes":44,"type-name":45,"validate.io-array-like":70}],44:[function(require,module,exports){
'use strict';

var DTYPES = {
	'Int8Array': 'int8',
	'Uint8Array': 'uint8',
	'Uint8ClampedArray': 'uint8_clamped',
	'Int16Array': 'int16',
	'Uint16Array': 'uint16',
	'Int32Array': 'int32',
	'Uint32Array': 'uint32',
	'Float32Array': 'float32',
	'Float64Array': 'float64',
	'Array': 'generic'
};


// EXPORTS //

module.exports = DTYPES;

},{}],45:[function(require,module,exports){
/**
 * type-name - Just a reasonable typeof
 * 
 * https://github.com/twada/type-name
 *
 * Copyright (c) 2014-2015 Takuto Wada
 * Licensed under the MIT license.
 *   http://twada.mit-license.org/2014-2015
 */
'use strict';

var toStr = Object.prototype.toString;

function funcName (f) {
    return f.name ? f.name : /^\s*function\s*([^\(]*)/im.exec(f.toString())[1];
}

function ctorName (obj) {
    var strName = toStr.call(obj).slice(8, -1);
    if (strName === 'Object' && obj.constructor) {
        return funcName(obj.constructor);
    }
    return strName;
}

function typeName (val) {
    var type;
    if (val === null) {
        return 'null';
    }
    type = typeof(val);
    if (type === 'object') {
        return ctorName(val);
    }
    return type;
}

module.exports = typeName;

},{}],46:[function(require,module,exports){
'use strict';

// MODULES //

var typeName = require( 'type-name' ),
	getType = require( 'compute-array-dtype' );


// DTYPE //

/**
* FUNCTION: dtype( value )
*	Determines the data type of an input value.
*
* @param {*} value - input value
* @returns {String} data type
*/
function dtype( value ) {
	var type,
		dt;
	if ( value === null ) {
		return 'null';
	}
	// Check for base types:
	type = typeof value;
	switch ( type ) {
		case 'undefined':
		case 'boolean':
		case 'number':
		case 'string':
		case 'function':
		case 'symbol':
			return type;
	}
	// Resort to slower look-up:
	type = typeName( value );

	// Is value a known array type?
	dt = getType( type );
	if ( dt ) {
		return dt;
	}
	// Is value a buffer object?
	if ( type === 'Buffer' || type === 'ArrayBuffer' ) {
		return 'binary';
	}
	// Assume the value is a generic object (Object|Class instance) which could contain any or multiple data types...
	return 'generic';
} // end FUNCTION dtype()


// EXPORTS //

module.exports = dtype;

},{"compute-array-dtype":48,"type-name":49}],47:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],48:[function(require,module,exports){
'use strict';

// DTYPES //

var DTYPES = require( './dtypes.js' );


// GET DTYPE //

/**
* FUNCTION: getType( name )
*	Returns an array data type corresponding to an array constructor name.
*
* @param {String} name - constructor name
* @returns {String|Null} array data type or null
*/
function getType( name ) {
	return DTYPES[ name ] || null;
} // end FUNCTION getType()


// EXPORTS //

module.exports = getType;

},{"./dtypes.js":47}],49:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],50:[function(require,module,exports){
'use strict';

/**
* FUNCTION: isArray( value )
*	Validates if a value is an array.
*
* @param {*} value - value to be validated
* @returns {Boolean} boolean indicating whether value is an array
*/
function isArray( value ) {
	return Object.prototype.toString.call( value ) === '[object Array]';
} // end FUNCTION isArray()

// EXPORTS //

module.exports = Array.isArray || isArray;

},{}],51:[function(require,module,exports){
/**
*
*	VALIDATE: contains
*
*
*	DESCRIPTION:
*		- Validates if an array contains an input value.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2015. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2015.
*
*/

'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isnan = require( 'validate.io-nan-primitive' );


// CONTAINS //

/**
* FUNCTION: contains( arr, value )
*	Validates if an array contains an input value.
*
* @param {Array} arr - search array
* @param {*} value - search value
* @returns {Boolean} boolean indicating if an array contains an input value
*/
function contains( arr, value ) {
	var len, i;
	if ( !isArray( arr ) ) {
		throw new TypeError( 'contains()::invalid input argument. First argument must be an array. Value: `' + arr + '`.' );
	}
	len = arr.length;
	if ( isnan( value ) ) {
		for ( i = 0; i < len; i++ ) {
			if ( isnan( arr[ i ] ) ) {
				return true;
			}
		}
		return false;
	}
	for ( i = 0; i < len; i++ ) {
		if ( arr[ i ] === value ) {
			return true;
		}
	}
	return false;
} // end FUNCTION contains()


// EXPORTS //

module.exports = contains;

},{"validate.io-array":50,"validate.io-nan-primitive":52}],52:[function(require,module,exports){
/**
*
*	VALIDATE: nan-primitive
*
*
*	DESCRIPTION:
*		- Validates if a value is a NaN primitive.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2015. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2015.
*
*/

'use strict';

/**
* FUNCTION: nan( value )
*	Validates if a value is a NaN primitive.
*
* @param {*} value - value to be validated
* @returns {Boolean} boolean indicating whether the value is a NaN primitive
*/
function nan( value ) {
	return typeof value === 'number' && value !== value;
} // end FUNCTION nan()


// EXPORTS //

module.exports = nan;

},{}],53:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' );


// IS INTEGER //

/**
* FUNCTION: isInteger( value )
*	Validates if a value is a number primitive, excluding `NaN`, and an integer.
*
* @param {*} value - value to be validated
* @returns {Boolean} boolean indicating if a value is a integer primitive
*/
function isInteger( value ) {
	return isNumber( value ) && value%1 === 0;
} // end FUNCTION isInteger()


// EXPORTS //

module.exports = isInteger;

},{"validate.io-number-primitive":77}],54:[function(require,module,exports){
/**
*
*	VALIDATE: nonnegative-integer-array
*
*
*	DESCRIPTION:
*		- Validates if a value is a nonnegative integer array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2015. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2015.
*
*/

'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isNonNegativeInteger = require( 'validate.io-nonnegative-integer' );


// IS NONNEGATIVE INTEGER ARRAY //

/**
* FUNCTION: isNonNegativeIntegerArray( value )
*	Validates if a value is a nonnegative integer array.
*
* @param {*} value - value to be validated
* @returns {Boolean} boolean indicating if a value is a nonnegative integer array
*/
function isNonNegativeIntegerArray( value ) {
	var len;
	if ( !isArray( value ) ) {
		return false;
	}
	len = value.length;
	if ( !len ) {
		return false;
	}
	for ( var i = 0; i < len; i++ ) {
		if ( !isNonNegativeInteger( value[i] ) ) {
			return false;
		}
	}
	return true;
} // end FUNCTION isNonNegativeIntegerArray()


// EXPORTS //

module.exports = isNonNegativeIntegerArray;

},{"validate.io-array":50,"validate.io-nonnegative-integer":55}],55:[function(require,module,exports){
/**
*
*	VALIDATE: nonnegative-integer
*
*
*	DESCRIPTION:
*		- Validates if a value is a nonnegative integer.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2015. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2015.
*
*/

'use strict';

// MODULES //

var isInteger = require( 'validate.io-integer' );


// IS NONNEGATIVE INTEGER //

/**
* FUNCTION: isNonNegativeInteger( value )
*	Validates if a value is a nonnegative integer.
*
* @param {*} value - value to be validated
* @returns {Boolean} boolean indicating if a value is a nonnegative integer
*/
function isNonNegativeInteger( value ) {
	return isInteger( value ) && value >= 0;
} // end FUNCTION isNonNegativeInteger()


// EXPORTS //

module.exports = isNonNegativeInteger;

},{"validate.io-integer":56}],56:[function(require,module,exports){
/**
*
*	VALIDATE: integer
*
*
*	DESCRIPTION:
*		- Validates if a value is an integer.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isNumber = require( 'validate.io-number' );


// ISINTEGER //

/**
* FUNCTION: isInteger( value )
*	Validates if a value is an integer.
*
* @param {Number} value - value to be validated
* @returns {Boolean} boolean indicating whether value is an integer
*/
function isInteger( value ) {
	return isNumber( value ) && value%1 === 0;
} // end FUNCTION isInteger()


// EXPORTS //

module.exports = isInteger;

},{"validate.io-number":57}],57:[function(require,module,exports){
/**
*
*	VALIDATE: number
*
*
*	DESCRIPTION:
*		- Validates if a value is a number.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

/**
* FUNCTION: isNumber( value )
*	Validates if a value is a number.
*
* @param {*} value - value to be validated
* @returns {Boolean} boolean indicating whether value is a number
*/
function isNumber( value ) {
	return ( typeof value === 'number' || Object.prototype.toString.call( value ) === '[object Number]' ) && value.valueOf() === value.valueOf();
} // end FUNCTION isNumber()


// EXPORTS //

module.exports = isNumber;

},{}],58:[function(require,module,exports){
'use strict';

/**
* FUNCTION: deepGet( obj, props )
*	Deep get a nested property.
*
* @param {Object|Array} obj - input object
* @param {Array} props - list of properties defining a key path
* @returns {*} nested property value
*/
function deepGet( obj, props ) {
	var len = props.length,
		v = obj,
		i;

	for ( i = 0; i < len; i++ ) {
		if ( typeof v === 'object' && v !== null && v.hasOwnProperty( props[i] ) ) {
			v = v[ props[i] ];
		} else {
			return;
		}
	}
	return v;
} // end FUNCTION deepGet()


// EXPORTS //

module.exports = deepGet;

},{}],59:[function(require,module,exports){
'use strict';

/**
* FUNCTION: defaults()
*	Returns default options.
*
* @returns {Object} default options
*/
function defaults() {
	return {
		'sep': '.'
	};
} // end FUNCTION defaults()


// EXPORTS //

module.exports = defaults;

},{}],60:[function(require,module,exports){
'use strict';

// MODULES //

var isString = require( 'validate.io-string-primitive' ),
	isArray = require( 'validate.io-array' ),
	validate = require( './validate.js' ),
	defaults = require( './defaults.js' ),
	dget = require( './deepget.js' );


// FACTORY //

/**
* FUNCTION: factory( path[, opts] )
*	Creates a reusable deep get factory.
*
* @param {String|Array} path - key path
* @param {Object} [opts] - function options
* @param {String} [opts.sep='.'] - key path separator
* @returns {Function} deep get factory
*/
function factory( path, options ) {
	var isStr = isString( path ),
		props,
		opts,
		err;
	if ( !isStr && !isArray( path ) ) {
		throw new TypeError( 'deepGet()::invalid input argument. Key path must be a string primitive or a key array. Value: `' + path + '`.' );
	}
	opts = defaults();
	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isStr ) {
		props = path.split( opts.sep );
	} else {
		props = path;
	}
	/**
	* FUNCTION: deepGet( obj )
	*	Deep get a nested property.
	*
	* @param {Object|Array} obj - input object
	* @returns {*} nested property value
	*/
	return function deepGet( obj ) {
		if ( typeof obj !== 'object' || obj === null ) {
			return;
		}
		return dget( obj, props );
	};
} // end FUNCTION factory()


// EXPORTS //

module.exports = factory;

},{"./deepget.js":58,"./defaults.js":59,"./validate.js":62,"validate.io-array":63,"validate.io-string-primitive":80}],61:[function(require,module,exports){
'use strict';

// MODULES //

var isString = require( 'validate.io-string-primitive' ),
	isArray = require( 'validate.io-array' ),
	validate = require( './validate.js' ),
	defaults = require( './defaults.js' ),
	dget = require( './deepget.js' );


// DEEP GET //

/**
* FUNCTION: deepGet( obj, path[, opts] )
*	Deep get a nested property.
*
* @param {Object|Array} obj - input object
* @param {String|Array} path - key path
* @param {Object} [opts] - function options
* @param {String} [opts.sep='.'] - key path separator
* @returns {*} nested property value
*/
function deepGet( obj, path, options ) {
	var isStr = isString( path ),
		props,
		opts,
		err;
	if ( typeof obj !== 'object' || obj === null ) {
		return;
	}
	if ( !isStr && !isArray( path ) ) {
		throw new TypeError( 'deepGet()::invalid input argument. Key path must be a string primitive or a key array. Value: `' + path + '`.' );
	}
	opts = defaults();
	if ( arguments.length > 2 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isStr ) {
		props = path.split( opts.sep );
	} else {
		props = path;
	}
	return dget( obj, props );
} // end FUNCTION deepGet()


// EXPORTS //

module.exports = deepGet;
module.exports.factory = require( './factory.js' );

},{"./deepget.js":58,"./defaults.js":59,"./factory.js":60,"./validate.js":62,"validate.io-array":63,"validate.io-string-primitive":80}],62:[function(require,module,exports){
'use strict';

// MODULES //

var isString = require( 'validate.io-string-primitive' ),
	isObject = require( 'validate.io-object' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for function options
* @param {Object} options - function options
* @param {String} [options.sep] - key path separator
* @returns {Error|Null} error or null
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'deepGet()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'deepGet()::invalid option. Key path separator must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-object":78,"validate.io-string-primitive":80}],63:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],64:[function(require,module,exports){
'use strict';

/**
* FUNCTION: deepSet( obj, props, create, value )
*	Deep sets a nested property.
*
* @param {Object|Array} obj - input object
* @param {Array} props - list of properties defining a key path
* @param {Boolean} create - boolean indicating whether to create a path if the key path does not already exist
* @param {*} value - value to set
* @returns {Boolean} boolean indicating if the property was successfully set
*/
function deepSet( obj, props, create, val ) {
	var len = props.length,
		bool = false,
		v = obj,
		p,
		i;

	for ( i = 0; i < len; i++ ) {
		p = props[ i ];
		if ( typeof v === 'object' && v !== null ) {
			if ( !v.hasOwnProperty( p ) ) {
				if ( create ) {
					v[ p ] = {};
				} else {
					break;
				}
			}
			if ( i === len-1 ) {
				if ( typeof val === 'function' ) {
					v[ p ] = val( v[ p ] );
				} else {
					v[ p ] = val;
				}
				bool = true;
			} else {
				v = v[ p ];
			}
		} else {
			break;
		}
	}
	return bool;
} // end FUNCTION deepSet()


// EXPORTS //

module.exports = deepSet;

},{}],65:[function(require,module,exports){
'use strict';

/**
* FUNCTION: defaults()
*	Returns default options.
*
* @returns {Object} default options
*/
function defaults() {
	return {
		'create': false,
		'sep': '.'
	};
} // end FUNCTION defaults()


// EXPORTS //

module.exports = defaults;

},{}],66:[function(require,module,exports){
'use strict';

// MODULES //

var isString = require( 'validate.io-string-primitive' ),
	isArray = require( 'validate.io-array' ),
	validate = require( './validate.js' ),
	defaults = require( './defaults.js' ),
	dset = require( './deepset.js' );


// FACTORY //

/**
* FUNCTION: factory( path[, opts] )
*	Creates a reusable deep set factory.
*
* @param {String|Array} path - key path
* @param {Object} [opts] - function options
* @param {Boolean} [opts.create=false] - boolean indicating whether to create a path if the key path does not already exist
* @param {String} [opts.sep='.'] - key path separator
* @returns {Function} deep set factory
*/
function factory( path, options ) {
	var isStr = isString( path ),
		props,
		opts,
		err;
	if ( !isStr && !isArray( path ) ) {
		throw new TypeError( 'deepSet()::invalid input argument. Key path must be a string primitive or a key array. Value: `' + path + '`.' );
	}
	opts = defaults();
	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isStr ) {
		props = path.split( opts.sep );
	} else {
		props = path;
	}
	/**
	* FUNCTION: deepSet( obj, value )
	*	Deep sets a nested property.
	*
	* @param {Object|Array} obj - input object
	* @param {*} value - value to set
	* @returns {Boolean} boolean indicating if the property was successfully set
	*/
	return function deepSet( obj, value ) {
		if ( typeof obj !== 'object' || obj === null ) {
			return false;
		}
		return dset( obj, props, opts.create, value );
	};
} // end FUNCTION factory()


// EXPORTS //

module.exports = factory;

},{"./deepset.js":64,"./defaults.js":65,"./validate.js":68,"validate.io-array":69,"validate.io-string-primitive":80}],67:[function(require,module,exports){
'use strict';

// MODULES //

var isString = require( 'validate.io-string-primitive' ),
	isArray = require( 'validate.io-array' ),
	validate = require( './validate.js' ),
	defaults = require( './defaults.js' ),
	dset = require( './deepset.js' );


// DEEP SET //

/**
* FUNCTION: deepSet( obj, path, value[, opts] )
*	Deep sets a nested property.
*
* @param {Object|Array} obj - input object
* @param {String|Array} path - key path
* @param {*} value - value to set
* @param {Object} [opts] - function options
* @param {Boolean} [opts.create=false] - boolean indicating whether to create a path if the key path does not already exist
* @param {String} [opts.sep='.'] - key path separator
* @returns {Boolean} boolean indicating if the property was successfully set
*/
function deepSet( obj, path, value, options ) {
	var isStr = isString( path ),
		props,
		opts,
		err;
	if ( typeof obj !== 'object' || obj === null ) {
		return false;
	}
	if ( !isStr && !isArray( path ) ) {
		throw new TypeError( 'deepSet()::invalid input argument. Key path must be a string primitive or a key array. Value: `' + path + '`.' );
	}
	opts = defaults();
	if ( arguments.length > 3 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isStr ) {
		props = path.split( opts.sep );
	} else {
		props = path;
	}
	return dset( obj, props, opts.create, value );
} // end FUNCTION deepSet()


// EXPORTS //

module.exports = deepSet;
module.exports.factory = require( './factory.js' );

},{"./deepset.js":64,"./defaults.js":65,"./factory.js":66,"./validate.js":68,"validate.io-array":69,"validate.io-string-primitive":80}],68:[function(require,module,exports){
'use strict';

// MODULES //

var isString = require( 'validate.io-string-primitive' ),
	isObject = require( 'validate.io-object' ),
	isBoolean = require( 'validate.io-boolean-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for function options
* @param {Object} options - function options
* @param {Boolean} [options.create] - boolean indicating whether to create a path if the key path does not already exist
* @param {String} [options.sep] - key path separator
* @returns {Error|Null} error or null
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'deepSet()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'create' ) ) {
		opts.create = options.create;
		if ( !isBoolean( opts.create ) ) {
			return new TypeError( 'deepSet()::invalid option. Create option must be a boolean primitive. Option: `' + opts.create + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'deepSet()::invalid option. Key path separator must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":73,"validate.io-object":78,"validate.io-string-primitive":80}],69:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],70:[function(require,module,exports){
'use strict';

// MODULES //

var isInteger = require( 'validate.io-integer-primitive' );


// CONSTANTS //

var MAX = require( 'compute-const-max-safe-integer' );


// IS ARRAY-LIKE //

/**
* FUNCTION: isArrayLike( value )
*	Validates if a value is array-like.
*
* @param {*} value - value to validate
* @param {Boolean} boolean indicating if a value is array-like
*/
function isArrayLike( value ) {
	return value !== void 0 && value !== null && typeof value !== 'function' && isInteger( value.length ) && value.length >= 0 && value.length <= MAX;
} // end FUNCTION isArrayLike()


// EXPORTS //

module.exports = isArrayLike;

},{"compute-const-max-safe-integer":71,"validate.io-integer-primitive":72}],71:[function(require,module,exports){
'use strict';

// EXPORTS //

module.exports = 9007199254740991; // Math.pow( 2, 53 ) - 1

},{}],72:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":77}],73:[function(require,module,exports){
/**
*
*	VALIDATE: boolean-primitive
*
*
*	DESCRIPTION:
*		- Validates if a value is a boolean primitive.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2015. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2015.
*
*/

'use strict';

/**
* FUNCTION: isBoolean( value )
*	Validates if a value is a boolean primitive.
*
* @param {*} value - value to be validated
* @returns {Boolean} boolean indicating if a value is a boolean primitive
*/
function isBoolean( value ) {
	return value === true || value === false;
} // end FUNCTION isBoolean()


// EXPORTS //

module.exports = isBoolean;

},{}],74:[function(require,module,exports){
/**
*
*	VALIDATE: function
*
*
*	DESCRIPTION:
*		- Validates if a value is a function.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

/**
* FUNCTION: isFunction( value )
*	Validates if a value is a function.
*
* @param {*} value - value to be validated
* @returns {Boolean} boolean indicating whether value is a function
*/
function isFunction( value ) {
	return ( typeof value === 'function' );
} // end FUNCTION isFunction()


// EXPORTS //

module.exports = isFunction;

},{}],75:[function(require,module,exports){
'use strict';

/**
* FUNCTION: matrixLike( value )
*	Validates if a value is matrix-like.
*
* @param {*} value - value to be validated
* @returns {Boolean} boolean indicating if a value is matrix-like
*/
function matrixLike( v ) {
	return v !== null &&
		typeof v === 'object' &&
		typeof v.data === 'object' &&
		typeof v.shape === 'object' &&
		typeof v.offset === 'number' &&
		typeof v.strides === 'object' &&
		typeof v.dtype === 'string' &&
		typeof v.length === 'number';
} // end FUNCTION matrixLike()


// EXPORTS //

module.exports = matrixLike;

},{}],76:[function(require,module,exports){
/**
*
*	VALIDATE: nan
*
*
*	DESCRIPTION:
*		- Validates if a value is NaN.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

/**
* FUNCTION: nan( value )
*	Validates if a value is not-a-number.
*
* @param {*} value - value to be validated
* @returns {Boolean} boolean indicating whether the value is a NaN
*/
function nan( value ) {
	return ( typeof value === 'number' || Object.prototype.toString.call( value ) === '[object Number]' ) && value.valueOf() !== value.valueOf();
} // end FUNCTION nan()


// EXPORTS //

module.exports = nan;

},{}],77:[function(require,module,exports){
/**
*
*	VALIDATE: number-primitive
*
*
*	DESCRIPTION:
*		- Validates if a value is a number primitive.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2015. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2015.
*
*/

'use strict';

/**
* FUNCTION: isNumber( value )
*	Validates if a value is a number primitive, excluding `NaN`.
*
* @param {*} value - value to be validated
* @returns {Boolean} boolean indicating if a value is a number primitive
*/
function isNumber( value ) {
	return (typeof value === 'number') && (value === value);
} // end FUNCTION isNumber()


// EXPORTS //

module.exports = isNumber;

},{}],78:[function(require,module,exports){
'use strict';

// MODULES //

var isArray = require( 'validate.io-array' );


// ISOBJECT //

/**
* FUNCTION: isObject( value )
*	Validates if a value is a object; e.g., {}.
*
* @param {*} value - value to be validated
* @returns {Boolean} boolean indicating whether value is a object
*/
function isObject( value ) {
	return ( typeof value === 'object' && value !== null && !isArray( value ) );
} // end FUNCTION isObject()


// EXPORTS //

module.exports = isObject;

},{"validate.io-array":79}],79:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],80:[function(require,module,exports){
/**
*
*	VALIDATE: string-primitive
*
*
*	DESCRIPTION:
*		- Validates if a value is a string primitive.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2015. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2015.
*
*/

'use strict';

/**
* FUNCTION: isString( value )
*	Validates if a value is a string primitive.
*
* @param {*} value - value to be validated
* @returns {Boolean} boolean indicating if a value is a string primitive
*/
function isString( value ) {
	return typeof value === 'string';
} // end FUNCTION isString()


// EXPORTS //

module.exports = isString;

},{}],81:[function(require,module,exports){
'use strict';

// MODULES //

var isInteger = require( 'validate.io-integer-primitive' );


// CONSTANTS //

var MAX = require( 'compute-const-max-safe-integer' );


// IS TYPED-ARRAY-LIKE //

/**
* FUNCTION: isTypedArrayLike( value )
*	Validates if a value is typed-array-like.
*
* @param {*} value - value to validate
* @param {Boolean} boolean indicating if a value is typed-array-like
*/
function isTypedArrayLike( value ) {
	return value !== null &&
		typeof value === 'object' &&
		isInteger( value.length ) &&
		value.length >= 0 &&
		value.length <= MAX &&
		typeof value.BYTES_PER_ELEMENT === 'number' &&
		typeof value.byteOffset === 'number' &&
		typeof value.byteLength === 'number';
} // end FUNCTION isTypedArrayLike()


// EXPORTS //

module.exports = isTypedArrayLike;

},{"compute-const-max-safe-integer":82,"validate.io-integer-primitive":83}],82:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],83:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":77}],84:[function(require,module,exports){
'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isObject = require( 'validate.io-object' ),
	isFunction = require( 'validate.io-function' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isNumber = require( 'validate.io-number-primitive' );


// ADD //

/**
* FUNCTION: add( arr, x[, opts] )
*	Computes an element-wise addition.
*
* @param {Number[]|Array} arr - input array
* @param {Number[]|Array|Number} x - either an array of equal length or a scalar
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating whether to return a new array
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @returns {Number[]} output array
*/
function add( arr, x, opts ) {
	var isArr = isArray( x ),
		copy = true,
		arity,
		clbk,
		out,
		len,
		i;

	if ( !isArray( arr ) ) {
		throw new TypeError( 'add()::invalid input argument. Must provide an array. Value: `' + arr + '`.' );
	}
	if ( !isArr && !isNumber( x ) ) {
		throw new TypeError( 'add()::invalid input argument. Second argument must either be an array or number primitive. Value: `' + x + '`.' );
	}
	if ( arguments.length > 2 ) {
		if ( !isObject( opts ) ) {
			throw new TypeError( 'add()::invalid input argument. Options argument must be an object. Value: `' + opts + '`.' );
		}
		if ( opts.hasOwnProperty( 'copy' ) ) {
			copy = opts.copy;
			if ( !isBoolean( copy ) ) {
				throw new TypeError( 'add()::invalid option. Copy option must be a boolean primitive. Option: `' + copy + '`.' );
			}
		}
		if ( opts.hasOwnProperty( 'accessor' ) ) {
			clbk = opts.accessor;
			if ( !isFunction( clbk ) ) {
				throw new TypeError( 'add()::invalid option. Accessor must be a function. Option: `' + clbk + '`.' );
			}
			arity = clbk.length;
		}
	}
	len = arr.length;
	if ( copy ) {
		out = new Array( len );
	} else {
		out = arr;
	}
	// Case 1: x is an array
	if ( isArr ) {
		if ( len !== x.length ) {
			throw new Error( 'add()::invalid input argument. Array to be added must have a length equal to that of the input array.' );
		}
		if ( arity === 3 ) { // clbk implied
			for ( i = 0; i < len; i++ ) {
				out[ i ] = clbk( arr[i], i, 0 ) + clbk( x[i], i, 1 );
			}
		}
		else if ( clbk ) {
			for ( i = 0; i < len; i++ ) {
				out[ i ] = clbk( arr[i], i ) + x[ i ];
			}
		}
		else {
			for ( i = 0; i < len; i++ ) {
				out[ i ] = arr[ i ] + x[ i ];
			}
		}
	}
	// Case 2: accessor and scalar
	else if ( clbk ) {
		for ( i = 0; i < len; i++ ) {
			out[ i ] = clbk( arr[i], i ) + x;
		}
	}
	// Case 3: scalar
	else {
		for ( i = 0; i < len; i++ ) {
			out[ i ] = arr[ i ] + x;
		}
	}
	return out;
} // end FUNCTION add()


// EXPORTS //

module.exports = add;

},{"validate.io-array":85,"validate.io-boolean-primitive":86,"validate.io-function":87,"validate.io-number-primitive":88,"validate.io-object":89}],85:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],86:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],87:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],88:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],89:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":85}],90:[function(require,module,exports){
/**
*
*	COMPUTE: argmax
*
*
*	DESCRIPTION:
*		- Computes the maximum value of a numeric array and returns the corresponding array indices.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

/**
* FUNCTION: argmax( arr )
*	Computes the maximum value of a numeric array and returns the corresponding array indices.
*
* @param {Array} arr - array of values
* @returns {Array} array indices
*/
function argmax( arr ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'argmax()::invalid input argument. Must provide an array.' );
	}
	var len = arr.length,
		max = arr[ 0 ],
		idx = [ 0 ],
		val;

	for ( var i = 1; i < len; i++ ) {
		val = arr[ i ];
		if ( val > max ) {
			max = val;
			idx.length = 0;
			idx.push( i );
		}
		else if ( val === max ) {
			idx.push( i );
		}
	}
	return idx;
} // end FUNCTION argmax()


// EXPORTS //

module.exports = argmax;

},{}],91:[function(require,module,exports){
/**
*
*	COMPUTE: argmin
*
*
*	DESCRIPTION:
*		- Computes the minimum value of a numeric array and returns the corresponding array indices.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

/**
* FUNCTION: argmin( arr )
*	Computes the minimum value of a numeric array and returns the corresponding array indices.
*
* @param {Array} arr - array of values
* @returns {Array} array indices
*/
function argmin( arr ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'argmin()::invalid input argument. Must provide an array.' );
	}
	var len = arr.length,
		min = arr[ 0 ],
		idx = [ 0 ],
		val;

	for ( var i = 1; i < len; i++ ) {
		val = arr[ i ];
		if ( val < min ) {
			min = val;
			idx.length = 0;
			idx.push( i );
		}
		else if ( val === min ) {
			idx.push( i );
		}
	}
	return idx;
} // end FUNCTION argmin()


// EXPORTS //

module.exports = argmin;

},{}],92:[function(require,module,exports){
/**
*
*	COMPUTE: argnanmax
*
*
*	DESCRIPTION:
*		- Computes the maximum value of an array ignoring non-numeric values and returns the corresponding array indices.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

/**
* FUNCTION: argnanmax( arr )
*	Computes the maximum value of an array ignoring non-numeric values and returns the corresponding array indices.
*
* @param {Array} arr - array of values
* @returns {Array} array indices
*/
function argnanmax( arr ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'argnanmax()::invalid input argument. Must provide an array.' );
	}
	var len = arr.length,
		max = Number.NEGATIVE_INFINITY,
		idx = [],
		val;

	for ( var i = 0; i < len; i++ ) {
		val = arr[ i ];
		if ( typeof val !== 'number' || val !== val ) {
			continue;
		}
		if ( val > max ) {
			max = val;
			idx.length = 0;
			idx.push( i );
		}
		else if ( val === max ) {
			idx.push( i );
		}
	}
	return idx;
} // end FUNCTION argnanmax()


// EXPORTS //

module.exports = argnanmax;

},{}],93:[function(require,module,exports){
/**
*
*	COMPUTE: argnanmin
*
*
*	DESCRIPTION:
*		- Computes the minimum value of an array ignoring non-numeric values and returns the corresponding array indices.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

/**
* FUNCTION: argnanmin( arr )
*	Computes the minimum value of an array ignoring non-numeric values and returns the corresponding array indices.
*
* @param {Array} arr - array of values
* @returns {Array} array indices
*/
function argnanmin( arr ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'argnanmin()::invalid input argument. Must provide an array.' );
	}
	var len = arr.length,
		min = Number.POSITIVE_INFINITY,
		idx = [],
		val;

	for ( var i = 0; i < len; i++ ) {
		val = arr[ i ];
		if ( typeof val !== 'number' || val !== val ) {
			continue;
		}
		if ( val < min ) {
			min = val;
			idx.length = 0;
			idx.push( i );
		}
		else if ( val === min ) {
			idx.push( i );
		}
	}
	return idx;
} // end FUNCTION argnanmin()


// EXPORTS //

module.exports = argnanmin;

},{}],94:[function(require,module,exports){
/**
*
*	COMPUTE: circshift
*
*
*	DESCRIPTION:
*		- Shifts array elements (or string characters) circularly.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isInteger = require( 'validate.io-integer' );


// CIRCSHIFT //

/**
* FUNCTION: circshift( x, k )
*	Shifts elements/characters circularly.
*
* @param {Array|String} x - array or string to be shifted
* @param {Number} k - integer specifying the number of positions to shift
* @param {Array|String} shifted results
*/
function circshift( x, k ) {
	var type = typeof x,
		dir = 1, // right
		len,
		i;
	if ( !Array.isArray( x ) && type !== 'string' ) {
		throw new TypeError( 'circshift()::invalid input argument. Must provide either an array or string to shift.' );
	}
	if ( !isInteger( k ) ) {
		throw new TypeError( 'circshift()::invalid input argument. Second argument must be an integer.' );
	}
	if ( type === 'string' ) {
		x = x.split( '' );
	}
	len = x.length;
	if ( k < 0 ) {
		// Get the equivalent positive number of positions...
		k = len + k; // len - |x|
	}
	// We only care about the remainder when we circularly shift. k === len means the elements stay in place.
	k = k % len;

	// Determine the direction which requires the fewest operations...
	if ( k > len/2 ) {
		dir = 0; // left
		k = len - k;
	}
	if ( dir ) {
		// Pop an element off the end and move to the front...
		for ( i = 0; i < k; i++ ) {
			x.unshift( x.pop() );
		}
	} else {
		// Shift an element off the front and move to the end...
		for ( i = 0; i < k; i++ ) {
			x.push( x.shift() );
		}
	}
	if ( type === 'string' ) {
		x = x.join( '' );
	}
	return x;
} // end FUNCTION circshift()


// EXPORTS //

module.exports = circshift;

},{"validate.io-integer":95}],95:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":96}],96:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],97:[function(require,module,exports){
/**
*
*	COMPUTE: cmax
*
*
*	DESCRIPTION:
*		- Computes the cumulative maximum of a numeric array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

/**
* FUNCTION: cmax( arr )
*	Computes the cumulative maximum of a numeric array.
*
* @param {Array} arr - numeric array
* @returns {Array} cumulative max
*/
function cmax( arr ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'cmax()::invalid input argument. Must provide an array.' );
	}
	var len = arr.length,
		v = new Array( len ),
		max;

	max = arr[ 0 ];
	v[ 0 ] = max;
	for ( var i = 1; i < len; i++ ) {
		if ( arr[ i ] > max ) {
			max = arr[ i ];
		}
		v[ i ] = max;
	}
	return v;
} // end FUNCTION cmax()


// EXPORTS //

module.exports = cmax;

},{}],98:[function(require,module,exports){
/**
*
*	COMPUTE: cmin
*
*
*	DESCRIPTION:
*		- Computes the cumulative minimum of a numeric array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

/**
* FUNCTION: cmin( arr )
*	Computes the cumulative minimum of a numeric array.
*
* @param {Array} arr - numeric array
* @returns {Array} cumulative min
*/
function cmin( arr ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'cmin()::invalid input argument. Must provide an array.' );
	}
	var len = arr.length,
		v = new Array( len ),
		min;

	min = arr[ 0 ];
	v[ 0 ] = min;
	for ( var i = 1; i < len; i++ ) {
		if ( arr[ i ] < min ) {
			min = arr[ i ];
		}
		v[ i ] = min;
	}
	return v;
} // end FUNCTION cmin()


// EXPORTS //

module.exports = cmin;

},{}],99:[function(require,module,exports){
/**
*
*	COMPUTE: covariance
*
*
*	DESCRIPTION:
*		- Computes the covariance between one or more numeric arrays.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isObject = require( 'validate.io-object' );


// COVARIANCE //

/**
* FUNCTION: covariance( arr1[, arr2,...,opts] )
*	Computes the covariance between one or more numeric arrays.
*
* @param {...Array} arr - numeric array
* @param {Object} [opts] - function options
* @param {Boolean} [opts.bias] - boolean indicating whether to calculate a biased or unbiased estimate of the covariance (default: false)
* @returns {Array} covariance matrix
*/
function covariance() {
	var bias = false,
		args,
		opts,
		nArgs,
		len,
		deltas,
		delta,
		means,
		C,
		cov,
		arr,
		N, r, A, B, sum, val,
		i, j, n;

	args = Array.prototype.slice.call( arguments );
	nArgs = args.length;

	if ( isObject( args[nArgs-1] ) ) {
		opts = args.pop();
		nArgs = nArgs - 1;
		if ( opts.hasOwnProperty( 'bias' ) ) {
			if ( typeof opts.bias !== 'boolean' ) {
				throw new TypeError( 'covariance()::invalid input argument. Bias option must be a boolean.' );
			}
			bias = opts.bias;
		}
	}
	if ( !nArgs ) {
		throw new Error( 'covariance()::insufficient input arguments. Must provide array arguments.' );
	}
	for ( i = 0; i < nArgs; i++ ) {
		if ( !Array.isArray( args[i] ) ) {
			throw new TypeError( 'covariance()::invalid input argument. Must provide array arguments.' );
		}
	}
	if ( Array.isArray( args[0][0] ) ) {
		// If the first argument is an array of arrays, calculate the covariance over the nested arrays, disregarding any other arguments...
		args = args[ 0 ];
	}
	nArgs = args.length;
	len = args[ 0 ].length;
	for ( i = 1; i < nArgs; i++ ) {
		if ( args[i].length !== len ) {
			throw new Error( 'covariance()::invalid input argument. All arrays must have equal length.' );
		}
	}
	// [0] Initialization...
	deltas = new Array( nArgs );
	means = new Array( nArgs );
	C = new Array( nArgs );
	cov = new Array( nArgs );
	for ( i = 0; i < nArgs; i++ ) {
		means[ i ] = args[ i ][ 0 ];
		arr = new Array( nArgs );
		for ( j = 0; j < nArgs; j++ ) {
			arr[ j ] = 0;
		}
		C[ i ] = arr;
		cov[ i ] = arr.slice(); // copy!
	}
	if ( len < 2 ) {
		return cov;
	}
	// [1] Compute the covariance...
	for ( n = 1; n < len; n++ ) {

		N = n + 1;
		r = n / N;

		// [a] Extract the values and compute the deltas...
		for ( i = 0; i < nArgs; i++ ) {
			deltas[ i ] = args[ i ][ n ] - means[ i ];
		}

		// [b] Update the covariance between one array and every other array...
		for ( i = 0; i < nArgs; i++ ) {
			arr = C[ i ];
			delta = deltas[ i ];
			for ( j = i; j < nArgs; j++ ) {
				A = arr[ j ];
				B = r * delta * deltas[ j ];
				sum = A + B;
				// Exploit the fact that the covariance matrix is symmetric...
				if ( i !== j ) {
					C[ j ][ i ] = sum;
				}
				arr[ j ] = sum;
			} // end FOR j
		} // end FOR i

		// [c] Update the means...
		for ( i = 0; i < nArgs; i++ ) {
			means[ i ] += deltas[ i ] / N;
		}
	} // end FOR n

	// [2] Normalize the co-moments...
	n = N - 1;
	if ( bias ) {
		n = N;
	}
	for ( i = 0; i < nArgs; i++ ) {
		arr = C[ i ];
		for ( j = i; j < nArgs; j++ ) {
			val = arr[ j ] / n;
			cov[ i ][ j ] = val;
			if ( i !== j ) {
				cov[ j ][ i ] = val;
			}
		}
	}
	return cov;
} // end FUNCTION covariance()


// EXPORTS //

module.exports = covariance;

},{"validate.io-object":100}],100:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":101}],101:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],102:[function(require,module,exports){
'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isObject = require( 'validate.io-object' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' );


// CUMULATIVE PRODUCT //

/**
* FUNCTION: cprod( arr[, options] )
*	Computes the cumulative product of an array.
*
* @param {Array} arr - input array
* @param {Object} [options] - function options
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {Boolean} [options.copy=true] - boolean indicating whether to return new array
* @returns {Number[]} cumulative product array
*/
function cprod( arr, opts ) {
	var copy = true,
		clbk;
	if ( !isArray( arr ) ) {
		throw new TypeError( 'cprod()::invalid input argument. Must provide an array. Value: `' + arr + '`.' );
	}
	if ( arguments.length > 1 ) {
		if ( !isObject( opts ) ) {
			throw new TypeError( 'cprod()::invalid input argument. Options argument must be an object. Value: `' + opts + '`.' );
		}
		if ( opts.hasOwnProperty( 'accessor' ) ) {
			clbk = opts.accessor;
			if ( !isFunction ( clbk ) ) {
				throw new TypeError( 'cprod()::invalid option. Accessor must be a function. Value: `' + clbk + '`.' );
			}
		}
		if ( opts.hasOwnProperty( 'copy' ) ) {
			copy = opts.copy;
			if ( !isBoolean( copy ) ) {
				throw new TypeError( 'cprod()::invalid option. Copy option must be a boolean primitive. Value: `' + copy + '`.' );
			}
		}
	}
	var len = arr.length,
		out,
		v,
		i;

	if ( copy === true ) {
		out = new Array( len );
	} else {
		out = arr;
	}
	if ( clbk ) {
		out[ 0 ] = clbk( arr[ 0 ], 0 );
		for ( i = 1; i < len; i++ ) {
			v = out[ i-1 ];
			if ( v === 0 ) {
				out[ i ] = 0;
			} else {
				out[ i ] = v * clbk( arr[ i ], i );
			}
		}
	} else {
		out[ 0 ] = arr[ 0 ];
		for ( i = 1; i < len; i++ ) {
			out[ i ] = out[ i-1 ] * arr[ i ];
		}
	}
	return out;
} // end FUNCTION cprod()


// EXPORTS //

module.exports = cprod;

},{"validate.io-array":103,"validate.io-boolean-primitive":104,"validate.io-function":105,"validate.io-object":106}],103:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],104:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],105:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],106:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":103}],107:[function(require,module,exports){
/**
*
*	COMPUTE: cross
*
*
*	DESCRIPTION:
*		- Computes the cross product between two numeric arrays.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// CROSS PRODUCT //

/**
* FUNCTION: cross( x, y )
*	Computes the cross product between two numeric arrays.
*
* @param {Array} x - numeric array
* @param {Array} y - numeric array
* @returns {Number} cross product
*/
function cross( x, y ) {
	if ( !Array.isArray( x ) ) {
		throw new TypeError( 'cross()::invalid input argument. First argument must be an array.' );
	}
	if ( !Array.isArray( y ) ) {
		throw new TypeError( 'cross()::invalid input argument. Second argument must be an array.' );
	}
	if ( x.length !== 3 || y.length !== 3 ) {
		throw new Error( 'cross()::invalid input argument. Input arrays must be of length 3.' );
	}
	return [
		x[1]*y[2]-x[2]*y[1],
		x[2]*y[0]-x[0]*y[2],
		x[0]*y[1]-x[1]*y[0]
	];
} // end FUNCTION cross()


// EXPORTS //

module.exports = cross;

},{}],108:[function(require,module,exports){
/**
*
*	COMPUTE: csum
*
*
*	DESCRIPTION:
*		- Computes the cumulative sum of a numeric array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

/**
* FUNCTION: csum( arr )
*	Computes the cumulative sum of a numeric array.
*
* @param {Array} arr - numeric array
* @returns {Array} cumulative sum
*/
function csum( arr ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'csum()::invalid input argument. Must provide an array.' );
	}
	var len = arr.length,
		v = new Array( len );

	v[ 0 ] = arr[ 0 ];
	for ( var i = 1; i < len; i++ ) {
		v[ i ] = v[ i-1 ] + arr[ i ];
	}
	return v;
} // end FUNCTION csum()


// EXPORTS //

module.exports = csum;

},{}],109:[function(require,module,exports){
/**
*
*	COMPUTE: datespace
*
*
*	DESCRIPTION:
*		- Generates an array of linearly spaced dates.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isInteger = require( 'validate.io-integer' );


// VARIABLES //

var timestamp = /^\d{10}$|^\d{13}$/,
	rounders = [ 'floor', 'ceil', 'round' ];


// FUNCTIONS //

/**
* FUNCTION: validDate( value, name )
*	Validates a date parameter.
*
* @private
* @param {*} value - value to be validated
* @param {String} name - name to be used in error messages
* @returns {Date} validated date
*/
function validDate( value, name ) {
	var type;

	type = typeof value;
	if ( type === 'string' ) {
		value = Date.parse( value );
		if ( value !== value ) {
			throw new Error( 'datespace()::invalid input argument. Unable to parse ' +  name.toLowerCase() + ' date.' );
		}
		value = new Date( value );
	}
	if ( type === 'number' ) {
		if ( !timestamp.test( value ) ) {
			throw new Error( 'datespace()::invalid input argument. Numeric ' + name.toLowerCase() + ' date must be either a Unix or Javascript timestamp.' );
		}
		if ( value.toString().length === 10 ) {
			value = value * 1000; // sec to ms
		}
		value = new Date( value );
	}
	if ( !(value instanceof Date) ) {
		throw new TypeError( 'datespace()::invalid input argument. ' + name + ' date must either be a date string, Date object, Unix timestamp, or JavaScript timestamp.' );
	}
	return value;
} // end FUNCTION validDate()


// DATESPACE //

/**
* FUNCTION: datespace( start, stop[, length, options])
*	Generates an array of linearly spaced dates.
*
* @param {Date|Number|String} start - start time as either a `Date` object, Unix timestamp, JavaScript timestamp, or date string
* @param {Data|Number|String} stop - stop time as either a `Date` object, Unix timestamp, JavaScript timestamp, or date string
* @param {Number} [length] - output array length (default: 100)
* @param {Object} [options] - function options
* @param {String} [options.round] - specifies how sub-millisecond times should be rounded: [ 'floor', 'ceil', 'round' ] (default: 'floor' )
* @returns {Array} array of dates
*/
function datespace( start, stop, length, options ) {
	var nArgs = arguments.length,
		opts = {
			'round': 'floor'
		},
		len = 100,
		flg = true,
		round,
		end,
		d,
		tmp,
		arr;

	start = validDate( start, 'Start' );
	stop = validDate( stop, 'Stop' );

	if ( nArgs > 2 ) {
		if ( nArgs === 3 ) {
			if ( isObject( length ) ) {
				opts = length;
			} else {
				len = length;

				// Turn off checking the options object...
				flg = false;
			}
		} else {
			opts = options;
			len = length;
		}
		if ( len === 0 ) {
			return [];
		}
		if ( !isInteger( len ) || len < 0 ) {
			throw new TypeError( 'datespace()::invalid input argument. Length must a positive integer.' );
		}
		if ( flg ) {
			if ( !isObject( opts ) ) {
				throw new TypeError( 'datespace()::invalid input argument. Options must be an object.' );
			}
			if ( opts.hasOwnProperty( 'round' ) ) {
				if ( typeof opts.round !== 'string' ) {
					throw new TypeError( 'datespace()::invalid input argument. Round option must be a string.' );
				}
				if ( rounders.indexOf( opts.round ) === -1 ) {
					throw new Error( 'datespace()::invalid input argument. Unrecognized round option. Must be one of [' + rounders.join( ',' ) + '].' );
				}
			} else {
				opts.round = 'floor';
			}
		}
	}
	round = Math[ opts.round ];

	// Calculate the increment...
	end = len - 1;
	d = ( stop.getTime() - start.getTime() ) / end;

	// Build the output array...
	arr = new Array( len );
	tmp = start;
	arr[ 0 ] = tmp;
	tmp = tmp.getTime();
	for ( var i = 1; i < end; i++ ) {
		tmp += d;
		arr[ i ] = new Date( round( tmp ) );
	}
	arr[ end ] = stop;
	return arr;
} // end FUNCTION datespace()


// EXPORTS //

module.exports = datespace;

},{"validate.io-integer":110,"validate.io-object":112}],110:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":111}],111:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],112:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":113}],113:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],114:[function(require,module,exports){
/**
*
*	COMPUTE: deg2rad
*
*
*	DESCRIPTION:
*		- Converts degrees to radians.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// DEGREES-TO-RADIANS //

/**
* FUNCTION: deg2rad( x )
*	Converts degrees to radians. Note: if provided an array, the array is mutated.
*
* @param {Array|Number} x - value(s) to be converted to radians
* @returns {Array|Number|Null} radian value(s). If `x` is an empty `array`, returns `null`.
*/
function deg2rad( x ) {
	var isArray = Array.isArray( x ),
		len;
	if ( !isArray && ( typeof x !== 'number' || x !== x ) ) {
		throw new TypeError( 'deg2rad()::invalid input argument. Must provide either a single numeric value or a numeric array.' );
	}
	if ( !isArray ) {
		return x * Math.PI / 180;
	}
	len = x.length;
	if ( !len ) {
		return null;
	}
	for ( var i = 0; i < len; i++ ) {
		x[ i ] *= Math.PI / 180;
	}
	return x;
} // end FUNCTION deg2rad()


// EXPORTS //

module.exports = deg2rad;

},{}],115:[function(require,module,exports){
/**
*
*	COMPUTE: diff
*
*
*	DESCRIPTION:
*		- Computes the differences between adjacent elements in an array of values.
*
*
*	NOTES:
*		[1] 
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

(function() {
	'use strict';

	// DIFF /

	/**
	* FUNCTION: diff( arr )
	*	Calculates the differences between adjacent elements in an array.
	*
	* @param {Array} arr - array of numerical values
	* @returns {Array} array of differences
	*/
	function diff( arr ) {
		if ( !Array.isArray( arr ) ) {
			throw new TypeError( 'diff()::invalid input argument. Must provide an array.' );
		}
		var len = arr.length,
			d = new Array( len-1 );

		for ( var i = 0; i < len-1; i++ ) {
			d[ i ] = arr[ i ] - arr[ i+1 ];
		}
		return d;
	} // end FUNCTION diff()


	// EXPORTS //

	module.exports = diff;

})();
},{}],116:[function(require,module,exports){
/**
*
*	COMPUTE: dims
*
*
*	DESCRIPTION:
*		- Computes array dimensions.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isInteger = require( 'validate.io-integer' );


// DIMS //

/**
* FUNCTION: dims( arr, d, max )
*	Computes array dimensions.
*
* @private
* @param {Array} arr - input array
* @param {Array} d - dimensions array
* @param {Number} max - max number of dimensions
* @returns {Array} dimensions array
*/
function dims( arr, d, max ) {
	if ( max && d.length === max ) {
		return;
	}
	if ( !Array.isArray( arr[0] ) ) {
		return;
	}
	d.push( arr[0].length );
	dims( arr[ 0 ], d, max );
} // end FUNCTION dims()

/**
* FUNCTION: check( arr, d )
*	Checks that all array elements have the same dimensions.
*
* @private
* @param {Array} arr - input array
* @param {Array} d - dimensions array
* @returns {Boolean} boolean indicating if all array elements have the same dimensions
*/
function check( arr, d ) {
	var len = arr.length,
		dim = d.shift(),
		nDims = d.length,
		val,
		flg;

	for ( var i = 0; i < len; i++ ) {
		val = arr[ i ];
		if ( !Array.isArray( val ) || val.length !== dim ) {
			return false;
		}
		if ( nDims ) {
			flg = check( val, d.slice() );
			if ( !flg ) {
				return false;
			}
		}
	}
	return true;
} // end FUNCTION check()

/**
* FUNCTION: compute( arr[, max] )
*	Computes array dimensions.
*
* @param {Array} arr - input array
* @param {Number} [max] - limits the number of dimensions returned
* @returns {Array|null} array of dimensions or null
*/
function compute( arr, max ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'dims()::invalid input argument. Must provide an array.' );
	}
	if ( arguments.length > 1 ) {
		if ( !isInteger( max ) || max < 1 ) {
			throw new TypeError( 'dims()::invalid input argument. `max` option must be a positive integer.' );
		}
	}
	var d, flg;

	// [0] Initialize the dimensions array:
	d = [ arr.length ];

	// [1] Recursively determine array dimensions:
	dims( arr, d, max );

	// [2] Check that all array element dimensions are consistent...
	if ( d.length > 1 ) {
		flg = check( arr, d.slice( 1 ) );
		if ( !flg ) {
			return null;
		}
	}
	return d;
} // end FUNCTION compute()


// EXPORTS //

module.exports = compute;

},{"validate.io-integer":117}],117:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":118}],118:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],119:[function(require,module,exports){
'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isObject = require( 'validate.io-object' ),
	isFunction = require( 'validate.io-function' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isNumber = require( 'validate.io-number-primitive' );


// DIVIDE //

/**
* FUNCTION: divide( arr, x[, opts] )
*	Computes an element-wise division.
*
* @param {Number[]|Array} arr - input array
* @param {Number[]|Array|Number} x - either an array of equal length or a scalar
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating whether to return a new array
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @returns {Number[]} output array
*/
function divide( arr, x, opts ) {
	var isArr = isArray( x ),
		copy = true,
		arity,
		clbk,
		out,
		len,
		i;

	if ( !isArray( arr ) ) {
		throw new TypeError( 'divide()::invalid input argument. Must provide an array. Value: `' + arr + '`.' );
	}
	if ( !isArr && !isNumber( x ) ) {
		throw new TypeError( 'divide()::invalid input argument. Second argument must either be an array or number primitive. Value: `' + x + '`.' );
	}
	if ( arguments.length > 2 ) {
		if ( !isObject( opts ) ) {
			throw new TypeError( 'divide()::invalid input argument. Options argument must be an object. Value: `' + opts + '`.' );
		}
		if ( opts.hasOwnProperty( 'copy' ) ) {
			copy = opts.copy;
			if ( !isBoolean( copy ) ) {
				throw new TypeError( 'divide()::invalid option. Copy option must be a boolean primitive. Option: `' + copy + '`.' );
			}
		}
		if ( opts.hasOwnProperty( 'accessor' ) ) {
			clbk = opts.accessor;
			if ( !isFunction( clbk ) ) {
				throw new TypeError( 'divide()::invalid option. Accessor must be a function. Option: `' + clbk + '`.' );
			}
			arity = clbk.length;
		}
	}
	len = arr.length;
	if ( copy ) {
		out = new Array( len );
	} else {
		out = arr;
	}
	// Case 1: x is an array
	if ( isArr ) {
		if ( len !== x.length ) {
			throw new Error( 'divide()::invalid input argument. Array to be divideed must have a length equal to that of the input array.' );
		}
		if ( arity === 3 ) { // clbk implied
			for ( i = 0; i < len; i++ ) {
				out[ i ] = clbk( arr[i], i, 0 ) / clbk( x[i], i, 1 );
			}
		}
		else if ( clbk ) {
			for ( i = 0; i < len; i++ ) {
				out[ i ] = clbk( arr[i], i ) / x[ i ];
			}
		}
		else {
			for ( i = 0; i < len; i++ ) {
				out[ i ] = arr[ i ] / x[ i ];
			}
		}
	}
	// Case 2: accessor and scalar
	else if ( clbk ) {
		for ( i = 0; i < len; i++ ) {
			out[ i ] = clbk( arr[i], i ) / x;
		}
	}
	// Case 3: scalar
	else {
		for ( i = 0; i < len; i++ ) {
			out[ i ] = arr[ i ] / x;
		}
	}
	return out;
} // end FUNCTION divide()


// EXPORTS //

module.exports = divide;

},{"validate.io-array":120,"validate.io-boolean-primitive":121,"validate.io-function":122,"validate.io-number-primitive":123,"validate.io-object":124}],120:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],121:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],122:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],123:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],124:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":120}],125:[function(require,module,exports){
'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isFunction = require( 'validate.io-function' );


// DOT PRODUCT //

/**
* FUNCTION: dot( x, y[, accessor] )
*	Computes the dot product between two arrays.
*
* @param {Array} x - input array
* @param {Array} y - input array
* @param {Function} [accessor] - accessor function for accessing array values
* @returns {Number|Null} dot product
*/
function dot( x, y, clbk ) {
	if ( !isArray( x ) ) {
		throw new TypeError( 'dot()::invalid input argument. First argument must be an array. Value: `' + x + '`.' );
	}
	if ( !isArray( y ) ) {
		throw new TypeError( 'dot()::invalid input argument. Second argument must be an array. Value: `' + y + '`.' );
	}
	if ( arguments.length > 2 ) {
		if ( !isFunction( clbk ) ) {
			throw new TypeError( 'dot()::invalid input argument. Accessor must be a function. Value: `' + clbk + '`.' );
		}
	}
	var len = x.length,
		sum = 0,
		i;

	if ( len !== y.length ) {
		throw new Error( 'dot()::invalid input argument. Arrays must be of equal length.' );
	}
	if ( !len ) {
		return null;
	}
	if ( clbk ) {
		for ( i = 0; i < len; i++ ) {
			sum += clbk( x[ i ], i, 0 ) * clbk( y[ i ], i, 1 );
		}
	} else {
		for ( i = 0; i < len; i++ ) {
			sum += x[ i ] * y[ i ];
		}
	}
	return sum;
} // end FUNCTION dot()


// EXPORTS //

module.exports = dot;

},{"validate.io-array":126,"validate.io-function":127}],126:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],127:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],128:[function(require,module,exports){
/**
*
*	COMPUTE: eq
*
*
*	DESCRIPTION:
*		- Computes an element-wise comparison (equality) of an array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isObject = require( 'validate.io-object' );


// EQUAL //

/**
* FUNCTION: eq( arr, x[, opts] )
*	Computes an element-wise comparison (equality) of an array.
*
* @param {Array} arr - input array
* @param {*} x - comparator
* @param {Object} [opts] - function options
* @param {Boolean} [opts.strict] - option indicating whether to enforce type equality (default: true)
* @param {Boolean} [opts.array] - option indicating whether to not perform element-by-element comparison when provided arrays of equal length (default: false)
* @returns {Array} array of 1s and 0s, where a `1` indicates that an input array element is equal to a compared value and `0` indicates that an input array element is not equal to a compared value
*/
function eq( arr, x, opts ) {
	var isArray = Array.isArray( x ),
		strict = true,
		arrCompare = false,
		out,
		len,
		i;

	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'eq()::invalid input argument. Must provide an array.' );
	}
	if ( arguments.length > 2 ) {
		if ( !isObject( opts ) ) {
			throw new TypeError( 'eq()::invalid input argument. Options must be an object.' );
		}
		if ( opts.hasOwnProperty( 'strict' ) ) {
			strict = opts.strict;
			if ( typeof strict !== 'boolean' ) {
				throw new TypeError( 'eq()::invalid input argument. Strict option must be a boolean.' );
			}
		}
		if ( opts.hasOwnProperty( 'array' ) ) {
			arrCompare = opts.array;
			if ( typeof arrCompare !== 'boolean' ) {
				throw new TypeError( 'eq()::invalid input argument. Array option must be a boolean.' );
			}
		}
	}
	len = arr.length;
	out = new Array( len );
	if ( strict ) {
		if ( !isArray || x.length !== len || arrCompare ) {
			for ( i = 0; i < len; i++ ) {
				if ( arr[ i ] === x ) {
					out[ i ] = 1;
				} else {
					out[ i ] = 0;
				}
			}
			return out;
		}
		for ( i = 0; i < len; i++ ) {
			if ( arr[ i ] === x[ i ] ) {
				out[ i ] = 1;
			} else {
				out[ i ] = 0;
			}
		}
		return out;
	}
	if ( !isArray || x.length !== len || arrCompare ) {
		for ( i = 0; i < len; i++ ) {
			/* jshint eqeqeq:false */
			if ( arr[ i ] == x ) {
				out[ i ] = 1;
			} else {
				out[ i ] = 0;
			}
		}
		return out;
	}
	for ( i = 0; i < len; i++ ) {
		/* jshint eqeqeq:false */
		if ( arr[ i ] == x[ i ] ) {
			out[ i ] = 1;
		} else {
			out[ i ] = 0;
		}
	}
	return out;
} // end FUNCTION eq()


// EXPORTS //

module.exports = eq;

},{"validate.io-object":129}],129:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":130}],130:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],131:[function(require,module,exports){
'use strict';

// FUNCTIONS

var ERF = require( './number.js' );


// ERROR FUNCTION //

/**
* FUNCTION: erf( out, arr, accessor )
*	Computes the error function for each array element using an accessor function.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function erf( y, x, clbk ) {
	var len = x.length,
		v, i;
	for ( i = 0; i < len; i++ ) {
		v = clbk( x[ i ], i );
		if ( typeof v === 'number' ) {
			y[ i ] = ERF( v );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION erf()


// EXPORTS //

module.exports = erf;

},{"./number.js":136}],132:[function(require,module,exports){
'use strict';

// MODULES //

var ERF = require( './number.js' );


// ERROR FUNCTION //

/**
* FUNCTION: erf( out, arr )
*	Computes the error function for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function erf( y, x ) {
	var len = x.length,
		i;
	for ( i = 0; i < len; i++ ) {
		if ( typeof x[ i ] === 'number' ) {
			y[ i ] = ERF( x[ i ] );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION erf()


// EXPORTS //

module.exports = erf;

},{"./number.js":136}],133:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory,
	ERF = require( './number.js' );


// ERROR FUNCTION //

/**
* FUNCTION: erf( arr, path[, sep] )
*	Computes the error function for each array element and deep sets the input array.
*
* @param {Array} arr - input array
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function erf( x, path, sep ) {
	var len = x.length,
		opts = {},
		dget,
		dset,
		v, i;
	if ( arguments.length > 2 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		for ( i = 0; i < len; i++ ) {
			v = dget( x[ i ] );
			if ( typeof v === 'number' ) {
				dset( x[ i ], ERF( v ) );
			} else {
				dset( x[ i ], NaN );
			}
		}
	}
	return x;
} // end FUNCTION erf()


// EXPORTS //

module.exports = erf;

},{"./number.js":136,"utils-deep-get":189,"utils-deep-set":195}],134:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isnan = require( 'validate.io-nan' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var erf1 = require( './number.js' ),
	erf2 = require( './array.js' ),
	erf3 = require( './accessor.js' ),
	erf4 = require( './deepset.js' ),
	erf5 = require( './matrix.js' ),
	erf6 = require( './typedarray.js' );


// ERROR FUNCTION //

/**
* FUNCTION: erf( x[, opts] )
*	Computes the error function.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} x - input value
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} error function value(s)
*/
function erf( x, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( isNumber( x ) || isnan( x ) ) {
		return erf1( x );
	}
	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isMatrixLike( x ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'erf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( x.length );
			out = matrix( d, x.shape, dt );
		} else {
			out = x;
		}
		return erf5( out, x );
	}
	if ( isTypedArrayLike( x ) ) {
		if ( opts.copy === false ) {
			out = x;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'erf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( x.length );
		}
		return erf6( out, x );
	}
	if ( isArrayLike( x ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return erf4( x, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = x;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new Error( 'erf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( x.length );
		}
		else {
			out = new Array( x.length );
		}
		if ( opts.accessor ) {
			return erf3( out, x, opts.accessor );
		}
		return erf2( out, x );
	}
	return NaN;
} // end FUNCTION erf()


// EXPORTS //

module.exports = erf;

},{"./accessor.js":131,"./array.js":132,"./deepset.js":133,"./matrix.js":135,"./number.js":136,"./typedarray.js":137,"./validate.js":138,"compute-array-constructors":140,"dstructs-matrix":149,"validate.io-array-like":198,"validate.io-matrix-like":203,"validate.io-nan":204,"validate.io-number-primitive":205,"validate.io-typed-array-like":209}],135:[function(require,module,exports){
'use strict';

// MODULES //

var ERF = require( './number.js' );


// ERROR FUNCTION //

/**
* FUNCTION: erf( out, matrix )
*	Evaluates the error function for each matrix element.
*
* @param {Matrix} out - output matrix
* @param {Matrix} matrix - input matrix
* @returns {Matrix} output matrix
*/
function erf( y, x ) {
	var len = x.length,
		i;
	if ( y.length !== len ) {
		throw new Error( 'erf()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	for ( i = 0; i < len; i++ ) {
		y.data[ i ] = ERF( x.data[ i ] );
	}
	return y;
} // end FUNCTION erf()


// EXPORTS //

module.exports = erf;

},{"./number.js":136}],136:[function(require,module,exports){
'use strict';

/**
* NOTE: the following copyright and license, as well as the long comment were part of the original implementation available as part of [FreeBSD]{@link https://svnweb.freebsd.org/base/release/9.3.0/lib/msun/src/s_erf.c?revision=268523&view=co}.
*
* The implementation follows the original, but has been modified for JavaScript.
*/

/**
* ===========================
* Copyright (C) 1993 by Sun Microsystems, Inc. All rights reserved.
*
* Developed at SunPro, a Sun Microsystems, Inc business.
* Permission to use, copy, modify, and distribute this software is freely granted, provided that this notice is preserved.
* ===========================
*/

/**
* double erf(double x)
*                               x
*                      2       |\
*       erf(x) = -----------   | exp(-t*t)dt
*                   sqrt(pi)  \|
*                              0
*
*		erfc(x) =  1-erf(x)
*   Note that
*		erf(-x) = -erf(x)
*		erfc(-x) = 2 - erfc(x)
*
* Method:
*	1. For |x| in [0, 0.84375)
*       erf(x)  = x + x*R(x^2)
*       erfc(x) = 1 - erf(x)           if x in [-.84375,0.25]
*               = 0.5 + ((0.5-x)-x*R)  if x in [0.25,0.84375]
*       where R = P/Q where P is an odd poly of degree 8 and Q is an odd poly of degree 10.
*                                  -57.90
*           | R - (erf(x)-x)/x | <= 2
*
*
*       Remark. The formula is derived by noting
*           erf(x) = (2/sqrt(pi))*(x - x^3/3 + x^5/10 - x^7/42 + ....)
*       and that
*           2/sqrt(pi) = 1.128379167095512573896158903121545171688
*       is close to one. The interval is chosen because the fix point of erf(x) is near 0.6174 (i.e., erf(x)=x when x is near 0.6174), and by some experiment, 0.84375 is chosen to guarantee the error is less than one ulp for erf.
*
*   2. For |x| in [0.84375,1.25), let s = |x| - 1, and c = 0.84506291151 rounded to single (24 bits)
*       erf(x)  = sign(x) * (c + P1(s)/Q1(s))
*       erfc(x) = (1-c) - P1(s)/Q1(s) if x > 0
*           1+(c+P1(s)/Q1(s))         if x < 0
*           |P1/Q1 - (erf(|x|)-c)| <= 2**-59.06
*   Remark: here we use the taylor series expansion at x=1.
*       erf(1+s) = erf(1) + s*Poly(s)
*                = 0.845.. + P1(s)/Q1(s)
*   That is, we use rational approximation to approximate
*       erf(1+s) - (c = (single)0.84506291151)
*   Note that |P1/Q1|< 0.078 for x in [0.84375,1.25] where
*       P1(s) = degree 6 poly in s
*       Q1(s) = degree 6 poly in s
*
*   3. For x in [1.25,1/0.35(~2.857143)),
*       erfc(x) = (1/x)*exp(-x*x-0.5625+R1/S1)
*       erf(x)  = 1 - erfc(x)
*   where
*       R1(z) = degree 7 poly in z, (z=1/x^2)
*       S1(z) = degree 8 poly in z
*
*   4. For x in [1/0.35,28]
*       erfc(x) = (1/x)*exp(-x*x-0.5625+R2/S2)       if x > 0
*               = 2.0 - (1/x)*exp(-x*x-0.5625+R2/S2) if -6 < x < 0
*               = 2.0 - tiny                         if x <= -6
*       erf(x)  = sign(x)*(1.0 - erfc(x))            if x < 6, else
*       erf(x)  = sign(x)*(1.0 - tiny)
*   where
*       R2(z) = degree 6 poly in z, (z=1/x^2)
*       S2(z) = degree 7 poly in z
*
*   Note1:
*       To compute exp(-x*x-0.5625+R/S), let s be a single precision number and s := x; then
*           -x*x = -s*s + (s-x)*(s+x)
*           exp(-x*x-0.5626+R/S) = exp(-s*s-0.5625)*exp((s-x)*(s+x)+R/S);
*   Note2:
*       Here 4 and 5 make use of the asymptotic series
*                   exp(-x*x)
*       erfc(x) ~  ----------- * ( 1 + Poly(1/x^2) )
*                   x*sqrt(pi)
*       We use rational approximation to approximate
*           g(s) = f(1/x^2) = log(erfc(x)*x) - x*x + 0.5625
*       Here is the error bound for R1/S1 and R2/S2
*           |R1/S1 - f(x)| < 2**(-62.57)
*           |R2/S2 - f(x)| < 2**(-61.52)
*
*   5. For inf > x >= 28
*       erf(x)  = sign(x) * (1 - tiny)   (raise inexact)
*       erfc(x) = tiny*tiny              (raise underflow) if x > 0
*               = 2 - tiny               if x<0
*
*   6. Special cases:
*       erf(0)  = 0, erf(inf)  = 1, erf(-inf) = -1,
*       erfc(0) = 1, erfc(inf) = 0, erfc(-inf) = 2,
*       erfc/erf(NaN) is NaN
*/

// CONSTANTS //

var INF = Number.POSITIVE_INFINITY,
	NINF = Number.NEGATIVE_INFINITY,

	TINY = 1e-300,
	SMALL = 1.0 / (1 << 28 ), /* 2**-28; equiv is Math.pow( 2, -28 ) */
	ERX = 8.45062911510467529297e-1, /* 0x3FEB0AC1, 0x60000000 */

	// Coefficients for approximation to erf on [0, 0.84375)
	EFX = 1.28379167095512586316e-1, /* 0x3FC06EBA, 0x8214DB69 */
	EFX8 = 1.02703333676410069053, /* 0x3FF06EBA, 0x8214DB69 */
	PP0 = 1.28379167095512558561e-1, /* 0x3FC06EBA, 0x8214DB68 */
	PP1 = -3.25042107247001499370e-1, /* 0xBFD4CD7D, 0x691CB913 */
	PP2 = -2.84817495755985104766e-2, /* 0xBF9D2A51, 0xDBD7194F */
	PP3 = -5.77027029648944159157e-3, /* 0xBF77A291, 0x236668E4 */
	PP4 = -2.37630166566501626084e-5, /* 0xBEF8EAD6, 0x120016AC */
	QQ1 = 3.97917223959155352819e-1, /* 0x3FD97779, 0xCDDADC09 */
	QQ2 = 6.50222499887672944485e-2, /* 0x3FB0A54C, 0x5536CEBA */
	QQ3 = 5.08130628187576562776e-3, /* 0x3F74D022, 0xC4D36B0F */
	QQ4 = 1.32494738004321644526e-4, /* 0x3F215DC9, 0x221C1A10 */
	QQ5 = -3.96022827877536812320e-6, /* 0xBED09C43, 0x42A26120 */

	// Coefficients for approximation to erf on [0.84375, 1.25)
	PA0 = -2.36211856075265944077e-3, /* 0xBF6359B8, 0xBEF77538 */
	PA1 = 4.14856118683748331666e-1, /* 0x3FDA8D00, 0xAD92B34D */
	PA2 = -3.72207876035701323847e-1, /* 0xBFD7D240, 0xFBB8C3F1 */
	PA3 = 3.18346619901161753674e-1, /* 0x3FD45FCA, 0x805120E4 */
	PA4 = -1.10894694282396677476e-1, /* 0xBFBC6398, 0x3D3E28EC */
	PA5 = 3.54783043256182359371e-2, /* 0x3FA22A36, 0x599795EB */
	PA6 = -2.16637559486879084300e-3, /* 0xBF61BF38, 0x0A96073F */
	QA1 = 1.06420880400844228286e-1, /* 0x3FBB3E66, 0x18EEE323 */
	QA2 = 5.40397917702171048937e-1, /* 0x3FE14AF0, 0x92EB6F33 */
	QA3 = 7.18286544141962662868e-2, /* 0x3FB2635C, 0xD99FE9A7 */
	QA4 = 1.26171219808761642112e-1, /* 0x3FC02660, 0xE763351F */
	QA5 = 1.36370839120290507362e-2, /* 0x3F8BEDC2, 0x6B51DD1C */
	QA6 = 1.19844998467991074170e-2, /* 0x3F888B54, 0x5735151D */

	// Coefficients for approximation to erfc on [1.25, 1/0.35)
	RA0 = -9.86494403484714822705e-3, /* 0xBF843412, 0x600D6435 */
	RA1 = -6.93858572707181764372e-1, /* 0xBFE63416, 0xE4BA7360 */
	RA2 = -1.05586262253232909814e1, /* 0xC0251E04, 0x41B0E726 */
	RA3 = -6.23753324503260060396e1, /* 0xC04F300A, 0xE4CBA38D */
	RA4 = -1.62396669462573470355e2, /* 0xC0644CB1, 0x84282266 */
	RA5 = -1.84605092906711035994e2, /* 0xC067135C, 0xEBCCABB2 */
	RA6 = -8.12874355063065934246e1, /* 0xC0545265, 0x57E4D2F2 */
	RA7 = -9.81432934416914548592, /* 0xC023A0EF, 0xC69AC25C */
	SA1 = 1.96512716674392571292e1, /* 0x4033A6B9, 0xBD707687 */
	SA2 = 1.37657754143519042600e2, /* 0x4061350C, 0x526AE721 */
	SA3 = 4.34565877475229228821e2, /* 0x407B290D, 0xD58A1A71 */
	SA4 = 6.45387271733267880336e2, /* 0x40842B19, 0x21EC2868 */
	SA5 = 4.29008140027567833386e2, /* 0x407AD021, 0x57700314 */
	SA6 = 1.08635005541779435134e2, /* 0x405B28A3, 0xEE48AE2C */
	SA7 = 6.57024977031928170135, /* 0x401A47EF, 0x8E484A93 */
	SA8 = -6.04244152148580987438e-2, /* 0xBFAEEFF2, 0xEE749A62 */

	// Coefficients for approximation to erfc on [1/0.35, 28]
	RB0 = -9.86494292470009928597e-3, /* 0xBF843412, 0x39E86F4A */
	RB1 = -7.99283237680523006574e-1, /* 0xBFE993BA, 0x70C285DE */
	RB2 = -1.77579549177547519889e1, /* 0xC031C209, 0x555F995A */
	RB3 = -1.60636384855821916062e2, /* 0xC064145D, 0x43C5ED98 */
	RB4 = -6.37566443368389627722e2, /* 0xC083EC88, 0x1375F228 */
	RB5 = -1.02509513161107724954e3, /* 0xC0900461, 0x6A2E5992 */
	RB6 = -4.83519191608651397019e2, /* 0xC07E384E, 0x9BDC383F */
	SB1 = 3.03380607434824582924e1, /* 0x403E568B, 0x261D5190 */
	SB2 = 3.25792512996573918826e2, /* 0x40745CAE, 0x221B9F0A */
	SB3 = 1.53672958608443695994e3, /* 0x409802EB, 0x189D5118 */
	SB4 = 3.19985821950859553908e3, /* 0x40A8FFB7, 0x688C246A */
	SB5 = 2.55305040643316442583e3, /* 0x40A3F219, 0xCEDF3BE6 */
	SB6 = 4.74528541206955367215e2, /* 0x407DA874, 0xE79FE763 */
	SB7 = -2.24409524465858183362e1; /* 0xC03670E2, 0x42712D62 */


// VARIABLES //

var EXP = Math.exp;


// ERF //

/**
* FUNCTION: erf( x )
*	Evaluates the error function for an input value.
*
* @param {Number} x - input value
* @returns {Number} evaluated error function
*/
function erf( x ) {
	var sign = false,
		tmp,
		z, r, s, y, p, q;

	// [1] Special cases...

	// NaN:
	if ( x !== x ) {
		return NaN;
	}
	// Positive infinity:
	if ( x === INF ) {
		return 1;
	}
	// Negative infinity:
	if ( x === NINF ) {
		return -1;
	}

	// [2] Get the sign:
	if ( x < 0 ) {
		x = -x;
		sign = true;
	}

	// [3] |x| < 0.84375
	if ( x < 0.84375 ) {
		if ( x < SMALL ) {
			if ( x < TINY ) {
				// Avoid underflow:
				tmp = 0.125 * (8.0*x + EFX8*x );
			} else {
				tmp = x + EFX*x;
			}
		} else {
			z = x * x;
			// Horner's method: http://en.wikipedia.org/wiki/Horner's_method
			r = PP0 + z*(PP1+z*(PP2+z*(PP3+z*PP4)));
			s = 1.0 + z*(QQ1+z*(QQ2+z*(QQ3+z*(QQ4+z*QQ5))));
			y = r / s;
			tmp = x + x*y;
		}
		if ( sign ) {
			return -tmp;
		}
		return tmp;
	}

	// [4] 0.84375 <= |x| < 1.25
	if ( x < 1.25 ) {
		s = x - 1;
		p = PA0 + s*(PA1+s*(PA2+s*(PA3+s*(PA4+s*(PA5+s*PA6)))));
		q = 1 + s*(QA1+s*(QA2+s*(QA3+s*(QA4+s*(QA5+s*QA6)))));
		if ( sign ) {
			return -ERX - p/q;
		}
		return ERX + p/q;
	}

	// [5] INF > |x| >=6
	if ( x >= 6 ) {
		if ( sign ) {
			return TINY - 1;
		}
		return 1 - TINY;
	}

	s = 1 / (x*x);

	// [6] |x| < 1 / 0.35 ~2.857143
	if ( x < 1/0.35 ) {
		r = RA0 + s*(RA1+s*(RA2+s*(RA3+s*(RA4+s*(RA5+s*(RA6+s*RA7))))));
		s = 1 + s*(SA1+s*(SA2+s*(SA3+s*(SA4+s*(SA5+s*(SA6+s*(SA7+s*SA8)))))));
	} else { // [7] |x| >= 1/0.35 ~2.857143
		r = RB0 + s*(RB1+s*(RB2+s*(RB3+s*(RB4+s*(RB5+s*RB6)))));
		s = 1 + s*(SB1+s*(SB2+s*(SB3+s*(SB4+s*(SB5+s*(SB6+s*SB7))))));
	}
	z = x & 0xffffffff00000000; // pseudo-single (20-bit) precision x;
	r = EXP( -z*z - 0.5625 ) * EXP( (z-x)*(z+x) + r/s );
	if ( sign ) {
		return r/x - 1;
	}
	return 1 - r/x;
} // end FUNCTION erf()


// EXPORTS //

module.exports = erf;

},{}],137:[function(require,module,exports){
'use strict';

// MODULES //

var ERF = require( './number.js' );


// ERROR FUNCTION //

/**
* FUNCTION: erf( out, arr )
*	Computes the error function for each typed-array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} arr - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function erf( y, x ) {
	var len = x.length,
		i;
	for ( i = 0; i < len; i++ ) {
		y[ i ] = ERF( x[ i ] );
	}
	return y;
} // end FUNCTION erf()


// EXPORTS //

module.exports = erf;

},{"./number.js":136}],138:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Boolean} [options.copy] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'erf()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'erf()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'erf()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'erf()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'erf()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'erf()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":201,"validate.io-function":202,"validate.io-object":206,"validate.io-string-primitive":208}],139:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],140:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":139,"dup":12}],141:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],142:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":145,"./iget.js":147,"./iset.js":150,"./mget.js":154,"./mset.js":156,"./set.js":164,"./sget.js":166,"./sset.js":168,"./toString.js":170,"dup":14}],143:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":146,"./iget.raw.js":148,"./iset.raw.js":151,"./mget.raw.js":155,"./mset.raw.js":157,"./set.raw.js":165,"./sget.raw.js":167,"./sset.raw.js":169,"./toString.js":170,"dup":15}],144:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],145:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":183}],146:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],147:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":181}],148:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],149:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":152,"./matrix.raw.js":153,"dup":21}],150:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":181,"validate.io-number-primitive":205}],151:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],152:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":141,"./ctor.js":142,"./dtypes.js":144,"compute-cast-arrays":171,"compute-dtype":174,"dup":24,"validate.io-array":178,"validate.io-contains":179,"validate.io-nonnegative-integer-array":182,"validate.io-string-primitive":208}],153:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":141,"./ctor.raw.js":143,"./dtypes.js":144,"compute-dtype":174,"dup":25,"validate.io-contains":179,"validate.io-string-primitive":208}],154:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":141,"dup":26,"validate.io-nonnegative-integer-array":182}],155:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":141,"dup":27}],156:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":158,"./mset2.js":159,"./mset3.js":160,"./mset4.js":161,"./mset5.js":162,"./mset6.js":163,"dup":28,"validate.io-function":202,"validate.io-nonnegative-integer-array":182,"validate.io-number-primitive":205}],157:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":158,"./mset2.js":159,"./mset3.js":160,"./mset4.js":161,"./mset5.js":162,"./mset6.js":163,"dup":29}],158:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],159:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],160:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],161:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],162:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],163:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],164:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":183,"validate.io-number-primitive":205}],165:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],166:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":141,"compute-indexspace":534,"dup":38,"validate.io-string-primitive":208}],167:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":141,"compute-indexspace":534,"dup":39}],168:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":534,"dup":40,"validate.io-function":202,"validate.io-number-primitive":205,"validate.io-string-primitive":208}],169:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":534,"dup":41}],170:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],171:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":139,"compute-array-dtype/lib/dtypes":172,"dup":43,"type-name":173,"validate.io-array-like":198}],172:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],173:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],174:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":176,"dup":46,"type-name":177}],175:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],176:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":175,"dup":48}],177:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],178:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],179:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":178,"validate.io-nan-primitive":180}],180:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],181:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":205}],182:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":178,"validate.io-nonnegative-integer":183}],183:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":184}],184:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":185}],185:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],186:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],187:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],188:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":186,"./defaults.js":187,"./validate.js":190,"dup":60,"validate.io-array":191,"validate.io-string-primitive":208}],189:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":186,"./defaults.js":187,"./factory.js":188,"./validate.js":190,"dup":61,"validate.io-array":191,"validate.io-string-primitive":208}],190:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":206,"validate.io-string-primitive":208}],191:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],192:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],193:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],194:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":192,"./defaults.js":193,"./validate.js":196,"dup":66,"validate.io-array":197,"validate.io-string-primitive":208}],195:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":192,"./defaults.js":193,"./factory.js":194,"./validate.js":196,"dup":67,"validate.io-array":197,"validate.io-string-primitive":208}],196:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":201,"validate.io-object":206,"validate.io-string-primitive":208}],197:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],198:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":199,"dup":70,"validate.io-integer-primitive":200}],199:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],200:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":205}],201:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],202:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],203:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],204:[function(require,module,exports){
arguments[4][76][0].apply(exports,arguments)
},{"dup":76}],205:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],206:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":207}],207:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],208:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],209:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":210,"dup":81,"validate.io-integer-primitive":211}],210:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],211:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":205}],212:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var ERFC = require( './number.js' );


// COMPLEMENTARY ERROR FUNCTION //

/**
* FUNCTION: erfc( out, arr, accessor )
*	Computes the complementary error function for each array element using an accessor function.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function erfc( out, x, clbk ) {
	var len = x.length,
		v, i;
	for ( i = 0; i < len; i++ ) {
		v = clbk( x[ i ], i );
		if ( typeof v === 'number' ) {
			out[ i ] = ERFC( v );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION erfc()


// EXPORTS //

module.exports = erfc;

},{"./number.js":217}],213:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var ERFC = require( './number.js' );


// COMPLEMENTARY ERROR FUNCTION //

/**
* FUNCTION: erfc( out, arr )
*	Computes the complementary error function for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function erfc( out, x ) {
	var len = x.length,
		i;
	for ( i = 0; i < len; i++ ) {
		if ( typeof x[ i ] === 'number' ) {
			out[ i ] = ERFC( x[ i ] );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION erfc()


// EXPORTS //

module.exports = erfc;

},{"./number.js":217}],214:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory;


// FUNCTIONS //

var ERFC = require( './number.js' );


// COMPLEMENTARY ERROR FUNCTION //

/**
* FUNCTION: erfc( arr, path[, sep] )
*	Computes the complementary error function for each array element and deep sets the input array.
*
* @param {Array} arr - input array
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function erfc( x, path, sep ) {
	var len = x.length,
		opts = {},
		dget,
		dset,
		v, i;
	if ( arguments.length > 2 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		for ( i = 0; i < len; i++ ) {
			v = dget( x[ i ] );
			if ( typeof v === 'number' ) {
				dset( x[i], ERFC( v ) );
			} else {
				dset( x[i], NaN );
			}
		}
	}
	return x;
} // end FUNCTION erfc()


// EXPORTS //

module.exports = erfc;

},{"./number.js":217,"utils-deep-get":270,"utils-deep-set":276}],215:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isnan = require( 'validate.io-nan' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var erfc1 = require( './number.js' ),
	erfc2 = require( './array.js' ),
	erfc3 = require( './accessor.js' ),
	erfc4 = require( './deepset.js' ),
	erfc5 = require( './matrix.js' ),
	erfc6 = require( './typedarray.js' );


// COMPLEMENTARY ERROR FUNCTION //

/**
* FUNCTION: erfc( x[, opts] )
*	Computes the complementary error function.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} x - input value
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} complementary error function value(s)
*/
function erfc( x, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( isNumber( x ) || isnan( x ) ) {
		return erfc1( x );
	}
	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isMatrixLike( x ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'erfc()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( x.length );
			out = matrix( d, x.shape, dt );
		} else {
			out = x;
		}
		return erfc5( out, x );
	}
	if ( isTypedArrayLike( x ) ) {
		if ( opts.copy === false ) {
			out = x;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'erfc()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( x.length );
		}
		return erfc6( out, x );
	}
	if ( isArrayLike( x ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return erfc4( x, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = x;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'erfc()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( x.length );
		}
		else {
			out = new Array( x.length );
		}
		if ( opts.accessor ) {
			return erfc3( out, x, opts.accessor );
		}
		return erfc2( out, x );
	}
	return NaN;
} // end FUNCTION erfc()


// EXPORTS //

module.exports = erfc;

},{"./accessor.js":212,"./array.js":213,"./deepset.js":214,"./matrix.js":216,"./number.js":217,"./typedarray.js":218,"./validate.js":219,"compute-array-constructors":221,"dstructs-matrix":230,"validate.io-array-like":279,"validate.io-matrix-like":284,"validate.io-nan":285,"validate.io-number-primitive":286,"validate.io-typed-array-like":290}],216:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var ERFC = require( './number.js' );


// COMPLEMENTARY ERROR FUNCTION //

/**
* FUNCTION: erfc( out, x )
*	Evaluates the complementary error function for each matrix element.
*
* @param {Matrix} out - output matrix
* @param {Matrix} x - input matrix
* @returns {Matrix} output matrix
*/
function erfc( out, x ) {
	var len = x.length,
		i;
	if ( out.length !== len ) {
		throw new Error( 'erfc()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	for ( i = 0; i < len; i++ ) {
		out.data[ i ] = ERFC( x.data[ i ] );
	}
	return out;
} // end FUNCTION erfc()


// EXPORTS //

module.exports = erfc;

},{"./number.js":217}],217:[function(require,module,exports){
'use strict';

/**
* NOTE: the following copyright and license, as well as the long comment were part of the original implementation available as part of [FreeBSD]{@link https://svnweb.freebsd.org/base/release/9.3.0/lib/msun/src/s_erf.c?revision=268523&view=co}.
*
* The implementation follows the original, but has been modified for JavaScript.
*/

/**
* ===========================
* Copyright (C) 1993 by Sun Microsystems, Inc. All rights reserved.
*
* Developed at SunPro, a Sun Microsystems, Inc business.
* Permission to use, copy, modify, and distribute this software is freely granted, provided that this notice is preserved.
* ===========================
*/

/**
* double erfc(double x)
*                               x
*                      2       |\
*       erf(x) = -----------   | exp(-t*t)dt
*                   sqrt(pi)  \|
*                              0
*
*       erfc(x) =  1 - erf(x)
*   Note that
*       erf(-x) = -erf(x)
*       erfc(-x) = 2 - erfc(x)
*
* Method:
*   1. For |x| in [0, 0.84375)
*       erf(x)  = x + x*R(x^2)
*       erfc(x) = 1 - erf(x)           if x in [-.84375,0.25]
*               = 0.5 + ((0.5-x)-x*R)  if x in [0.25,0.84375]
*       where R = P/Q where P is an odd poly of degree 8 and Q is an odd poly of degree 10.
*                                  -57.90
*           | R - (erf(x)-x)/x | <= 2
*
*
*       Remark. The formula is derived by noting
*           erf(x) = (2/sqrt(pi))*(x - x^3/3 + x^5/10 - x^7/42 + ....)
*       and that
*           2/sqrt(pi) = 1.128379167095512573896158903121545171688
*       is close to one. The interval is chosen because the fix point of erf(x) is near 0.6174 (i.e., erf(x)=x when x is near 0.6174), and by some experiment, 0.84375 is chosen to guarantee the error is less than one ulp for erf.
*
*   2. For |x| in [0.84375,1.25), let s = |x| - 1, and c = 0.84506291151 rounded to single (24 bits)
*       erf(x)  = sign(x) * (c  + P1(s)/Q1(s))
*       erfc(x) = (1-c) - P1(s)/Q1(s)      if x > 0
*           1+(c+P1(s)/Q1(s))              if x < 0
*           |P1/Q1 - (erf(|x|)-c)|         <= 2**-59.06
*   Remark: here we use the taylor series expansion at x=1.
*       erf(1+s) = erf(1) + s*Poly(s)
*                = 0.845.. + P1(s)/Q1(s)
*   That is, we use rational approximation to approximate
*       erf(1+s) - (c = (single)0.84506291151)
*   Note that |P1/Q1|< 0.078 for x in [0.84375,1.25] where
*       P1(s) = degree 6 poly in s
*       Q1(s) = degree 6 poly in s
*
*   3. For x in [1.25,1/0.35(~2.857143)),
*       erfc(x) = (1/x)*exp(-x*x-0.5625+R1/S1)
*       erf(x)  = 1 - erfc(x)
*   where
*       R1(z) = degree 7 poly in z, (z=1/x^2)
*       S1(z) = degree 8 poly in z
*
*   4. For x in [1/0.35,28]
*       erfc(x) = (1/x)*exp(-x*x-0.5625+R2/S2)       if x > 0
*               = 2.0 - (1/x)*exp(-x*x-0.5625+R2/S2) if -6 < x < 0
*               = 2.0 - tiny                         if x <= -6
*       erf(x)  = sign(x)*(1.0 - erfc(x))            if x < 6, else
*       erf(x)  = sign(x)*(1.0 - tiny)
*   where
*       R2(z) = degree 6 poly in z, (z=1/x^2)
*       S2(z) = degree 7 poly in z
*
*   Note1:
*       To compute exp(-x*x-0.5625+R/S), let s be a single precision number and s := x; then
*           -x*x = -s*s + (s-x)*(s+x)
*           exp(-x*x-0.5626+R/S) = exp(-s*s-0.5625)*exp((s-x)*(s+x)+R/S);
*   Note2:
*       Here 4 and 5 make use of the asymptotic series
*                   exp(-x*x)
*       erfc(x) ~  ----------- * ( 1 + Poly(1/x^2) )
*                   x*sqrt(pi)
*       We use rational approximation to approximate
*           g(s)=f(1/x^2) = log(erfc(x)*x) - x*x + 0.5625
*       Here is the error bound for R1/S1 and R2/S2
*           |R1/S1 - f(x)| < 2**(-62.57)
*           |R2/S2 - f(x)| < 2**(-61.52)
*
*   5. For inf > x >= 28
*       erf(x)  = sign(x) * (1 - tiny)  (raise inexact)
*       erfc(x) = tiny*tiny             (raise underflow) if x > 0
*               = 2 - tiny              if x < 0
*
*   6. Special case:
*       erf(0)  = 0, erf(inf)  = 1, erf(-inf) = -1,
*       erfc(0) = 1, erfc(inf) = 0, erfc(-inf) = 2,
*       erfc/erf(NaN) is NaN
*/

// CONSTANTS //

var INF = Number.POSITIVE_INFINITY,
	NINF = Number.NEGATIVE_INFINITY,

	TINY = 1e-300,
	SMALL = 1.0 / (1 << 56 ), /* 2**-56; equiv is Math.pow( 2, -56 ) */
	ERX = 8.45062911510467529297e-1, /* 0x3FEB0AC1, 0x60000000 */

	// Coefficients for approximation to erfc on [0, 0.84375)
	PP0 = 1.28379167095512558561e-1, /* 0x3FC06EBA, 0x8214DB68 */
	PP1 = -3.25042107247001499370e-1, /* 0xBFD4CD7D, 0x691CB913 */
	PP2 = -2.84817495755985104766e-2, /* 0xBF9D2A51, 0xDBD7194F */
	PP3 = -5.77027029648944159157e-3, /* 0xBF77A291, 0x236668E4 */
	PP4 = -2.37630166566501626084e-5, /* 0xBEF8EAD6, 0x120016AC */
	QQ1 = 3.97917223959155352819e-1, /* 0x3FD97779, 0xCDDADC09 */
	QQ2 = 6.50222499887672944485e-2, /* 0x3FB0A54C, 0x5536CEBA */
	QQ3 = 5.08130628187576562776e-3, /* 0x3F74D022, 0xC4D36B0F */
	QQ4 = 1.32494738004321644526e-4, /* 0x3F215DC9, 0x221C1A10 */
	QQ5 = -3.96022827877536812320e-6, /* 0xBED09C43, 0x42A26120 */

	// Coefficients for approximation to erfc on [0.84375, 1.25)
	PA0 = -2.36211856075265944077e-3, /* 0xBF6359B8, 0xBEF77538 */
	PA1 = 4.14856118683748331666e-1, /* 0x3FDA8D00, 0xAD92B34D */
	PA2 = -3.72207876035701323847e-1, /* 0xBFD7D240, 0xFBB8C3F1 */
	PA3 = 3.18346619901161753674e-1, /* 0x3FD45FCA, 0x805120E4 */
	PA4 = -1.10894694282396677476e-1, /* 0xBFBC6398, 0x3D3E28EC */
	PA5 = 3.54783043256182359371e-2, /* 0x3FA22A36, 0x599795EB */
	PA6 = -2.16637559486879084300e-3, /* 0xBF61BF38, 0x0A96073F */
	QA1 = 1.06420880400844228286e-1, /* 0x3FBB3E66, 0x18EEE323 */
	QA2 = 5.40397917702171048937e-1, /* 0x3FE14AF0, 0x92EB6F33 */
	QA3 = 7.18286544141962662868e-2, /* 0x3FB2635C, 0xD99FE9A7 */
	QA4 = 1.26171219808761642112e-1, /* 0x3FC02660, 0xE763351F */
	QA5 = 1.36370839120290507362e-2, /* 0x3F8BEDC2, 0x6B51DD1C */
	QA6 = 1.19844998467991074170e-2, /* 0x3F888B54, 0x5735151D */

	// Coefficients for approximation to erfc on [1.25, 1/0.35)
	RA0 = -9.86494403484714822705e-3, /* 0xBF843412, 0x600D6435 */
	RA1 = -6.93858572707181764372e-1, /* 0xBFE63416, 0xE4BA7360 */
	RA2 = -1.05586262253232909814e1, /* 0xC0251E04, 0x41B0E726 */
	RA3 = -6.23753324503260060396e1, /* 0xC04F300A, 0xE4CBA38D */
	RA4 = -1.62396669462573470355e2, /* 0xC0644CB1, 0x84282266 */
	RA5 = -1.84605092906711035994e2, /* 0xC067135C, 0xEBCCABB2 */
	RA6 = -8.12874355063065934246e1, /* 0xC0545265, 0x57E4D2F2 */
	RA7 = -9.81432934416914548592, /* 0xC023A0EF, 0xC69AC25C */
	SA1 = 1.96512716674392571292e1, /* 0x4033A6B9, 0xBD707687 */
	SA2 = 1.37657754143519042600e2, /* 0x4061350C, 0x526AE721 */
	SA3 = 4.34565877475229228821e2, /* 0x407B290D, 0xD58A1A71 */
	SA4 = 6.45387271733267880336e2, /* 0x40842B19, 0x21EC2868 */
	SA5 = 4.29008140027567833386e2, /* 0x407AD021, 0x57700314 */
	SA6 = 1.08635005541779435134e2, /* 0x405B28A3, 0xEE48AE2C */
	SA7 = 6.57024977031928170135, /* 0x401A47EF, 0x8E484A93 */
	SA8 = -6.04244152148580987438e-2, /* 0xBFAEEFF2, 0xEE749A62 */

	// Coefficients for approximation to erfc on [1/0.35, 28]
	RB0 = -9.86494292470009928597e-3, /* 0xBF843412, 0x39E86F4A */
	RB1 = -7.99283237680523006574e-1, /* 0xBFE993BA, 0x70C285DE */
	RB2 = -1.77579549177547519889e1, /* 0xC031C209, 0x555F995A */
	RB3 = -1.60636384855821916062e2, /* 0xC064145D, 0x43C5ED98 */
	RB4 = -6.37566443368389627722e2, /* 0xC083EC88, 0x1375F228 */
	RB5 = -1.02509513161107724954e3, /* 0xC0900461, 0x6A2E5992 */
	RB6 = -4.83519191608651397019e2, /* 0xC07E384E, 0x9BDC383F */
	SB1 = 3.03380607434824582924e1, /* 0x403E568B, 0x261D5190 */
	SB2 = 3.25792512996573918826e2, /* 0x40745CAE, 0x221B9F0A */
	SB3 = 1.53672958608443695994e3, /* 0x409802EB, 0x189D5118 */
	SB4 = 3.19985821950859553908e3, /* 0x40A8FFB7, 0x688C246A */
	SB5 = 2.55305040643316442583e3, /* 0x40A3F219, 0xCEDF3BE6 */
	SB6 = 4.74528541206955367215e2, /* 0x407DA874, 0xE79FE763 */
	SB7 = -2.24409524465858183362e1; /* 0xC03670E2, 0x42712D62 */


// VARIABLES //

var EXP = Math.exp;


// ERFC //

/**
* FUNCTION: erfc( x )
*	Evaluates the complementary error function for an input value.
*
* @param {Number} x - input value
* @returns {Number} evaluated complementary error function
*/
function erfc( x ) {
	var sign = false,
		tmp,
		z, r, s, y, p, q;

	// [1] Special cases...

	// NaN:
	if ( x !== x ) {
		return NaN;
	}
	// Positive infinity:
	if ( x === INF ) {
		return 0;
	}
	// Negative infinity:
	if ( x === NINF ) {
		return 2;
	}

	// [2] Get the sign:
	if ( x < 0 ) {
		x = -x;
		sign = true;
	}

	// [3] |x| < 0.84375
	if ( x < 0.84375 ) {
		// |x| < 2**-56
		if ( x < SMALL ) {
			tmp = x;
		} else {
			z = x * x;
			r = PP0 + z*(PP1+z*(PP2+z*(PP3+z*PP4)));
			s = 1 + z*(QQ1+z*(QQ2+z*(QQ3+z*(QQ4+z*QQ5))));
			y = r / s;
			if ( x < 0.25 ) { // |x| < 1/4
				tmp = x + x*y;
			} else {
				tmp = 0.5 + (x*y + (x-0.5));
			}
		}
		if ( sign ) {
			return 1 + tmp;
		}
		return 1 - tmp;
	}

	// [4] 0.84375 <= |x| < 1.25
	if ( x < 1.25 ) {
		s = x - 1;
		p = PA0 + s*(PA1+s*(PA2+s*(PA3+s*(PA4+s*(PA5+s*PA6)))));
		q = 1 + s*(QA1+s*(QA2+s*(QA3+s*(QA4+s*(QA5+s*QA6)))));
		if ( sign ) {
			return 1 + ERX + p/q;
		}
		return 1 - ERX - p/q;
	}

	// [5] |x| < 28
	if ( x < 28 ) {
		s = 1 / (x*x);
		// |x| < 1/0.35 ~ 2.857143
		if ( x < 1/0.35 ) {
			r = RA0 + s*(RA1+s*(RA2+s*(RA3+s*(RA4+s*(RA5+s*(RA6+s*RA7))))));
			s = 1 + s*(SA1+s*(SA2+s*(SA3+s*(SA4+s*(SA5+s*(SA6+s*SA7))))));
		} else { // |x| >= 1/0.35 ~ 2.857143
			if ( sign && x > 6 ) { // x < -6
				return 2 - TINY;
			}
			r = RB0 + s*(RB1+s*(RB2+s*(RB3+s*(RB4+s*(RB5+s*RB6)))));
			s = 1 + s*(SB1+s*(SB2+s*(SB3+s*(SB4+s*(SB5+s*(SB6+s*SB7))))));
		}
		z = x & 0xffffffff00000000; // pseudo-single (20-bit) precision x;
		r = EXP( -z*z - 0.5625 ) * EXP( (z-x)*(z+x) + r/s );
		if ( sign ) {
			return 2 - r/x;
		}
		return r/x;
	}

	if ( sign ) {
		return 2 - TINY; // ~2
	}
	return TINY * TINY; // ~0
} // end FUNCTION erfc()


// EXPORTS

module.exports = erfc;

},{}],218:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var ERFC = require( './number.js' );


// COMPLEMENTARY ERROR FUNCTION //

/**
* FUNCTION: erfc( out, arr )
*	Computes the complementary error function for each typed-array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} arr - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function erfc( out, x ) {
	var len = x.length,
		i;
	for ( i = 0; i < len; i++ ) {
		out[ i ] = ERFC( x[ i ] );
	}
	return out;
} // end FUNCTION erfc()


// EXPORTS //

module.exports = erfc;

},{"./number.js":217}],219:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Boolean} [options.copy] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'erfc()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'erfc()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'erfc()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'erfc()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'erfc()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'erfc()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":282,"validate.io-function":283,"validate.io-object":287,"validate.io-string-primitive":289}],220:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],221:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":220,"dup":12}],222:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],223:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":226,"./iget.js":228,"./iset.js":231,"./mget.js":235,"./mset.js":237,"./set.js":245,"./sget.js":247,"./sset.js":249,"./toString.js":251,"dup":14}],224:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":227,"./iget.raw.js":229,"./iset.raw.js":232,"./mget.raw.js":236,"./mset.raw.js":238,"./set.raw.js":246,"./sget.raw.js":248,"./sset.raw.js":250,"./toString.js":251,"dup":15}],225:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],226:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":264}],227:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],228:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":262}],229:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],230:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":233,"./matrix.raw.js":234,"dup":21}],231:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":262,"validate.io-number-primitive":286}],232:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],233:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":222,"./ctor.js":223,"./dtypes.js":225,"compute-cast-arrays":252,"compute-dtype":255,"dup":24,"validate.io-array":259,"validate.io-contains":260,"validate.io-nonnegative-integer-array":263,"validate.io-string-primitive":289}],234:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":222,"./ctor.raw.js":224,"./dtypes.js":225,"compute-dtype":255,"dup":25,"validate.io-contains":260,"validate.io-string-primitive":289}],235:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":222,"dup":26,"validate.io-nonnegative-integer-array":263}],236:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":222,"dup":27}],237:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":239,"./mset2.js":240,"./mset3.js":241,"./mset4.js":242,"./mset5.js":243,"./mset6.js":244,"dup":28,"validate.io-function":283,"validate.io-nonnegative-integer-array":263,"validate.io-number-primitive":286}],238:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":239,"./mset2.js":240,"./mset3.js":241,"./mset4.js":242,"./mset5.js":243,"./mset6.js":244,"dup":29}],239:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],240:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],241:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],242:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],243:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],244:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],245:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":264,"validate.io-number-primitive":286}],246:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],247:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":222,"compute-indexspace":534,"dup":38,"validate.io-string-primitive":289}],248:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":222,"compute-indexspace":534,"dup":39}],249:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":534,"dup":40,"validate.io-function":283,"validate.io-number-primitive":286,"validate.io-string-primitive":289}],250:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":534,"dup":41}],251:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],252:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":220,"compute-array-dtype/lib/dtypes":253,"dup":43,"type-name":254,"validate.io-array-like":279}],253:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],254:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],255:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":257,"dup":46,"type-name":258}],256:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],257:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":256,"dup":48}],258:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],259:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],260:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":259,"validate.io-nan-primitive":261}],261:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],262:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":286}],263:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":259,"validate.io-nonnegative-integer":264}],264:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":265}],265:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":266}],266:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],267:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],268:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],269:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":267,"./defaults.js":268,"./validate.js":271,"dup":60,"validate.io-array":272,"validate.io-string-primitive":289}],270:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":267,"./defaults.js":268,"./factory.js":269,"./validate.js":271,"dup":61,"validate.io-array":272,"validate.io-string-primitive":289}],271:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":287,"validate.io-string-primitive":289}],272:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],273:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],274:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],275:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":273,"./defaults.js":274,"./validate.js":277,"dup":66,"validate.io-array":278,"validate.io-string-primitive":289}],276:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":273,"./defaults.js":274,"./factory.js":275,"./validate.js":277,"dup":67,"validate.io-array":278,"validate.io-string-primitive":289}],277:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":282,"validate.io-object":287,"validate.io-string-primitive":289}],278:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],279:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":280,"dup":70,"validate.io-integer-primitive":281}],280:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],281:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":286}],282:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],283:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],284:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],285:[function(require,module,exports){
arguments[4][76][0].apply(exports,arguments)
},{"dup":76}],286:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],287:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":288}],288:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],289:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],290:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":291,"dup":81,"validate.io-integer-primitive":292}],291:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],292:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":286}],293:[function(require,module,exports){
'use strict';

// MODULES //

var ERFCINV = require( './number.js' );


// INVERSE COMPLEMENTARY ERROR FUNCTION //

/**
* FUNCTION: erfcinv( out, arr, accessor )
*	Computes the inverse complementary error function for each array element using an accessor function.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function erfcinv( out, x, clbk ) {
	var len = x.length,
		v, i;
	for ( i = 0; i < len; i++ ) {
		v = clbk( x[ i ], i );
		if ( typeof v === 'number' ) {
			out[ i ] = ERFCINV( v );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION erfcinv()


// EXPORTS //

module.exports = erfcinv;

},{"./number.js":298}],294:[function(require,module,exports){
'use strict';

// MODULES //

var ERFCINV = require( './number.js' );


// INVERSE COMPLEMENTARY ERROR FUNCTION //

/**
* FUNCTION: erfcinv( out, arr )
*	Computes the inverse complementary error function for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function erfcinv( out, x ) {
	var len = x.length,
		i;
	for ( i = 0; i < len; i++ ) {
		if ( typeof x[ i ] === 'number' ) {
			out[ i ] = ERFCINV( x[ i ] );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION erfcinv()


// EXPORTS //

module.exports = erfcinv;

},{"./number.js":298}],295:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory,
	ERFCINV = require( './number.js' );


// INVERSE COMPLEMENTARY ERROR FUNCTION //

/**
* FUNCTION: erfcinv( arr, path[, sep] )
*	Computes the inverse complementary error function for each array element and deep sets the input array.
*
* @param {Array} arr - input array
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function erfcinv( x, path, sep ) {
	var len = x.length,
		opts = {},
		dget,
		dset,
		v, i;
	if ( arguments.length > 2 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		for ( i = 0; i < len; i++ ) {
			v = dget( x[ i ] );
			if ( typeof v === 'number' ) {
				dset( x[ i ], ERFCINV( v ) );
			} else {
				dset( x[ i ], NaN );
			}
		}
	}
	return x;
} // end FUNCTION erfcinv()


// EXPORTS //

module.exports = erfcinv;

},{"./number.js":298,"utils-deep-get":351,"utils-deep-set":357}],296:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isnan = require( 'validate.io-nan' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var erfcinv1 = require( './number.js' ),
	erfcinv2 = require( './array.js' ),
	erfcinv3 = require( './accessor.js' ),
	erfcinv4 = require( './deepset.js' ),
	erfcinv5 = require( './matrix.js' ),
	erfcinv6 = require( './typedarray.js' );


// INVERSE COMPLEMENTARY ERROR FUNCTION //

/**
* FUNCTION: erfcinv( x[, opts] )
*	Computes the inverse complementary error function.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} x - input value
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} evaluated complementary inverse error function value(s)
*/
function erfcinv( x, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( isNumber( x ) || isnan( x ) ) {
		return erfcinv1( x );
	}
	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isMatrixLike( x ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'erfcinv()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( x.length );
			out = matrix( d, x.shape, dt );
		} else {
			out = x;
		}
		return erfcinv5( out, x );
	}
	if ( isTypedArrayLike( x ) ) {
		if ( opts.copy === false ) {
			out = x;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'erfcinv()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( x.length );
		}
		return erfcinv6( out, x );
	}
	if ( isArrayLike( x ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return erfcinv4( x, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = x;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'erfcinv()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( x.length );
		}
		else {
			out = new Array( x.length );
		}
		if ( opts.accessor ) {
			return erfcinv3( out, x, opts.accessor );
		}
		return erfcinv2( out, x );
	}
	return NaN;
} // end FUNCTION erfcinv()


// EXPORTS //

module.exports = erfcinv;

},{"./accessor.js":293,"./array.js":294,"./deepset.js":295,"./matrix.js":297,"./number.js":298,"./typedarray.js":299,"./validate.js":300,"compute-array-constructors":302,"dstructs-matrix":311,"validate.io-array-like":360,"validate.io-matrix-like":365,"validate.io-nan":366,"validate.io-number-primitive":367,"validate.io-typed-array-like":371}],297:[function(require,module,exports){
'use strict';

// MODULES //

var ERFCINV = require( './number.js' );


// INVERSE COMPLEMENTARY ERROR FUNCTION //

/**
* FUNCTION: erfcinv( out, x )
*	Evaluates the inverse complementary error function for each matrix element.
*
* @param {Matrix} out - output matrix
* @param {Matrix} x - input matrix
* @returns {Matrix} output matrix
*/
function erfcinv( out, x ) {
	var len = x.length,
		i;
	if ( out.length !== len ) {
		throw new Error( 'erfcinv()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	for ( i = 0; i < len; i++ ) {
		out.data[ i ] = ERFCINV( x.data[ i ] );
	}
	return out;
} // end FUNCTION erfcinv()


// EXPORTS //

module.exports = erfcinv;

},{"./number.js":298}],298:[function(require,module,exports){
'use strict';

/**
* erfcinv( x )
*
* Method:
*	1. For `|x| <= 0.5`, evaluate inverse erf using the rational approximation:
*
*		`erfcinv = x(x+10)(Y+R(x))`
*
*	where `Y` is a constant and `R(x)` is optimized for a low absolute error compared to `|Y|`. Max error `~2e-18`.
*
*	2. For `0.5 > 1-|x| >= 0`, evaluate inverse erf using the rational approximation:
*
*		`erfcinv = sqrt(-2*log(1-x)) / (Y + R(1-x))`
*
*	where `Y `is a constant, and R(q) is optimised for a low absolute error compared to `Y`. Max error `~7e-17`.
*
*	3. For `1-|x| < 0.25`, we have a series of rational approximations all of the general form:
*
*		`p = sqrt(-log(1-x))`
*
*	Then the result is given by:
*
*		`erfcinv = p(Y+R(p-B))`
*
*	where `Y` is a constant, `B` is the lowest value of `p` for which the approximation is valid, and `R(x-B)` is optimized for a low absolute error compared to `Y`.
*
*	Note that almost all code will really go through the first or maybe second approximation.  After than we are dealing with very small input values.
*
*	If `p < 3`, max error `~1e-20`.
*	If `p < 6`, max error `~8e-21`.
*	If `p < 18`, max error `~1e-19`.
*	If `p < 44`, max error `~6e-20`.
*	If `p >= 44`, max error `~1e-20`.
*/

// MODULES //

var polyval = require( 'compute-polynomial' );


// CONSTANTS //

var // Coefficients for erfcinv on [0, 0.5]:
	Y1 = 8.91314744949340820313e-2,
	P1 = [
		-5.38772965071242932965e-3,
		8.22687874676915743155e-3,
		2.19878681111168899165e-2,
		-3.65637971411762664006e-2,
		-1.26926147662974029034e-2,
		3.34806625409744615033e-2,
		-8.36874819741736770379e-3,
		-5.08781949658280665617e-4
	],
	Q1 = [
		8.86216390456424707504e-4,
		-2.33393759374190016776e-3,
		7.95283687341571680018e-2,
		-5.27396382340099713954e-2,
		-7.1228902341542847553e-1,
		6.62328840472002992063e-1,
		1.56221558398423026363,
		-1.56574558234175846809,
		-9.70005043303290640362e-1,
		1
	],

	// Coefficients for erfcinv for 0.5 > 1-x >= 0:
	Y2 = 2.249481201171875,
	P2 = [
		-3.67192254707729348546,
		2.11294655448340526258e1,
		1.7445385985570866523e1,
		-4.46382324441786960818e1,
		-1.88510648058714251895e1,
		1.76447298408374015486e1,
		8.37050328343119927838,
		1.05264680699391713268e-1,
		-2.02433508355938759655e-1
	],
	Q2 = [
		1.72114765761200282724,
		2.26436933413139721736e1,
		1.08268667355460159008e1,
		4.85609213108739935468e1,
		-2.01432634680485188801e1,
		-2.86608180499800029974e1,
		3.9713437953343869095,
		6.24264124854247537712,
		1
	],

	// Coefficients for erfcinv for sqrt( -log(1-x)):
	Y3 = 8.07220458984375e-1,
	P3 = [
		-6.81149956853776992068e-10,
		2.85225331782217055858e-8,
		-6.79465575181126350155e-7,
		2.14558995388805277169e-3,
		2.90157910005329060432e-2,
		1.42869534408157156766e-1,
		3.37785538912035898924e-1,
		3.87079738972604337464e-1,
		1.17030156341995252019e-1,
		-1.63794047193317060787e-1,
		-1.31102781679951906451e-1
	],
	Q3 = [
		1.105924229346489121e-2,
		1.52264338295331783612e-1,
		8.48854343457902036425e-1,
		2.59301921623620271374,
		4.77846592945843778382,
		5.38168345707006855425,
		3.46625407242567245975,
		1
	],

	Y4 = 9.3995571136474609375e-1,
	P4 = [
		2.66339227425782031962e-12,
		-2.30404776911882601748e-10,
		4.60469890584317994083e-6,
		1.57544617424960554631e-4,
		1.87123492819559223345e-3,
		9.50804701325919603619e-3,
		1.85573306514231072324e-2,
		-2.22426529213447927281e-3,
		-3.50353787183177984712e-2
	],
	Q4 = [
		7.64675292302794483503e-5,
		2.63861676657015992959e-3,
		3.41589143670947727934e-2,
		2.20091105764131249824e-1,
		7.62059164553623404043e-1,
		1.3653349817554063097,
		1
	],

	Y5 = 9.8362827301025390625e-1,
	P5 = [
		9.9055709973310326855e-17,
		-2.81128735628831791805e-14,
        4.62596163522878599135e-9,
        4.49696789927706453732e-7,
        1.49624783758342370182e-5,
        2.09386317487588078668e-4,
        1.05628862152492910091e-3,
        -1.12951438745580278863e-3,
		-1.67431005076633737133e-2
	],
	Q5 = [
		2.82243172016108031869e-7,
		2.75335474764726041141e-5,
        9.64011807005165528527e-4,
        1.60746087093676504695e-2,
        1.38151865749083321638e-1,
        5.91429344886417493481e-1,
        1
	];


// FUNCTIONS //

/**
* FUNCTION: calc( x, v, P, Q, Y )
*	Calculates a rational approximation.
*
* @private
* @param {Number} x
* @param {Number} v
* @param {Array} P - array of polynomial coefficients
* @param {Array} Q - array of polynomial coefficients
* @param {Number} Y
* @returns {Number} rational approximation
*/
function calc( x, v, P, Q, Y ) {
	var s, r;
	s = x - v;
	r = polyval( P, s ) / polyval( Q, s );
	return Y*x + r*x;
} // end FUNCTION calc()


// ERFINV //

/**
* FUNCTION: erfcinv( x )
*	Evaluates the complementary inverse error function for an input value.
*
* @private
* @param {Number} x - input value
* @returns {Number} evaluated complementary inverse error function
*/
function erfcinv( x ) {
	var sign = false,
		val,
		q, g, r;

	// [1] Special cases...

	// NaN:
	if ( x !== x ) {
		return NaN;
	}
	// x not on the interval: [0,2]
	if ( x < 0 || x > 2 ) {
		throw new RangeError ( 'erfcinv()::invalid input argument. Value must be on the interval [0,2]. Value: `' + x + '`.' );
	}
	if ( x === 0 ) {
		return Number.POSITIVE_INFINITY;
	}
	if ( x === 2 ) {
		return Number.NEGATIVE_INFINITY;
	}
	if ( x === 1 ) {
		return 0;
	}
	// [2] Get the sign and make use of `erfc` reflection formula: `erfc(-z) = 2 - erfc(z)`...
	if ( x > 1 ) {
		q = 2 - x;
		x = 1 - q;
		sign = true;
	} else {
		q = x;
		x = 1 - x;
	}
	// [3] |x| <= 0.5
	if ( x <= 0.5 ) {
		g = x * (x+10);
		r = polyval( P1, x ) / polyval( Q1, x );
		val = g*Y1 + g*r;
		return ( sign ) ? -val : val;
	}

	// [4] 1-|x| >= 0.25
	if ( q >= 0.25 ) {
		g = Math.sqrt( -2 * Math.log( q ) );
		q = q - 0.25;
		r = polyval( P2, q ) / polyval( Q2, q );
		val = g / (Y2+r);
		return ( sign ) ? -val : val;
	}
	q = Math.sqrt( -Math.log( q ) );

	// [5] q < 3
	if ( q < 3 ) {
		return calc( q, 1.125, P3, Q3, Y3 );
	}
	// [6] q < 6
	if ( q < 6 ) {
		return calc( q, 3, P4, Q4, Y4 );
	}
	// Note that the smallest number in JavaScript is 5e-324. Math.sqrt( -Math.log( 5e-324 ) ) ~27.2844
	return calc( q, 6, P5, Q5, Y5 );

	// Note that in the boost library, they are able to go to much smaller values, as 128 bit long doubles support ~1e-5000; something which JavaScript does not natively support.
} // end FUNCTION erfcinv()


// EXPORTS //

module.exports = erfcinv;

},{"compute-polynomial":724}],299:[function(require,module,exports){
'use strict';

// MODULES //

var ERFCINV = require( './number.js' );


// INVERSE COMPLEMENTARY ERROR FUNCTION //

/**
* FUNCTION: erfcinv( out, arr )
*	Computes the inverse complementary error function for each typed-array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} arr - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function erfcinv( out, x ) {
	var len = x.length,
		i;
	for ( i = 0; i < len; i++ ) {
		out[ i ] = ERFCINV( x[ i ] );
	}
	return out;
} // end FUNCTION erfcinv()


// EXPORTS //

module.exports = erfcinv;

},{"./number.js":298}],300:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Boolean} [options.copy] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'erfcinv()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'erfcinv()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'erfcinv()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'erfcinv()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'erfcinv()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'erfcinv()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":363,"validate.io-function":364,"validate.io-object":368,"validate.io-string-primitive":370}],301:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],302:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":301,"dup":12}],303:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],304:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":307,"./iget.js":309,"./iset.js":312,"./mget.js":316,"./mset.js":318,"./set.js":326,"./sget.js":328,"./sset.js":330,"./toString.js":332,"dup":14}],305:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":308,"./iget.raw.js":310,"./iset.raw.js":313,"./mget.raw.js":317,"./mset.raw.js":319,"./set.raw.js":327,"./sget.raw.js":329,"./sset.raw.js":331,"./toString.js":332,"dup":15}],306:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],307:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":345}],308:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],309:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":343}],310:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],311:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":314,"./matrix.raw.js":315,"dup":21}],312:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":343,"validate.io-number-primitive":367}],313:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],314:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":303,"./ctor.js":304,"./dtypes.js":306,"compute-cast-arrays":333,"compute-dtype":336,"dup":24,"validate.io-array":340,"validate.io-contains":341,"validate.io-nonnegative-integer-array":344,"validate.io-string-primitive":370}],315:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":303,"./ctor.raw.js":305,"./dtypes.js":306,"compute-dtype":336,"dup":25,"validate.io-contains":341,"validate.io-string-primitive":370}],316:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":303,"dup":26,"validate.io-nonnegative-integer-array":344}],317:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":303,"dup":27}],318:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":320,"./mset2.js":321,"./mset3.js":322,"./mset4.js":323,"./mset5.js":324,"./mset6.js":325,"dup":28,"validate.io-function":364,"validate.io-nonnegative-integer-array":344,"validate.io-number-primitive":367}],319:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":320,"./mset2.js":321,"./mset3.js":322,"./mset4.js":323,"./mset5.js":324,"./mset6.js":325,"dup":29}],320:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],321:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],322:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],323:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],324:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],325:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],326:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":345,"validate.io-number-primitive":367}],327:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],328:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":303,"compute-indexspace":534,"dup":38,"validate.io-string-primitive":370}],329:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":303,"compute-indexspace":534,"dup":39}],330:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":534,"dup":40,"validate.io-function":364,"validate.io-number-primitive":367,"validate.io-string-primitive":370}],331:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":534,"dup":41}],332:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],333:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":301,"compute-array-dtype/lib/dtypes":334,"dup":43,"type-name":335,"validate.io-array-like":360}],334:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],335:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],336:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":338,"dup":46,"type-name":339}],337:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],338:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":337,"dup":48}],339:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],340:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],341:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":340,"validate.io-nan-primitive":342}],342:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],343:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":367}],344:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":340,"validate.io-nonnegative-integer":345}],345:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":346}],346:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":347}],347:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],348:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],349:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],350:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":348,"./defaults.js":349,"./validate.js":352,"dup":60,"validate.io-array":353,"validate.io-string-primitive":370}],351:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":348,"./defaults.js":349,"./factory.js":350,"./validate.js":352,"dup":61,"validate.io-array":353,"validate.io-string-primitive":370}],352:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":368,"validate.io-string-primitive":370}],353:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],354:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],355:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],356:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":354,"./defaults.js":355,"./validate.js":358,"dup":66,"validate.io-array":359,"validate.io-string-primitive":370}],357:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":354,"./defaults.js":355,"./factory.js":356,"./validate.js":358,"dup":67,"validate.io-array":359,"validate.io-string-primitive":370}],358:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":363,"validate.io-object":368,"validate.io-string-primitive":370}],359:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],360:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":361,"dup":70,"validate.io-integer-primitive":362}],361:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],362:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":367}],363:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],364:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],365:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],366:[function(require,module,exports){
arguments[4][76][0].apply(exports,arguments)
},{"dup":76}],367:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],368:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":369}],369:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],370:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],371:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":372,"dup":81,"validate.io-integer-primitive":373}],372:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],373:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":367}],374:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var ERFINV = require( './number.js' );


// INVERSE ERROR FUNCTION //

/**
* FUNCTION: erfinv( out, arr, accessor )
*	Computes the inverse error function for each array element using an accessor function.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function erfinv( y, x, clbk ) {
	var len = x.length,
		v, i;
	for ( i = 0; i < len; i++ ) {
		v = clbk( x[ i ], i );
		if ( typeof v === 'number' ) {
			y[ i ] = ERFINV( v );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION erfinv()


// EXPORTS //

module.exports = erfinv;

},{"./number.js":379}],375:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var ERFINV = require( './number.js' );


// INVERSE ERROR FUNCTION //

/**
* FUNCTION: erfinv( out, arr )
*	Computes the inverse error function for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function erfinv( y, x ) {
	var len = x.length,
		i;
	for ( i = 0; i < len; i++ ) {
		if ( typeof x[ i ] === 'number' ) {
			y[ i ] = ERFINV( x[ i ] );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION erfinv()


// EXPORTS //

module.exports = erfinv;

},{"./number.js":379}],376:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory;


// FUNCTIONS //

var ERFINV = require( './number.js' );


// INVERSE ERROR FUNCTION //

/**
* FUNCTION: erfinv( arr, path[, sep] )
*	Computes the inverse error function for each array element and deep sets the input array.
*
* @param {Array} arr - input array
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function erfinv( x, path, sep ) {
	var len = x.length,
		opts = {},
		dget,
		dset,
		v, i;
	if ( arguments.length > 2 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		for ( i = 0; i < len; i++ ) {
			v = dget( x[ i ] );
			if ( typeof v === 'number' ) {
				dset( x[ i ], ERFINV( v ) );
			} else {
				dset( x[ i ], NaN );
			}
		}
	}
	return x;
} // end FUNCTION erfinv()


// EXPORTS //

module.exports = erfinv;

},{"./number.js":379,"utils-deep-get":432,"utils-deep-set":438}],377:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isnan = require( 'validate.io-nan' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var erfinv1 = require( './number.js' ),
	erfinv2 = require( './array.js' ),
	erfinv3 = require( './accessor.js' ),
	erfinv4 = require( './deepset.js' ),
	erfinv5 = require( './matrix.js' ),
	erfinv6 = require( './typedarray.js' );


// INVERSE ERROR FUNCTION //

/**
* FUNCTION: erfinv( x[, opts] )
*	Evaluates the inverse error function element-wise.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} x - input value
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} inverse error function value(s)
*/
function erfinv( x, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( isNumber( x ) || isnan( x ) ) {
		return erfinv1( x );
	}
	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isMatrixLike( x ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'erfinv()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( x.length );
			out = matrix( d, x.shape, dt );
		} else {
			out = x;
		}
		return erfinv5( out, x );
	}
	if ( isTypedArrayLike( x ) ) {
		if ( opts.copy === false ) {
			out = x;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'erfinv()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( x.length );
		}
		return erfinv6( out, x );
	}
	if ( isArrayLike( x ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return erfinv4( x, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = x;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'erfinv()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( x.length );
		}
		else {
			out = new Array( x.length );
		}
		if ( opts.accessor ) {
			return erfinv3( out, x, opts.accessor );
		}
		return erfinv2( out, x );
	}
	return NaN;
} // end FUNCTION erfinv()


// EXPORTS //

module.exports = erfinv;

},{"./accessor.js":374,"./array.js":375,"./deepset.js":376,"./matrix.js":378,"./number.js":379,"./typedarray.js":380,"./validate.js":381,"compute-array-constructors":383,"dstructs-matrix":392,"validate.io-array-like":441,"validate.io-matrix-like":446,"validate.io-nan":447,"validate.io-number-primitive":448,"validate.io-typed-array-like":452}],378:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var ERFINV = require( './number.js' );


// INVERSE ERROR FUNCTION //

/**
* FUNCTION: erfinv( out, matrix )
*	Evaluates the inverse error function for each matrix element.
*
* @param {Matrix} out - output matrix
* @param {Matrix} matrix - input matrix
* @returns {Matrix} output matrix
*/
function erfinv( y, x ) {
	var len = x.length,
		i;
	if ( y.length !== len ) {
		throw new Error( 'erfinv()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	for ( i = 0; i < len; i++ ) {
		y.data[ i ] = ERFINV( x.data[ i ] );
	}
	return y;
} // end FUNCTION erfinv()


// EXPORTS //

module.exports = erfinv;

},{"./number.js":379}],379:[function(require,module,exports){
'use strict';

/**
* erfinv( x )
*
* Method:
*	1. For `|x| <= 0.5`, evaluate inverse erf using the rational approximation:
*
*		`erfinv = x(x+10)(Y+R(x))`
*
*	where `Y` is a constant and `R(x)` is optimized for a low absolute error compared to `|Y|`. Max error `~2e-18`.
*
*	2. For `0.5 > 1-|x| >= 0`, evaluate inverse erf using the rational approximation:
*
*		`erfinv = sqrt(-2*log(1-x)) / (Y + R(1-x))`
*
*	where `Y `is a constant, and R(q) is optimised for a low absolute error compared to `Y`. Max error `~7e-17`.
*
*	3. For `1-|x| < 0.25`, we have a series of rational approximations all of the general form:
*
*		`p = sqrt(-log(1-x))`
*
*	Then the result is given by:
*
*		`erfinv = p(Y+R(p-B))`
*
*	where `Y` is a constant, `B` is the lowest value of `p` for which the approximation is valid, and `R(x-B)` is optimized for a low absolute error compared to `Y`.
*
*	Note that almost all code will really go through the first or maybe second approximation.  After that we are dealing with very small input values.
*
*	If `p < 3`, max error `~1e-20`.
*	If `p < 6`, max error `~8e-21`.
*	If `p < 18`, max error `~1e-19`.
*	If `p < 44`, max error `~6e-20`.
*	If `p >= 44`, max error `~1e-20`.
*/

// MODULES //

var polyval = require( 'compute-polynomial' );


// CONSTANTS //

var // Coefficients for erfinv on [0, 0.5]:
	Y1 = 8.91314744949340820313e-2,
	P1 = [
		-5.38772965071242932965e-3,
		8.22687874676915743155e-3,
		2.19878681111168899165e-2,
		-3.65637971411762664006e-2,
		-1.26926147662974029034e-2,
		3.34806625409744615033e-2,
		-8.36874819741736770379e-3,
		-5.08781949658280665617e-4
	],
	Q1 = [
		8.86216390456424707504e-4,
		-2.33393759374190016776e-3,
		7.95283687341571680018e-2,
		-5.27396382340099713954e-2,
		-7.1228902341542847553e-1,
		6.62328840472002992063e-1,
		1.56221558398423026363,
		-1.56574558234175846809,
		-9.70005043303290640362e-1,
		1,
	],

	// Coefficients for erfinv for 0.5 > 1-x >= 0:
	Y2 = 2.249481201171875,
	P2 = [
		-3.67192254707729348546,
		2.11294655448340526258e1,
		1.7445385985570866523e1,
		-4.46382324441786960818e1,
		-1.88510648058714251895e1,
		1.76447298408374015486e1,
		8.37050328343119927838,
		1.05264680699391713268e-1,
		-2.02433508355938759655e-1
	],
	Q2 = [
		1.72114765761200282724,
		2.26436933413139721736e1,
		1.08268667355460159008e1,
		4.85609213108739935468e1,
		-2.01432634680485188801e1,
		-2.86608180499800029974e1,
		3.9713437953343869095,
		6.24264124854247537712,
		1
	],

	// Coefficients for erfinv for sqrt( -log(1-x) ):
	Y3 = 8.07220458984375e-1,
	P3 = [
		-6.81149956853776992068e-10,
		2.85225331782217055858e-8,
		-6.79465575181126350155e-7,
		2.14558995388805277169e-3,
		2.90157910005329060432e-2,
		1.42869534408157156766e-1,
		3.37785538912035898924e-1,
		3.87079738972604337464e-1,
		1.17030156341995252019e-1,
		-1.63794047193317060787e-1,
		-1.31102781679951906451e-1
	],
	Q3 = [
		1.105924229346489121e-2,
		1.52264338295331783612e-1,
		8.48854343457902036425e-1,
		2.59301921623620271374,
		4.77846592945843778382,
		5.38168345707006855425,
		3.46625407242567245975,
		1
	],

	Y4 = 9.3995571136474609375e-1,
	P4 = [
		2.66339227425782031962e-12,
		-2.30404776911882601748e-10,
		4.60469890584317994083e-6,
		1.57544617424960554631e-4,
		1.87123492819559223345e-3,
		9.50804701325919603619e-3,
		1.85573306514231072324e-2,
		-2.22426529213447927281e-3,
		-3.50353787183177984712e-2
	],
	Q4 = [
		7.64675292302794483503e-5,
		2.63861676657015992959e-3,
		3.41589143670947727934e-2,
		2.20091105764131249824e-1,
		7.62059164553623404043e-1,
		1.3653349817554063097,
		1
	],

	Y5 = 9.8362827301025390625e-1,
	P5 = [
		9.9055709973310326855e-17,
		-2.81128735628831791805e-14,
		4.62596163522878599135e-9,
		4.49696789927706453732e-7,
		1.49624783758342370182e-5,
		2.09386317487588078668e-4,
		1.05628862152492910091e-3,
		-1.12951438745580278863e-3,
		-1.67431005076633737133e-2
	],
	Q5 = [
		2.82243172016108031869e-7,
		2.75335474764726041141e-5,
		9.64011807005165528527e-4,
		1.60746087093676504695e-2,
		1.38151865749083321638e-1,
		5.91429344886417493481e-1,
		1
	];


// FUNCTIONS //

/**
* FUNCTION: calc( x, v, P, Q, Y, sign )
*	Calculates a rational approximation.
*
* @private
* @param {Number} x
* @param {Number} v
* @param {Array} P - array of polynomial coefficients
* @param {Array} Q - array of polynomial coefficients
* @param {Number} Y
* @param {Boolean} sign - indicates if positive or negative
* @returns {Number} rational approximation
*/
function calc( x, v, P, Q, Y, sign ) {
	var val, s, r;
	s = x - v;
	r = polyval( P, s ) / polyval( Q, s );
	val = Y*x + r*x;
	return ( sign ) ? -val : val;
} // end FUNCTION calc()


// ERFINV //

/**
* FUNCTION: erfinv( x )
*	Evaluates the inverse error function for an input value.
*
* @private
* @param {Number} x - input value
* @returns {Number} evaluated inverse error function
*/
function erfinv( x ) {
	var sign = false,
		val,
		q, g, r;

	// [1] Special cases...

	// NaN:
	if ( x !== x ) {
		return NaN;
	}
	// x not on the interval: [-1,1]
	if ( x < -1 || x > 1 ) {
		throw new RangeError( 'erfinv()::invalid input argument. Value must be on the interval [-1,1]. Value: `' + x + '`.' );
	}
	if ( x === 1 ) {
		return Number.POSITIVE_INFINITY;
	}
	if ( x === -1 ) {
		return Number.NEGATIVE_INFINITY;
	}
	if ( x === 0 ) {
		return 0;
	}
	// [2] Get the sign and make use of `erf` reflection formula: `erf(-z) = -erf(z)`...
	if ( x < 0 ) {
		x = -x;
		sign = true;
	}
	q = 1 - x;

	// [3] |x| <= 0.5
	if ( x <= 0.5 ) {
		g = x * (x+10);
		r = polyval( P1, x ) / polyval( Q1, x );
		val = g*Y1 + g*r;
		return ( sign ) ? -val : val;
	}
	// [4] 1-|x| >= 0.25
	if ( q >= 0.25 ) {
		g = Math.sqrt( -2 * Math.log( q ) );
		q = q - 0.25;
		r = polyval( P2, q ) / polyval( Q2, q );
		val = g / (Y2+r);
		return ( sign ) ? -val : val;
	}
	q = Math.sqrt( -Math.log( q ) );

	// [5] q < 3
	if ( q < 3 ) {
		return calc( q, 1.125, P3, Q3, Y3, sign );
	}
	// [6] q < 6
	if ( q < 6 ) {
		return calc( q, 3, P4, Q4, Y4, sign );
	}
	// Note that the smallest number in JavaScript is 5e-324. Math.sqrt( -Math.log( 5e-324 ) ) ~27.2844
	return calc( q, 6, P5, Q5, Y5, sign );

	// Note that, in the boost library, they are able to go to much smaller values, as 128 bit long doubles support ~1e-5000; something which JavaScript does not natively support.
} // end FUNCTION erfinv()


// EXPORTS //

module.exports = erfinv;

},{"compute-polynomial":724}],380:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var ERFINV = require( './number.js' );


// INVERSE ERROR FUNCTION //

/**
* FUNCTION: erfinv( out, arr )
*	Computes the inverse error function for each typed-array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} arr - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function erfinv( y, x ) {
	var len = x.length,
		i;
	for ( i = 0; i < len; i++ ) {
		y[ i ] = ERFINV( x[ i ] );
	}
	return y;
} // end FUNCTION erfinv()


// EXPORTS //

module.exports = erfinv;

},{"./number.js":379}],381:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Boolean} [options.copy] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'erfinv()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'erfinv()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'erfinv()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'erfinv()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'erfinv()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'erfinv()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":444,"validate.io-function":445,"validate.io-object":449,"validate.io-string-primitive":451}],382:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],383:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":382,"dup":12}],384:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],385:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":388,"./iget.js":390,"./iset.js":393,"./mget.js":397,"./mset.js":399,"./set.js":407,"./sget.js":409,"./sset.js":411,"./toString.js":413,"dup":14}],386:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":389,"./iget.raw.js":391,"./iset.raw.js":394,"./mget.raw.js":398,"./mset.raw.js":400,"./set.raw.js":408,"./sget.raw.js":410,"./sset.raw.js":412,"./toString.js":413,"dup":15}],387:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],388:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":426}],389:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],390:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":424}],391:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],392:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":395,"./matrix.raw.js":396,"dup":21}],393:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":424,"validate.io-number-primitive":448}],394:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],395:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":384,"./ctor.js":385,"./dtypes.js":387,"compute-cast-arrays":414,"compute-dtype":417,"dup":24,"validate.io-array":421,"validate.io-contains":422,"validate.io-nonnegative-integer-array":425,"validate.io-string-primitive":451}],396:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":384,"./ctor.raw.js":386,"./dtypes.js":387,"compute-dtype":417,"dup":25,"validate.io-contains":422,"validate.io-string-primitive":451}],397:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":384,"dup":26,"validate.io-nonnegative-integer-array":425}],398:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":384,"dup":27}],399:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":401,"./mset2.js":402,"./mset3.js":403,"./mset4.js":404,"./mset5.js":405,"./mset6.js":406,"dup":28,"validate.io-function":445,"validate.io-nonnegative-integer-array":425,"validate.io-number-primitive":448}],400:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":401,"./mset2.js":402,"./mset3.js":403,"./mset4.js":404,"./mset5.js":405,"./mset6.js":406,"dup":29}],401:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],402:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],403:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],404:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],405:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],406:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],407:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":426,"validate.io-number-primitive":448}],408:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],409:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":384,"compute-indexspace":534,"dup":38,"validate.io-string-primitive":451}],410:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":384,"compute-indexspace":534,"dup":39}],411:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":534,"dup":40,"validate.io-function":445,"validate.io-number-primitive":448,"validate.io-string-primitive":451}],412:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":534,"dup":41}],413:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],414:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":382,"compute-array-dtype/lib/dtypes":415,"dup":43,"type-name":416,"validate.io-array-like":441}],415:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],416:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],417:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":419,"dup":46,"type-name":420}],418:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],419:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":418,"dup":48}],420:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],421:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],422:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":421,"validate.io-nan-primitive":423}],423:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],424:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":448}],425:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":421,"validate.io-nonnegative-integer":426}],426:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":427}],427:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":428}],428:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],429:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],430:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],431:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":429,"./defaults.js":430,"./validate.js":433,"dup":60,"validate.io-array":434,"validate.io-string-primitive":451}],432:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":429,"./defaults.js":430,"./factory.js":431,"./validate.js":433,"dup":61,"validate.io-array":434,"validate.io-string-primitive":451}],433:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":449,"validate.io-string-primitive":451}],434:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],435:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],436:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],437:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":435,"./defaults.js":436,"./validate.js":439,"dup":66,"validate.io-array":440,"validate.io-string-primitive":451}],438:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":435,"./defaults.js":436,"./factory.js":437,"./validate.js":439,"dup":67,"validate.io-array":440,"validate.io-string-primitive":451}],439:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":444,"validate.io-object":449,"validate.io-string-primitive":451}],440:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],441:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":442,"dup":70,"validate.io-integer-primitive":443}],442:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],443:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":448}],444:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],445:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],446:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],447:[function(require,module,exports){
arguments[4][76][0].apply(exports,arguments)
},{"dup":76}],448:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],449:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":450}],450:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],451:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],452:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":453,"dup":81,"validate.io-integer-primitive":454}],453:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],454:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":448}],455:[function(require,module,exports){
/**
*
*	COMPUTE: find
*
*
*	DESCRIPTION:
*		- Finds array elements which satisfy a test condition.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isInteger = require( 'validate.io-integer' );


// FIND //

/**
* FUNCTION: find( arr, [opts,], clbk )
*	Finds array elements which satisfy a test condition.
*
* @param {Array} arr - array from which elements will be tested
* @param {Object} [opts] - function options
* @param {Number} [opts.k] - limits the number of returned elements (default: `*`)
* @param {String} [opts.returns] - if `values`, values are returned; if `indices`, indices are returned; if `*`, both indices and values are returned (default: `indices`)
* @param {Function} clbk - function invoked for each array element. If the return value is truthy, the value is considered to have satisfied the test condition.
* @returns {Array} array of indices, element values, or arrays of index-value pairs
*/
function find( arr, opts, clbk ) {
	var returns = [ 'values', 'indices', '*' ],
		mode = 0,
		ret,
		len,
		k,
		v,
		i,
		count,
		out;

	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'find()::invalid input argument. Must provide an array.' );
	}
	len = arr.length;
	if ( arguments.length < 3 ) {
		clbk = opts;
		opts = {};
	}
	if ( typeof clbk !== 'function' ) {
		throw new TypeError( 'find()::invalid input argument. Callback argument must be a function.' );
	}
	if ( !isObject( opts ) ) {
		throw new TypeError( 'find()::invalid input argument. Options must be an object.' );
	}
	if ( opts.hasOwnProperty( 'k' ) ) {
		k = opts.k;
		if ( typeof k !== 'number' || k !== k || !isInteger( k ) ) {
			throw new TypeError( 'find()::invalid input argument. `k` must be an integer.' );
		}
	} else {
		k = len;
	}
	if ( opts.hasOwnProperty( 'returns' ) ) {
		ret = opts.returns;
		if ( typeof ret !== 'string' || returns.indexOf( ret ) === -1 ) {
			throw new TypeError( 'find()::invalid input argument. `returns` option must be a string and have one of the following values: `values`, `indices`, `all`.' );
		}
		if ( ret === 'values' ) {
			mode = 1;
		} else if ( ret === '*' ) {
			mode = 2;
		}
	}
	out = [];
	count = 0;

	if ( k === 0 ) {
		return out;
	}
	if ( k > 0 ) {
		// Search moving from begin-to-end [0,1,...]:
		for ( i = 0; i < len; i++ ) {
			v = arr[ i ];
			if ( clbk( v, i, arr ) ) {
				if ( mode === 2 ) {
					out.push( [ i, v ] );
				} else if ( mode === 1 ) {
					out.push( v );
				} else {
					out.push( i );
				}
				if ( ++count === k ) {
					break;
				}
			}
		}
		return out;
	}
	// Search moving from end-to-begin [...,2,1,0]:
	k = -k;
	for ( i = len-1; i >= 0; i-- ) {
		v = arr[ i ];
		if ( clbk( v, i, arr ) ) {
			if ( mode === 2 ) {
				out.push( [ i, v ] );
			} else if ( mode === 1 ) {
				out.push( v );
			} else {
				out.push( i );
			}
			if ( ++count === k ) {
				break;
			}
		}
	}
	return out;
} // end FUNCTION find()


// EXPORTS //

module.exports = find;

},{"validate.io-integer":456,"validate.io-object":458}],456:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":457}],457:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],458:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":459}],459:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],460:[function(require,module,exports){
'use strict';

// MODULES //

var createCopy = require( 'utils-copy' ),
	isArray = require( 'validate.io-array' ),
	isObject = require( 'validate.io-object' ),
	isNonNegativeInteger = require( 'validate.io-nonnegative-integer' ),
	isPositiveIntegerArray = require( 'validate.io-positive-integer-array' ),
	isBoolean = require( 'validate.io-boolean-primitive');


// FUNCTIONS //

/**
* FUNCTION: dims( arr, d, depth )
*	Determines matrix dimensions.
*
* @param {Array} arr - input array
* @param {Number[]} d - dimensions array
* @param {Number} depth - maximum number of dimensions
* @returns {Number[]} array of dimensions
*/
function dims( arr, d, depth ) {
	if ( depth && isArray( arr ) ) {
		d.push( arr.length );
		dims( arr[ 0 ], d, depth-1 );
	}
	return d;
} // end FUNCTION dims()

/**
* FUNCTION: createMatrixFcn( d )
*	Returns a function to flatten a matrix.
*
* @private
* @param {Number[d]} d - matrix dimensions
* @returns {Function} function which can be used to flatten a matrix having specified dimensions
*/
function createMatrixFcn( d ) {
	var len = d.length,
		n = len - 1,
		fcn,
		i;

	// Code generation. Create the variables...
	fcn = 'var o=[];var ';
	for ( i = 0; i < len; i++ ) {
		fcn += 'i' + i;
		if ( i < n ) {
			fcn += ',';
		} else {
			fcn += ';';
		}
	}
	// Create the nested for loops...
	for ( i = 0; i < len; i++ ) {
		fcn += 'for(i' + i + '=0;i' + i + '<' + d[ i ] + ';i' + i + '++){';
	}
	// Create the code which accesses the nested array values and pushes them onto the flattened array.
	fcn += 'o.push(x';
	for ( i = 0; i < len; i++ ) {
		fcn += '[i' + i + ']';
	}
	fcn += ');';

	// Tidy up:
	for ( i = 0; i < len; i++ ) {
		fcn += '}';
	}
	fcn += 'return o;';

	// Create a new function:
	return new Function( 'x', fcn );
} // end FUNCTION createMatrixFcn()

/**
* FUNCTION: recurse( out, arr, depth )
*	Recursively flattens an array.
*
* @private
* @param {Array} out - output array
* @param {Array} arr - input array
* @param {Number} depth - recursion depth
* @returns {Array} flattened array
*/
function recurse( out, arr, depth ) {
	var len = arr.length,
		val,
		i;

	for ( i = 0; i < len; i++ ) {
		val = arr[ i ];
		if ( depth && isArray( val ) ) {
			recurse( out, val, depth-1 );
		} else {
			out.push( val );
		}
	}
	return out;
} // end FUNCTION recurse()


// FLATTEN //

/**
* FUNCTION: flatten( arr[, options] )
*	Flattens an array.
*
* @param {Array} arr - input array
* @param {Object} [options] - function options
* @param {Number} [options.depth=Infinity] - maximum depth
* @param {Boolean} [options.matrix=false] - indicates whether the input array can be regarded as a matrix; i.e., all elements having same array dimensions
* @param {Boolean} [options.copy=false] - indicates whether array elements should be deep copied
* @returns {Array} flattened array
*/
function flatten( arr, opts ) {
	var isMatrix = false,
		depth = Number.POSITIVE_INFINITY,
		copy = false,
		out,
		fcn,
		d;

	if ( !isArray( arr ) ) {
		throw new TypeError( 'flatten()::invalid input argument. Must provide an array. Value: `' + arr + '`.' );
	}
	if ( arguments.length > 1 ) {
		if ( !isObject( opts ) ) {
			throw new TypeError( 'flatten()::invalid input argument. Options argument must be an object. Value: `' + opts + '`.' );
		}
		if ( opts.hasOwnProperty( 'matrix' ) ) {
			isMatrix = opts.matrix;
			if ( !isBoolean( isMatrix ) ) {
				throw new TypeError( 'flatten()::invalid option. Matrix option must be a boolean primitive. Option: `' + isMatrix + '`.' );
			}
		}
		if ( opts.hasOwnProperty( 'depth' ) ) {
			depth = opts.depth;
			if ( !isNonNegativeInteger( depth ) ) {
				throw new TypeError( 'flatten()::invalid option. Depth option must be a nonnegative integer. Option: `' + depth + '`.' );
			}
		}
		if ( opts.hasOwnProperty( 'copy' ) ) {
			copy = opts.copy;
			if ( !isBoolean( copy ) ) {
				throw new TypeError( 'flatten()::invalid option. Copy option must be a boolean primitive. Option: `' + copy + '`.' );
			}
		}
	}
	if ( depth === 0 ) {
		out = arr;
	}
	else if ( isMatrix ) {
		d = dims( arr, [], depth );
		fcn = createMatrixFcn( d );
		out = fcn( arr );
	}
	else {
		out = recurse( [], arr, depth );
	}
	if ( copy ) {
		return createCopy( out );
	}
	return out;
} // end FUNCTION flatten()


// CREATE FLATTEN //

/**
* FUNCTION: createFlatten( d[, opts] )
*	Creates a customized function for flattening arrays having specified dimensions.
*
* @param {Number[]} d - dimension array
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy] - indicates whether to deep copy array elements
* @returns {Function} function for flattening arrays having specified dimensions
*/
function createFlatten( d, opts ) {
	var copy = false,
		fcn;
	if ( !isPositiveIntegerArray( d ) ) {
		throw new TypeError( 'createFlatten()::invalid input argument. Dimensions must be a positive integer array. Value: `' + d + '`.' );
	}
	if ( arguments.length > 1 ) {
		if ( !isObject( opts ) ) {
			throw new TypeError( 'createFlatten()::invalid input argument. Options argument must be an object. Value: `' + opts + '`.' );
		}
		if ( opts.hasOwnProperty( 'copy' ) ) {
			copy = opts.copy;
			if ( !isBoolean( copy ) ) {
				throw new TypeError( 'createFlatten()::invalid option. Copy option must be a boolean primitive. Option: `' + copy + '`.' );
			}
		}
	}
	fcn = createMatrixFcn( d );
	if ( copy ) {
		return function flatten( arr ) {
			return createCopy( fcn( arr ) );
		};
	}
	return fcn;
} // end FUNCTION createFlatten()


// EXPORTS //

module.exports = flatten;
module.exports.createFlatten = createFlatten;

},{"utils-copy":461,"validate.io-array":464,"validate.io-boolean-primitive":465,"validate.io-nonnegative-integer":466,"validate.io-object":469,"validate.io-positive-integer-array":470}],461:[function(require,module,exports){
/**
*
*	UTILS: copy
*
*
*	DESCRIPTION:
*		- Copy or deep clone a value to an arbitrary depth.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2015. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2015.
*
*/

'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isBuffer = require( 'validate.io-buffer' ),
	isNonNegativeInteger = require( 'validate.io-nonnegative-integer' ),
	typeName = require( 'type-name' );


// FUNCTIONS //

/**
* FUNCTION: indexOf( arr, val )
*	Returns the array index of a value. If the array does not contain the value, the function returns `-1`.
*
* @private
* @param {Array} arr - array
* @param {*} val - value for which to search
* @returns {Number} array index
*/
function indexOf( arr, val ) {
	var len = arr.length,
		i;
	for ( i = 0; i < len; i++ ) {
		if ( arr[ i ] === val ) {
			return i;
		}
	}
	return -1;
} // end FUNCTION indexOf()

/**
* FUNCTION: objectKeys( obj )
*	Returns an object's keys.
*
* @private
* @param {Array|Object} obj - object
* @returns {Array} array of keys
*/
function objectKeys( obj ) {
	var keys = [],
		key;
	for ( key in obj ) {
		if ( obj.hasOwnProperty( key ) ) {
			keys.push( key );
		}
	}
	return keys;
} // end METHOD objectKeys()

/**
* Create functions for copying typed arrays.
*/
var typedArrays = {
	'Int8Array': null,
	'Uint8Array': null,
	'Uint8ClampedArray': null,
	'Int16Array': null,
	'Uint16Array': null,
	'Int32Array': null,
	'Uint32Array': null,
	'Float32Array': null,
	'Float64Array': null
};

(function createTypedArrayFcns() {
	/* jshint evil:true */
	var keys = objectKeys( typedArrays ),
		len = keys.length,
		key,
		i;
	for ( i = 0; i < len; i++ ) {
		key = keys[ i ];
		typedArrays[ key ] = new Function( 'arr', 'return new '+key+'( arr );' );
	}
})();

/**
* FUNCTION: cloneInstance( val )
*	Clones a class instance.
*
*	WARNING: this should only be used for simple cases. Any instances with privileged access to variables (e.g., within closures) cannot be cloned. This approach should be considered fragile.
*
*	NOTE: the function is greedy, disregarding the notion of a 'level'. Instead, the function deep copies all properties, as we assume the concept of 'level' applies only to the class instance reference but not to its internal state. This prevents, in theory, two instances from sharing state.
*
* @private
* @param {Object} val - class instance
* @returns {Object} new instance
*/
function cloneInstance( val ) {
	var cache = [],
		refs = [],
		names,
		name,
		desc,
		tmp,
		ref,
		i;

	ref = Object.create( Object.getPrototypeOf( val ) );
	cache.push( val );
	refs.push( ref );

	names = Object.getOwnPropertyNames( val );
	for ( i = 0; i < names.length; i++ ) {
		name = names[ i ];
		desc = Object.getOwnPropertyDescriptor( val, name );
		if ( desc.hasOwnProperty( 'value' ) ) {
			tmp = ( isArray( val[name] ) ) ? [] : {};
			desc.value = deepCopy( val[name], tmp, cache, refs, -1 );
		}
		Object.defineProperty( ref, name, desc );
	}
	if ( !Object.isExtensible( val ) ) {
		Object.preventExtensions( ref );
	}
	if ( Object.isSealed( val ) ) {
		Object.seal( ref );
	}
	if ( Object.isFrozen( val ) ) {
		Object.freeze( ref );
	}
	return ref;
} // end FUNCTION cloneInstance()


// DEEP COPY //

/**
* FUNCTION: deepCopy( val, copy, cache, refs, level )
*	Recursively performs a deep copy of an input object.
*
* @private
* @param {Array|Object} val - value to copy
* @param {Array|Object} copy - copy
* @param {Array} cache - an array of visited objects
* @param {Array} refs - an array of object references
* @param {Number} level - copy depth
* @returns {*} deep copy
*/
function deepCopy( val, copy, cache, refs, level ) {
	var keys,
		name,
		key,
		ref,
		x,
		i, j;

	// [-] Decrement the level...
	level = level - 1;

	// [0] Primitives and functions...
	if ( typeof val !== 'object' || val === null ) {
		return val;
	}
	// [1] Node.js Buffer objects...
	if ( isBuffer( val ) ) {
		return new val.constructor( val );
	}
	name = typeName( val );

	// [2] Sets and Maps...
	if ( name === 'Set' || name === 'Map' ) {
		console.warn( 'copy()::not supported. Copying Sets and Maps is not currently supported. File an issue, submit a pull request, or contact the author directly at kgryte@gmail.com.' );
		return {};
	}
	// [3] Number, String, and Boolean objects...
	if ( name === 'String' || name === 'Boolean' || name === 'Number' ) {
		// Return an equivalent primitive!
		return val.valueOf();
	}
	// [4] Date objects...
	if ( name === 'Date' ) {
		return new Date( +val );
	}
	// [5] Regular expressions...
	if ( name === 'RegExp' ) {
		val = val.toString();
		i = val.lastIndexOf( '/' );
		return new RegExp( val.slice( 1, i ), val.slice( i+1 ) );
	}
	// [6] Typed arrays...
	if ( typedArrays.hasOwnProperty( name ) ) {
		return typedArrays[ name ]( val );
	}
	// [7] Class instances...
	if ( name !== 'Array' && name !== 'Object' ) {
		// Require ES5 or higher...
		if ( typeof Object.freeze === 'function' ) {
			return cloneInstance( val );
		}
		return {};
	}
	// [8] Arrays and objects...
	keys = objectKeys( val );
	if ( level > 0 ) {
		for ( j = 0; j < keys.length; j++ ) {
			key = keys[ j ];
			x = val[ key ];

			// [8.1] Primitive, Buffer, special class instance...
			name = typeName( x );
			if ( typeof x !== 'object' || x === null || (name !== 'Array' && name !== 'Object') || isBuffer( x ) ) {
				copy[ key ] = deepCopy( x );
				continue;
			}
			// [8.2] Circular reference...
			i = indexOf( cache, x );
			if ( i !== -1 ) {
				copy[ key ] = refs[ i ];
				continue;
			}
			// [8.3] Plain array or object...
			ref = ( isArray(x) ) ? [] : {};
			cache.push( x );
			refs.push( ref );
			copy[ key ] = deepCopy( x, ref, cache, refs, level );
		}
	} else {
		for ( j = 0; j < keys.length; j++ ) {
			key = keys[ j ];
			copy[ key ] = val[ key ];
		}
	}
	return copy;
} // end FUNCTION deepCopy()

/**
* FUNCTION: createCopy( value[, level] )
*	Copy or deep clone a value to an arbitrary depth.
*
* @param {*} value - value to be copied
* @param {Number} [level=+infinity] - option to control copy depth. For example, set to `0` for a shallow copy. Default behavior returns a full deep copy.
* @returns {*} copy
*/
function createCopy( val, level ) {
	var copy;
	if ( arguments.length > 1 ) {
		if ( !isNonNegativeInteger( level ) ) {
			throw new TypeError( 'copy()::invalid input argument. Level must be a nonnegative integer. Value: `' + level + '`.' );
		}
		if ( level === 0 ) {
			return val;
		}
	} else {
		level = Number.POSITIVE_INFINITY;
	}
	copy = ( isArray(val) ) ? [] : {};
	return deepCopy( val, copy, [val], [copy], level );
} // end FUNCTION createCopy()


// EXPORTS //

module.exports = createCopy;

},{"type-name":462,"validate.io-array":464,"validate.io-buffer":463,"validate.io-nonnegative-integer":466}],462:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],463:[function(require,module,exports){
/**
*
*	VALIDATE: buffer
*
*
*	DESCRIPTION:
*		- Validates if a value is a Buffer object.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2015. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2015.
*
*/

'use strict';

/**
* FUNCTION: isBuffer( value )
*	Validates if a value is a Buffer object.
*
* @param {*} value - value to validate
* @returns {Boolean} boolean indicating if a value is a Buffer object
*/
function isBuffer( val ) {
	return typeof val === 'object' && val !== null && val.constructor && typeof val.constructor.isBuffer === 'function' && val.constructor.isBuffer( val );
} // end FUNCTION isBuffer()


// EXPORTS //

module.exports = isBuffer;

},{}],464:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],465:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],466:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":467}],467:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":468}],468:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],469:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":464}],470:[function(require,module,exports){
'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isPositiveInteger = require( 'validate.io-positive-integer' );


// IS POSITIVE INTEGER ARRAY //

/**
* FUNCTION: isPositiveIntegerArray( value )
*	Validates if a value is a positive integer array.
*
* @param {*} value - value to be validated
* @returns {Boolean} boolean indicating if a value is a positive integer array
*/
function isPositiveIntegerArray( value ) {
	var len, i;
	if ( !isArray( value ) ) {
		return false;
	}
	len = value.length;
	if ( !len ) {
		return false;
	}
	for ( i = 0; i < len; i++ ) {
		if ( !isPositiveInteger( value[i] ) ) {
			return false;
		}
	}
	return true;
} // end FUNCTION isPositiveIntegerArray()


// EXPORTS //

module.exports = isPositiveIntegerArray;

},{"validate.io-array":464,"validate.io-positive-integer":471}],471:[function(require,module,exports){
/**
*
*	VALIDATE: positive-integer
*
*
*	DESCRIPTION:
*		- Validates if a value is a positive integer.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2015. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2015.
*
*/

'use strict';

// MODULES //

var isInteger = require( 'validate.io-integer' );


// IS POSITIVE INTEGER //

/**
* FUNCTION: isPositiveInteger( value )
*	Validates if a value is a positive integer.
*
* @param {*} value - value to be validated
* @returns {Boolean} boolean indicating if a value is a positive integer
*/
function isPositiveInteger( value ) {
	return isInteger( value ) && value > 0;
} // end FUNCTION isPositiveInteger()


// EXPORTS //

module.exports = isPositiveInteger;

},{"validate.io-integer":472}],472:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":473}],473:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],474:[function(require,module,exports){
'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isIntegerArray = require( 'validate.io-integer-array' ),
	isFunction = require( 'validate.io-function' );


// VARIABLES //

var MAXINT = Math.pow( 2, 53 ) - 1;


// FUNCTIONS //

/**
* FUNCTION: gcd( a, b )
*	Computes the greatest common divisor of two integers `a` and `b`, using the binary GCD algorithm.
*
* @param {Number} a - integer
* @param {Number} b - integer
* @returns {Number} greatest common divisor
*/
function gcd( a, b ) {
	var k = 1,
		t;
	// Simple cases:
	if ( a === 0 ) {
		return b;
	}
	if ( b === 0 ) {
		return a;
	}
	// Reduce `a` and/or `b` to odd numbers and keep track of the greatest power of 2 dividing both `a` and `b`...
	while ( a%2 === 0 && b%2 === 0 ) {
		a = a / 2; // right shift
		b = b / 2; // right shift
		k = k * 2; // left shift
	}
	// Reduce `a` to an odd number...
	while ( a%2 === 0 ) {
		a = a / 2; // right shift
	}
	// Henceforth, `a` is always odd...
	while ( b ) {
		// Remove all factors of 2 in `b`, as they are not common...
		while ( b%2 === 0 ) {
			b = b / 2; // right shift
		}
		// `a` and `b` are both odd. Swap values such that `b` is the larger of the two values, and then set `b` to the difference (which is even)...
		if ( a > b ) {
			t = b;
			b = a;
			a = t;
		}
		b = b - a; // b=0 iff b=a
	}
	// Restore common factors of 2...
	return k * a;
} // end FUNCTION gcd()

/**
* FUNCTION: bitwise( a, b )
*	Computes the greatest common divisor of two integers `a` and `b`, using the binary GCD algorithm and bitwise operations.
*
* @param {Number} a - safe integer
* @param {Number} b - safe integer
* @returns {Number} greatest common divisor
*/
function bitwise( a, b ) {
	var k = 0,
		t;
	// Simple cases:
	if ( a === 0 ) {
		return b;
	}
	if ( b === 0 ) {
		return a;
	}
	// Reduce `a` and/or `b` to odd numbers and keep track of the greatest power of 2 dividing both `a` and `b`...
	while ( (a & 1) === 0 && (b & 1) === 0 ) {
		a >>>= 1; // right shift
		b >>>= 1; // right shift
		k++;
	}
	// Reduce `a` to an odd number...
	while ( (a & 1) === 0 ) {
		a >>>= 1; // right shift
	}
	// Henceforth, `a` is always odd...
	while ( b ) {
		// Remove all factors of 2 in `b`, as they are not common...
		while ( (b & 1) === 0 ) {
			b >>>= 1; // right shift
		}
		// `a` and `b` are both odd. Swap values such that `b` is the larger of the two values, and then set `b` to the difference (which is even)...
		if ( a > b ) {
			t = b;
			b = a;
			a = t;
		}
		b = b - a; // b=0 iff b=a
	}
	// Restore common factors of 2...
	return a << k;
} // end FUNCTION bitwise()


// GREATEST COMMON DIVISOR //

/**
* FUNCTION: compute( arr[, clbk] )
*	Computes the greatest common divisor.
*
* @param {Number[]|Number} arr - input array of integers
* @param {Function|Number} [clbk] - accessor function for accessing array values
* @returns {Number|Null} greatest common divisor or null
*/
function compute() {
	var nargs = arguments.length,
		args,
		clbk,
		arr,
		len,
		a, b,
		i;

	// Copy the input arguments to an array...
	args = new Array( nargs );
	for ( i = 0; i < nargs; i++ ) {
		args[ i ] = arguments[ i ];
	}
	// Have we been provided with integer arguments?
	if ( isIntegerArray( args ) ) {
		if ( nargs === 2 ) {
			a = args[ 0 ];
			b = args[ 1 ];
			if ( a < 0 ) {
				a = -a;
			}
			if ( b < 0 ) {
				b = -b;
			}
			if ( a <= MAXINT && b <= MAXINT ) {
				return bitwise( a, b );
			} else {
				return gcd( a, b );
			}
		}
		arr = args;
	}
	// If not integers, ensure the first argument is an array...
	else if ( !isArray( args[ 0 ] ) ) {
		throw new TypeError( 'gcd()::invalid input argument. Must provide an array of integers. Value: `' + args[ 0 ] + '`.' );
	}
	// Have we been provided with more than one argument? If so, ensure that the accessor argument is a function...
	else if ( nargs > 1 ) {
		arr = args[ 0 ];
		clbk = args[ 1 ];
		if ( !isFunction( clbk ) ) {
			throw new TypeError( 'gcd()::invalid input argument. Accessor must be a function. Value: `' + clbk + '`.' );
		}
	}
	// We have been provided an array...
	else {
		arr = args[ 0 ];
	}
	len = arr.length;

	// Check if a sufficient number of values have been provided...
	if ( len < 2 ) {
		return null;
	}
	// If an accessor is provided, extract the array values...
	if ( clbk ) {
		a = new Array( len );
		for ( i = 0; i < len; i++ ) {
			a[ i ] = clbk( arr[ i ], i );
		}
		arr = a;
	}
	// Given an input array, ensure all array values are integers...
	if ( nargs < 3 ) {
		if ( !isIntegerArray( arr ) ) {
			throw new TypeError( 'gcd()::invalid input argument. Accessed array values must be integers. Value: `' + arr + '`.' );
		}
	}
	// Convert any negative integers to positive integers...
	for ( i = 0; i < len; i++ ) {
		a = arr[ i ];
		if ( a < 0 ) {
			arr[ i ] = -a;
		}
	}
	// Exploit the fact that the gcd is an associative function...
	a = arr[ 0 ];
	for ( i = 1; i < len; i++ ) {
		b = arr[ i ];
		if ( b <= MAXINT && a <= MAXINT ) {
			a = bitwise( a, b );
		} else {
			a = gcd( a, b );
		}
	}
	return a;
} // end FUNCTION compute()


// EXPORTS //

module.exports = compute;

},{"validate.io-array":475,"validate.io-function":476,"validate.io-integer-array":477}],475:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],476:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],477:[function(require,module,exports){
/**
*
*	VALIDATE: integer-array
*
*
*	DESCRIPTION:
*		- Validates if a value is an integer array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2015. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2015.
*
*/

'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isInteger = require( 'validate.io-integer' );


// IS INTEGER ARRAY //

/**
* FUNCTION: isIntegerArray( value )
*	Validates if a value is an integer array.
*
* @param {*} value - value to be validated
* @returns {Boolean} boolean indicating if a value is an integer array
*/
function isIntegerArray( value ) {
	var len;
	if ( !isArray( value ) ) {
		return false;
	}
	len = value.length;
	if ( !len ) {
		return false;
	}
	for ( var i = 0; i < len; i++ ) {
		if ( !isInteger( value[i] ) ) {
			return false;
		}
	}
	return true;
} // end FUNCTION isIntegerArray()


// EXPORTS //

module.exports = isIntegerArray;

},{"validate.io-array":475,"validate.io-integer":478}],478:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":479}],479:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],480:[function(require,module,exports){
'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isNumber = require( 'validate.io-number-primitive' ),
	isString = require( 'validate.io-string-primitive' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isObject = require( 'validate.io-object' );


// GREATER THAN OR EQUAL TO //

/**
* FUNCTION: geq( arr, x[, opts] )
*	Computes an element-wise comparison (greater than or equal to) of an array.
*
* @param {Number[]|Array} arr - input array
* @param {Number[]|Array|Number|String} x - comparator
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating whether to return a new array
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @returns {Number[]} array of 1s and 0s, where a `1` indicates that an input array element is greater than or equal to a compared value and `0` indicates that an input array element is not greater than or equal to a compared value
*/
function geq( arr, x, opts ) {
	var isArr = isArray( x ),
		copy = true,
		arity,
		clbk,
		out,
		len,
		i;

	if ( !isArray( arr ) ) {
		throw new TypeError( 'geq()::invalid input argument. Must provide an array. Value: `' + arr + '`.' );
	}
	if ( !isArr && !isNumber( x ) && !isString( x ) ) {
		throw new TypeError( 'geq()::invalid input argument. Comparison input must either be an array, number primitive, or string primitive. Value: `' + x + '`.' );
	}
	if ( arguments.length > 2 ) {
		if ( !isObject( opts ) ) {
			throw new TypeError( 'geq()::invalid input argument. Options argument must be an object. Value: `' + opts + '`.' );
		}
		if ( opts.hasOwnProperty( 'copy' ) ) {
			copy = opts.copy;
			if ( !isBoolean( copy ) ) {
				throw new TypeError( 'geq()::invalid option. Copy option must be a boolean primitive. Option: `' + copy + '`.' );
			}
		}
		if ( opts.hasOwnProperty( 'accessor' ) ) {
			clbk = opts.accessor;
			if ( !isFunction( clbk ) ) {
				throw new TypeError( 'geq()::invalid option. Accessor must be a function. Option: `' + clbk + '`.' );
			}
			arity = clbk.length;
		}
	}
	len = arr.length;
	if ( copy ) {
		out = new Array( len );
	} else {
		out = arr;
	}
	// Case 1: comparison array
	if ( isArr ) {
		if ( len !== x.length ) {
			throw new Error( 'geq()::invalid input argument. Comparison array must have a length equal to that of the input array.' );
		}
		if ( arity === 3 ) { // clbk implied
			for ( i = 0; i < len; i++ ) {
				if ( clbk( arr[i], i, 0 ) >= clbk( x[i], i, 1 ) ) {
					out[ i ] = 1;
				} else {
					out[ i ] = 0;
				}
			}
		}
		else if ( clbk ) {
			for ( i = 0; i < len; i++ ) {
				if ( clbk( arr[i], i ) >= x[ i ] ) {
					out[ i ] = 1;
				} else {
					out[ i ] = 0;
				}
			}
		}
		else {
			for ( i = 0; i < len; i++ ) {
				if ( arr[ i ] >= x[ i ] ) {
					out[ i ] = 1;
				} else {
					out[ i ] = 0;
				}
			}
		}
	}
	// Case 2: accessor and single comparator
	else if ( clbk ) {
		for ( i = 0; i < len; i++ ) {
			if ( clbk( arr[ i ], i ) >= x ) {
				out[ i ] = 1;
			} else {
				out[ i ] = 0;
			}
		}
	}
	// Case 3: single comparator
	else {
		for ( i = 0; i < len; i++ ) {
			if ( arr[ i ] >= x ) {
				out[ i ] = 1;
			} else {
				out[ i ] = 0;
			}
		}
	}
	return out;
} // end FUNCTION geq()


// EXPORTS //

module.exports = geq;

},{"validate.io-array":481,"validate.io-boolean-primitive":482,"validate.io-function":483,"validate.io-number-primitive":484,"validate.io-object":485,"validate.io-string-primitive":486}],481:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],482:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],483:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],484:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],485:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":481}],486:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],487:[function(require,module,exports){
/**
*
*	COMPUTE: gmean
*
*
*	DESCRIPTION:
*		- Computes the geometric mean of an array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014-2015. Rebekah Smith.
*
*
*	AUTHOR:
*		Rebekah Smith. rebekahjs17@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isFunction = require( 'validate.io-function' );


// GEOMETRIC MEAN //

/**
* FUNCTION: gmean( arr[, accessor ] )
*	Computes the geometric mean of an array.
*
* @param {Array} arr - input array
* @param {Function} [accessor] - accessor function for accessing numeric array values
* @returns {Number|null} geometric mean
*/
function gmean( arr, clbk ) {
	if ( !isArray( arr ) ) {
		throw new TypeError( 'gmean()::invalid input argument. Must provide an array. Value: `' + arr + '`.' );
	}
	if ( arguments.length > 1 && !isFunction( clbk ) ) {
		throw new TypeError( 'gmean()::invalid input argument. Accessor must be a function. Value: `' + clbk + '`.' );
	}
	var len = arr.length,
		sum = 0,
		val,
		i;

	if ( !len ) {
		return null;
	}
	if ( clbk ) {
		for ( i = 0; i < len; i++ ) {
			val = clbk( arr[ i ] );
			if ( val <= 0 ) {
				return NaN;
			}
			sum += Math.log( val ) / len;
		}
	} else {
		for ( i = 0; i < len; i++ ) {
			val = arr[ i ];
			if ( val <= 0 ) {
				return NaN;
			}
			sum += Math.log( val ) / len;
		}
	}
	return Math.exp( sum );
} // end FUNCTION gmean()


// EXPORTS //

module.exports = gmean;

},{"validate.io-array":488,"validate.io-function":489}],488:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],489:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],490:[function(require,module,exports){
'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isNumber = require( 'validate.io-number-primitive' ),
	isString = require( 'validate.io-string-primitive' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isObject = require( 'validate.io-object' );


// GREATER THAN //

/**
* FUNCTION: gt( arr, x[, opts] )
*	Computes an element-wise comparison (greater than) of an array.
*
* @param {Number[]|Array} arr - input array
* @param {Number[]|Array|Number|String} x - comparator
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating whether to return a new array
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @returns {Number[]} array of 1s and 0s, where a `1` indicates that an input array element is greater than a compared value and `0` indicates that an input array element is not greater than a compared value
*/
function gt( arr, x, opts ) {
	var isArr = isArray( x ),
		copy = true,
		arity,
		clbk,
		out,
		len,
		i;

	if ( !isArray( arr ) ) {
		throw new TypeError( 'gt()::invalid input argument. Must provide an array. Value: `' + arr + '`.' );
	}
	if ( !isArr && !isNumber( x ) && !isString( x ) ) {
		throw new TypeError( 'gt()::invalid input argument. Comparison input must either be an array, number primitive, or string primitive. Value: `' + x + '`.' );
	}
	if ( arguments.length > 2 ) {
		if ( !isObject( opts ) ) {
			throw new TypeError( 'gt()::invalid input argument. Options argument must be an object. Value: `' + opts + '`.' );
		}
		if ( opts.hasOwnProperty( 'copy' ) ) {
			copy = opts.copy;
			if ( !isBoolean( copy ) ) {
				throw new TypeError( 'gt()::invalid option. Copy option must be a boolean primitive. Option: `' + copy + '`.' );
			}
		}
		if ( opts.hasOwnProperty( 'accessor' ) ) {
			clbk = opts.accessor;
			if ( !isFunction( clbk ) ) {
				throw new TypeError( 'gt()::invalid option. Accessor must be a function. Option: `' + clbk + '`.' );
			}
			arity = clbk.length;
		}
	}
	len = arr.length;
	if ( copy ) {
		out = new Array( len );
	} else {
		out = arr;
	}
	// Case 1: comparison array
	if ( isArr ) {
		if ( len !== x.length ) {
			throw new Error( 'gt()::invalid input argument. Comparison array must have a length equal to that of the input array.' );
		}
		if ( arity === 3 ) { // clbk implied
			for ( i = 0; i < len; i++ ) {
				if ( clbk( arr[i], i, 0 ) > clbk( x[i], i, 1 ) ) {
					out[ i ] = 1;
				} else {
					out[ i ] = 0;
				}
			}
		}
		else if ( clbk ) {
			for ( i = 0; i < len; i++ ) {
				if ( clbk( arr[i], i ) > x[ i ] ) {
					out[ i ] = 1;
				} else {
					out[ i ] = 0;
				}
			}
		}
		else {
			for ( i = 0; i < len; i++ ) {
				if ( arr[ i ] > x[ i ] ) {
					out[ i ] = 1;
				} else {
					out[ i ] = 0;
				}
			}
		}
	}
	// Case 2: accessor and single comparator
	else if ( clbk ) {
		for ( i = 0; i < len; i++ ) {
			if ( clbk( arr[ i ], i ) > x ) {
				out[ i ] = 1;
			} else {
				out[ i ] = 0;
			}
		}
	}
	// Case 3: single comparator
	else {
		for ( i = 0; i < len; i++ ) {
			if ( arr[ i ] > x ) {
				out[ i ] = 1;
			} else {
				out[ i ] = 0;
			}
		}
	}
	return out;
} // end FUNCTION gt()


// EXPORTS //

module.exports = gt;

},{"validate.io-array":491,"validate.io-boolean-primitive":492,"validate.io-function":493,"validate.io-number-primitive":494,"validate.io-object":495,"validate.io-string-primitive":496}],491:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],492:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],493:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],494:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],495:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":491}],496:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],497:[function(require,module,exports){
'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isString = require( 'validate.io-string' ),
	isFunction = require( 'validate.io-function' );


// HAMMING DISTANCE //

/**
* FUNCTION: hamming( a, b, accessor )
*	Computes the Hamming distance between two sequences.
*
* @param {String|Array} a - array or string sequence
* @param {String|Array} b - array or string sequence
* @param {Function} [accessor] - accessor function for accessing array values
* @returns {Number} Hamming distance
*/
function hamming( a, b, clbk ) {
	var aType = isString( a ),
		bType = isString( b ),
		len,
		d, i;

	if ( !isArray( a ) && !aType ) {
		throw new TypeError( 'hamming()::invalid input argument. Sequence must be either an array or a string. Value: `' + a + '`.' );
	}
	if ( !isArray( b ) && !bType ) {
		throw new TypeError( 'hamming()::invalid input argument. Sequence must be either an array or a string. Value: `' + b + '`.' );
	}
	if ( aType !== bType ) {
		throw new TypeError( 'hamming()::invalid input arguments. Sequences must be the same type; i.e., both strings or both arrays.' );
	}
	if ( arguments.length > 2 ) {
		if ( !isFunction( clbk ) ) {
			throw new TypeError( 'hamming()::invalid input argument. Accessor must be a function. Value: `' + clbk + '`.' );
		}
	}
	len = a.length;
	if ( len !== b.length ) {
		throw new Error( 'hamming()::invalid input arguments. Sequences must be the same length.' );
	}
	d = 0;
	if ( clbk ) {
		for ( i = 0; i < len; i++ ) {
			if ( clbk( a[i], i, 0 ) !== clbk( b[i], i, 1 ) ) {
				d += 1;
			}
		}
	} else {
		for ( i = 0; i < len; i++ ) {
			if ( a[ i ] !== b[ i ] ) {
				d += 1;
			}
		}
	}
	return d;
} // end FUNCTION hamming()


// EXPORTS //

module.exports = hamming;

},{"validate.io-array":498,"validate.io-function":499,"validate.io-string":500}],498:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],499:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],500:[function(require,module,exports){
/**
*
*	VALIDATE: string
*
*
*	DESCRIPTION:
*		- Validates if a value is a string.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

/**
* FUNCTION: isString( value )
*	Validates if a value is a string.
*
* @param {*} value - value to be validated
* @returns {Boolean} boolean indicating whether value is a string
*/
function isString( value ) {
	return typeof value === 'string' || Object.prototype.toString.call( value ) === '[object String]';
} // end FUNCTION isString()


// EXPORTS //

module.exports = isString;

},{}],501:[function(require,module,exports){
/**
*
*	COMPUTE: hmean
*
*
*	DESCRIPTION:
*		- Computes the harmonic mean over an array of values.
*
*
*	NOTES:
*		[1] 
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Rebekah Smith.
*
*
*	AUTHOR:
*		Rebekah Smith. rebekahjs17@gmail.com. 2014.
*
*/

(function() {
	'use strict';

	// HMEAN //

	/**
	* FUNCTION: hmean( arr )
	*	Computes the harmonic mean over an array of values.
	*
	* @param {Array} arr - array of values
	* @returns {Number} harmonic mean
	*/
	function hmean( arr ) {
		if ( !Array.isArray( arr ) ) {
			throw new TypeError( 'hmean()::invalid input argument. Must provide an array.' );
		}
		var len = arr.length,
			sum = 0,
			val;
		for ( var i = 0; i < len; i++ ) {
			val = arr[ i ];
			if ( val <= 0 ) {
				return NaN;
			}
			sum += 1 / val;
		}
		return len / sum;
	} // end FUNCTION hmean()


	// EXPORTS //

	module.exports = hmean;

})();
},{}],502:[function(require,module,exports){
/**
*
*	COMPUTE: hypot
*
*
*	DESCRIPTION:
*		- Computes the hypotenuse of a right triangle.
*
*
*	NOTES:
*		[1] Based on http://www.johndcook.com/blog/2010/06/02/whats-so-hard-about-finding-a-hypotenuse/ .
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

(function() {
	'use strict';

	// MODULES //

	var isNumeric = require( 'validate.io-number' );


	// HYPOT //

	/**
	* FUNCTION: hypot( a, b )
	*	Computes the hypotenuse of a right triangle.
	*
	* @param {Number} a - length of one side of triangle
	* @param {Number} b - length of other side of a triangle
	* @returns {Number} hypotenuse
	*/
	function hypot( a, b ) {
		if ( !isNumeric( a ) || !isNumeric( b ) ) {
			throw new TypeError( 'hypot()::invalid input argument. Input arguments must be numeric.' );
		}
		var min, max, r;
		a = Math.abs( a );
		b = Math.abs( b );
		if ( a > b ) {
			min = b;
			max = a;
		} else  {
			min = a;
			max = b;
		}
		r = min / max;
		return max * Math.sqrt( 1 + r*r );
	} // end FUNCTION hypot()


	// EXPORTS //

	module.exports = hypot;

})();
},{"validate.io-number":503}],503:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],504:[function(require,module,exports){
/**
*
*	COMPUTE: idr
*
*
*	DESCRIPTION:
*		- Computes the interdecile range for an array of values.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Rebekah Smith.
*
*
*	AUTHOR:
*		Rebekah Smith. rebekahjs17@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	quantile = require( 'compute-quantile' );


// FUNCTIONS //

/**
* FUNCTION: ascending( a, b )
*	Comparator function used to sort values in ascending order.
*
* @private
* @param {Number} a
* @param {Number} b
* @returns {Number} difference between `a` and `b`
*/
function ascending( a, b ) {
	return a - b;
} // end FUNCTION ascending()


// INTERDECILE RANGE //

/**
* FUNCTION: idr( arr[, opts] )
*	Computes the interdecile range for an array.
*
* @param {Array} arr - array of values
* @param {Object} [opts] - quantile options
* @returns {Number} interdecile range
*/
function idr( arr, opts ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'idr()::invalid input argument. Must provide an array.' );
	}
	if ( arguments.length > 1 ) {
		if ( !isObject( opts ) ) {
			throw new TypeError( 'idr()::invalid input argument. Options should be an object.' );
		}
	} else {
		opts = {
			'sorted': false
		};
	}
	if ( !opts.sorted ) {
		arr = arr.slice();
		arr.sort( ascending );
		opts.sorted = true;
	}
	return quantile( arr, 0.90, opts ) - quantile( arr, 0.10, opts );
} // end FUNCTION idr()


// EXPORTS //

module.exports = idr;

},{"compute-quantile":797,"validate.io-object":505}],505:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":506}],506:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],507:[function(require,module,exports){
/**
*
*	COMPUTE: incrdatespace
*
*
*	DESCRIPTION:
*		- Generates an array of linearly spaced dates using a provided increment.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

var isObject = require( 'validate.io-object' );


// VARIABLES //

var rounders,
	convert,
	timestamp,
	units,
	reUnits,
	fmt;

// Rounding options:
rounders = [
	'floor',
	'ceil',
	'round'
];

// Units:
units = [
	'y',
	'b',
	'w',
	'd',
	'h',
	'm',
	's',
	'ms'
];

// Conversions to milliseconds:
convert = {
	'ms': 1,
	's': 1000, // 1*1000
	'm': 60000, // 60*1000
	'h': 3600000, // 60000*60
	'd': 86400000, // 3600000*24
	'w': 604800000, // 86400000*7
	'b': 2629800000, // 365.25/12*86400000
	'y': 31557600000 // 365.25*86400000
};

// Regular expressions...
timestamp = /^\d{10}$|^\d{13}$/;

reUnits = {
	'ms': /^\d{0,}ms$|^\d{0,}millisecond$|^\d{0,}milliseconds$/,
	's': /^\d{0,}s$|^\d{0,}sec$|^\d{0,}secs$|^\d{0,}second$|^\d{0,}seconds$/,
	'm': /^\d{0,}m$|^\d{0,}min$|^\d{0,}mins$|^\d{0,}minute$|^\d{0,}minutes$/,
	'h': /^\d{0,}h$|^\d{0,}hr$|^\d{0,}hrs$|^\d{0,}hour$|^\d{0,}hours$/,
	'd': /^\d{0,}d$|^\d{0,}day$|^\d{0,}days$/,
	'w': /^\d{0,}w$|^\d{0,}wk$|^\d{0,}wks$|^\d{0,}week$|^\d{0,}weeks$/,
	'b': /^\d{0,}b$|^\d{0,}month$|^\d{0,}months$/,
	'y': /^\d{0,}y$|^\d{0,}yr$|^\d{0,}yrs$|^\d{0,}year$|^\d{0,}years$/
};

fmt = /^\d{0,}[a-z]+$/;


// FUNCTIONS //

/**
* FUNCTION: validDate( value, name )
*	Validates a date parameter.
*
* @private
* @param {*} value - value to be validated
* @param {String} name - name to be used in error messages
* @returns {Date} validated date
*/
function validDate( value, name ) {
	var type;

	type = typeof value;
	if ( type === 'string' ) {
		value = Date.parse( value );
		if ( value !== value ) {
			throw new Error( 'incrdatespace()::invalid input argument. Unable to parse ' +  name.toLowerCase() + ' date.' );
		}
		value = new Date( value );
	}
	if ( type === 'number' ) {
		if ( !timestamp.test( value ) ) {
			throw new Error( 'incrdatespace()::invalid input argument. Numeric ' + name.toLowerCase() + ' date must be either a Unix or Javascript timestamp.' );
		}
		if ( value.toString().length === 10 ) {
			value = value * 1000; // sec to ms
		}
		value = new Date( value );
	}
	if ( !(value instanceof Date) ) {
		throw new TypeError( 'incrdatespace()::invalid input argument. ' + name + ' date must either be a date string, Date object, Unix timestamp, or JavaScript timestamp.' );
	}
	return value;
} // end FUNCTION validDate()

/**
* FUNCTION: validIncrement( x )
*	Validates an increment.
*
* @private
* @param {String|Number} x - increment to be validated
* @returns {Number} increment in milliseconds
*/
function validIncrement( x ) {
	var N = units.length,
		sign = false,
		parts,
		len,
		unit,
		val,
		flg,
		i;

	if ( typeof x === 'number' ) {
		if ( x !== x ) {
			throw new TypeError( 'incrdatespace()::invalid value. Increment must be a valid number.' );
		}
		return x;
	}
	if ( typeof x !== 'string' ) {
		throw new TypeError( 'incrdatespace()::invalid value. Increment must be either a string or number.' );
	}
	// Convert the formatted string to milliseconds...
	if ( x[ 0 ] === '-' ) {
		sign = true;
		x = x.substr( 1 );
	}
	parts = x.split( '.' );
	len = parts.length;
	x = 0;
	while ( len ) {
		flg = false;
		val = parts.pop();
		if ( !fmt.test( val ) ) {
			throw new Error( 'incrdatespace()::invalid value. Scalar unit pair must have the following format: `ms`, `5ms`, `5days`, etc.' );
		}
		for ( i = 0; i < N; i++ ) {
			unit = units[ i ];
			if ( reUnits[ unit ].test( val ) ) {
				flg = true;
				val = parseInt( val, 10 );
				if ( val !== val ) {
					// No scalar...
					val = 1;
				}
				x += val * convert[ unit ];
				break;
			}
		}
		if ( !flg ) {
			throw new Error( 'incrdatespace()::invalid value. Unrecognized unit: `' + val + '`.' );
		}
		len = parts.length;
	}
	if ( sign ) {
		return -x;
	}
	return x;
} // end FUNCTION validIncrement()


// INCRDATESPACE //

/**
* FUNCTION: incrdatespace( start, stop[, increment, options])
*	Generates an array of linearly spaced dates using a provided increment.
*
* @param {Date|Number|String} start - start time as either a `Date` object, Unix timestamp, JavaScript timestamp, or date string
* @param {Data|Number|String} stop - stop time as either a `Date` object, Unix timestamp, JavaScript timestamp, or date string
* @param {Number|String} [increment] - value by which to increment successive dates (default: 'day')
* @param {Object} [options] - function options
* @param {String} [options.round] - specifies how sub-millisecond times should be rounded: [ 'floor', 'ceil', 'round' ] (default: 'floor' )
* @returns {Array} array of dates
*/
function incrdatespace( start, stop, increment, options ) {
	var nArgs = arguments.length,
		opts = {
			'round': 'floor'
		},
		incr = convert[ 'd' ],
		flg = true,
		round,
		len,
		i,
		tmp,
		arr;

	start = validDate( start, 'Start' );
	stop = validDate( stop, 'Stop' );

	if ( nArgs > 2 ) {
		if ( nArgs === 3 ) {
			if ( isObject( increment ) ) {
				opts = increment;
			} else {
				incr = increment;

				// Turn off checking the options object...
				flg = false;
			}
		} else {
			opts = options;
			incr = increment;
		}
		incr = validIncrement( incr );
		if ( flg ) {
			if ( !isObject( opts ) ) {
				throw new TypeError( 'incrdatespace()::invalid input argument. Options must be an object.' );
			}
			if ( opts.hasOwnProperty( 'round' ) ) {
				if ( typeof opts.round !== 'string' ) {
					throw new TypeError( 'incrdatespace()::invalid input argument. Round option must be a string.' );
				}
				if ( rounders.indexOf( opts.round ) === -1 ) {
					throw new Error( 'incrdatespace()::invalid input argument. Unrecognized round option. Must be one of [' + rounders.join( ',' ) + '].' );
				}
			} else {
				opts.round = 'floor';
			}
		}
	}
	round = Math[ opts.round ];

	// Calculate the array length:
	len = Math.ceil( ( stop-start ) / incr );
	if ( len < 0 ) {
		return [ start ];
	}

	// Build the output array...
	if ( len > 64000 ) {
		// Ensure fast elements...
		arr = [];
		arr.push( start );
		tmp = start.getTime();
		for ( i = 1; i < len; i++ ) {
			tmp += incr;
			arr.push( new Date( round( tmp ) ) );
		}
		return arr;
	}
	arr = new Array( len );
	arr[ 0 ] = start;
	tmp = start.getTime();
	for ( i = 1; i < len; i++ ) {
		tmp += incr;
		arr[ i ] = new Date( round( tmp ) );
	}
	return arr;
} // end FUNCTION incrdatespace()


// EXPORTS //

module.exports = incrdatespace;

},{"validate.io-object":508}],508:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":509}],509:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],510:[function(require,module,exports){
'use strict';

// INCREMENTAL MAX //

/**
* FUNCTION: incrmax()
*	Returns a method to compute the maximum value incrementally.
*
* @returns {Function} method to compute the max incrementally
*/
function incrmax() {
	var max = null;
	/**
	* FUNCTION: incrmax( [value] )
	*	If a `value` is provided, updates and returns the updated max. If no `value` is provided, returns the current max.
	*
	* @param {Number} [value] - value used to update the max
	* @returns {Number|Null} max value or null
	*/
	return function incrmax( x ) {
		if ( !arguments.length ) {
			return max;
		}
		if ( x > max || max === null ) {
			max = x;
		}
		return max;
	};
} // end FUNCTION incrmax()


// EXPORTS //

module.exports = incrmax;

},{}],511:[function(require,module,exports){
/**
*
*	COMPUTE: incrmean
*
*
*	DESCRIPTION:
*		- Provides a method to compute an arithmetic mean incrementally.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// INCREMENTAL MEAN //

/**
* FUNCTION: incrmean()
*	Returns a method to compute an arithmetic mean incrementally.
*
* @returns {Function} method to compute an arithmetic mean incrementally
*/
function incrmean() {
	var mu = 0,
		N = 0,
		delta;
	/**
	* FUNCTION: incrmean( [value] )
	*	If a `value` is provided, updates and returns the updated mean. If no `value` is provided, returns the current mean.
	*
	* @param {Number} [value] - value used to update the mean
	* @returns {Number} mean value
	*/
	return function incrmean( x ) {
		if ( !arguments.length ) {
			return mu;
		}
		N += 1;
		delta = x - mu;
		mu += delta / N;
		return mu;
	};
} // end FUNCTION incrmean()


// EXPORTS //

module.exports = incrmean;

},{}],512:[function(require,module,exports){
'use strict';

// INCREMENTAL MIN //

/**
* FUNCTION: incrmin()
*	Returns a method to compute the minimum value incrementally.
*
* @returns {Function} method to compute the min incrementally
*/
function incrmin() {
	var min = null;
	/**
	* FUNCTION: incrmin( [value] )
	*	If a `value` is provided, updates and returns the updated min. If no `value` is provided, returns the current min.
	*
	* @param {Number} [value] - value used to update the min
	* @returns {Number|Null} min value
	*/
	return function incrmin( x ) {
		if ( !arguments.length ) {
			return min;
		}
		if ( x < min || min === null ) {
			min = x;
		}
		return min;
	};
} // end FUNCTION incrmin()


// EXPORTS //

module.exports = incrmin;

},{}],513:[function(require,module,exports){
'use strict';

// MODULES //

var isPositiveInteger = require( 'validate.io-positive-integer' );


// INCREMENTAL MOVING MEAN //

/**
* FUNCTION: incrmmean( W )
*	Returns a method to compute a moving arithmetic mean incrementally.
*
* @param {Number} W - window size
* @returns {Function} method to compute a moving arithmetic mean incrementally
*/
function incrmmean( W ) {
	if ( !isPositiveInteger( W ) ) {
		throw new TypeError( 'incrmmean()::invalid input argument. Window size must be a positive integer. Value: `' + W + '`.' );
	}
	var arr = new Array( W ),
		mu = 0,
		N = 0,
		i = -1,
		delta;
	/**
	* FUNCTION: incrmmean( [value] )
	*	If a `value` is provided, updates and returns the updated mean. If no `value` is provided, returns the current mean.
	*
	* @param {Number} [value] - value used to update the moving mean
	* @returns {Number|Null} mean or null
	*/
	return function incrmmean( x ) {
		if ( !arguments.length ) {
			if ( N === 0 ) {
				return null;
			}
			return mu;
		}
		// Update the index for managing the circular buffer...
		i = (i+1) % W;

		// Fill up the initial window; else, update the existing window...
		if ( N < W ) {
			N += 1;
			delta = x - mu;
			mu += delta / N;
		} else {
			delta = x - arr[ i ];
			mu += delta / W;
		}
		arr[ i ] = x;
		return mu;
	};
} // end FUNCTION incrmmean()


// EXPORTS //

module.exports = incrmmean;

},{"validate.io-positive-integer":514}],514:[function(require,module,exports){
arguments[4][471][0].apply(exports,arguments)
},{"dup":471,"validate.io-integer":515}],515:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":516}],516:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],517:[function(require,module,exports){
'use strict';

// MODULES //

var isPositiveInteger = require( 'validate.io-positive-integer' );


// INCREMENTAL MOVING SAMPLE STANDARD DEVIATION //

/**
* FUNCTION: incrmstdev( W )
*	Returns a method to compute a moving sample standard deviation incrementally.
*
* @param {Number} W - window size
* @returns {Function} method to compute a moving sample standard deviation incrementally
*/
function incrmstdev( W ) {
	if ( !isPositiveInteger( W ) ) {
		throw new TypeError( 'incrmstdev()::invalid input argument. Window size must be a positive integer. Value: `' + W + '`.' );
	}
	var arr = new Array( W ),
		n = W - 1,
		M2 = 0,
		mu = 0,
		N = 0,
		i = -1,
		delta,
		tmp,
		d1,
		d2;
	/**
	* FUNCTION: incrmstdev( [value] )
	*	If a `value` is provided, updates and returns the updated sample standard deviation. If no `value` is provided, returns the current sample standard deviation.
	*
	* @param {Number} [value] - value used to update the moving sample standard deviation
	* @returns {Number|Null} sample standard deviation or null
	*/
	return function incrmstdev( x ) {
		if ( !arguments.length ) {
			if ( N === 0 ) {
				return null;
			}
			if ( N === 1 ) {
				return 0;
			}
			if ( N < W ) {
				return Math.sqrt( M2 / (N-1) );
			}
			return Math.sqrt( M2 / n );
		}
		// Update the index for managing the circular buffer...
		i = (i+1) % W;

		// Fill up the initial window; else, update the existing window...
		if ( N < W ) {
			arr[ i ] = x;
			N += 1;
			delta = x - mu;
			mu += delta / N;
			M2 += delta * (x - mu);
			if ( N === 1 ) {
				return 0;
			}
			return Math.sqrt( M2 / (N-1) );
		}
		if ( N === 1 ) {
			return 0;
		}
		tmp = arr[ i ];
		arr[ i ] = x;
		delta = x - tmp;
		d1 = tmp - mu;
		mu += delta / W;
		d2 = x - mu;
		M2 += delta * (d1 + d2);

		return Math.sqrt( M2 / n );
	};
} // end FUNCTION incrmstdev()


// EXPORTS //

module.exports = incrmstdev;

},{"validate.io-positive-integer":518}],518:[function(require,module,exports){
arguments[4][471][0].apply(exports,arguments)
},{"dup":471,"validate.io-integer":519}],519:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":520}],520:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],521:[function(require,module,exports){
'use strict';

// MODULES //

var isPositiveInteger = require( 'validate.io-positive-integer' );


// INCREMENTAL MOVING SUM //

/**
* FUNCTION: incrmsum( W )
*	Returns a method to compute a moving sum incrementally.
*
* @param {Number} W - window size
* @returns {Function} method to compute a moving sum incrementally
*/
function incrmsum( W ) {
	if ( !isPositiveInteger( W ) ) {
		throw new TypeError( 'incrmsum()::invalid input argument. Window size must be a positive integer. Value: `' + W + '`.' );
	}
	var arr = new Array( W ),
		sum = 0,
		N = 0,
		i = -1;
	/**
	* FUNCTION: incrmsum( [value] )
	*	If a `value` is provided, updates and returns the updated sum. If no `value` is provided, returns the current sum.
	*
	* @param {Number} [value] - value used to update the moving sum
	* @returns {Number|Null} sum or null
	*/
	return function incrmsum( x ) {
		if ( !arguments.length ) {
			if ( N === 0 ) {
				return null;
			}
			return sum;
		}
		// Update the index for managing the circular buffer...
		i = (i+1) % W;

		// Fill up the initial window; else, update the existing window...
		if ( N < W ) {
			N += 1;
			sum += x;
		} else {
			sum += x - arr[ i ];
		}
		arr[ i ] = x;
		return sum;
	};
} // end FUNCTION incrmsum()


// EXPORTS //

module.exports = incrmsum;

},{"validate.io-positive-integer":522}],522:[function(require,module,exports){
arguments[4][471][0].apply(exports,arguments)
},{"dup":471,"validate.io-integer":523}],523:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":524}],524:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],525:[function(require,module,exports){
'use strict';

// MODULES //

var isPositiveInteger = require( 'validate.io-positive-integer' );


// INCREMENTAL MOVING SAMPLE VARIANCE //

/**
* FUNCTION: incrmvariance( W )
*	Returns a method to compute a moving sample variance incrementally.
*
* @param {Number} W - window size
* @returns {Function} method to compute a moving sample variance incrementally
*/
function incrmvariance( W ) {
	if ( !isPositiveInteger( W ) ) {
		throw new TypeError( 'incrmvariance()::invalid input argument. Window size must be a positive integer. Value: `' + W + '`.' );
	}
	var arr = new Array( W ),
		n = W - 1,
		M2 = 0,
		mu = 0,
		N = 0,
		i = -1,
		delta,
		tmp,
		d1,
		d2;
	/**
	* FUNCTION: incrmvariance( [value] )
	*	If a `value` is provided, updates and returns the updated sample variance. If no `value` is provided, returns the current sample variance.
	*
	* @param {Number} [value] - value used to update the moving sample variance
	* @returns {Number|Null} sample variance or null
	*/
	return function incrmvariance( x ) {
		if ( !arguments.length ) {
			if ( N === 0 ) {
				return null;
			}
			if ( N === 1 ) {
				return 0;
			}
			if ( N < W ) {
				return M2 / (N-1);
			}
			return M2 / n;
		}
		// Update the index for managing the circular buffer...
		i = (i+1) % W;

		// Fill up the initial window; else, update the existing window...
		if ( N < W ) {
			arr[ i ] = x;
			N += 1;
			delta = x - mu;
			mu += delta / N;
			M2 += delta * (x - mu);
			if ( N === 1 ) {
				return 0;
			}
			return M2 / (N-1);
		}
		if ( N === 1 ) {
			return 0;
		}
		tmp = arr[ i ];
		arr[ i ] = x;
		delta = x - tmp;
		d1 = tmp - mu;
		mu += delta / W;
		d2 = x - mu;
		M2 += delta * (d1 + d2);

		return M2 / n;
	};
} // end FUNCTION incrmvariance()


// EXPORTS //

module.exports = incrmvariance;

},{"validate.io-positive-integer":526}],526:[function(require,module,exports){
arguments[4][471][0].apply(exports,arguments)
},{"dup":471,"validate.io-integer":527}],527:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":528}],528:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],529:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' );


// VARIABLES //

var MAXLENGTH = Math.pow( 2, 32 ) - 1;


// INCRSPACE //

/**
* FUNCTION: incrspace( start, stop[, increment] )
*	Generates a linearly spaced numeric array using a provided increment.
*
* @param {Number} start - first array value
* @param {Number} stop - array element bound
* @param {Number} [increment] - increment
* @returns {Array} linearly spaced numeric array
*/
function incrspace( x1, x2, increment ) {
	var arr,
		len,
		inc,
		i;
	if ( !isNumber( x1 ) ) {
		throw new TypeError( 'incrspace()::invalid input argument. Start must be numeric. Value: `' + x1 + '`.' );
	}
	if ( !isNumber( x2 ) ) {
		throw new TypeError( 'incrspace()::invalid input argument. Stop must be numeric. Value: `' + x2 + '`.' );
	}
	if ( arguments.length < 3 ) {
		inc = 1;
	} else {
		inc = increment;
		if ( !isNumber( inc ) ) {
			throw new TypeError( 'incrspace()::invalid input argument. Increment must be numeric. Value: `' + inc + '`.' );
		}
	}
	len = Math.ceil( ( x2-x1 ) / inc );

	if ( len > MAXLENGTH ) {
		throw new RangeError( 'incrspace()::invalid input arguments. Generated array exceeds maximum array length.' );
	}
	if ( len <= 1 ) {
		return [ x1 ];
	}
	if ( len > 64000 ) {
		// Ensure fast elements...
		arr = [];
		arr.push( x1 );
		for ( i = 1; i < len; i++ ) {
			arr.push( x1 + inc*i );
		}
	} else {
		arr = new Array( len );
		arr[ 0 ] = x1;
		for ( i = 1; i < len; i++ ) {
			arr[ i ] = x1 + inc*i;
		}
	}
	return arr;
} // end FUNCTION incrspace()


// EXPORTS //

module.exports = incrspace;

},{"validate.io-number-primitive":530}],530:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],531:[function(require,module,exports){
/**
*
*	COMPUTE: incrstdev
*
*
*	DESCRIPTION:
*		- Provides a method to compute a sample standard deviation incrementally.
*
*
*	NOTES:
*		[1] 
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

(function() {
	'use strict';

	// INCREMENTAL //

	/**
	* FUNCTION: incrstdev()
	*	Returns a method to compute the sample standard deviation incrementally.
	*
	* @returns {Function} method to compute the sample standard deviation incrementally
	*/
	function incrstdev() {
		var mu = 0,
			N = 0,
			M2 = 0,
			delta;
		/**
		* FUNCTION: incrstdev( [value] )
		*	If a `value` is provided, updates and returns the updated sample standard deviation. If no `value` is provided, returns the current sample standard deviation.
		*
		* @param {Number} [value] - value used to update the sample standard deviation
		* @returns {Number} sample standard deviation
		*/
		return function incrstdev( x ) {
			if ( !arguments.length ) {
				if ( N < 2 ) {
					return 0;
				}
				return Math.sqrt( M2 / (N-1) );
			}
			N += 1;
			delta = x - mu;
			mu += delta / N;
			M2 += delta * ( x - mu );
			if ( N < 2 ) {
				return 0;
			}
			return Math.sqrt( M2 / (N-1) );
		};
	} // end FUNCTION incrstdev()


	// EXPORTS //

	module.exports = incrstdev;

})();
},{}],532:[function(require,module,exports){
'use strict';

// INCREMENTAL SUM //

/**
* FUNCTION: incrsum()
*	Returns a method to compute the sum incrementally.
*
* @returns {Function} method to compute the sum incrementally
*/
function incrsum() {
	var sum = 0;
	/**
	* FUNCTION: incrsum( [value] )
	*	If a `value` is provided, updates and returns the updated sum. If no `value` is provided, returns the current sum.
	*
	* @param {Number} [value] - value used to update the sum
	* @returns {Number} sum
	*/
	return function incrsum( x ) {
		if ( !arguments.length ) {
			return sum;
		}
		sum += x;
		return sum;
	};
} // end FUNCTION incrsum()


// EXPORTS //

module.exports = incrsum;

},{}],533:[function(require,module,exports){
/**
*
*	COMPUTE: incrvariance
*
*
*	DESCRIPTION:
*		- Provides a method to compute a sample variance incrementally.
*
*
*	NOTES:
*		[1] 
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

(function() {
	'use strict';

	// INCREMENTAL SAMPLE VARIANCE //

	/**
	* FUNCTION: incrvariance()
	*	Returns a method to compute the sample variance incrementally.
	*
	* @returns {Function} method to compute the sample variance incrementally
	*/
	function incrvariance() {
		var mu = 0,
			N = 0,
			M2 = 0,
			delta;
		/**
		* FUNCTION: incrvariance( [value] )
		*	If a `value` is provided, updates and returns the updated sample variance. If no `value` is provided, returns the current sample variance.
		*
		* @param {Number} [value] - value used to update the sample variance
		* @returns {Number} sample variance
		*/
		return function incrvariance( x ) {
			if ( !arguments.length ) {
				if ( N < 2 ) {
					return 0;
				}
				return M2 / (N-1);
			}
			N += 1;
			delta = x - mu;
			mu += delta / N;
			M2 += delta * ( x - mu );
			if ( N < 2 ) {
				return 0;
			}
			return M2 / (N-1);
		};
	} // end FUNCTION incrvariance()


	// EXPORTS //

	module.exports = incrvariance;

})();
},{}],534:[function(require,module,exports){
/**
*
*	COMPUTE: indexspace
*
*
*	DESCRIPTION:
*		- Generates a linearly spaced index array from a subsequence string.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2015. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2015.
*
*/

'use strict';

// MODULES //

var isString = require( 'validate.io-string-primitive' ),
	isNonNegativeInteger = require( 'validate.io-nonnegative-integer' );


// VARIABLES //

var re = /^(?:(?:-(?=\d+))?\d*|end(?:-\d+|\/\d+)?):(?:(?:-(?=\d+))?\d*|end(?:-\d+|\/\d+)?)(?:\:(?=-?\d*)(?:-?\d+)?)?$/;

/**
*	^(...)
*	=> require that the string begin with either a digit (+ or -), an `end` keyword, or a colon
*
*	(?:(?:-(?=\d+))?\d*|end(?:-?\d+|/\\d+)?)
*	=> match but do not capture
*		(?:-(?=\d+))?
*		=> match but do not capture a minus sign but only if followed by one or more digits
*		\d*
*		=> 0 or more digits
*		|
*		=> OR
*		end(?:-\d+|/\d+)?
*		=> the word `end` exactly, which may be followed by either a minus sign and 1 or more digits or a division sign and 1 or more digits
*
*	:
*	=> match a colon exactly
*
*	(?:(?:-(?=\d+))?\d*|end(?:-\d+|/\\d+)?)
*	=> same as above
*
*	(?:\:(?=-?\d*)(?:-?\d+)?)?
*	=> match but do not capture
*		\:(?=-?\d*)
*		=> a colon but only if followed by either a possible minus sign and 0 or more digits
*		(?:-?\d+)?
*		=> match but do not capture a possible minus sign and 1 or more digits
*
*	$
*	=> require that the string end with either a digit, `end` keyword, or a colon.
*
*
* Examples:
*	-	:
*	-	::
*	-	4:
*	-	:4
*	-	::-1
*	-	3::-1
*	-	:10:2
*	-	1:3:1
*	-	9:1:-3
*	-	1:-1
*	-	:-1
*	-	-5:
*	-	1:-5:2
*	-	-9:10:1
*	-	-9:-4:2
*	-	-4:-9:-2
*	-	1:end:2
*	-	:end/2
*	-	end/2:end:5
*/

var reEnd = /^end/,
	reMatch = /(-|\/)(?=\d+)(\d+)?$/;


// INDEXSPACE

/**
* FUNCTION: indexspace( str, len )
*	Generates a linearly spaced index array from a subsequence string.
*
* @param {String} str - subsequence string
* @param {Number} len - reference array length
* @returns {Number[]} array of indices
*/
function indexspace( str, len ) {
	var x1,
		x2,
		tmp,
		inc,
		arr;
	if ( !isString( str ) || !re.test( str ) ) {
		throw new Error( 'indexspace()::invalid input argument. Invalid subsequence syntax. Please consult documentation. Value: `' + str + '`.' );
	}
	if ( !isNonNegativeInteger( len ) ) {
		throw new TypeError( 'indexspace()::invalid input argument. Reference array length must be a nonnegative integer. Value: `' + len + '`.' );
	}
	if ( !len ) {
		return [];
	}
	str = str.split( ':' );
	x1 = str[ 0 ];
	x2 = str[ 1 ];

	if ( str.length === 2 ) {
		inc = 1;
	} else {
		inc = parseInt( str[ 2 ], 10 );
	}
	// Handle zero increment...
	if ( inc === 0 ) {
		throw new Error( 'indexspace()::invalid syntax. Increment must be an integer not equal to 0. Value: `' + inc + '`.' );
	}

	// START //

	// Handle use of 'end' keyword...
	if ( reEnd.test( x1 ) ) {
		tmp = x1.match( reMatch );
		if ( tmp ) {
			if ( tmp[ 1 ] === '-' ) {
				x1 = len - 1 - parseInt( tmp[ 2 ], 10 );
				if ( x1 < 0 ) {
					// WARNING: forgive the user for exceeding the range bounds...
					x1 = 0;
				}
			} else {
				x1 = (len-1) / parseInt( tmp[ 2 ], 10 );
				x1 = Math.ceil( x1 );
			}
		} else {
			x1 = len - 1;
		}
	} else {
		x1 = parseInt( x1, 10 );

		// Handle empty index...
		if ( x1 !== x1 ) {
			// :-?\d*:-?\d+
			if ( inc < 0 ) {
				// Max index:
				x1 = len - 1;
			} else {
				// Min index:
				x1 = 0;
			}
		}
		// Handle negative index...
		else if ( x1 < 0 ) {
			x1 = len + x1; // len-x1
			if ( x1 < 0 ) {
				// WARNING: forgive the user for exceeding index bounds...
				x1 = 0;
			}
		}
		// Handle exceeding bounds...
		else if ( x1 >= len ) {
			return [];
		}
	}

	// END //

	// NOTE: here, we determine an inclusive `end` value; i.e., the last acceptable index value.

	// Handle use of 'end' keyword...
	if ( reEnd.test( x2 ) ) {
		tmp = x2.match( reMatch );
		if ( tmp ) {
			if ( tmp[ 1 ] === '-' ) {
				x2 = len - 1 - parseInt( tmp[ 2 ], 10 );
				if ( x2 < 0 ) {
					// WARNING: forgive the user for exceeding the range bounds...
					x2 = 0;
				}
			} else {
				x2 = (len-1) / parseInt( tmp[ 2 ], 10 );
				x2 = Math.ceil( x2 ) - 1;
			}
		} else {
			x2 = len - 1;
		}
	} else {
		x2 = parseInt( x2, 10 );

		// Handle empty index...
		if ( x2 !== x2 ) {
			// -?\d*::-?\d+
			if ( inc < 0 ) {
				// Min index:
				x2 = 0;
			} else {
				// Max index:
				x2 = len - 1;
			}
		}
		// Handle negative index...
		else if ( x2 < 0 ) {
			x2 = len + x2; // len-x2
			if ( x2 < 0 ) {
				// WARNING: forgive the user for exceeding index bounds...
				x2 = 0;
			}
			if ( inc > 0 ) {
				x2 = x2 - 1;
			}
		}
		// Handle positive index...
		else {
			if ( inc < 0 ) {
				x2 = x2 + 1;
			}
			else if ( x2 >= len ) {
				x2 = len - 1;
			}
			else {
				x2 = x2 - 1;
			}
		}
	}

	// INDICES //

	arr = [];
	if ( inc < 0 ) {
		if ( x2 > x1 ) {
			return arr;
		}
		while ( x1 >= x2 ) {
			arr.push( x1 );
			x1 += inc;
		}
	} else {
		if ( x1 > x2 ) {
			return arr;
		}
		while ( x1 <= x2 ) {
			arr.push( x1 );
			x1 += inc;
		}
	}
	return arr;
} // end FUNCTION indexspace()


// EXPORTS //

module.exports = indexspace;

},{"validate.io-nonnegative-integer":535,"validate.io-string-primitive":538}],535:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":536}],536:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":537}],537:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],538:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],539:[function(require,module,exports){
/**
*
*	COMPUTE: iqr
*
*
*	DESCRIPTION:
*		- Computes the interquartile range for an array of values.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	quantile = require( 'compute-quantile' );


// FUNCTIONS //

/**
* FUNCTION: ascending( a, b )
*	Comparator function used to sort values in ascending order.
*
* @private
* @param {Number} a
* @param {Number} b
* @returns {Number} difference between `a` and `b`
*/
function ascending( a, b ) {
	return a - b;
} // end FUNCTION ascending()


// INTERQUARTILE RANGE //

/**
* FUNCTION: iqr( arr )
*	Computes the interquartile range for an array.
*
* @param {Array} arr - array of values
* @returns {Number} interquartile range
*/
function iqr( arr, opts ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'iqr()::invalid input argument. Must provide an array.' );
	}
	if ( arguments.length > 1 ) {
		if ( !isObject( opts ) ) {
			throw new TypeError( 'iqr()::invalid input argument. Options should be an object.' );
		}
	} else {
		opts = {
			'sorted': false
		};
	}
	if ( !opts.sorted ) {
		arr = arr.slice();
		arr.sort( ascending );
		opts.sorted = true;
	}
	return quantile( arr, 0.75, opts ) - quantile( arr, 0.25, opts );
} // end FUNCTION iqr()


// EXPORTS //

module.exports = iqr;

},{"compute-quantile":797,"validate.io-object":540}],540:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":541}],541:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],542:[function(require,module,exports){
/**
*
*	COMPUTE: isfinite
*
*
*	DESCRIPTION:
*		- Computes for each array element whether an element is a finite number.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// VARIABLES //

var pinf = Number.POSITIVE_INFINITY,
	ninf = Number.NEGATIVE_INFINITY;


// ISFINITE //

/**
* FUNCTION: isfinite( arr )
*	Computes for each array element whether an element is a finite number.
*
* @param {Array} arr - input array
* @param {Array} array of 1s and 0s indicating if an element is a finite number
*/
function isfinite( arr ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'isfinite()::invalid input argument. Must provide an array.' );
	}
	var len = arr.length,
		out = new Array( len ),
		val;

	for ( var i = 0; i < len; i++ ) {
		out[ i ] = 0;
		val = arr[ i ];
		if ( typeof val === 'number' && val === val && val < pinf && val > ninf ) {
			out[ i ] = 1;
		}
	}
	return out;
} // end FUNCTION isfinite()


// EXPORTS //

module.exports = isfinite;

},{}],543:[function(require,module,exports){
/**
*
*	COMPUTE: isinf
*
*
*	DESCRIPTION:
*		- Computes for each array element whether an element is infinite.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// VARIABLES //

var pinf = Number.POSITIVE_INFINITY,
	ninf = Number.NEGATIVE_INFINITY;


// ISINF //

/**
* FUNCTION: isinf( arr )
*	Computes for each array element whether an element is infinite.
*
* @param {Array} arr - input array
* @param {Array} array of 1s and 0s indicating if an element is infinite
*/
function isinf( arr ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'isinf()::invalid input argument. Must provide an array.' );
	}
	var len = arr.length,
		out = new Array( len ),
		val;

	for ( var i = 0; i < len; i++ ) {
		out[ i ] = 0;
		val = arr[ i ];
		if ( val === pinf || val === ninf ) {
			out[ i ] = 1;
		}
	}
	return out;
} // end FUNCTION isinf()


// EXPORTS //

module.exports = isinf;

},{}],544:[function(require,module,exports){
/**
*
*	COMPUTE: isinteger
*
*
*	DESCRIPTION:
*		- Computes for each array element whether an element is an integer.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// ISINTEGER //

/**
* FUNCTION: isinteger( arr )
*	Computes for each array element whether an element is an integer.
*
* @param {Array} arr - input array
* @param {Array} array of 1s and 0s indicating if an element is an integer
*/
function isinteger( arr ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'isinteger()::invalid input argument. Must provide an array.' );
	}
	var len = arr.length,
		out = new Array( len ),
		val;

	for ( var i = 0; i < len; i++ ) {
		out[ i ] = 0;
		val = arr[ i ];
		if ( typeof val === 'number' && val === val && val%1 === 0 ) {
			out[ i ] = 1;
		}
	}
	return out;
} // end FUNCTION isinteger()


// EXPORTS //

module.exports = isinteger;

},{}],545:[function(require,module,exports){
/**
*
*	COMPUTE: isnan
*
*
*	DESCRIPTION:
*		- Computes for each array element whether an element is NaN.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// ISNAN //

/**
* FUNCTION: isnan( arr )
*	Computes for each array element whether an element is NaN.
*
* @param {Array} arr - input array
* @param {Array} array of 1s and 0s indicating if an element is NaN
*/
function isnan( arr ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'isnan()::invalid input argument. Must provide an array.' );
	}
	var len = arr.length,
		out = new Array( len ),
		val;

	for ( var i = 0; i < len; i++ ) {
		out[ i ] = 0;
		val = arr[ i ];
		if ( typeof val !== 'number' || val !== val ) {
			out[ i ] = 1;
		}
	}
	return out;
} // end FUNCTION isnan()


// EXPORTS //

module.exports = isnan;

},{}],546:[function(require,module,exports){
/**
*
*	COMPUTE: isnumeric
*
*
*	DESCRIPTION:
*		- Computes for each array element whether an element is numeric.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// ISNUMERIC //

/**
* FUNCTION: isnumeric( arr )
*	Computes for each array element whether an element is numeric.
*
* @param {Array} arr - input array
* @param {Array} array of 1s and 0s indicating if an element is numeric
*/
function isnumeric( arr ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'isnumeric()::invalid input argument. Must provide an array.' );
	}
	var len = arr.length,
		out = new Array( len ),
		val;

	for ( var i = 0; i < len; i++ ) {
		out[ i ] = 0;
		val = arr[ i ];
		if ( typeof val === 'number' && val === val ) {
			out[ i ] = 1;
		}
	}
	return out;
} // end FUNCTION isnumeric()


// EXPORTS //

module.exports = isnumeric;

},{}],547:[function(require,module,exports){
/**
*
*	COMPUTE: issorted
*
*
*	DESCRIPTION:
*		- Returns a boolean indicating if an input array is sorted.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// ISSORTED //

/**
* FUNCTION: issorted( arr[, comparator] )
*	Returns a boolean indicating if an input array is sorted.
*
* @param {Array} arr - input array
* @param {Function} [comparator] - comparator function invoked for each pair of consecutive elements which returns a numeric value indicating if elements are ordered
* @returns {Boolean} boolean indicating if array is sorted
*/
function issorted( arr, clbk ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'issorted()::invalid input argument. Must provide an array.' );
	}
	if ( arguments.length > 1 ) {
		if ( typeof clbk !== 'function' ) {
			throw new TypeError( 'issorted()::invalid input argument. Comparator must be a function.' );
		}
	}
	var len = arr.length,
		i;
	if ( !clbk ) {
		// Default: ascending order...
		for ( i = 0; i < len-1; i++ ) {
			if ( arr[ i ] > arr[ i+1 ] ) {
				return false;
			}
		}
		return true;
	}
	// Impose arbitrary sort order...
	for ( i = 0; i < len-1; i++ ) {
		if ( clbk( arr[i], arr[i+1] ) > 0 ) {
			return false;
		}
	}
	return true;
} // end FUNCTION issorted()


// EXPORTS //

module.exports = issorted;

},{}],548:[function(require,module,exports){
/**
*
*	COMPUTE: kurtosis
*
*
*	DESCRIPTION:
*		- Computes the sample excess kurtosis of an array of values.
*
*
*	NOTES:
*		[1] 
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

(function() {
	'use strict';

	/**
	* FUNCTION: kurtosis( arr )
	*	Computes the sample excess kurtosis of an array of values.
	*
	* @param {Array} arr - array of values
	* @returns {Number} sample excess kurtosis
	*/
	function kurtosis( arr ) {
		if ( !Array.isArray( arr ) ) {
			throw new TypeError( 'kurtosis()::invalid input argument. Must provide an array.' );
		}
		var len = arr.length,
			delta = 0,
			delta_n = 0,
			delta_n2 = 0,
			term1 = 0,
			N = 0,
			mean = 0,
			M2 = 0,
			M3 = 0,
			M4 = 0,
			g;

		for ( var i = 0; i < len; i++ ) {
			N += 1;

			delta = arr[ i ] - mean;
			delta_n = delta / N;
			delta_n2 = delta_n * delta_n;

			term1 = delta * delta_n * (N-1);

			M4 += term1*delta_n2*(N*N - 3*N + 3) + 6*delta_n2*M2 - 4*delta_n*M3;
			M3 += term1*delta_n*(N-2) - 3*delta_n*M2;
			M2 += term1;
			mean += delta_n;
		}
		// Calculate the population excess kurtosis:
		g = N*M4 / ( M2*M2 ) - 3;
		// Return the corrected sample excess kurtosis:
		return (N-1) / ( (N-2)*(N-3) ) * ( (N+1)*g + 6 );
	} // end FUNCTION kurtosis()


	// EXPORTS //

	module.exports = kurtosis;

})();
},{}],549:[function(require,module,exports){
/**
*
*	COMPUTE: l1norm
*
*
*	DESCRIPTION:
*		- Computes the L1 norm (Manhattan norm) of an array of values.
*
*
*	NOTES:
*		[1] 
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

(function() {
	'use strict';

	// L1NORM //

	/**
	* FUNCTION: l1norm( arr )
	*	Calculates the L1 norm (Manhattan/Taxicab norm) of an array of values.
	*
	* @param {Array} arr - array of values
	* @returns {Number} L1 norm
	*/
	function l1norm( arr ) {
		if ( !Array.isArray( arr ) ) {
			throw new TypeError( 'l1norm()::invalid input argument. Must provide an array.' );
		}
		var len = arr.length,
			sum = 0,
			val;
		for ( var i = 0; i < len; i++ ) {
			val = arr[ i ];
			// Math.abs( val )
			if ( val < 0 ) {
				val = -val;
			}
			sum += val;
		}
		return sum;
	} // end FUNCTION l1norm()


	// EXPORTS //

	module.exports = l1norm;

})();
},{}],550:[function(require,module,exports){
'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isFunction = require( 'validate.io-function' );


// L2NORM //

/**
* FUNCTION: l2norm( arr[, accessor] )
*	Calculates the L2 norm (Euclidean norm) of an array.
*
* @param {Array} arr - input array
* @param {Function} [accessor] - accessor function for accessing array values
* @returns {Number|Null} L2 norm or null
*/
function l2norm( arr, clbk ) {
	if ( !isArray( arr ) ) {
		throw new TypeError( 'l2norm()::invalid input argument. Must provide an array.  Value: `' + arr + '`.' );
	}
	if ( arguments.length > 1 ) {
		if ( !isFunction( clbk ) ) {
			throw new TypeError( 'l2norm()::invalid input argument. Accessor must be a function. Value: `' + clbk + '`.' );
		}
	}
	var len = arr.length,
		t = 0,
		s = 1,
		r,
		val,
		abs,
		i;

	if ( !len ) {
		return null;
	}
	if ( clbk ) {
		for ( i = 0; i < len; i++ ) {
			val = clbk( arr[ i ], i );
			abs = ( val < 0 ) ? -val : val;
			if ( abs > 0 ) {
				if ( abs > t ) {
					r = t / val;
					s = 1 + s*r*r;
					t = abs;
				} else {
					r = val / t;
					s = s + r*r;
				}
			}
		}
	} else {
		for ( i = 0; i < len; i++ ) {
			val = arr[ i ];
			abs = ( val < 0 ) ? -val : val;
			if ( abs > 0 ) {
				if ( abs > t ) {
					r = t / val;
					s = 1 + s*r*r;
					t = abs;
				} else {
					r = val / t;
					s = s + r*r;
				}
			}
		}
	}
	return t * Math.sqrt( s );
} // end FUNCTION l2norm()


// EXPORTS //

module.exports = l2norm;

},{"validate.io-array":551,"validate.io-function":552}],551:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],552:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],553:[function(require,module,exports){
'use strict';

// MODULES //

var gcd = require( 'compute-gcd' ),
	isArray = require( 'validate.io-array' ),
	isIntegerArray = require( 'validate.io-integer-array' ),
	isFunction = require( 'validate.io-function' );


// LEAST COMMON MULTIPLE //

/**
* FUNCTION: lcm( arr[, clbk] )
*	Computes the least common multiple (lcm).
*
* @param {Number[]|Number} arr - input array of integers
* @param {Function|Number} [accessor] - accessor function for accessing array values
* @returns {Number|Null} least common multiple or null
*/
function lcm() {
	var nargs = arguments.length,
		args,
		clbk,
		arr,
		len,
		a, b,
		i;

	// Copy the input arguments to an array...
	args = new Array( nargs );
	for ( i = 0; i < nargs; i++ ) {
		args[ i ] = arguments[ i ];
	}
	// Have we been provided with integer arguments?
	if ( isIntegerArray( args ) ) {
		if ( nargs === 2 ) {
			a = args[ 0 ];
			b = args[ 1 ];
			if ( a < 0 ) {
				a = -a;
			}
			if ( b < 0 ) {
				b = -b;
			}
			if ( a === 0 || b === 0 ) {
				return 0;
			}
			return ( a/gcd(a,b) ) * b;
		}
		arr = args;
	}
	// If not integers, ensure that the first argument is an array...
	else if ( !isArray( args[ 0 ] ) ) {
		throw new TypeError( 'lcm()::invalid input argument. Must provide an array of integers. Value: `' + args[ 0 ] + '`.' );
	}
	// Have we been provided with more than one argument? If so, ensure that the accessor argument is a function...
	else if ( nargs > 1 ) {
		arr = args[ 0 ];
		clbk = args[ 1 ];
		if ( !isFunction( clbk ) ) {
			throw new TypeError( 'lcm()::invalid input argument. Accessor must be a function. Value: `' + clbk + '`.' );
		}
	}
	// We have been provided an array...
	else {
		arr = args[ 0 ];
	}
	len = arr.length;

	// Check if a sufficient number of values have been provided...
	if ( len < 2 ) {
		return null;
	}
	// If an accessor is provided, extract the array values...
	if ( clbk ) {
		a = new Array( len );
		for ( i = 0; i < len; i++ ) {
			a[ i ] = clbk( arr[ i ], i );
		}
		arr = a;
	}
	// Given an input array, ensure all array values are integers...
	if ( nargs < 3 ) {
		if ( !isIntegerArray( arr ) ) {
			throw new TypeError( 'lcm()::invalid input argument. Accessed array values must be integers. Value: `' + arr + '`.' );
		}
	}
	// Convert any negative integers to positive integers...
	for ( i = 0; i < len; i++ ) {
		a = arr[ i ];
		if ( a < 0 ) {
			arr[ i ] = -a;
		}
	}
	// Exploit the fact that the lcm is an associative function...
	a = arr[ 0 ];
	for ( i = 1; i < len; i++ ) {
		b = arr[ i ];
		if ( a === 0 || b === 0 ) {
			return 0;
		}
		a = ( a/gcd(a,b) ) * b;
	}
	return a;
} // end FUNCTION lcm()


// EXPORTS //

module.exports = lcm;

},{"compute-gcd":474,"validate.io-array":554,"validate.io-function":555,"validate.io-integer-array":556}],554:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],555:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],556:[function(require,module,exports){
arguments[4][477][0].apply(exports,arguments)
},{"dup":477,"validate.io-array":554,"validate.io-integer":557}],557:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":558}],558:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],559:[function(require,module,exports){
'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isNumber = require( 'validate.io-number-primitive' ),
	isString = require( 'validate.io-string-primitive' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isObject = require( 'validate.io-object' );


// LESS THAN OR EQUAL TO //

/**
* FUNCTION: leq( arr, x[, opts] )
*	Computes an element-wise comparison (less than or equal to) of an array.
*
* @param {Number[]|Array} arr - input array
* @param {Number[]|Array|Number|String} x - comparator
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating whether to return a new array
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @returns {Number[]} array of 1s and 0s, where a `1` indicates that an input array element is less than or equal to a compared value and `0` indicates that an input array element is not less than or equal to a compared value
*/
function leq( arr, x, opts ) {
	var isArr = isArray( x ),
		copy = true,
		arity,
		clbk,
		out,
		len,
		i;

	if ( !isArray( arr ) ) {
		throw new TypeError( 'leq()::invalid input argument. Must provide an array. Value: `' + arr + '`.' );
	}
	if ( !isArr && !isNumber( x ) && !isString( x ) ) {
		throw new TypeError( 'leq()::invalid input argument. Comparison input must either be an array, number primitive, or string primitive. Value: `' + x + '`.' );
	}
	if ( arguments.length > 2 ) {
		if ( !isObject( opts ) ) {
			throw new TypeError( 'leq()::invalid input argument. Options argument must be an object. Value: `' + opts + '`.' );
		}
		if ( opts.hasOwnProperty( 'copy' ) ) {
			copy = opts.copy;
			if ( !isBoolean( copy ) ) {
				throw new TypeError( 'leq()::invalid option. Copy option must be a boolean primitive. Option: `' + copy + '`.' );
			}
		}
		if ( opts.hasOwnProperty( 'accessor' ) ) {
			clbk = opts.accessor;
			if ( !isFunction( clbk ) ) {
				throw new TypeError( 'leq()::invalid option. Accessor must be a function. Option: `' + clbk + '`.' );
			}
			arity = clbk.length;
		}
	}
	len = arr.length;
	if ( copy ) {
		out = new Array( len );
	} else {
		out = arr;
	}
	// Case 1: comparison array
	if ( isArr ) {
		if ( len !== x.length ) {
			throw new Error( 'leq()::invalid input argument. Comparison array must have a length equal to that of the input array.' );
		}
		if ( arity === 3 ) { // clbk implied
			for ( i = 0; i < len; i++ ) {
				if ( clbk( arr[i], i, 0 ) <= clbk( x[i], i, 1 ) ) {
					out[ i ] = 1;
				} else {
					out[ i ] = 0;
				}
			}
		}
		else if ( clbk ) {
			for ( i = 0; i < len; i++ ) {
				if ( clbk( arr[i], i ) <= x[ i ] ) {
					out[ i ] = 1;
				} else {
					out[ i ] = 0;
				}
			}
		}
		else {
			for ( i = 0; i < len; i++ ) {
				if ( arr[ i ] <= x[ i ] ) {
					out[ i ] = 1;
				} else {
					out[ i ] = 0;
				}
			}
		}
	}
	// Case 2: accessor and single comparator
	else if ( clbk ) {
		for ( i = 0; i < len; i++ ) {
			if ( clbk( arr[ i ], i ) <= x ) {
				out[ i ] = 1;
			} else {
				out[ i ] = 0;
			}
		}
	}
	// Case 3: single comparator
	else {
		for ( i = 0; i < len; i++ ) {
			if ( arr[ i ] <= x ) {
				out[ i ] = 1;
			} else {
				out[ i ] = 0;
			}
		}
	}
	return out;
} // end FUNCTION leq()


// EXPORTS //

module.exports = leq;


},{"validate.io-array":560,"validate.io-boolean-primitive":561,"validate.io-function":562,"validate.io-number-primitive":563,"validate.io-object":564,"validate.io-string-primitive":565}],560:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],561:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],562:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],563:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],564:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":560}],565:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],566:[function(require,module,exports){
/**
*
*	COMPUTE: linfnorm
*
*
*	DESCRIPTION:
*		- Computes the infinity norm (Chebyshev/supremum norm) of an array of values.
*
*
*	NOTES:
*		[1] 
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

(function() {
	'use strict';

	// LPNORM //

	/**
	* FUNCTION: linfnorm( arr )
	*	Calculates the infinity norm of an array of values.
	*
	* @param {Array} arr - array of values
	* @returns {Number} infinity norm
	*/
	function linfnorm( arr ) {
		if ( !Array.isArray( arr ) ) {
			throw new TypeError( 'linfnorm()::invalid input argument. Must provide an array.' );
		}
		var len = arr.length,
			val = arr[ 0 ],
			max;

		if ( val < 0 ) {
			val = -val;
		}
		max = val;
		for ( var i = 1; i < len; i++ ) {
			val = arr[ i ];
			if ( val < 0 ) {
				val = -val;
			}
			if ( val > max ) {
				max = val;
			}
		}
		return max;
	} // end FUNCTION linfnorm()


	// EXPORTS //

	module.exports = linfnorm;

})();
},{}],567:[function(require,module,exports){
/**
*
*	COMPUTE: linspace
*
*
*	DESCRIPTION:
*		- Generates a linearly spaced numeric array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isInteger = require( 'validate.io-integer' );


// LINSPACE //

/**
* FUNCTION: linspace( start, stop[, length] )
*	Generates a linearly spaced numeric array.
*
* @param {Number} start - first array value
* @param {Number} stop - last array value
* @param {Number} [length] - length of output array
* @returns {Array} linearly spaced numeric array
*/
function linspace( x1, x2, len ) {
	var arr,
		end,
		tmp,
		d;

	if ( typeof x1 !== 'number' || x1 !== x1 ) {
		throw new TypeError( 'linspace()::invalid input argument. Start must be numeric.' );
	}
	if ( typeof x2 !== 'number' || x2 !== x2 ) {
		throw new TypeError( 'linspace()::invalid input argument. Stop must be numeric.' );
	}
	if ( arguments.length < 3 ) {
		len = 100;
	} else {
		if ( !isInteger( len ) || len < 0 ) {
			throw new TypeError( 'linspace()::invalid input argument. Length must be a positive integer.' );
		}
		if ( len === 0 ) {
			return [];
		}
	}
	// Calculate the increment:
	end = len - 1;
	d = ( x2-x1 ) / end;

	// Build the output array...
	arr = new Array( len );
	tmp = x1;
	arr[ 0 ] = tmp;
	for ( var i = 1; i < end; i++ ) {
		tmp += d;
		arr[ i ] = tmp;
	}
	arr[ end ] = x2;
	return arr;
} // end FUNCTION linspace()


// EXPORTS //

module.exports = linspace;

},{"validate.io-integer":568}],568:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":569}],569:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],570:[function(require,module,exports){
/**
*
*	COMPUTE: lmidmean
*
*
*	DESCRIPTION:
*		- Computes the interquartile mean of the values below the median for a numeric array (lower midmean).
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Rebekah Smith.
*
*
*	AUTHOR:
*		Rebekah Smith. rebekahjs17@gmail.com. 2014.
*
*/

'use strict';

// FUNCTIONS //

/**
* FUNCTION: ascending( a, b )
*	Comparator function used to sort values in ascending order.
*
* @private
* @param {Number} a
* @param {Number} b
* @returns {Number} difference between `a` and `b`
*/
function ascending( a, b ) {
	return a - b;
} // end FUNCTION ascending()


// LOWER MIDMEAN //

/**
* FUNCTION: lmidmean( arr[, sorted] )
*	Computes the interquartile mean of the values below the median in a numeric array (lower midmean).
*
* @param {Array} arr - numeric array
* @param {Boolean} [sorted] - boolean flag indicating if the input array is sorted in ascending order
* @returns {Number} lower midmean
*/
function lmidmean( arr, sorted ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'lmidmean()::invalid input argument. Must provide an array.' );
	}
	if ( arguments.length > 1 && typeof sorted !== 'boolean' ) {
		throw new TypeError( 'lmidmean()::invalid input argument. Second argument must be a boolean.' );
	}
	if ( arr.length < 6 ) {
		throw new Error( 'lmidmean()::invalid input argument. Input array must have 6 or more elements.' );
	}
	if ( !sorted ) {
		arr = arr.slice();
		arr.sort( ascending );
	}
	var len = arr.length,
		mean = 0,
		N = 0,
		delta,
		low,
		high;

	// Quartiles sit between values
	if ( len%8 === 0 ) {
		low = len*0.125;
		high = len*0.375 - 1;
	}
	else {
		low = Math.ceil( len*0.125 );
		high = Math.floor( len*0.375 ) - 1;
	}

	// Compute an arithmetic mean...
	for ( var i = low; i <= high; i++ ) {
		N += 1;
		delta = arr[ i ] - mean;
		mean += delta / N;
	}

	return mean;
} // end FUNCTION lmidmean()


// EXPORTS //

module.exports = lmidmean;

},{}],571:[function(require,module,exports){
/**
*
*	COMPUTE: logspace
*
*
*	DESCRIPTION:
*		- Generates a logarithmically spaced numeric array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isInteger = require( 'validate.io-integer' );


// LOGSPACE //

/**
* FUNCTION: logspace( a, b[, length] )
*	Generates a logarithmically spaced numeric array.
*
* @param {Number} a - exponent of start value
* @param {Number} b - exponent of end value
* @param {Number} [length] - length of output array (default: 10)
* @returns {Array} logarithmically spaced numeric array
*/
function logspace( a, b, len ) {
	var arr,
		end,
		tmp,
		d;

	if ( typeof a !== 'number' || a !== a ) {
		throw new TypeError( 'logspace()::invalid input argument. Exponent of start value must be numeric.' );
	}
	if ( typeof b !== 'number' || b !== b ) {
		throw new TypeError( 'logspace()::invalid input argument. Exponent of stop value must be numeric.' );
	}
	if ( arguments.length < 3 ) {
		len = 10;
	} else {
		if ( !isInteger( len ) || len < 0 ) {
			throw new TypeError( 'logspace()::invalid input argument. Length must be a positive integer.' );
		}
		if ( len === 0 ) {
			return [];
		}
	}
	// Calculate the increment:
	end = len - 1;
	d = ( b-a ) / end;

	// Build the output array...
	arr = new Array( len );
	tmp = a;
	arr[ 0 ] = Math.pow( 10, tmp );
	for ( var i = 1; i < end; i++ ) {
		tmp += d;
		arr[ i ] = Math.pow( 10, tmp );
	}
	arr[ end ] = Math.pow( 10, b );
	return arr;
} // end FUNCTION logspace()


// EXPORTS //

module.exports = logspace;

},{"validate.io-integer":572}],572:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":573}],573:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],574:[function(require,module,exports){
/**
*
*	COMPUTE: lpnorm
*
*
*	DESCRIPTION:
*		- Computes the Lp norm of an array of values.
*
*
*	NOTES:
*		[1] 
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

(function() {
	'use strict';

	// MODULES //

	var isInteger = require( 'validate.io-integer' ),
		l1norm = require( 'compute-l1norm' ),
		l2norm = require( 'compute-l2norm' ),
		linfnorm = require( 'compute-linfnorm' );


	// LPNORM //

	/**
	* FUNCTION: lpnorm( arr[, p] )
	*	Calculates the LP norm of an array of values.
	*
	* @param {Array} arr - array of values
	* @param {Number} [p] - norm
	* @returns {Number} LP norm
	*/
	function lpnorm( arr, p ) {
		var inf = Number.POSITIVE_INFINITY;
		if ( !Array.isArray( arr ) ) {
			throw new TypeError( 'lpnorm()::invalid input argument. First argument must be an array.' );
		}
		if ( arguments.length === 1 ) {
			p = 2;
		} else if ( (!isInteger( p ) && p !== inf ) || p < 1 ) {
			throw new TypeError( 'lpnorm()::invalid input argument. Second argument should be a positive integer greater than 0.' );
		}
		if ( p === 1 ) {
			return l1norm( arr );
		}
		else if ( p === 2 ) {
			return l2norm( arr );
		}
		else if ( p === inf ) {
			return linfnorm( arr );
		}
		var len = arr.length,
			sum = 0,
			val;
		for ( var i = 0; i < len; i++ ) {
			val = arr[ i ];
			if ( val < 0 ) {
				val = -val;
			}
			sum += Math.pow( val, p );
		}
		return Math.pow( sum, 1/p );
	} // end FUNCTION lpnorm()


	// EXPORTS //

	module.exports = lpnorm;

})();
},{"compute-l1norm":549,"compute-l2norm":550,"compute-linfnorm":566,"validate.io-integer":575}],575:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":576}],576:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],577:[function(require,module,exports){
'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isNumber = require( 'validate.io-number-primitive' ),
	isString = require( 'validate.io-string-primitive' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isObject = require( 'validate.io-object' );


// LESS THAN //

/**
* FUNCTION: lt( arr, x[, opts] )
*	Computes an element-wise comparison (less than) of an array.
*
* @param {Number[]|Array} arr - input array
* @param {Number[]|Array|Number|String} x - comparator
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating whether to return a new array
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @returns {Number[]} array of 1s and 0s, where a `1` indicates that an input array element is less than a compared value and `0` indicates that an input array element is not less than a compared value
*/
function lt( arr, x, opts ) {
	var isArr = isArray( x ),
		copy = true,
		arity,
		clbk,
		out,
		len,
		i;

	if ( !isArray( arr ) ) {
		throw new TypeError( 'lt()::invalid input argument. Must provide an array. Value: `' + arr + '`.' );
	}
	if ( !isArr && !isNumber( x ) && !isString( x ) ) {
		throw new TypeError( 'lt()::invalid input argument. Comparison input must either be an array, number primitive, or string primitive. Value: `' + x + '`.' );
	}
	if ( arguments.length > 2 ) {
		if ( !isObject( opts ) ) {
			throw new TypeError( 'lt()::invalid input argument. Options argument must be an object. Value: `' + opts + '`.' );
		}
		if ( opts.hasOwnProperty( 'copy' ) ) {
			copy = opts.copy;
			if ( !isBoolean( copy ) ) {
				throw new TypeError( 'lt()::invalid option. Copy option must be a boolean primitive. Option: `' + copy + '`.' );
			}
		}
		if ( opts.hasOwnProperty( 'accessor' ) ) {
			clbk = opts.accessor;
			if ( !isFunction( clbk ) ) {
				throw new TypeError( 'lt()::invalid option. Accessor must be a function. Option: `' + clbk + '`.' );
			}
			arity = clbk.length;
		}
	}
	len = arr.length;
	if ( copy ) {
		out = new Array( len );
	} else {
		out = arr;
	}
	// Case 1: comparison array
	if ( isArr ) {
		if ( len !== x.length ) {
			throw new Error( 'lt()::invalid input argument. Comparison array must have a length equal to that of the input array.' );
		}
		if ( arity === 3 ) { // clbk implied
			for ( i = 0; i < len; i++ ) {
				if ( clbk( arr[i], i, 0 ) < clbk( x[i], i, 1 ) ) {
					out[ i ] = 1;
				} else {
					out[ i ] = 0;
				}
			}
		}
		else if ( clbk ) {
			for ( i = 0; i < len; i++ ) {
				if ( clbk( arr[i], i ) < x[ i ] ) {
					out[ i ] = 1;
				} else {
					out[ i ] = 0;
				}
			}
		}
		else {
			for ( i = 0; i < len; i++ ) {
				if ( arr[ i ] < x[ i ] ) {
					out[ i ] = 1;
				} else {
					out[ i ] = 0;
				}
			}
		}
	}
	// Case 2: accessor and single comparator
	else if ( clbk ) {
		for ( i = 0; i < len; i++ ) {
			if ( clbk( arr[ i ], i ) < x ) {
				out[ i ] = 1;
			} else {
				out[ i ] = 0;
			}
		}
	}
	// Case 3: single comparator
	else {
		for ( i = 0; i < len; i++ ) {
			if ( arr[ i ] < x ) {
				out[ i ] = 1;
			} else {
				out[ i ] = 0;
			}
		}
	}
	return out;
} // end FUNCTION lt()


// EXPORTS //

module.exports = lt;

},{"validate.io-array":578,"validate.io-boolean-primitive":579,"validate.io-function":580,"validate.io-number-primitive":581,"validate.io-object":582,"validate.io-string-primitive":583}],578:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],579:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],580:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],581:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],582:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":578}],583:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],584:[function(require,module,exports){
/**
*
*	COMPUTE: max
*
*
*	DESCRIPTION:
*		- Computes the maximum value of a numeric array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

/**
* FUNCTION: max( arr )
*	Computes the maximum value of a numeric array.
*
* @param {Array} arr - array of values
* @returns {Number} max value
*/
function max( arr ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'max()::invalid input argument. Must provide an array.' );
	}
	var len = arr.length,
		val = arr[ 0 ];

	for ( var i = 1; i < len; i++ ) {
		if ( arr[ i ] > val ) {
			val = arr[ i ];
		}
	}
	return val;
} // end FUNCTION max()


// EXPORTS //

module.exports = max;

},{}],585:[function(require,module,exports){
'use strict';

/**
* FUNCTION: mean( arr, clbk )
*	Computes the arithmetic mean of an array using an accessor function.
*
* @param {Array} arr - input array
* @param {Function} clbk - accessor function for accessing array values
* @returns {Number|Null} arithmetic mean or null
*/
function mean( arr, clbk ) {
	var len = arr.length,
		delta,
		mu,
		i;

	if ( !len ) {
		return null;
	}
	mu = 0;
	for ( i = 0; i < len; i++ ) {
		delta = clbk( arr[ i ], i ) - mu;
		mu += delta / (i+1);
	}
	return mu;
} // end FUNCTION mean()


// EXPORTS //

module.exports = mean;

},{}],586:[function(require,module,exports){
'use strict';

/**
* FUNCTION: mean( arr )
*	Computes the arithmetic mean of a numeric array.
*
* @param {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} arr - input array
* @returns {Number|Null} arithmetic mean or null
*/
function mean( arr ) {
	var len = arr.length,
		delta,
		mu,
		i;

	if ( !len ) {
		return null;
	}
	mu = 0;
	for ( i = 0; i < len; i++ ) {
		delta = arr[ i ] - mu;
		mu += delta / (i+1);
	}
	return mu;
} // end FUNCTION mean()


// EXPORTS //

module.exports = mean;

},{}],587:[function(require,module,exports){
'use strict';

// MODULES //

var isArrayLike = require( 'validate.io-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ).raw,
	validate = require( './validate.js' );


// FUNCTIONS //

var mean1 = require( './array.js' ),
	mean2 = require( './accessor.js' ),
	mean3 = require( './matrix.js' );


// MEAN //

/**
* FUNCTION: mean( x[, opts] )
*	Computes the arithmetic mean.
*
* @param {Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} x - input value
* @param {Object} [opts] - function options
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {Number} [opts.dim=2] - dimension along which to compute the arithmetic mean.
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Matrix|Null} mean value(s) or null
*/
function mean( x, options ) {
	/* jshint newcap:false */
	var opts = {},
		shape,
		ctor,
		err,
		len,
		dim,
		dt,
		d,
		m;

	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isMatrixLike( x ) ) {
		dt = opts.dtype || 'float64';
		dim = opts.dim;

		// Determine if provided a vector...
		if ( x.shape[ 0 ] === 1 || x.shape[ 1 ] === 1 ) {
			// Treat as an array-like object:
			return mean1( x.data );
		}
		if ( dim > 2 ) {
			throw new RangeError( 'mean()::invalid option. Dimension option exceeds number of matrix dimensions. Option: `' + dim + '`.' );
		}
		if ( dim === void 0 || dim === 2 ) {
			len = x.shape[ 0 ];
			shape = [ len, 1 ];
		} else {
			len = x.shape[ 1 ];
			shape = [ 1, len ];
		}
		ctor = ctors( dt );
		if ( ctor === null ) {
			throw new Error( 'mean()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
		}
		// Create an output matrix and calculate the means:
		d = new ctor( len );
		m = matrix( d, shape, dt );
		return mean3( m, x, dim );
	}
	if ( isArrayLike( x ) ) {
		if ( opts.accessor ) {
			return mean2( x, opts.accessor );
		}
		return mean1( x );
	}
	throw new TypeError( 'mean()::invalid input argument. First argument must be either an array or a matrix. Value: `' + x + '`.' );
} // end FUNCTION mean()


// EXPORTS //

module.exports = mean;

},{"./accessor.js":585,"./array.js":586,"./matrix.js":588,"./validate.js":589,"compute-array-constructors":591,"dstructs-matrix":600,"validate.io-array-like":638,"validate.io-matrix-like":643}],588:[function(require,module,exports){
'use strict';

/**
* FUNCTION: mean( out, mat[, dim] )
*	Computes the arithmetic mean along a matrix dimension.
*
* @param {Matrix} out - output matrix
* @param {Matrix} mat - input matrix
* @param {Number} [dim=2] - matrix dimension along which to compute an arithmetic mean. If `dim=1`, compute along matrix rows. If `dim=2`, compute along matrix columns.
* @returns {Matrix|Null} arithmetic means or null
*/
function mean( out, mat, dim ) {
	var delta,
		mu,
		M, N,
		s0, s1,
		o,
		i, j, k;

	if ( dim === 1 ) {
		// Compute along the rows...
		M = mat.shape[ 1 ];
		N = mat.shape[ 0 ];
		s0 = mat.strides[ 1 ];
		s1 = mat.strides[ 0 ];
	} else {
		// Compute along the columns...
		M = mat.shape[ 0 ];
		N = mat.shape[ 1 ];
		s0 = mat.strides[ 0 ];
		s1 = mat.strides[ 1 ];
	}
	if ( M === 0 || N === 0 ) {
		return null;
	}
	o = mat.offset;
	for ( i = 0; i < M; i++ ) {
		k = o + i*s0;
		mu = 0;
		for ( j = 0; j < N; j++ ) {
			delta = mat.data[ k + j*s1 ] - mu;
			mu += delta / (j+1);
		}
		out.data[ i ] = mu;
	}
	return out;
} // end FUNCTION mean()


// EXPORTS //

module.exports = mean;

},{}],589:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' ),
	isPositiveInteger = require( 'validate.io-positive-integer' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {Number} [options.dim] - dimension
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'mean()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'mean()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dim' ) ) {
		opts.dim = options.dim;
		if ( !isPositiveInteger( opts.dim ) ) {
			return new TypeError( 'mean()::invalid option. Dimension option must be a positive integer. Option: `' + opts.dim + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'mean()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-function":642,"validate.io-object":644,"validate.io-positive-integer":646,"validate.io-string-primitive":649}],590:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],591:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":590,"dup":12}],592:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],593:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":596,"./iget.js":598,"./iset.js":601,"./mget.js":605,"./mset.js":607,"./set.js":615,"./sget.js":617,"./sset.js":619,"./toString.js":621,"dup":14}],594:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":597,"./iget.raw.js":599,"./iset.raw.js":602,"./mget.raw.js":606,"./mset.raw.js":608,"./set.raw.js":616,"./sget.raw.js":618,"./sset.raw.js":620,"./toString.js":621,"dup":15}],595:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],596:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":634}],597:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],598:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":632}],599:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],600:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":603,"./matrix.raw.js":604,"dup":21}],601:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":632,"validate.io-number-primitive":637}],602:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],603:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":592,"./ctor.js":593,"./dtypes.js":595,"compute-cast-arrays":622,"compute-dtype":625,"dup":24,"validate.io-array":629,"validate.io-contains":630,"validate.io-nonnegative-integer-array":633,"validate.io-string-primitive":649}],604:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":592,"./ctor.raw.js":594,"./dtypes.js":595,"compute-dtype":625,"dup":25,"validate.io-contains":630,"validate.io-string-primitive":649}],605:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":592,"dup":26,"validate.io-nonnegative-integer-array":633}],606:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":592,"dup":27}],607:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":609,"./mset2.js":610,"./mset3.js":611,"./mset4.js":612,"./mset5.js":613,"./mset6.js":614,"dup":28,"validate.io-function":642,"validate.io-nonnegative-integer-array":633,"validate.io-number-primitive":637}],608:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":609,"./mset2.js":610,"./mset3.js":611,"./mset4.js":612,"./mset5.js":613,"./mset6.js":614,"dup":29}],609:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],610:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],611:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],612:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],613:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],614:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],615:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":634,"validate.io-number-primitive":637}],616:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],617:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":592,"compute-indexspace":534,"dup":38,"validate.io-string-primitive":649}],618:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":592,"compute-indexspace":534,"dup":39}],619:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":534,"dup":40,"validate.io-function":642,"validate.io-number-primitive":637,"validate.io-string-primitive":649}],620:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":534,"dup":41}],621:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],622:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":590,"compute-array-dtype/lib/dtypes":623,"dup":43,"type-name":624,"validate.io-array-like":638}],623:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],624:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],625:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":627,"dup":46,"type-name":628}],626:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],627:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":626,"dup":48}],628:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],629:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],630:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":629,"validate.io-nan-primitive":631}],631:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],632:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":637}],633:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":629,"validate.io-nonnegative-integer":634}],634:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":635}],635:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":636}],636:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],637:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],638:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":639,"dup":70,"validate.io-integer-primitive":640}],639:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],640:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":641}],641:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],642:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],643:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],644:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":645}],645:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],646:[function(require,module,exports){
arguments[4][471][0].apply(exports,arguments)
},{"dup":471,"validate.io-integer":647}],647:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":648}],648:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],649:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],650:[function(require,module,exports){
/**
*
*	COMPUTE: median
*
*
*	DESCRIPTION:
*		- Computes the median of an array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isObject = require( 'validate.io-object' ),
	isBoolean = require( 'validate.io-boolean' ),
	isFunction = require( 'validate.io-function' );


// FUNCTIONS //

/**
* FUNCTION: ascending( a, b )
*	Comparator function used to sort values in ascending order.
*
* @private
* @param {Number} a
* @param {Number} b
* @returns {Number} difference between `a` and `b`
*/
function ascending( a, b ) {
	return a - b;
} // end FUNCTION ascending()


// MEDIAN //

/**
* FUNCTION: median( arr[, options] )
*	Computes the median of an array.
*
* @param {Array} arr - input array
* @param {Object} [options] - function options
* @param {Boolean} [options.sorted] - boolean flag indicating if the array is sorted in ascending order
* @param {Function} [options.accessor] - accessor function for accessing array values
* @returns {Number|null} median value or null
*/
function median( arr, options ) {
	var sorted,
		clbk,
		len,
		id,
		d, i;

	if ( !isArray( arr ) ) {
		throw new TypeError( 'median()::invalid input argument. Must provide an array. Value: `' + arr + '`.' );
	}
	if ( arguments.length > 1 ) {
		if ( !isObject( options) ) {
			throw new TypeError( 'median()::invalid input argument. Options must be an object. Value: `' + options + '`.' );
		}
		if ( options.hasOwnProperty( 'sorted' ) ) {
			sorted = options.sorted;
			if ( !isBoolean( sorted ) ) {
				throw new TypeError( 'median()::invalid option. Sorted flag must be a boolean. Option: `' + sorted + '`.' );
			}
		}
		if ( options.hasOwnProperty( 'accessor' ) ) {
			clbk = options.accessor;
			if ( !isFunction( clbk ) ) {
				throw new TypeError( 'median()::invalid option. Accessor must be a function. Option: `' + clbk + '`.' );
			}
		}
	}
	len = arr.length;
	if ( !len ) {
		return null;
	}
	if ( !clbk && sorted ) {
		d = arr;
	}
	else if ( !clbk ) {
		d = [];
		for ( i = 0; i < len; i++ ) {
			d.push( arr[ i ] );
		}
	}
	else {
		d = [];
		for ( i = 0; i < len; i++ ) {
			d.push( clbk( arr[ i ] ) );
		}
	}
	if ( !sorted ) {
		d.sort( ascending );
	}
	// Get the middle index:
	id = Math.floor( len / 2 );

	if ( len % 2 ) {
		// The number of elements is not evenly divisible by two, hence we have a middle index:
		return d[ id ];
	}
	// Even number of elements, so must take the mean of the two middle values:
	return ( d[ id-1 ] + d[ id ] ) / 2.0;
} // end FUNCTION median()


// EXPORTS //

module.exports = median;

},{"validate.io-array":651,"validate.io-boolean":652,"validate.io-function":653,"validate.io-object":654}],651:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],652:[function(require,module,exports){
/**
*
*	VALIDATE: boolean
*
*
*	DESCRIPTION:
*		- Validates if a value is a boolean.
*
*
*	NOTES:
*		[1] 
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

/**
* FUNCTION: isBoolean( value )
*	Validates if a value is a boolean.
*
* @param {*} value - value to be validated
* @returns {Boolean} boolean indicating whether value is a boolean
*/
function isBoolean( value ) {
	return ( typeof value === 'boolean' || Object.prototype.toString.call( value ) === '[object Boolean]' );
} // end FUNCTION isBoolean()


// EXPORTS //

module.exports = isBoolean;
},{}],653:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],654:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":651}],655:[function(require,module,exports){
/**
*
*	COMPUTE: midhinge
*
*
*	DESCRIPTION:
*		- Computes the midhinge of a numeric array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Rebekah Smith.
*
*
*	AUTHOR:
*		Rebekah Smith. rebekahjs17@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	quantile = require( 'compute-quantile' );


// FUNCTIONS //

/**
* FUNCTION: ascending( a, b )
*	Comparator function used to sort values in ascending order.
*
* @private
* @param {Number} a
* @param {Number} b
* @returns {Number} difference between `a` and `b`
*/
function ascending( a, b ) {
	return a - b;
} // end FUNCTION ascending()


// MIDHINGE //

/**
* FUNCTION: midhinge( arr[, opts] )
*	Computes the midhinge of a numeric array.
*
* @param {Array} arr - numeric array
* @param {Object} [opts] - quantile options
* @returns {Number} midhinge
*/
function midhinge( arr, opts ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'midhinge()::invalid input argument. Must provide an array.' );
	}
	if ( arguments.length > 1 ) {
		if ( !isObject( opts ) ) {
			throw new TypeError( 'midhinge()::invalid input argument. Options should be an object.' );
		}
	} else {
		opts = {
			'sorted': false
		};
	}
	if ( !opts.sorted ) {
		arr = arr.slice();
		arr.sort( ascending );
		opts.sorted = true;
	}
	return ( quantile( arr, 0.25, opts ) + quantile( arr, 0.75, opts ) ) / 2.0;
} // end FUNCTION midhinge()


// EXPORTS //

module.exports = midhinge;

},{"compute-quantile":797,"validate.io-object":656}],656:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":657}],657:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],658:[function(require,module,exports){
/**
*
*	COMPUTE: midmean
*
*
*	DESCRIPTION:
*		- Computes the interquartile mean (midmean) of a numeric array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Rebekah Smith.
*
*
*	AUTHOR:
*		Rebekah Smith. rebekahjs17@gmail.com. 2014.
*
*/

'use strict';

// FUNCTIONS //

/**
* FUNCTION: ascending( a, b )
*	Comparator function used to sort values in ascending order.
*
* @private
* @param {Number} a
* @param {Number} b
* @returns {Number} difference between `a` and `b`
*/
function ascending( a, b ) {
	return a - b;
} // end FUNCTION ascending()


// MIDMEAN //

/**
* FUNCTION: midmean( arr[, sorted] )
*	Computes the interquartile mean (midmean) of a numeric array.
*
* @param {Array} arr - numeric array
* @param {Boolean} [sorted] - boolean flag indicating if the input array is sorted in ascending order
* @returns {Number} midmean
*/
function midmean( arr, sorted ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'midmean()::invalid input argument. Must provide an array.' );
	}
	if ( arguments.length > 1 && typeof sorted !== 'boolean' ) {
		throw new TypeError( 'midmean()::invalid input argument. Second argument must be a boolean.' );
	}
	if ( arr.length < 3 ) {
		throw new TypeError( 'midmean()::invalid input argument. Midmean not applicable.' );
	}
	if ( !sorted ) {
		arr = arr.slice();
		arr.sort( ascending );
	}
	var len = arr.length,
		mean = 0,
		N = 0,
		delta,
		low,
		high;

	// Quartiles sit between values...
	if ( len%4 === 0 ) {
		low = len*0.25;
		high = len*0.75 - 1;
	}
	else {
		low = Math.ceil( len*0.25 );
		high = Math.floor( len*0.75 ) - 1;
	}

	// Compute an arithmetic mean...
	for ( var i = low; i <= high; i++ ) {
		N += 1;
		delta = arr[ i ] - mean;
		mean += delta / N;
	}
	return mean;
} // end FUNCTION midmean()


// EXPORTS //

module.exports = midmean;

},{}],659:[function(require,module,exports){
/**
*
*	COMPUTE: midrange
*
*
*	DESCRIPTION:
*		- Computes the midrange of a numeric array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Rebekah Smith.
*
*
*	AUTHOR:
*		Rebekah Smith. rebekahjs17@gmail.com. 2014.
*
*/

'use strict';

// MIDRANGE //

/**
* FUNCTION: midrange( arr[, sorted] )
*	Computes the midrange of a numeric array.
*
* @param {Array} arr - numeric array
* @param {Boolean} [sorted] - boolean flag indicating if the input array is already sorted in ascending order
* @returns {Number} midrange
*/
function midrange( arr, sorted ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'midrange()::invalid input argument. Must provide an array.' );
	}
	if ( arguments.length > 1 && typeof sorted !== 'boolean') {
		throw new TypeError( 'midrange()::invalid input argument. Sorted flag should be a boolean.' );
	}
	var len = arr.length,
		val,
		min,
		max;

	if ( sorted ) {
		min = arr[ 0 ];
		max = arr[ len - 1 ];
	} else {
		// Find min and max values in the range...
		min = arr[ 0 ];
		max = arr[ 0 ];
		for ( var i = 1; i < len; i++) {
			val = arr[ i ];
			if ( val > max ) {
				max = val;
			}
			else if ( val < min ) {
				min = val;
			}
		}
	}
	return ( min + max ) / 2.0;
} // end FUNCTION midrange()


// EXPORTS //

module.exports = midrange;


},{}],660:[function(require,module,exports){
/**
*
*	COMPUTE: midsummary
*
*
*	DESCRIPTION:
*		- Computes a trimmed midrange of a numeric array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Rebekah Smith.
*
*
*	AUTHOR:
*		Rebekah Smith. rebekahjs17@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	quantile = require( 'compute-quantile' );


// FUNCTIONS //

/**
* FUNCTION: ascending( a, b )
*	Comparator function used to sort values in ascending order.
*
* @private
* @param {Number} a
* @param {Number} b
* @returns {Number} difference between `a` and `b`
*/
function ascending( a, b ) {
	return a - b;
} // end FUNCTION ascending()


// MIDSUMMARY //

/**
* FUNCTION: midsummary( arr, n[, opts] )
*	Computes the midsummary of a numeric array.
*
* @param {Array} arr - numeric array
* @param {Number} n - % value between 0 and 0.50 by which to trim the data set
* @param {Object} [opts] - quantile options
* @returns {Number} midsummary
*/
function midsummary( arr, n, opts ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'midsummary()::invalid input argument. Must provide an array.' );
	}
	if ( typeof n !== 'number' || n !== n ) {
		throw new TypeError( 'midsummary()::invalid input argument. Percentage must be numeric.' );
	}
	if ( n < 0.0 || n > 0.50 ) {
		throw new TypeError( 'midsummary()::invalid input argument. Must provide a percentage between 0 and 0.5 inclusive.' );
	}
	if ( arguments.length > 2 ) {
		if ( !isObject( opts ) ) {
			throw new TypeError( 'midsummary()::invalid input argument. Options should be an object.' );
		}
	} else {
		opts = {
			'sorted': false
		};
	}
	if ( !opts.sorted ) {
		arr = arr.slice();
		arr.sort( ascending );
		opts.sorted = true;
	}
	var len = arr.length,
		el = 0;

	if ( n === 0 ) {
		// special case: midrange
		return ( arr[0] + arr[len-1] ) / 2.0;
	}
	else if ( n === 0.5 ) {
		// special case: median
		el = Math.floor( len / 2.0 );
 		if ( len % 2.0 ) {
 			// Middle index exists
 			return arr[ el ];
 		}
 		else {
 			return ( arr[ el ] + arr[ el-1 ] ) / 2.0;
 		}
	}	
	else {
		return ( quantile( arr, n, opts ) + quantile( arr, 1 - n, opts ) ) / 2.0;
	}
} // end FUNCTION midsummary()


// EXPORTS //

module.exports = midsummary;

},{"compute-quantile":797,"validate.io-object":661}],661:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":662}],662:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],663:[function(require,module,exports){
/**
*
*	COMPUTE: min
*
*
*	DESCRIPTION:
*		- Computes the minimum value of an array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

/**
* FUNCTION: min( arr )
*	Computes the minimum value of an array.
*
* @param {Array} arr - array of values
* @returns {Number} min value
*/
function min( arr ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'min()::invalid input argument. Must provide an array.' );
	}
	var len = arr.length,
		val = arr[ 0 ];

	for ( var i = 1; i < len; i++ ) {
		if ( arr[ i ] < val ) {
			val = arr[ i ];
		}
	}
	return val;
} // end FUNCTION min()


// EXPORTS //

module.exports = min;

},{}],664:[function(require,module,exports){
/**
*
*	COMPUTE: mmax
*
*
*	DESCRIPTION:
*		- Computes a moving maximum over a numeric array.
*
*
*	NOTES:
*		[1] 
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Rebekah Smith.
*
*
*	AUTHOR:
*		Rebekah Smith. rebekahjs17@gmail.com. 2014.
*
*/

(function() {
	'use strict';

    /**
	* FUNCTION: mmax( arr , window )
	*	Computes a moving maximum over a numeric array.
	*
	* @param {Array} arr - array of data values
	* @param {Number} window - size of moving window
	* @returns {Array} array of maximum values
	*/
	function mmax( arr , W ) {
		if ( !Array.isArray( arr ) ) {
			throw new TypeError( 'mmax()::invalid input argument. Must provide an array.' );
		}
		if ( typeof W !== 'number' || W !== W ) {
            throw new TypeError( 'mmax()::invalid input argument. Window must be numeric.' );
        }
        if ( Math.floor( W ) !== W || W < 1 ) {
            throw new TypeError( 'mmax()::invalid input argument. Window must be a positive integer.' );
        }
		if ( W > arr.length ) {
			throw new TypeError( 'mmax()::invalid input argument. Window cannot exceed array length.' );
		}
		var len = arr.length,
			out = new Array( len-W+1 ),
			max = arr[ 0 ],
			val,
			i, j, k, n;

		// Compute the maximum value for the first window...
		for ( i = 1; i < W; i++ ) {
			val = arr[ i ];
			if ( val > max ) {
				max = val;
			}
		}
		out[ 0 ] = max;

		// Compute the remaining window maximums...
		for ( j = W; j < len; j++ ) {
			val = arr[ j ];
			k = j - W;

			// Cases:
			// [1] Incoming value is greater than current maximum. New maximum value.
			// [2] Outgoing value is the current maximum and the new value is less than the maximum. Find a new maximum among the current values.
			// [3] Maximum does not change. Move along.
			
			if ( val > max ) {
				max = val;
			}
			else if ( arr[ k ] === max && val < max ) {
				max = arr[ k+1 ];
				for ( n = k+2; n <= j; n++ ) {
					val = arr[ n ];
					if ( val > max ) {
						max = val;
					}
				}
			}
			out[ k+1 ] = max;
		}
		return out;
	} // end FUNCTION mmax()


	// EXPORTS //

	module.exports = mmax;

})();
},{}],665:[function(require,module,exports){
/**
*
*	COMPUTE: mmean
*
*
*	DESCRIPTION:
*		- Compute a moving mean (sliding window average) over an array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014-2015. Rebekah Smith.
*
*
*	AUTHOR:
*		Rebekah Smith. rebekahjs17@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isObject = require( 'validate.io-object' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isPositiveInteger = require( 'validate.io-positive-integer' ),
	isFunction = require( 'validate.io-function' );


// MOVING MEAN //

/**
* FUNCTION: mmean( arr, W[, options] )
*	Computes a moving mean over an array.
*
* @param {Array} arr - input array
* @param {Number} W - size of moving window
* @param {Object} [options] - function options
* @param {Function} [options.accessor] - accessor function for accessing numeric values
* @param {Boolean} [options.copy=true] - boolean indicating whether to return a new array of window means
* @returns {Array} array of window mean values
*/
function mmean( arr , W, options ) {
	var copy = true,
		clbk;

	if ( !isArray( arr ) ) {
		throw new TypeError( 'mmean()::invalid input argument. Must provide an array. Value: `' + arr + '`.' );
	}
	if ( !isPositiveInteger( W ) ) {
    	throw new TypeError( 'mmean()::invalid input argument. Window must be a positive integer. Value: `' + W + '`.' );
	}
	if ( arguments.length > 2 ) {
		if ( !isObject( options ) ) {
			throw new TypeError( 'mmean()::invalid input argument. Options must be an object. Value: `' + options + '`.' );
		}
		if ( options.hasOwnProperty( 'accessor' ) ) {
			clbk = options.accessor;
			if ( !isFunction( clbk ) ) {
				throw new TypeError( 'mmean()::invalid option. Accessor option must be a function. Value: `' + clbk + '`.' );
			}
		}
		if ( options.hasOwnProperty( 'copy' ) ) {
			copy = options.copy;
			if ( !isBoolean( copy ) ) {
				throw new TypeError( 'mmean()::invalid option. Copy option must be a boolean primitive. Value: `' + copy + '`.' );
			}
		}
	}
	if ( W > arr.length ) {
		throw new Error( 'mmean()::invalid input argument. Window cannot exceed the array length.' );
	}

	var len = arr.length,
		delta = 0,
		mu = 0,
		dropVal = 0,
		out;

	if ( copy && !clbk ) {
		// Case 1: numeric array and return a copy...
		len = len - W + 1;
		out = new Array( len );

		// Calculate the mean for the first window...
		for ( var i = 0; i < W; i++ ) {
			delta = arr[ i ] - mu;
			mu += delta / (i+1);
		}
		out[ 0 ] = mu;

		// Calculate means for the remaining windows...
		for ( var j = 0; j < len-1; j++ ) {
			delta = arr[ j+W ] - arr[ j ];
			mu += delta / W;
			out[ j+1 ] = mu;
		}
		return out;
	} 
	else if ( clbk ) {
		if ( copy ) {
			// Case 2: non-numeric array and return a copy...
			out = new Array( len );
			for ( var i = 0; i < len; i++ ) {
				out[ i ] = clbk( arr[i] );
			}
		} else {
			// Case 3: non-numeric array and mutate the input array...
			out = arr;
			for ( var i = 0; i < len; i++ ) {
				out[ i ] = clbk( arr[i] );
			}
		}
	} 
	else {
		// Case 4: numeric array and mutate the input array...
		out = arr;
	}

	len = len - W + 1;

	// Calculate the mean for the first window...
	for ( var i = 0; i < W; i++ ) {
		delta = out[ i ] - mu;
		mu += delta / (i+1);
	}
	dropVal = out[ 0 ];
	out[ 0 ] = mu;

	// Calculate means for the remaining windows...
	for ( var j = 1; j < len; j++ ) {
		delta = out[ j+W-1 ] - dropVal;
		mu += delta / W;
		dropVal = out[ j ];
		out[ j ] = mu;
	}

	// Trim the output array:
	out.length = len;
	return out;
	
} // end FUNCTION mmean()

// EXPORTS //

module.exports = mmean;

},{"validate.io-array":666,"validate.io-boolean-primitive":667,"validate.io-function":668,"validate.io-object":669,"validate.io-positive-integer":670}],666:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],667:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],668:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],669:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":666}],670:[function(require,module,exports){
arguments[4][471][0].apply(exports,arguments)
},{"dup":471,"validate.io-integer":671}],671:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":672}],672:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],673:[function(require,module,exports){
/**
*
*	COMPUTE: mmin
*
*
*	DESCRIPTION:
*		- Computes a moving minimum over a numeric array.
*
*
*	NOTES:
*		[1] 
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Rebekah Smith.
*
*
*	AUTHOR:
*		Rebekah Smith. rebekahjs17@gmail.com. 2014.
*
*/

(function() {
	'use strict';

	// MOVING MIN //

    /**
	* FUNCTION: mmin( arr , window )
	*	Computes a moving minimum over a numeric array.
	*
	* @param {Array} arr - array of data values
	* @param {Number} window - size of moving window
	* @returns {Array} array of minimum values
	*/
	function mmin( arr , W ) {
		if ( !Array.isArray( arr ) ) {
			throw new TypeError( 'mmin()::invalid input argument. Must provide an array.' );
		}
		if ( typeof W !== 'number' || W !== W ) {
            throw new TypeError( 'mmin()::invalid input argument. Window must be numeric.' );
        }
        if ( Math.floor( W ) !== W || W < 1 ) {
            throw new TypeError( 'mmin()::invalid input argument. Window must be a positive integer.' );
        }
		if ( W > arr.length ) {
			throw new TypeError( 'mmin()::invalid input argument. Window cannot exceed array length.' );
		}
		var len = arr.length,
			out = new Array( len - W + 1 ),
			min = arr[ 0 ],
			val,
			i, j, k, n;

		// Compute the minimum for the first window...
		for ( i = 1; i < W; i++ ) {
			val = arr[ i ];
			if ( val < min ) {
				min = val;
			}
		}
		out[ 0 ] = min;

		// Compute the remaining window minimums..
		for ( j = W; j < len; j++ ) {
			val = arr[ j ];
			k = j - W;
			
			// Cases:
			// [1] Incoming value is less than current minimum. New minimum value.
			// [2] Outgoing value is the current minimum and the new value is greater than the minimum. Find a new minimum among the current values.
			// [3] Minimum does not change. Move along.

			if ( val < min ) {
				min = val;
			}
			else if ( arr[ k ] === min && val > min ) {
				min = arr[ k+1 ];
				for ( n = k+2; n <= j; n++ ) {
					val = arr[ n ];
					if ( val < min ) {
						min = val;
					}
				}
			}
			out[ k+1 ] = min;
		}
		return out;
	} // end FUNCTION mmin()


	// EXPORTS //

	module.exports = mmin;

})();
},{}],674:[function(require,module,exports){
/**
*
*	COMPUTE: mode
*
*
*	DESCRIPTION:
*		- Computes the mode of an array.
*
*
*	NOTES:
*		[1] 
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

(function() {
	'use strict';

	/**
	* FUNCTION: mode( arr )
	*	Computes the mode of an array.
	*
	* @param {Array} arr - array of values
	* @returns {Array} mode
	*/
	function mode( arr ) {
		if ( !Array.isArray( arr ) ) {
			throw new TypeError( 'mode()::invalid input argument. Must provide an array.' );
		}
		var len = arr.length,
			count = {},
			max = 0,
			vals = [],
			val;

		for ( var i = 0; i < len; i++ ) {
			val = arr[ i ];
			if ( !count[ val ] ) {
				count[ val ] = 0;
			}
			count[ val ] += 1;
			if ( count[ val ] === max ) {
				vals.push( val );
			} else {
				max = count[ val ];
				vals = [ val ];
			}
		}
		return vals.sort( function sort( a, b ) {
			return a - b;
		});
	} // end FUNCTION mode()


	// EXPORTS //

	module.exports = mode;

})();
},{}],675:[function(require,module,exports){
/**
*
*	COMPUTE: mprod
*
*
*	DESCRIPTION:
*		- Computes a moving product over an array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2015. Philipp Burckhardt.
*
*
*	AUTHOR:
*		Philipp Burckhardt. pburckhardt@outlook.com. 2015.
*
*/

'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isPositiveInteger = require( 'validate.io-positive-integer' );


// MOVING PRODUCT //

/**
* FUNCTION: mprod( arr, window[, accessor] )
*	Computes a moving product over an array.
*
* @param {Array} arr - array of values
* @param {Number} window - size of moving window
* @param {Function} [accessor] - accessor function for accessing array values
* @returns {Array} array of window products
*/
function mprod( arr, W, clbk ) {
	var len,
		out,
		val,
		p,
		i, j;
	if ( !isArray( arr ) ) {
		throw new TypeError( 'mprod()::invalid input argument. Must provide an array. Value: `' + arr + '`.' );
	}
	len = arr.length;
	if ( !isPositiveInteger( W ) ) {
		throw new TypeError( 'mprod()::invalid input argument. Window must be a positive integer. Value: `' + W + '`.' );
	}
	if ( W > len ) {
		throw new Error( 'mprod()::invalid input argument. Window cannot exceed the array length.' );
	}
	if ( arguments.length > 2 ) {
		if ( typeof clbk !== 'function' ) {
			throw new TypeError( 'mprod()::invalid input argument. Accessor must be a function. Value: `' + clbk + '`.' );
		}
	}
	len = len - W + 1;
	out = new Array( len );

	// NOTE: unlike sum, mean, and other moving calculations, a simple in-place update procedure is hard to come by. Instead, the straightforward solution of two FOR loops is used. While possibly not optimal, this solution requires fewer conditionals and is arguably easier to maintain.
	if ( clbk ) {
		for ( i = 0; i < len; i++ ) {
			p = 1;
			for ( j = i; j < i+W; j++ ) {
				val = clbk( arr[ j ] );
				if ( val === 0 ) {
					p = 0;
					break;
				}
				p *= val;
			}
			out[ i ] = p;
		}
	} else {
		for ( i = 0; i < len; i++ ) {
			p = 1;
			for ( j = i; j < i+W; j++ ) {
				val = arr[ j ];
				if ( val === 0 ) {
					p = 0;
					break;
				}
				p *= val;
			}
			out[ i ] = p;
		}
	}
	return out;
} // end FUNCTION mprod()


// EXPORTS //

module.exports = mprod;

},{"validate.io-array":676,"validate.io-positive-integer":677}],676:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],677:[function(require,module,exports){
arguments[4][471][0].apply(exports,arguments)
},{"dup":471,"validate.io-integer":678}],678:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":679}],679:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],680:[function(require,module,exports){
/**
*
*	COMPUTE: mstdev
*
*
*	DESCRIPTION:
*		- Computes a moving sample standard deviation over a numeric array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// MOVING SAMPLE STANDARD DEVIATION //

/**
* FUNCTION: mstdev( arr, window )
*	Computes a moving sample standard deviation over a numeric array.
*
* @param {Array} arr - array of data values
* @param {Number} window - size of moving window
* @returns {Array} array of sample standard deviation values
*/
function mstdev( arr, W ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'mstdev()::invalid input argument. Must provide an array.' );
	}
	if ( typeof W !== 'number' || W !== W ) {
        throw new TypeError( 'mstdev()::invalid input argument. Window must be numeric.' );
    }
    if ( (W%1) !== 0 || W < 1 ) {
        throw new TypeError( 'mstdev()::invalid input argument. Window must be a positive integer.' );
    }
	if ( W > arr.length ) {
		throw new TypeError( 'mstdev()::invalid input argument. Window cannot exceed the array length.' );
	}
	var len = arr.length,
		out = new Array( len-W+1 ),
		n = W - 1,
		mu = 0,
		M2 = 0,
		delta,
		x1,
		x2,
		d1,
		d2,
		i, j;

	if ( W === 1 ) {
		for ( i = 0; i < out.length; i++ ) {
			out[ i ] = 0;
		}
		return out;
	}

	// Compute the sample standard deviation for the first window...
	for ( i = 0; i < W; i++ ) {
		x1 = arr[ i ];
		delta = x1 - mu;
		mu += delta / (i+1);
		M2 += delta * (x1 - mu);
	}
	out[ 0 ] = Math.sqrt( M2 / n );

	// Compute the remaining sample standard deviations...
	for ( j = W; j < len; j++ ) {
		i = j - W;
		x1 = arr[ i ];
		x2 = arr[ j ];
		delta = x2 - x1;
		d1 = x1 - mu;
		mu += delta / W;
		d2 = x2 - mu;
		M2 += delta * (d1+d2);
		out[ i+1 ] = Math.sqrt( M2 / n );
	}
	return out;
} // end FUNCTION mstdev()


// EXPORTS //

module.exports = mstdev;

},{}],681:[function(require,module,exports){
/**
*
*	COMPUTE: msum
*
*
*	DESCRIPTION:
*		- Compute a moving sum over an array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Rebekah Smith.
*
*
*	AUTHOR:
*		Rebekah Smith. rebekahjs17@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isObject = require( 'validate.io-object' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isPositiveInteger = require( 'validate.io-positive-integer' ),
	isFunction = require( 'validate.io-function' );


// MOVING SUM //

/**
* FUNCTION: msum( arr, W[, options] )
*	Computes a moving sum over an array.
*
* @param {Array} arr - input array
* @param {Number} W - size of moving window
* @param {Object} [options] - function options
* @param {Function} [options.accessor] - accessor function for accessing numeric values
* @param {Boolean} [options.copy=true] - boolean indicating whether to return a new array of window sums
* @returns {Array} array of window sum values
*/
function msum( arr, W, options ) {
	var copy = true,
		clbk,
		len,
		sum,
		dropVal,
		out,
		i;
	if ( !isArray( arr ) ) {
		throw new TypeError( 'msum()::invalid input argument. Must provide an array. Value: `' + arr + '`.' );
	}
	if ( !isPositiveInteger( W ) ) {
        throw new TypeError( 'msum()::invalid input argument. Window must be a positive integer. Value: `' + W + '`.' );
    }
	if ( arguments.length > 2 ) {
		if ( !isObject( options ) ) {
			throw new TypeError( 'msum()::invalid input argument. Options must be an object. Value: `' + options + '`.' );
		}
		if ( options.hasOwnProperty( 'accessor' ) ) {
			clbk = options.accessor;
			if ( !isFunction( clbk ) ) {
				throw new TypeError( 'msum()::invalid option. Accessor option must be a function. Value: `' + clbk + '`.' );
			}
		}
		if ( options.hasOwnProperty( 'copy' ) ) {
			copy = options.copy;
			if ( !isBoolean( copy ) ) {
				throw new TypeError( 'msum()::invalid option. Copy option must be a boolean primitive. Value: `' + copy + '`.' );
			}
		}
	}
	if ( W > arr.length ) {
		throw new Error( 'msum()::invalid input argument. Window cannot exceed the array length.' );
	}
	len = arr.length;
	sum = 0;

	if ( copy && !clbk ) {
		// Case 1: numeric array and return a copy...
		len = len - W + 1;
		out = new Array( len );

		// Calculate the sum for the first window...
		for ( i = 0; i < W; i++ ) {
			sum += arr[ i ];
		}
		out[ 0 ] = sum;

		// Calculate sums for the remaining windows...
		for ( i = 0; i < len-1; i++ ){
			sum += arr[ i+W ] - arr[ i ];
			out[ i+1 ] = sum;
		}
		return out;
	}
	else if ( clbk ) {
		if ( copy ) {
			// Case 2: non-numeric array and return a copy...
			out = new Array( len );
			for ( i = 0; i < len; i++ ){
				out[ i ] = clbk( arr[i] );
			}
		} else {
			// Case 3: non-numeric array and mutate the input array...
			out = arr;
			for ( i = 0; i < len; i++ ) {
				out[ i ] = clbk( arr[i] );
			}
		}
	}
	else {
		// Case 4: numeric array and mutate the input array...
		out = arr;
	}
	len = len - W + 1;

	// Calculate the sum for the first window...
	for ( i = 0; i < W; i++ ) {
		sum += out[ i ];
	}
	dropVal = out[ 0 ];
	out[ 0 ] = sum;

	// Calculate sums for the remaining windows...
	for ( i = 1; i < len; i++ ){
		sum += out[ i+W-1 ] - dropVal;
		dropVal = out[ i ];
		out[ i ] = sum;
	}
	// Trim the output array:
	out.length = len;
	return out;
} // end FUNCTION msum()


// EXPORTS //

module.exports = msum;


},{"validate.io-array":682,"validate.io-boolean-primitive":683,"validate.io-function":684,"validate.io-object":685,"validate.io-positive-integer":686}],682:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],683:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],684:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],685:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":682}],686:[function(require,module,exports){
arguments[4][471][0].apply(exports,arguments)
},{"dup":471,"validate.io-integer":687}],687:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":688}],688:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],689:[function(require,module,exports){
/**
*
*	COMPUTE: multiply
*
*
*	DESCRIPTION:
*		- Computes an element-wise multiplication of a numeric array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// MULTIPLY //

/**
* FUNCTION: multiply( arr, x )
*	Computes an element-wise multiplication of an array.
*
* @param {Array} arr - numeric array
* @param {Array|Number} x - either an array of equal length or a scalar
*/
function multiply( arr, x ) {
	var isArray = Array.isArray( x ),
		len,
		i;

	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'multiply()::invalid input argument. Must provide an array.' );
	}
	len = arr.length;
	if ( !isArray && ( typeof x !== 'number' || x !== x ) ) {
		throw new TypeError( 'multiply()::invalid input argument. Second argument must be either an array or a scalar.' );
	}
	if ( isArray ) {
		if ( len !== x.length ) {
			throw new Error( 'multiply()::invalid input argument. Arrays must be of equal length.' );
		}
		for ( i = 0; i < len; i++ ) {
			arr[ i ] *= x[ i ];
		}
		return;
	}
	for ( i = 0; i < len; i++ ) {
		arr[ i ] *= x;
	}
} // end FUNCTION multiply()


// EXPORTS //

module.exports = multiply;

},{}],690:[function(require,module,exports){
/**
*
*	COMPUTE: mvariance
*
*
*	DESCRIPTION:
*		- Computes a moving sample variance over a numeric array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// MOVING SAMPLE VARIANCE //

/**
* FUNCTION: mvariance( arr , window )
*	Computes a moving sample variance over a numeric array.
*
* @param {Array} arr - array of data values
* @param {Number} window - size of moving window
* @returns {Array} array of sample variance values
*/
function mvariance( arr , W ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'mvariance()::invalid input argument. Must provide an array.' );
	}
	if ( typeof W !== 'number' || W !== W ) {
        throw new TypeError( 'mvariance()::invalid input argument. Window must be numeric.' );
    }
    if ( (W%1) !== 0 || W < 1 ) {
        throw new TypeError( 'mvariance()::invalid input argument. Window must be a positive integer.' );
    }
	if ( W > arr.length ) {
		throw new TypeError( 'mvariance()::invalid input argument. Window cannot exceed the array length.' );
	}
	var len = arr.length,
		out = new Array( len-W+1 ),
		n = W - 1,
		mu = 0,
		M2 = 0,
		delta,
		x1,
		x2,
		d1,
		d2,
		i, j;

	if ( W === 1 ) {
		for ( i = 0; i < out.length; i++ ) {
			out[ i ] = 0;
		}
		return out;
	}

	// Compute the sample variance for the first window...
	for ( i = 0; i < W; i++ ) {
		x1 = arr[ i ];
		delta = x1 - mu;
		mu += delta / (i+1);
		M2 += delta * (x1 - mu);
	}
	out[ 0 ] = M2 / n;

	// Compute the remaining sample variances...
	for ( j = W; j < len; j++ ) {
		i = j - W;
		x1 = arr[ i ];
		x2 = arr[ j ];
		delta = x2 - x1;
		d1 = x1 - mu;
		mu += delta / W;
		d2 = x2 - mu;
		M2 += delta * (d1+d2);
		out[ i+1 ] = M2 / n;
	}
	return out;
} // end FUNCTION mvariance()


// EXPORTS //

module.exports = mvariance;

},{}],691:[function(require,module,exports){
/**
*
*	COMPUTE: nangmean
*
*
*	DESCRIPTION:
*		- Computes the geometric mean of an array of values ignoring any values which are not numeric.
*
*
*	NOTES:
*		[1] 
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

(function() {
	'use strict';

	// NANGMEAN //

	/**
	* FUNCTION: nangmean( arr )
	*	Computes the geometric mean over an array of values ignoring non-numeric values.
	*
	* @param {Array} arr - array of values
	* @returns {Number} gmean value
	*/
	function nangmean( arr ) {
		if ( !Array.isArray( arr ) ) {
			throw new TypeError( 'gmean()::invalid input argument. Must provide an array.' );
		}
		var len = arr.length,
			N = 0,
			sum = 0,
			val;

		for ( var i = 0; i < len; i++ ) {
			val = arr[ i ];
			if ( typeof val !== 'number' || val !== val ) {
				continue;
			}
			if ( val <= 0 ) {
				return NaN;
			}
			N += 1;
			sum += Math.log( val );
		}
		return Math.exp( sum / N );
	} // end FUNCTION nangmean()


	// EXPORTS //

	module.exports = nangmean;

})();
},{}],692:[function(require,module,exports){
/**
*
*	COMPUTE: nanhmean
*
*
*	DESCRIPTION:
*		- Computes the harmonic mean of an array of values ignoring any values which are not numeric.
*
*
*	NOTES:
*		[1] 
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

(function() {
	'use strict';

	// NANHMEAN //

	/**
	* FUNCTION: nanhmean( arr )
	*	Computes the harmonic mean over an array of values ignoring non-numeric values.
	*
	* @param {Array} arr - array of values
	* @returns {Number} harmonic mean
	*/
	function nanhmean( arr ) {
		if ( !Array.isArray( arr ) ) {
			throw new TypeError( 'hmean()::invalid input argument. Must provide an array.' );
		}
		var len = arr.length,
			sum = 0,
			N = 0,
			val;
		for ( var i = 0; i < len; i++ ) {
			val = arr[ i ];
			if ( typeof val !== 'number' || val !== val ) {
				continue;
			}
			if ( val <= 0 ) {
				return NaN;
			}
			N += 1;
			sum += 1 / val;
		}
		return N / sum;
	} // end FUNCTION nanhmean()


	// EXPORTS //

	module.exports = nanhmean;

})();
},{}],693:[function(require,module,exports){
/**
*
*	COMPUTE: nanmax
*
*
*	DESCRIPTION:
*		- Computes the maximum value of an array ignoring non-numeric values.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

/**
* FUNCTION: nanmax( arr )
*	Computes the maximum value of an array ignoring any non-numeric values.
*
* @param {Array} arr - array of values
* @returns {Number} max value
*/
function nanmax( arr ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'nanmax()::invalid input argument. Must provide an array.' );
	}
	var len = arr.length,
		max = null,
		val;

	for ( var i = 0; i < len; i++ ) {
		val = arr[ i ];
		if ( typeof val !== 'number' || val !== val ) {
			continue;
		}
		if ( max === null || val > max ) {
			max = val;
		}
	}
	return max;
} // end FUNCTION nanmax()


// EXPORTS //

module.exports = nanmax;

},{}],694:[function(require,module,exports){
/**
*
*	COMPUTE: nanmean
*
*
*	DESCRIPTION:
*		- Computes the arithmetic mean over an array of values ignoring any values which are not numeric.
*
*
*	NOTES:
*		[1] 
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

(function() {
	'use strict';

	// NANMEAN //

	/**
	* FUNCTION: nanmean( arr )
	*	Computes the arithmetic mean over an array of values ignoring any non-numeric values.
	*
	* @param {Array} arr - array of values
	* @returns {Number} mean value
	*/
	function nanmean( arr ) {
		if ( !Array.isArray( arr ) ) {
			throw new TypeError( 'mean()::invalid input argument. Must provide an array.' );
		}
		var len = arr.length,
			N = 0,
			mu = 0,
			diff = 0,
			val;

		for ( var i = 0; i < len; i++ ) {
			val = arr[ i ];
			if ( typeof val !== 'number' || val !== val ) {
				continue;
			}
			N += 1;
			diff = val - mu;
			mu += diff / N;
		}
		return mu;
	} // end FUNCTION nanmean()


	// EXPORTS //

	module.exports = nanmean;

})();
},{}],695:[function(require,module,exports){
/**
*
*	COMPUTE: nanmedian
*
*
*	DESCRIPTION:
*		- Computes the median of an array ignoring non-numeric values.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2015. Philipp Burckhardt.
*
*
*	AUTHOR:
*		Philipp Burckhardt. pburckhardt@outlook.com. 2015.
*
*/

'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isObject = require( 'validate.io-object' ),
	isBoolean = require( 'validate.io-boolean' ),
	isNumber = require( 'validate.io-number' );


// FUNCTIONS //

/**
* FUNCTION: ascending( a, b )
*	Comparator function used to sort values in ascending order.
*
* @private
* @param {Number} a
* @param {Number} b
* @returns {Number} difference between `a` and `b`
*/
function ascending( a, b ) {
	return a - b;
} // end FUNCTION ascending()


// MEDIAN //

/**
* FUNCTION: nanmedian( arr[, options] )
*	Computes the median of an array ignoring non-numeric values.
*
* @param {Array} arr - input array
* @param {Object} [options] - function options
* @param {Boolean} [options.sorted] - boolean flag indicating if the array is sorted in ascending order
* @param {Function} [options.accessor] - accessor function for accessing array values
* @returns {Number|null} median value or null
*/
function nanmedian( arr, options ) {
	var sorted,
		clbk,
		len,
		id,
		d,
		x;
	if ( !isArray( arr ) ) {
		throw new TypeError( 'nanmedian()::invalid input argument. Must provide an array. Value: `' + arr + '`.' );
	}
	if ( arguments.length > 1 ) {
		if ( !isObject( options ) ) {
			throw new TypeError( 'nanmedian()::invalid input argument. Options must be an object. Value: `' + options + '`.' );
		}
		if ( options.hasOwnProperty( 'sorted' ) ) {
			sorted = options.sorted;
			if ( !isBoolean( sorted ) ) {
				throw new TypeError( 'nanmedian()::invalid option. Sorted flag must be a boolean. Option: `' + sorted + '`.' );
			}
		}
		if ( options.hasOwnProperty( 'accessor' ) ) {
			clbk = options.accessor;
			if ( typeof clbk !== 'function' ) {
				throw new TypeError( 'nanmedian()::invalid option. Accessor must be a function. Option: `' + clbk + '`.' );
			}
		}
	}
	d = [];
	for ( var i = 0; i < arr.length; i++ ) {
		x = ( clbk ) ? clbk( arr[ i ] ) : arr[ i ];
		if ( isNumber( x ) ) {
			d.push( x );
		}
	}
	len = d.length;
	if ( !len ) {
		return null;
	}
	if ( !sorted ) {
		d.sort( ascending );
	}
	// Get the middle index:
	id = Math.floor( len / 2 );

	if ( len % 2 ) {
		// The number of elements is not evenly divisible by two, hence we have a middle index:
		return d[ id ];
	}
	// Even number of elements, so must take the mean of the two middle values:
	return ( d[ id-1 ] + d[ id ] ) / 2.0;
} // end FUNCTION nanmedian()


// EXPORTS //

module.exports = nanmedian;

},{"validate.io-array":696,"validate.io-boolean":697,"validate.io-number":698,"validate.io-object":699}],696:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],697:[function(require,module,exports){
arguments[4][652][0].apply(exports,arguments)
},{"dup":652}],698:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],699:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":696}],700:[function(require,module,exports){
/**
*
*	COMPUTE: nanmin
*
*
*	DESCRIPTION:
*		- Computes the minimum value of an array ignoring non-numeric values.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

/**
* FUNCTION: nanmin( arr )
*	Computes the minimum value of an array ignoring any non-numeric values.
*
* @param {Array} arr - array of values
* @returns {Number} min value
*/
function nanmin( arr ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'nanmin()::invalid input argument. Must provide an array.' );
	}
	var len = arr.length,
		min = null,
		val;

	for ( var i = 0; i < len; i++ ) {
		val = arr[ i ];
		if ( typeof val !== 'number' || val !== val ) {
			continue;
		}
		if ( min === null || val < min ) {
			min = val;
		}
	}
	return min;
} // end FUNCTION nanmin()


// EXPORTS //

module.exports = nanmin;

},{}],701:[function(require,module,exports){
/**
*
*	COMPUTE: nanprod
*
*
*	DESCRIPTION:
*		- Computes the product of an array ignoring any non-numeric values.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2015. Philipp Burckhardt.
*
*
*	AUTHOR:
*		Philipp Burckhardt. pburckhardt@outlook.com. 2015.
*
*/

'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isNumber = require( 'validate.io-number' );


// NANPROD //

/**
* FUNCTION: nanprod( arr[, accessor] )
*	Computes the product of an array ignoring any non-numeric values.
*
* @param {Array} arr - array of values
* @param {Function} [accessor] - accessor function for accessing array values
* @returns {Number|Null} product
*/
function nanprod( arr, clbk ) {
	if ( !isArray( arr ) ) {
		throw new TypeError( 'nanprod()::invalid input argument. Must provide an array. Value: `' + arr + '`.' );
	}
	if ( arguments.length > 1 ) {
		if ( typeof clbk !== 'function' ) {
			throw new TypeError( 'nanprod()::invalid input argument. Accessor must be a function. Value: `' + clbk + '`.' );
		}
	}
	var len = arr.length,
		flg = false,
		p = 1,
		val,
		i;

	if ( !len ) {
		return null;
	}
	if ( clbk ) {
		for ( i = 0; i < len; i++ ) {
			val = clbk( arr[ i ] );
			if ( !isNumber( val ) ) {
				continue;
			} else {
				flg = true;
			}
			if ( val === 0 ) {
				return 0;
			}
			p *= val;
		}
	} else {
		for ( i = 0; i < len; i++ ) {
			val = arr[ i ];
			if ( !isNumber( val ) ) {
				continue;
			} else {
				flg = true;
			}
			if ( val === 0 ) {
				return 0;
			}
			p *= val;
		}
	}
	return ( flg ) ? p : NaN;
} // end FUNCTION nanprod()


// EXPORTS //

module.exports = nanprod;

},{"validate.io-array":702,"validate.io-number":703}],702:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],703:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],704:[function(require,module,exports){
/**
*
*	COMPUTE: nanqmean
*
*
*	DESCRIPTION:
*		- Computes the quadratic mean (root mean square) of an array of values ignoring any values which are not numeric.
*
*
*	NOTES:
*		[1] 
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

(function() {
	'use strict';

	// NANQMEAN //

	/**
	* FUNCTION: nanqmean( arr )
	*	Calculates the quadratic mean (root mean square) ignoring any non-numeric values.
	*
	* @param {Array} arr - array of values
	* @returns {Number} quadratic mean
	*/
	function nanqmean( arr ) {
		if ( !Array.isArray( arr ) ) {
			throw new TypeError( 'nanqmean()::invalid input argument. Must provide an array.' );
		}
		var len = arr.length,
			N = 0,
			t = 0,
			s = 1,
			r,
			val,
			abs;
		for ( var i = 0; i < len; i++ ) {
			val = arr[ i ];
			if ( typeof val !== 'number' || val !== val ) {
				continue;
			}
			N += 1;
			abs = val;
			if ( abs < 0 ) {
				abs = -abs;
			}
			if ( abs > 0 ) {
				if ( abs > t ) {
					r = t / val;
					s = 1 + s*r*r;
					t = abs;
				} else {
					r = val / t;
					s = s + r*r;
				}
			}
		}
		return t * Math.sqrt( s/N );
	} // end FUNCTION nanqmean()


	// EXPORTS //

	module.exports = nanqmean;

})();
},{}],705:[function(require,module,exports){
/**
*
*	COMPUTE: nanquantiles
*
*
*	DESCRIPTION:
*		-  Computes quantiles for an array ignoring non-numeric values.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2015. Philipp Burckhardt.
*
*
*	AUTHOR:
*		Philipp Burckhardt. pburckhardt@outlook.com. 2015.
*
*/

'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isObject = require( 'validate.io-object' ),
	isNonNegativeInteger = require( 'validate.io-nonnegative-integer' ),
	isNumber = require( 'validate.io-number' ),
	isBoolean = require( 'validate.io-boolean' );


// FUNCTIONS //

/**
* FUNCTION: ascending( a, b )
*	Comparator function used to sort values in ascending order.
*
* @private
* @param {Number} a
* @param {Number} b
* @returns {Number} difference between `a` and `b`
*/
function ascending( a, b ) {
	return a - b;
} // end FUNCTION ascending()


// NANQUANTILES //

/**
* FUNCTION: nanquantiles( arr, num[, opts] )
*	Computes quantiles for an array ignoring non-numeric values.
*
* @param {Array} arr - array of values
* @param {Number} num - number of quantiles
* @param {Object} [opts] - function options
* @param {Boolean} [opts.sorted=false] - boolean flag indicating if the input array is sorted in ascending order.
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @returns {Array|null} array of quantiles or null
*/
function nanquantiles( arr, num, opts ) {
	var sorted,
		clbk,
		qValues,
		id,
		val,
		len,
		d,
		x,
		i;

	if ( !isArray( arr ) ) {
		throw new TypeError( 'nanquantiles()::invalid input argument. First argument must be an array. Value: `' + arr + '`.' );
	}
	if ( !isNonNegativeInteger( num ) ) {
		throw new TypeError( 'nanquantiles()::invalid input argument. Number of quantiles must be a nonnegative integer. Value: `' + num + '`.' );
	}
	if ( arguments.length > 2 ) {
		if ( !isObject( opts ) ) {
			throw new TypeError( 'nanquantiles()::invalid input argument. Options must be an object. Value: `' + opts + '`.' );
		}
		if ( opts.hasOwnProperty( 'sorted' ) ) {
			sorted = opts.sorted;
			if ( !isBoolean( sorted ) ) {
				throw new TypeError( 'nanquantiles()::invalid option. Sorted flag must be a boolean. Option: `' + sorted + '`.' );
			}
		}
		if ( opts.hasOwnProperty( 'accessor' ) ) {
			clbk = opts.accessor;
			if ( typeof clbk !== 'function' ) {
				throw new TypeError( 'nanquantiles()::invalid option. Accessor must be a function. Option: `' + clbk + '`.' );
			}
		}
	}
	d = [];
	for ( i = 0; i < arr.length; i++ ) {
		x = ( clbk ) ? clbk( arr[i] ) : arr[ i ];
		if ( isNumber( x ) ) {
			d.push( x );
		}
	}
	len = d.length;
	if ( !len ) {
		return null;
	}
	if ( !sorted ) {
		d.sort( ascending );
	}
	qValues = new Array( num+1 );

	// 0th quantile is the min:
	qValues[ 0 ] = d[ 0 ];

	// Max defines the quantile upper bound:
	qValues[ num ] = d[ len-1 ];

	// Get the quantiles...
	for ( i = 1; i < num; i++ ) {
		// Calculate the vector index marking the quantile:
		id = ( len * i / num ) - 1;

		// Is the index an integer?
		if ( id%1 === 0 ) {
			// Value is the average between the value at id and id+1:
			val = ( d[ id ] + d[ id+1 ] ) / 2.0;
		} else {
			// Round up to the next index:
			id = Math.ceil( id );
			val = d[ id ];
		}
		qValues[ i ] = val;
	} // end FOR i
	return qValues;
} // end FUNCTION nanquantiles()


// EXPORTS //

module.exports = nanquantiles;

},{"validate.io-array":706,"validate.io-boolean":707,"validate.io-nonnegative-integer":708,"validate.io-number":710,"validate.io-object":711}],706:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],707:[function(require,module,exports){
arguments[4][652][0].apply(exports,arguments)
},{"dup":652}],708:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":709}],709:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":710}],710:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],711:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":706}],712:[function(require,module,exports){
/**
*
*	COMPUTE: nanrange
*
*
*	DESCRIPTION:
*		- Computes the arithmetic range of an array ignoring non-numeric values.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2015. Philipp Burckhardt.
*
*
*	AUTHOR:
*		Philipp Burckhardt. pburckhardt@outlook.com. 2015.
*
*/

'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),isNumber = require( 'validate.io-number' );


// NANRANGE //

/**
* FUNCTION: nanrange( arr[, accessor] )
*	Computes the arithmetic range of an array ignoring non-numeric values.
*
* @param {Array} arr - input array
* @param {Function} [accessor] - accessor function for accessing array values
* @returns {Array|null} arithmetic range or null
*/
function nanrange( arr, clbk ) {
	if ( !isArray( arr ) ) {
		throw new TypeError( 'range()::invalid input argument. Must provide an array. Value: `' + arr + '`.' );
	}
	if ( arguments.length > 1 && typeof clbk !== 'function' ) {
		throw new TypeError( 'range()::invalid input argument. Accessor must be a function. Value: `' + clbk + '`.' );
	}
	var len = arr.length,
		min = null,
		max = min,
		flg = true,
		x;

	for ( var i = 0; i < len; i++ ) {
		x = ( clbk ) ? clbk( arr[i] ) : arr[ i ];
		if ( !isNumber( x ) ) {
			continue;
		}
		if ( flg ) {
			min = x;
			max = x;
			flg = false;
			continue;
		}
		if ( x < min ) {
			min = x;
		} else if ( x > max ) {
			max = x;
		}
	}
	return ( flg ) ? null : [ min, max ];
} // end FUNCTION nanrange()


// EXPORTS //

module.exports = nanrange;

},{"validate.io-array":713,"validate.io-number":714}],713:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],714:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],715:[function(require,module,exports){
/**
*
*	COMPUTE: nanstdev
*
*
*	DESCRIPTION:
*		- Computes the sample standard deviation over an array of values ignoring any values which are not numeric.
*
*
*	NOTES:
*		[1] 
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

(function() {
	'use strict';

	// NANSTDEV //

	/**
	* FUNCTION: nanstdev( arr )
	*	Computes the sample standard deviation over an array of values ignoring non-numeric values.
	*
	* @param {Array} arr - array of values
	* @returns {Number} sample standard deviation
	*/
	function nanstdev( arr ) {
		if ( !Array.isArray( arr ) ) {
			throw new TypeError( 'nanstdev()::invalid input argument. Must provide an array.' );
		}
		var len = arr.length,
			N = 0,
			mean = 0,
			M2 = 0,
			delta = 0,
			x;

		for ( var i = 0; i < len; i++ ) {
			x = arr[ i ];
			if ( typeof x !== 'number' || x !== x ) {
				continue;
			}
			N += 1;
			delta = x - mean;
			mean += delta / N;
			M2 += delta * ( x - mean );
		}
		if ( N < 2 ) {
			return 0;
		}
		return Math.sqrt( M2 / ( N-1 ) );
	} // end FUNCTION nanstdev()


	// EXPORTS //

	module.exports = nanstdev;

})();
},{}],716:[function(require,module,exports){
/**
*
*	COMPUTE: nansum
*
*
*	DESCRIPTION:
*		- Computes the sum of an array ignoring non-numeric values.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014-2015. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/


'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isNumber = require( 'validate.io-number' );


// NANSUM //

/**
* FUNCTION: nansum( arr[, accessor] )
*	Computes the sum of an array ignoring non-numeric values.
*
* @param {Array} arr - input array
* @param {Function} [accessor] - accessor function for accessing array values
* @returns {Number|null} sum or null
*/
function nansum( arr, clbk ) {
	if ( !isArray( arr ) ) {
		throw new TypeError( 'nansum()::invalid input argument. Must provide an array. Value: `' + arr + '`.' );
	}
	if ( arguments.length > 1 && typeof clbk !== 'function' ) {
		throw new TypeError( 'nansum()::invalid input argument. Accessor must be a function. Value: `' + clbk + '`.' );
	}
	var len = arr.length,
		s = 0,
		val,
		i;

	if ( !len ) {
		return null;
	}
	if ( clbk ) {
		for ( i = 0; i < len; i++ ) {
			val = clbk( arr[ i ] );
			if ( !isNumber( val ) ) {
				continue;
			}
			s += val;
		}
	} else {
		for ( i = 0; i < len; i++ ) {
			val = arr[ i ];
			if ( !isNumber( val ) ) {
				continue;
			}
			s += val;
		}
	}
	return s;
} // end FUNCTION nansum()


// EXPORTS //

module.exports = nansum;

},{"validate.io-array":717,"validate.io-number":718}],717:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],718:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],719:[function(require,module,exports){
/**
*
*	COMPUTE: nanvariance
*
*
*	DESCRIPTION:
*		- Computes the sample variance over an array of values ignoring any values which are not numeric.
*
*
*	NOTES:
*		[1] 
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

(function() {
	'use strict';

	// NANVARIANCE //

	/**
	* FUNCTION: nanvariance( arr )
	*	Computes the sample variance over an array of values ignoring non-numeric values.
	*
	* @param {Array} arr - array of values
	* @returns {Number} sample variance
	*/
	function nanvariance( arr ) {
		if ( !Array.isArray( arr ) ) {
			throw new TypeError( 'nanvariance()::invalid input argument. Must provide an array.' );
		}
		var len = arr.length,
			N = 0,
			mean = 0,
			M2 = 0,
			delta = 0,
			x;

		for ( var i = 0; i < len; i++ ) {
			x = arr[ i ];
			if ( typeof x !== 'number' || x !== x ) {
				continue;
			}
			N += 1;
			delta = x - mean;
			mean += delta / N;
			M2 += delta * ( x - mean );
		}
		if ( N < 2 ) {
			return 0;
		}
		return M2 / ( N-1 );
	} // end FUNCTION nanvariance()


	// EXPORTS //

	module.exports = nanvariance;

})();
},{}],720:[function(require,module,exports){
/**
*
*	COMPUTE: neq
*
*
*	DESCRIPTION:
*		- Computes an element-wise comparison (not equal) of an array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isObject = require( 'validate.io-object' );


// NOT EQUAL //

/**
* FUNCTION: neq( arr, x[, opts] )
*	Computes an element-wise comparison (not equal) of an array.
*
* @param {Array} arr - input array
* @param {*} x - comparator
* @param {Object} [opts] - function options
* @param {Boolean} [opts.strict] - option indicating whether to enforce type equality (default: true)
* @param {Boolean} [opts.array] - option indicating whether to not perform element-by-element comparison when provided arrays of equal length (default: false)
* @returns {Array} array of 1s and 0s, where a `1` indicates that an input array element is not equal to a compared value and `0` indicates that an input array element is equal to a compared value
*/
function neq( arr, x, opts ) {
	var isArray = Array.isArray( x ),
		strict = true,
		arrCompare = false,
		out,
		len,
		i;

	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'neq()::invalid input argument. Must provide an array.' );
	}
	if ( arguments.length > 2 ) {
		if ( !isObject( opts ) ) {
			throw new TypeError( 'neq()::invalid input argument. Options must be an object.' );
		}
		if ( opts.hasOwnProperty( 'strict' ) ) {
			strict = opts.strict;
			if ( typeof strict !== 'boolean' ) {
				throw new TypeError( 'neq()::invalid input argument. Strict option must be a boolean.' );
			}
		}
		if ( opts.hasOwnProperty( 'array' ) ) {
			arrCompare = opts.array;
			if ( typeof arrCompare !== 'boolean' ) {
				throw new TypeError( 'neq()::invalid input argument. Array option must be a boolean.' );
			}
		}
	}
	len = arr.length;
	out = new Array( len );
	if ( strict ) {
		if ( !isArray || x.length !== len || arrCompare ) {
			for ( i = 0; i < len; i++ ) {
				if ( arr[ i ] !== x ) {
					out[ i ] = 1;
				} else {
					out[ i ] = 0;
				}
			}
			return out;
		}
		for ( i = 0; i < len; i++ ) {
			if ( arr[ i ] !== x[ i ] ) {
				out[ i ] = 1;
			} else {
				out[ i ] = 0;
			}
		}
		return out;
	}
	if ( !isArray || x.length !== len || arrCompare ) {
		for ( i = 0; i < len; i++ ) {
			/* jshint eqeqeq:false */
			if ( arr[ i ] != x ) {
				out[ i ] = 1;
			} else {
				out[ i ] = 0;
			}
		}
		return out;
	}
	for ( i = 0; i < len; i++ ) {
		/* jshint eqeqeq:false */
		if ( arr[ i ] != x[ i ] ) {
			out[ i ] = 1;
		} else {
			out[ i ] = 0;
		}
	}
	return out;
} // end FUNCTION neq()


// EXPORTS //

module.exports = neq;

},{"validate.io-object":721}],721:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":722}],722:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],723:[function(require,module,exports){
/**
*
*	COMPUTE: pcorr
*
*
*	DESCRIPTION:
*		- Computes Pearson product-moment correlation coefficients between one or more numeric arrays.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// LINEAR CORRELATION //

/**
* FUNCTION: pcorr( arr1[, arr2,...] )
*	Computes Pearson product-moment correlation coefficients between one or more numeric arrays.
*
* @param {...Array} arr - numeric array
* @returns {Array} correlation matrix
*/
function pcorr() {
	var args,
		nArgs,
		len,
		deltas,
		delta,
		means,
		stdevs,
		C,
		cov,
		corr,
		arr,
		N, r, A, B, sum, val, sigma,
		i, j, n;

	args = Array.prototype.slice.call( arguments );
	nArgs = args.length;

	if ( !nArgs ) {
		throw new Error( 'pcorr()::insufficient input arguments. Must provide array arguments.' );
	}
	for ( i = 0; i < nArgs; i++ ) {
		if ( !Array.isArray( args[i] ) ) {
			throw new TypeError( 'pcorr()::invalid input argument. Must provide array arguments.' );
		}
	}
	if ( Array.isArray( args[0][0] ) ) {
		// If the first argument is an array of arrays, calculate the correlation matrix over the nested arrays, disregarding any other arguments...
		args = args[ 0 ];
	}
	nArgs = args.length;
	len = args[ 0 ].length;
	for ( i = 1; i < nArgs; i++ ) {
		if ( args[i].length !== len ) {
			throw new Error( 'pcorr()::invalid input argument. All arrays must have equal length.' );
		}
	}
	// [0] Initialization...
	deltas = new Array( nArgs );
	means = new Array( nArgs );
	stdevs = new Array( nArgs );
	C = new Array( nArgs );
	cov = new Array( nArgs );
	corr = new Array( nArgs );
	for ( i = 0; i < nArgs; i++ ) {
		means[ i ] = args[ i ][ 0 ];
		arr = new Array( nArgs );
		for ( j = 0; j < nArgs; j++ ) {
			arr[ j ] = 0;
		}
		C[ i ] = arr;
		cov[ i ] = arr.slice(); // copy!
		corr[ i ] = arr.slice(); // copy!
	}
	if ( len < 2 ) {
		return corr;
	}
	// [1] Compute the covariance...
	for ( n = 1; n < len; n++ ) {

		N = n + 1;
		r = n / N;

		// [a] Extract the values and compute the deltas...
		for ( i = 0; i < nArgs; i++ ) {
			deltas[ i ] = args[ i ][ n ] - means[ i ];
		}

		// [b] Update the covariance between one array and every other array...
		for ( i = 0; i < nArgs; i++ ) {
			arr = C[ i ];
			delta = deltas[ i ];
			for ( j = i; j < nArgs; j++ ) {
				A = arr[ j ];
				B = r * delta * deltas[ j ];
				sum = A + B;
				// Exploit the fact that the covariance matrix is symmetric...
				if ( i !== j ) {
					C[ j ][ i ] = sum;
				}
				arr[ j ] = sum;
			} // end FOR j
		} // end FOR i

		// [c] Update the means...
		for ( i = 0; i < nArgs; i++ ) {
			means[ i ] += deltas[ i ] / N;
		}
	} // end FOR n

	// [2] Normalize the co-moments...
	n = N - 1;
	for ( i = 0; i < nArgs; i++ ) {
		arr = C[ i ];
		for ( j = i; j < nArgs; j++ ) {
			val = arr[ j ] / n;
			cov[ i ][ j ] = val;
			if ( i !== j ) {
				cov[ j ][ i ] = val;
			}
		}
	}

	// [3] Compute the standard deviations...
	for ( i = 0; i < nArgs; i++ ) {
		// Diagonal elements of covariance matrix...
		stdevs[ i ] = Math.sqrt( cov[i][i] );
	}

	// [4] Set the diagonal elements to 1:
	for ( i = 0; i < nArgs; i++ ) {
		corr[ i ][ i ] = 1;
	}

	// [5] Compute the correlation coefficients...
	for ( i = 0; i < nArgs; i++ ) {
		arr = cov[ i ];
		sigma = stdevs[ i ];
		for ( j = i+1; j < nArgs; j++ ) {
			val = arr[ j ] / ( sigma*stdevs[j] );
			// Address floating point errors introduced by taking the sqrt and enforce strict [-1,1] bounds...
			if ( val > 1 ) {
				val = 1;
			} else if ( val < -1 ) {
				val = -1;
			}
			corr[ i ][ j ] = val;
			corr[ j ][ i ] = val;
		}
	}
	return corr;
} // end FUNCTION pcorr()


// EXPORTS //

module.exports = pcorr;

},{}],724:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isArray = require( 'validate.io-array' ),
	isNumberArray = require( 'validate.io-number-primitive-array' ),
	isObject = require( 'validate.io-object' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' );


// POLYVAL //

/**
* FUNCTION: polyval( coef, x )
*	Evaluates a polynomial.
*
* @private
* @param {Number[]} coef - array of coefficients sorted in descending degree
* @param {Number} x - value at which to evaluate the polynomial
* @return {Number} evaluated polynomial
*/
function polyval( c, x ) {
	var len = c.length,
		p = 0,
		i = 0;
	for ( ; i < len; i++ ) {
		p = p*x + c[ i ];
	}
	return p;
} // end FUNCTION polyval()


// EVALUATE //

/**
* FUNCTION: evaluate( coef, x[, options] )
*	Evaluates a polynomial.
*
* @param {Number[]} coef - array of coefficients sorted in descending degree
* @param {Array|Number[]|Number} x - value(s) at which to evaluate the polynomial
* @param {Object} [options] - function options
* @param {Boolean} [options.copy=true] - boolean indicating whether to return a new array
* @param {Function} [options.accessor] - accessor function for accessing array values
* @returns {Number|Number[]} evaluated polynomial
*/
function evaluate( c, x, opts ) {
	var copy = true,
		clbk,
		len,
		arr,
		v, i;
	if ( !isNumberArray( c ) ) {
		throw new TypeError( 'polynomial()::invalid input argument. Coefficients must be provided as an array of number primitives. Value: `' + c + '`.' );
	}
	if ( isNumber( x ) ) {
		return polyval( c, x );
	}
	if ( !isArray( x ) ) {
		throw new TypeError( 'polynomial()::invalid input argument. Second argument must be either a single number primitive or an array of values. Value: `' + x + '`.' );
	}
	if ( arguments.length > 2 ) {
		if ( !isObject( opts ) ) {
			throw new TypeError( 'polynomial()::invalid input argument. Options argument must be an object. Value: `' + opts + '`.' );
		}
		if ( opts.hasOwnProperty( 'copy' ) ) {
			copy = opts.copy;
			if ( !isBoolean( copy ) ) {
				throw new TypeError( 'polynomial()::invalid option. Copy option must be a boolean primitive. Option: `' + copy + '`.' );
			}
		}
		if ( opts.hasOwnProperty( 'accessor' ) ) {
			clbk = opts.accessor;
			if ( !isFunction( clbk ) ) {
				throw new TypeError( 'polynomial()::invalid option. Accessor must be a function. Option: `' + clbk + '`.' );
			}
		}
	}
	len = x.length;
	if ( copy ) {
		arr = new Array( len );
	} else {
		arr = x;
	}
	if ( clbk ) {
		for ( i = 0; i < len; i++ ) {
			v = clbk( x[ i ], i );
			if ( !isNumber( v ) ) {
				throw new TypeError( 'polynomial()::invalid input argument. Accessed array values must be number primitives. Value: `' + v + '`.' );
			}
			arr[ i ] = polyval( c, v );
		}
	} else {
		for ( i = 0; i < len; i++ ) {
			v = x[ i ];
			if ( !isNumber( v ) ) {
				throw new TypeError( 'polynomial()::invalid input argument. Array values must be number primitives. Value: `' + v + '`.' );
			}
			arr[ i ] = polyval( c, v );
		}
	}
	return arr;
} // end FUNCTION evaluate()


// EXPORTS //

module.exports = evaluate;

},{"validate.io-array":725,"validate.io-boolean-primitive":726,"validate.io-function":727,"validate.io-number-primitive":729,"validate.io-number-primitive-array":728,"validate.io-object":730}],725:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],726:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],727:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],728:[function(require,module,exports){
/**
*
*	VALIDATE: number-primitive-array
*
*
*	DESCRIPTION:
*		- Validates if a value is an array of primitive numbers.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2015. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2015.
*
*/

'use strict';

// MODULES //

var isArray = require( 'validate.io-array' );


// IS NUMBER ARRAY //

/**
* FUNCTION: isNumberArray( value )
*	Validates if a value is an array of number primitives, excluding NaN.
*
* @param {*} value - value to be validated
* @returns {Boolean} boolean indicating if a value is an array of number primitives
*/
function isNumberArray( value ) {
	var len, v;
	if ( !isArray( value ) ) {
		return false;
	}
	len = value.length;
	if ( !len ) {
		return false;
	}
	for ( var i = 0; i < len; i++ ) {
		v = value[ i ];
		if ( typeof v !== 'number' || v !== v ) {
			return false;
		}
	}
	return true;
} // end FUNCTION isNumberArray()


// EXPORTS //

module.exports = isNumberArray;

},{"validate.io-array":725}],729:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],730:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":725}],731:[function(require,module,exports){
'use strict';

/**
* FUNCTION: prod( arr, clbk )
*	Computes an array product using an accessor.
*
* @param {Array} arr - input array
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number} product
*/
function prod( arr, clbk ) {
	var len = arr.length,
		p = 1,
		v,
		i;
	for ( i = 0; i < len; i++ ) {
		v = clbk( arr[ i ], i );
		if ( v === 0 ) {
			return 0;
		}
		p *= v;
	}
	return p;
} // end FUNCTION prod()


// EXPORTS //

module.exports = prod;

},{}],732:[function(require,module,exports){
'use strict';

/**
* FUNCTION: prod( arr )
*	Computes an array product.
*
* @param {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} arr - input array
* @returns {Number} product
*/
function prod( arr ) {
	var len = arr.length,
		p = 1,
		i;
	for ( i = 0; i < len; i++ ) {
		if ( arr[ i ] === 0 ) {
			return 0;
		}
		p *= arr[ i ];
	}
	return p;
} // end FUNCTION prod()


// EXPORTS //

module.exports = prod;

},{}],733:[function(require,module,exports){
'use strict';

// MODULES //

var isArrayLike = require( 'validate.io-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ).raw,
	validate = require( './validate.js' );


// FUNCTIONS //

var prod1 = require( './array.js' ),
	prod2 = require( './accessor.js' ),
	prod3 = require( './matrix.js' );


// PROD //

/*
* FUNCTION: prod( x[, options] )
*	Computes the product of elements in x.
*
* @param {Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} x - input value
* @param {Object} [options] - function options
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {Number} [options.dim=2] - dimension along which to compute the product
* @param {String} [options.dtype="float64"] - output data type
* @returns {Number|Matrix} product(s)
*/
function prod( x, options ) {
	/* jshint newcap:false */
	var opts = {},
		shape,
		ctor,
		err,
		len,
		dim,
		dt,
		d,
		m;

	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isMatrixLike( x ) ) {
		dt = opts.dtype || 'float64';
		dim = opts.dim;

		// Determine if provided a vector...
		if ( x.shape[ 0 ] === 1 || x.shape[ 1 ] === 1 ) {
			// Treat as an array-like object:
			return prod1( x.data );
		}
		if ( dim > 2 ) {
			throw new RangeError( 'prod()::invalid option. Dimension option exceeds number of matrix dimensions. Option: `' + dim + '`.' );
		}
		if ( dim === void 0 || dim === 2 ) {
			len = x.shape[ 0 ];
			shape = [ len, 1 ];
		} else {
			len = x.shape[ 1 ];
			shape = [ 1, len ];
		}
		ctor = ctors( dt );
		if ( ctor === null ) {
			throw new Error( 'prod()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
		}
		// Create an output matrix and calculate the products:
		d = new ctor( len );
		m = matrix( d, shape, dt );
		return prod3( m, x, dim );
	}
	if ( isArrayLike( x ) ) {
		if ( opts.accessor ) {
			return prod2( x, opts.accessor );
		}
		return prod1( x );
	}
	throw new TypeError( 'prod()::invalid input argument. First argument must be either an array or a matrix. Value: `' + x + '`.' );
} // end FUNCTION prod()


// EXPORTS //

module.exports = prod;

},{"./accessor.js":731,"./array.js":732,"./matrix.js":734,"./validate.js":735,"compute-array-constructors":737,"dstructs-matrix":746,"validate.io-array-like":784,"validate.io-matrix-like":789}],734:[function(require,module,exports){
'use strict';

/**
* FUNCTION: prod( out, mat[, dim] )
*	Computes the product along a matrix dimension.
*
* @param {Matrix} out - output matrix
* @param {Matrix} mat - input matrix
* @param {Number} [dim=2] - matrix dimension along which to compute the product. If `dim=1`, compute along matrix rows. If `dim=2`, compute along matrix columns.
* @returns {Matrix|Number} product or 1
*/
function prod( out, mat, dim ) {
	var p,
		M, N,
		s0, s1,
		o,
		i, j, k;

	if ( dim === 1 ) {
		// Compute along the rows...
		M = mat.shape[ 1 ];
		N = mat.shape[ 0 ];
		s0 = mat.strides[ 1 ];
		s1 = mat.strides[ 0 ];
	} else {
		// Compute along the columns...
		M = mat.shape[ 0 ];
		N = mat.shape[ 1 ];
		s0 = mat.strides[ 0 ];
		s1 = mat.strides[ 1 ];
	}
	if ( M === 0 || N === 0 ) {
		return 1;
	}
	o = mat.offset;
	for ( i = 0; i < M; i++ ) {
		k = o + i*s0;
		p = 1;
		for ( j = 0; j < N; j++ ) {
			p *= mat.data[ k + j*s1 ];
		}
		out.data[ i ] = p;
	}
	return out;
} // end FUNCTION prod()


// EXPORTS //

module.exports = prod;

},{}],735:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' ),
	isPositiveInteger = require( 'validate.io-positive-integer' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {Number} [options.dim] - dimension
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'prod()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'prod()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dim' ) ) {
		opts.dim = options.dim;
		if ( !isPositiveInteger( opts.dim ) ) {
			return new TypeError( 'prod()::invalid option. Dimension option must be a positive integer. Option: `' + opts.dim + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'prod()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-function":788,"validate.io-object":790,"validate.io-positive-integer":792,"validate.io-string-primitive":795}],736:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],737:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":736,"dup":12}],738:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],739:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":742,"./iget.js":744,"./iset.js":747,"./mget.js":751,"./mset.js":753,"./set.js":761,"./sget.js":763,"./sset.js":765,"./toString.js":767,"dup":14}],740:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":743,"./iget.raw.js":745,"./iset.raw.js":748,"./mget.raw.js":752,"./mset.raw.js":754,"./set.raw.js":762,"./sget.raw.js":764,"./sset.raw.js":766,"./toString.js":767,"dup":15}],741:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],742:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":780}],743:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],744:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":778}],745:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],746:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":749,"./matrix.raw.js":750,"dup":21}],747:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":778,"validate.io-number-primitive":783}],748:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],749:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":738,"./ctor.js":739,"./dtypes.js":741,"compute-cast-arrays":768,"compute-dtype":771,"dup":24,"validate.io-array":775,"validate.io-contains":776,"validate.io-nonnegative-integer-array":779,"validate.io-string-primitive":795}],750:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":738,"./ctor.raw.js":740,"./dtypes.js":741,"compute-dtype":771,"dup":25,"validate.io-contains":776,"validate.io-string-primitive":795}],751:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":738,"dup":26,"validate.io-nonnegative-integer-array":779}],752:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":738,"dup":27}],753:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":755,"./mset2.js":756,"./mset3.js":757,"./mset4.js":758,"./mset5.js":759,"./mset6.js":760,"dup":28,"validate.io-function":788,"validate.io-nonnegative-integer-array":779,"validate.io-number-primitive":783}],754:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":755,"./mset2.js":756,"./mset3.js":757,"./mset4.js":758,"./mset5.js":759,"./mset6.js":760,"dup":29}],755:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],756:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],757:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],758:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],759:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],760:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],761:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":780,"validate.io-number-primitive":783}],762:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],763:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":738,"compute-indexspace":534,"dup":38,"validate.io-string-primitive":795}],764:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":738,"compute-indexspace":534,"dup":39}],765:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":534,"dup":40,"validate.io-function":788,"validate.io-number-primitive":783,"validate.io-string-primitive":795}],766:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":534,"dup":41}],767:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],768:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":736,"compute-array-dtype/lib/dtypes":769,"dup":43,"type-name":770,"validate.io-array-like":784}],769:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],770:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],771:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":773,"dup":46,"type-name":774}],772:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],773:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":772,"dup":48}],774:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],775:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],776:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":775,"validate.io-nan-primitive":777}],777:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],778:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":783}],779:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":775,"validate.io-nonnegative-integer":780}],780:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":781}],781:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":782}],782:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],783:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],784:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":785,"dup":70,"validate.io-integer-primitive":786}],785:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],786:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":787}],787:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],788:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],789:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],790:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":791}],791:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],792:[function(require,module,exports){
arguments[4][471][0].apply(exports,arguments)
},{"dup":471,"validate.io-integer":793}],793:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":794}],794:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],795:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],796:[function(require,module,exports){
/**
*
*	COMPUTE: qmean
*
*
*	DESCRIPTION:
*		- Computes the quadratic mean (root mean square) of an array of values.
*
*
*	NOTES:
*		[1] 
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

(function() {
	'use strict';

	// QMEAN //

	/**
	* FUNCTION: qmean( arr )
	*	Calculates the quadratic mean (root mean square) of an array of values.
	*
	* @param {Array} arr - array of values
	* @returns {Number} quadratic mean
	*/
	function qmean( arr ) {
		if ( !Array.isArray( arr ) ) {
			throw new TypeError( 'qmean()::invalid input argument. Must provide an array.' );
		}
		var len = arr.length,
			t = 0,
			s = 1,
			r,
			val,
			abs;
		for ( var i = 0; i < len; i++ ) {
			val = arr[ i ];
			abs = val;
			if ( abs < 0 ) {
				abs = -abs;
			}
			if ( abs > 0 ) {
				if ( abs > t ) {
					r = t / val;
					s = 1 + s*r*r;
					t = abs;
				} else {
					r = val / t;
					s = s + r*r;
				}
			}
		}
		return t * Math.sqrt( s/len );
	} // end FUNCTION qmean()


	// EXPORTS //

	module.exports = qmean;

})();
},{}],797:[function(require,module,exports){
/**
*
*	COMPUTE: quantile
*
*
*	DESCRIPTION:
*		- Computes a quantile for a numeric array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isObject = require( 'validate.io-object' );


// FUNCTIONS //

/**
* FUNCTION: ascending( a, b )
*	Comparator function used to sort values in ascending order.
*
* @private
* @param {Number} a
* @param {Number} b
* @returns {Number} difference between `a` and `b`
*/
function ascending( a, b ) {
	return a - b;
} // end FUNCTION ascending()


// QUANTILE //

/**
* FUNCTION: quantile( arr, prob[, opts] )
*	Computes a quantile for a numeric array.
*
* @private
* @param {Array} arr - 1d array
* @param {Number} prob - quantile prob [0,1]
* @param {Object} [opts] - method options:
	`method`: method used to interpolate a quantile value
	`sorted`: boolean flag indicating if the input array is sorted
* @returns {Number} quantile value
*/
function quantile( arr, p, opts ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'quantile()::invalid input argument. First argument must be an array.' );
	}
	if ( typeof p !== 'number' || p !== p ) {
		throw new TypeError( 'quantile()::invalid input argument. Quantile probability must be numeric.' );
	}
	if ( p < 0 || p > 1 ) {
		throw new TypeError( 'quantile()::invalid input argument. Quantile probability must be on the interval [0,1].' );
	}
	if ( arguments.length > 2 ) {
		if ( !isObject( opts ) ) {
			throw new TypeError( 'quantile()::invalid input argument. Options must be an object.' );
		}
		if ( opts.hasOwnProperty( 'sorted' ) && typeof opts.sorted !== 'boolean' ) {
			throw new TypeError( 'quantile()::invalid input argument. Sorted flag must be a boolean.' );
		}
		if ( opts.hasOwnProperty( 'method' ) && typeof opts.method !== 'string' ) {
			throw new TypeError( 'quantile()::invalid input argument. Method must be a string.' );
		}
		// TODO: validate that the requested method is supported. list.indexOf( method )
	} else {
		opts = {};
	}
	var len = arr.length,
		id;

	if ( !opts.sorted ) {
		arr = arr.slice();
		arr.sort( ascending );
	}

	// Cases...

	// [0] 0th percentile is the minimum value...
	if ( p === 0.0 ) {
		return arr[ 0 ];
	}
	// [1] 100th percentile is the maximum value...
	if ( p === 1.0 ) {
		return arr[ len-1 ];
	}
	// Calculate the vector index marking the quantile:
	id = ( len*p ) - 1;

	// [2] Is the index an integer?
	if ( id === Math.floor( id ) ) {
		// Value is the average between the value at id and id+1:
		return ( arr[ id ] + arr[ id+1 ] ) / 2.0;
	}
	// [3] Round up to the next index:
	id = Math.ceil( id );
	return arr[ id ];
} // end FUNCTION quantile()


// EXPORTS //

module.exports = quantile;

},{"validate.io-object":798}],798:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":799}],799:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],800:[function(require,module,exports){
/**
*
*	COMPUTE: quantiles
*
*
*	DESCRIPTION:
*		- Computes quantiles for numeric array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isInteger = require( 'validate.io-integer' );


// FUNCTIONS //

/**
* FUNCTION: ascending( a, b )
*	Comparator function used to sort values in ascending order.
*
* @private
* @param {Number} a
* @param {Number} b
* @returns {Number} difference between `a` and `b`
*/
function ascending( a, b ) {
	return a - b;
} // end FUNCTION ascending()


// QUANTILES //

/**
* FUNCTION: quantiles( arr, num[, opts] )
*	Computes quantiles for a numeric array.
*
* @param {Array} arr - array of values
* @param {Number} num - number of quantiles
* @param {Object} [options] - function options
* @returns {Array} quantiles
*/
function quantiles( arr, num, opts ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'quantiles()::invalid input argument. First argument must be an array.' );
	}
	if ( !isInteger( num ) || num <= 0 ) {
		throw new TypeError( 'quantiles()::invalid input argument. Second argument must be a positive integer.' );
	}
	if ( arguments.length > 2 ) {
		if ( !isObject( opts ) ) {
			throw new TypeError( 'quantiles()::invalid input argument. Options should be an object.' );
		}
		if ( opts.hasOwnProperty( 'sorted' ) && typeof opts.sorted !== 'boolean' ) {
			throw new TypeError( 'quantiles()::invalid input argument. Sorted flag must be a boolean.' );
		}
		if ( opts.hasOwnProperty( 'method' ) && typeof opts.method !== 'string' ) {
			throw new TypeError( 'quantiles()::invalid input argument. Method must be a string.' );
		}
		// TODO: validate that the requested method is supported. list.indexOf( method )
	} else {
		opts = {};
	}
	var len = arr.length,
		qValues = new Array( num+1 ),
		id,
		val;

	if ( !opts.sorted ) {
		arr = arr.slice();
		arr.sort( ascending );
	}

	// 0th quantile is the min:
	qValues[ 0 ] = arr[ 0 ];

	// Max defines the quantile upper bound:
	qValues[ num ] = arr[ len-1 ];

	// Get the quantiles...
	for ( var i = 1; i < num; i++ ) {

		// Calculate the vector index marking the quantile:
		id = ( len * i / num ) - 1;

		// Is the index an integer?
		if ( id === Math.floor( id ) ) {
			// Value is the average between the value at id and id+1:
			val = ( arr[ id ] + arr[ id+1 ] ) / 2.0;
		} else {
			// Round up to the next index:
			id = Math.ceil( id );
			val = arr[ id ];
		}
		qValues[ i ] = val;
	} // end FOR i
	return qValues;
} // end FUNCTION quantiles()


// EXPORTS //

module.exports = quantiles;

},{"validate.io-integer":801,"validate.io-object":803}],801:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":802}],802:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],803:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":804}],804:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],805:[function(require,module,exports){
/**
*
*	COMPUTE: rad2deg
*
*
*	DESCRIPTION:
*		- Converts radians to degrees.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// RADIANS-TO-DEGREES //

/**
* FUNCTION: rad2deg( x )
*	Converts radians to degrees. Note: if provided an array, the array is mutated.
*
* @param {Array|Number} x - value(s) to be converted to degrees
* @returns {Array|Number|Null} degree value(s). If `x` is an empty `array`, returns `null`.
*/
function rad2deg( x ) {
	var isArray = Array.isArray( x ),
		len;
	if ( !isArray && ( typeof x !== 'number' || x !== x ) ) {
		throw new TypeError( 'rad2deg()::invalid input argument. Must provide either a single numeric value or a numeric array.' );
	}
	if ( !isArray ) {
		return x * 180 / Math.PI;
	}
	len = x.length;
	if ( !len ) {
		return null;
	}
	for ( var i = 0; i < len; i++ ) {
		x[ i ] *= 180 / Math.PI;
	}
	return x;
} // end FUNCTION rad2deg()


// EXPORTS //

module.exports = rad2deg;

},{}],806:[function(require,module,exports){
/**
*
*	COMPUTE: range
*
*
*	DESCRIPTION:
*		- Computes the arithmetic range of an array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014-2015. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isFunction = require( 'validate.io-function' );


// RANGE //

/**
* FUNCTION: range( arr[, accessor] )
*	Returns the arithmetic range of an array.
*
* @param {Array} arr - input array
* @param {Function} [accessor] - accessor function for accessing array values
* @returns {Array|null} arithmetic range or null
*/
function range( arr, clbk ) {
	if ( !isArray( arr ) ) {
		throw new TypeError( 'range()::invalid input argument. Must provide an array. Value: `' + arr + '`.' );
	}

	if ( arguments.length > 1 && !isFunction( clbk ) ) {
		throw new TypeError( 'range()::invalid input argument. Accessor must be a function. Value: `' + clbk + '`.' );
	}
	var len = arr.length,
		min,
		max,
		x, i;

	if ( len === 0 ) {
		return null;
	}
	if ( clbk ) {
		min = clbk( arr[ 0 ] );
		max = min;
		for ( i = 1; i < len; i++ ) {
			x = clbk( arr[ i ] );
			if ( x < min ) {
				min = x;
			} else if ( x > max ) {
				max = x;
			}
		}
	} else {
		min = arr[ 0 ];
		max = min;
		for ( i = 1; i < len; i++ ) {
			x = arr[ i ];
			if ( x < min ) {
				min = x;
			} else if ( x > max ) {
				max = x;
			}
		}
	}
	return [ min, max ];
} // end FUNCTION range()


// EXPORTS //

module.exports = range;

},{"validate.io-array":807,"validate.io-function":808}],807:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],808:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],809:[function(require,module,exports){
'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isObject = require( 'validate.io-object' ),
	isBoolean = require( 'validate.io-boolean-primitive' );


// REVERSE //

/**
* FUNCTION: reverse( arr[, options] )
*	Reverses an `array`.
*
* @param {Array} arr - input array
* @param {Object} [options] - function options
* @param {Boolean} [options.copy=true] - boolean indicating whether to return a new array
* @returns {Array} reversed array
*/
function reverse( arr, opts ) {
	var copy,
		half,
		len,
		tmp,
		out,
		N,
		i, j;

	if ( !isArray( arr ) ) {
		throw new TypeError( 'reverse()::invalid input argument. Must provide an array. Value: `' + arr + '`.' );
	}
	if ( arguments.length > 1 ) {
		if ( !isObject( opts ) ) {
			throw new TypeError( 'reverse()::invalid input argument. Options argument must be an object. Value: `' + opts + '`.' );
		}
		if ( opts.hasOwnProperty( 'copy' ) ) {
			copy = opts.copy;
			if ( !isBoolean( copy ) ) {
				throw new TypeError( 'reverse()::invalid option. Copy option must be a boolean primitive. Option: `' + copy + '`.' );
			}
		}
	}
	len = arr.length;
	N = len - 1;
	if ( copy ) {
		out = new Array( len );
		for ( i = 0; i < len; i++ ) {
			out[ i ] = arr[ N-i ];
		}
		return out;
	}
	half = Math.floor( len / 2 );
	for ( i = 0; i < half; i++ ) {
		tmp = arr[ i ];
		j = N - i;
		arr[ i ] = arr[ j ];
		arr[ j ] = tmp;
	}
	return arr;
} // end FUNCTION reverse()


// EXPORTS //

module.exports = reverse;

},{"validate.io-array":810,"validate.io-boolean-primitive":811,"validate.io-object":812}],810:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],811:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],812:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":810}],813:[function(require,module,exports){
/**
*
*	COMPUTE: roundn
*
*
*	DESCRIPTION:
*		- Round values to the nearest multiple of 10^n.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

/**
* FUNCTION: roundn( x, n )
*	Rounds values to the nearest multiple of 10^n. Notes: if provided an array, mutates the array.
*
* @param {Array|Number} x - value(s) to be rounded
* @param {Number} n - power of 10; should be an integer value
* @returns {Array|Number} rounded value(s). If `x` is an empty array, returns `null`.
*/
function roundn( x, n ) {
	var isArray = Array.isArray( x ),
		scalar,
		len;
	if ( !isArray && ( typeof x !== 'number' || x !== x ) ) {
		throw new TypeError( 'roundn()::invalid input argument. Must provide either a single numeric value or a numeric array.' );
	}
	if ( typeof n !== 'number' || n !== n || n !== ( n | 0) ) {
		throw new TypeError( 'roundn()::invalid input argument. Power of 10 must be an integer value.' );
	}
	n = -n;
	scalar = Math.pow( 10, n );
	if ( !isArray ) {
		return Math.round( x*scalar ) / scalar;
	}
	len = x.length;
	if ( !len ) {
		return null;
	}
	for ( var i = 0; i < len; i++ ) {
		x[ i ] = Math.round( x[i]*scalar ) / scalar;
	}
	return x;
} // end FUNCTION roundn()


// EXPORTS //

module.exports = roundn;

},{}],814:[function(require,module,exports){
/**
*
*	COMPUTE: shuffle
*
*
*	DESCRIPTION:
*		- Shuffles array elements in place.
*
*
*	NOTES:
*		[1] 
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

(function() {
	'use strict';

	// SHUFFLE //

	/**
	* FUNCTION: shuffle( arr )
	*	Mutates an input `array` to generate a random permutation of `array` elements.
	*
	* @param {Array} arr - `array` to shuffle
	*/
	function shuffle( arr ) {
		if ( !Array.isArray( arr ) ) {
			throw new TypeError( 'shuffle()::invalid input argument. Must provide an array.' );
		}
		var N = arr.length,
			j,
			tmp;

		// Note: we skip the first element, as no further swaps are possible given that all other indices are excluded from swapping...
		for ( var i = N - 1; i > 0; i-- ) {
			// Generate an integer index on the interval: [0,i]
			j = Math.floor( Math.random() * (i+1) );

			// Swap elements:
			tmp = arr[ i ];
			arr[ i ] = arr[ j ];
			arr[ j ] = tmp;
		}
	} // end FUNCTION shuffle()


	// EXPORTS //

	module.exports = shuffle;

})();
},{}],815:[function(require,module,exports){
'use strict';

// FUNCTIONS

var SIGN = require( './number.js' );


// SIGNUM FUNCTION //

/**
* FUNCTION: signum( out, arr, accessor )
*	Evaluates the signum function for each array element using an accessor function.
*
* @param {Array} out - output array
* @param {Array} arr - input array
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]} output array
*/
function signum( y, x, clbk ) {
	var len = x.length,
		i;
	for ( i = 0; i < len; i++ ) {
		y[ i ] = SIGN( clbk( x[ i ], i ) );
	}
	return y;
} // end FUNCTION signum()


// EXPORTS //

module.exports = signum;

},{"./number.js":820}],816:[function(require,module,exports){
'use strict';

// MODULES //

var SIGN = require( './number.js' );


// SIGNUM FUNCTION //

/**
* FUNCTION: signum( out, arr )
*	Evaluates the signum function for each array element.
*
* @param {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} arr - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function signum( y, x ) {
	var len = x.length,
		i;
	for ( i = 0; i < len; i++ ) {
		y[ i ] = SIGN( x[ i ] );
	}
	return y;
} // end FUNCTION signum()


// EXPORTS //

module.exports = signum;

},{"./number.js":820}],817:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory,
	SIGN = require( './number.js' );


// SIGNUM FUNCTION //

/**
* FUNCTION: signum( arr, path[, sep] )
*	Evaluates the signum function for each array element and deep sets the input array.
*
* @param {Array} arr - input array
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function signum( x, path, sep ) {
	var len = x.length,
		opts = {},
		dget,
		dset,
		i;
	if ( arguments.length > 2 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		for ( i = 0; i < len; i++ ) {
			dset( x[i], SIGN( dget( x[i] ) ) );
		}
	}
	return x;
} // end FUNCTION signum()


// EXPORTS //

module.exports = signum;

},{"./number.js":820,"utils-deep-get":870,"utils-deep-set":875}],818:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isNaN = require( 'validate.io-nan'),
	isArray = require( 'validate.io-array' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var signum1 = require( './number.js' ),
	signum2 = require( './array.js' ),
	signum3 = require( './accessor.js' ),
	signum4 = require( './deepset.js' ),
	signum5 = require( './matrix.js' );


// SIGNUM FUNCTION //

/**
* FUNCTION: signum( x[, opts] )
*	Evaluates the signum function.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} x - input value
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix|Null} signum function value(s) or null
*/
function signum( x, options ) {
	/* jshint newcap:false */
	var opts = {},
		err,
		out,
		d;

	if ( isNumber( x ) || isNaN( x ) ) {
		return signum1( x );
	}
	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isMatrixLike( x ) ) {
		if ( opts.copy !== false ) {
			d = new Int8Array( x.length );
			out = matrix( d, x.shape, 'int8' );
		} else {
			out = x;
		}
		return signum5( out, x );
	}
	if ( isArrayLike( x ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return signum4( x, opts.path, opts.sep );
		}
		// Handle regular, typed, and accessor arrays next...
		if ( opts.copy === false ) {
			out = x;
		}
		else if ( !isArray( x ) ) {
			out = new Int8Array( x.length );
		} else {
			out = new Array( x.length );
		}
		if ( opts.accessor ) {
			return signum3( out, x, opts.accessor );
		}
		return signum2( out, x );
	}
	throw new TypeError( 'signum()::invalid input argument. Input value type not currently supported. Value: `' + x + '`.' );
} // end FUNCTION signum()


// EXPORTS //

module.exports = signum;

},{"./accessor.js":815,"./array.js":816,"./deepset.js":817,"./matrix.js":819,"./number.js":820,"./validate.js":821,"dstructs-matrix":831,"validate.io-array":880,"validate.io-array-like":877,"validate.io-matrix-like":883,"validate.io-nan":884,"validate.io-number-primitive":885}],819:[function(require,module,exports){
'use strict';

// MODULES //

var SIGN = require( './number.js' );


// SIGNUM FUNCTION //

/**
* FUNCTION: signum( out, matrix )
*	Evaluates the signum function for each matrix element.
*
* @param {Matrix} out - output matirx
* @param {Matrix} arr - input matrix
* @returns {Matrix} output matrix
*/
function signum( y, x ) {
	var len = x.length,
		i;
	if ( y.length !== len ) {
		throw new Error( 'signum()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	for ( i = 0; i < len; i++ ) {
		y.data[ i ] = SIGN( x.data[ i ] );
	}
	return y;
} // end FUNCTION signum()


// EXPORTS //

module.exports = signum;

},{"./number.js":820}],820:[function(require,module,exports){
'use strict';

// SIGNUM //

/**
* FUNCTION: signum( x )
*	Determines the sign of a numeric value.
*
* @param {Number} x - input value
* @returns {Number} value indicating the sign
*/
function signum( x ) {
	// [0] NaN
	if ( x !== x ) {
		return NaN;
	}
	// [1] +-0
	if ( !x ) {
		return x;
	}
	// [2] x < 0
	if ( x < 0 ) {
		return -1;
	}
	// [3] x > 0
	return 1;
} // end FUNCTION signum()


// EXPORTS //

module.exports = signum;

},{}],821:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Boolean} [options.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'signum()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'signum()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'signum()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'signum()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'signum()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":881,"validate.io-function":882,"validate.io-object":886,"validate.io-string-primitive":887}],822:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],823:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],824:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":827,"./iget.js":829,"./iset.js":832,"./mget.js":836,"./mset.js":838,"./set.js":846,"./sget.js":848,"./sset.js":850,"./toString.js":852,"dup":14}],825:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":828,"./iget.raw.js":830,"./iset.raw.js":833,"./mget.raw.js":837,"./mset.raw.js":839,"./set.raw.js":847,"./sget.raw.js":849,"./sset.raw.js":851,"./toString.js":852,"dup":15}],826:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],827:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":864}],828:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],829:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":862}],830:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],831:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":834,"./matrix.raw.js":835,"dup":21}],832:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":862,"validate.io-number-primitive":885}],833:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],834:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":823,"./ctor.js":824,"./dtypes.js":826,"compute-cast-arrays":853,"compute-dtype":856,"dup":24,"validate.io-array":880,"validate.io-contains":860,"validate.io-nonnegative-integer-array":863,"validate.io-string-primitive":887}],835:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":823,"./ctor.raw.js":825,"./dtypes.js":826,"compute-dtype":856,"dup":25,"validate.io-contains":860,"validate.io-string-primitive":887}],836:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":823,"dup":26,"validate.io-nonnegative-integer-array":863}],837:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":823,"dup":27}],838:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":840,"./mset2.js":841,"./mset3.js":842,"./mset4.js":843,"./mset5.js":844,"./mset6.js":845,"dup":28,"validate.io-function":882,"validate.io-nonnegative-integer-array":863,"validate.io-number-primitive":885}],839:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":840,"./mset2.js":841,"./mset3.js":842,"./mset4.js":843,"./mset5.js":844,"./mset6.js":845,"dup":29}],840:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],841:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],842:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],843:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],844:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],845:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],846:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":864,"validate.io-number-primitive":885}],847:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],848:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":823,"compute-indexspace":534,"dup":38,"validate.io-string-primitive":887}],849:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":823,"compute-indexspace":534,"dup":39}],850:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":534,"dup":40,"validate.io-function":882,"validate.io-number-primitive":885,"validate.io-string-primitive":887}],851:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":534,"dup":41}],852:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],853:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":822,"compute-array-dtype/lib/dtypes":854,"dup":43,"type-name":855,"validate.io-array-like":877}],854:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],855:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],856:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":858,"dup":46,"type-name":859}],857:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],858:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":857,"dup":48}],859:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],860:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":880,"validate.io-nan-primitive":861}],861:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],862:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":885}],863:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":880,"validate.io-nonnegative-integer":864}],864:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":865}],865:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":866}],866:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],867:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],868:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],869:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":867,"./defaults.js":868,"./validate.js":871,"dup":60,"validate.io-array":880,"validate.io-string-primitive":887}],870:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":867,"./defaults.js":868,"./factory.js":869,"./validate.js":871,"dup":61,"validate.io-array":880,"validate.io-string-primitive":887}],871:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":886,"validate.io-string-primitive":887}],872:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],873:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],874:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":872,"./defaults.js":873,"./validate.js":876,"dup":66,"validate.io-array":880,"validate.io-string-primitive":887}],875:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":872,"./defaults.js":873,"./factory.js":874,"./validate.js":876,"dup":67,"validate.io-array":880,"validate.io-string-primitive":887}],876:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":881,"validate.io-object":886,"validate.io-string-primitive":887}],877:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":878,"dup":70,"validate.io-integer-primitive":879}],878:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],879:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":885}],880:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],881:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],882:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],883:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],884:[function(require,module,exports){
arguments[4][76][0].apply(exports,arguments)
},{"dup":76}],885:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],886:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":880}],887:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],888:[function(require,module,exports){
/**
*
*	COMPUTE: skewness
*
*
*	DESCRIPTION:
*		- Computes the sample skewness of an array of values.
*
*
*	NOTES:
*		[1] The formula for corrected sample skewness comes from: Jones and Gill (1998). Comparing measures of sample skewness and kurtosis. The Statistician. DOI: 10.1111/1467-9884.00122
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

(function() {
	'use strict';

	/**
	* FUNCTION: skewness( arr )
	*	Computes the sample skewness of an array of values.
	*
	* @param {Array} arr - array of values
	* @returns {Number} sample skewness
	*/
	function skewness( arr ) {
		if ( !Array.isArray( arr ) ) {
			throw new TypeError( 'skewness()::invalid input argument. Must provide an array.' );
		}
		var len = arr.length,
			delta = 0,
			delta_n = 0,
			term1 = 0,
			N = 0,
			mean = 0,
			M2 = 0,
			M3 = 0,
			g;

		for ( var i = 0; i < len; i++ ) {
			N += 1;

			delta = arr[ i ] - mean;
			delta_n = delta / N;

			term1 = delta * delta_n * (N-1);

			M3 += term1*delta_n*(N-2) - 3*delta_n*M2;
			M2 += term1;
			mean += delta_n;
		}
		// Calculate the population skewness:
		g = Math.sqrt( N )*M3 / Math.pow( M2, 3/2 );
		// Return the corrected sample skewness:
		return Math.sqrt( N*N-1)*g / (N-2);
	} // end FUNCTION skewness()


	// EXPORTS //

	module.exports = skewness;

})();
},{}],889:[function(require,module,exports){
'use strict';

// MODULES //

var SQRT = require( './number.js' );


// SQUARE ROOT //

/**
* FUNCTION: sqrt( out, arr, accessor )
*	Computes an element-wise principal square root using an accessor function.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function sqrt( out, arr, clbk ) {
	var len = arr.length,
		v, i;
	for ( i = 0; i < len; i++ ) {
		v = clbk( arr[ i ], i );
		if ( typeof v === 'number' ) {
			out[ i ] = SQRT( v );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION sqrt()


// EXPORTS //

module.exports = sqrt;

},{"./number.js":894}],890:[function(require,module,exports){
'use strict';

// MODULES //

var SQRT = require( './number.js' );


// SQUARE ROOT //

/**
* FUNCTION: sqrt( out, arr )
*	Computes an element-wise principal square root.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function sqrt( out, arr ) {
	var len = arr.length,
		i;
	for ( i = 0; i < len; i++ ) {
		if ( typeof arr[ i ] === 'number' ) {
			out[ i ] = SQRT( arr[ i ] );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION sqrt()


// EXPORTS //

module.exports = sqrt;

},{"./number.js":894}],891:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory,
	SQRT = require( './number.js' );


// SQUARE ROOT //

/**
* FUNCTION: sqrt( arr, path[, sep] )
*	Computes an element-wise principal square root and deep sets the input array.
*
* @param {Array} arr - input array
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function sqrt( x, path, sep ) {
	var len = x.length,
		opts = {},
		dget,
		dset,
		v, i;
	if ( arguments.length > 2 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		for ( i = 0; i < len; i++ ) {
			v = dget( x[ i ] );
			if ( typeof v === 'number' ) {
				dset( x[i], SQRT( v ) );
			} else {
				dset( x[i], NaN );
			}
		}
	}
	return x;
} // end FUNCTION sqrt()


// EXPORTS //

module.exports = sqrt;

},{"./number.js":894,"utils-deep-get":947,"utils-deep-set":953}],892:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isnan = require( 'validate.io-nan' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var sqrt1 = require( './number.js' ),
	sqrt2 = require( './array.js' ),
	sqrt3 = require( './accessor.js' ),
	sqrt4 = require( './deepset.js' ),
	sqrt5 = require( './matrix.js' ),
	sqrt6 = require( './typedarray.js' );


// SQUARE ROOT //

/**
* FUNCTION: sqrt( x[, opts] )
*	Computes an element-wise principal square root.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} x - input value
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} square root(s)
*/
function sqrt( x, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( isNumber( x ) || isnan( x ) ) {
		return sqrt1( x );
	}
	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isMatrixLike( x ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'sqrt()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( x.length );
			out = matrix( d, x.shape, dt );
		} else {
			out = x;
		}
		return sqrt5( out, x );
	}
	if ( isTypedArrayLike( x ) ) {
		if ( opts.copy === false ) {
			out = x;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'sqrt()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( x.length );
		}
		return sqrt6( out, x );
	}
	if ( isArrayLike( x ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return sqrt4( x, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = x;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'sqrt()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( x.length );
		}
		else {
			out = new Array( x.length );
		}
		if ( opts.accessor ) {
			return sqrt3( out, x, opts.accessor );
		}
		return sqrt2( out, x );
	}
	return NaN;
} // end FUNCTION sqrt()


// EXPORTS //

module.exports = sqrt;

},{"./accessor.js":889,"./array.js":890,"./deepset.js":891,"./matrix.js":893,"./number.js":894,"./typedarray.js":895,"./validate.js":896,"compute-array-constructors":898,"dstructs-matrix":907,"validate.io-array-like":956,"validate.io-matrix-like":961,"validate.io-nan":962,"validate.io-number-primitive":963,"validate.io-typed-array-like":967}],893:[function(require,module,exports){
'use strict';

// MODULES //

var SQRT = require( './number.js' );


// SQUARE ROOT //

/**
* FUNCTION: sqrt( out, x )
*	Computes an element-wise principal square root.
*
* @param {Matrix} out - output matrix
* @param {Matrix} x - input matrix
* @returns {Matrix} output matrix
*/
function sqrt( out, x ) {
	var len = x.length,
		i;
	if ( out.length !== len ) {
		throw new Error( 'sqrt()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	for ( i = 0; i < len; i++ ) {
		out.data[ i ] = SQRT( x.data[ i ] );
	}
	return out;
} // end FUNCTION sqrt()


// EXPORTS //

module.exports = sqrt;

},{"./number.js":894}],894:[function(require,module,exports){
'use strict';

// EXPORTS //

module.exports = Math.sqrt;

},{}],895:[function(require,module,exports){
'use strict';

// MODULES //

var SQRT = require( './number.js' );


// SQUARE ROOT //

/**
* FUNCTION: sqrt( out, arr )
*	Computes an element-wise principal square root.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} arr - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function sqrt( out, arr ) {
	var len = arr.length,
		i;
	for ( i = 0; i < len; i++ ) {
		out[ i ] = SQRT( arr[ i ] );
	}
	return out;
} // end FUNCTION sqrt()


// EXPORTS //

module.exports = sqrt;

},{"./number.js":894}],896:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Boolean} [options.copy] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'sqrt()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'sqrt()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'sqrt()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'sqrt()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'sqrt()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'sqrt()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":959,"validate.io-function":960,"validate.io-object":964,"validate.io-string-primitive":966}],897:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],898:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":897,"dup":12}],899:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],900:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":903,"./iget.js":905,"./iset.js":908,"./mget.js":912,"./mset.js":914,"./set.js":922,"./sget.js":924,"./sset.js":926,"./toString.js":928,"dup":14}],901:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":904,"./iget.raw.js":906,"./iset.raw.js":909,"./mget.raw.js":913,"./mset.raw.js":915,"./set.raw.js":923,"./sget.raw.js":925,"./sset.raw.js":927,"./toString.js":928,"dup":15}],902:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],903:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":941}],904:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],905:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":939}],906:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],907:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":910,"./matrix.raw.js":911,"dup":21}],908:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":939,"validate.io-number-primitive":963}],909:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],910:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":899,"./ctor.js":900,"./dtypes.js":902,"compute-cast-arrays":929,"compute-dtype":932,"dup":24,"validate.io-array":936,"validate.io-contains":937,"validate.io-nonnegative-integer-array":940,"validate.io-string-primitive":966}],911:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":899,"./ctor.raw.js":901,"./dtypes.js":902,"compute-dtype":932,"dup":25,"validate.io-contains":937,"validate.io-string-primitive":966}],912:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":899,"dup":26,"validate.io-nonnegative-integer-array":940}],913:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":899,"dup":27}],914:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":916,"./mset2.js":917,"./mset3.js":918,"./mset4.js":919,"./mset5.js":920,"./mset6.js":921,"dup":28,"validate.io-function":960,"validate.io-nonnegative-integer-array":940,"validate.io-number-primitive":963}],915:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":916,"./mset2.js":917,"./mset3.js":918,"./mset4.js":919,"./mset5.js":920,"./mset6.js":921,"dup":29}],916:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],917:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],918:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],919:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],920:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],921:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],922:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":941,"validate.io-number-primitive":963}],923:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],924:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":899,"compute-indexspace":534,"dup":38,"validate.io-string-primitive":966}],925:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":899,"compute-indexspace":534,"dup":39}],926:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":534,"dup":40,"validate.io-function":960,"validate.io-number-primitive":963,"validate.io-string-primitive":966}],927:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":534,"dup":41}],928:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],929:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":897,"compute-array-dtype/lib/dtypes":930,"dup":43,"type-name":931,"validate.io-array-like":956}],930:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],931:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],932:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":934,"dup":46,"type-name":935}],933:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],934:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":933,"dup":48}],935:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],936:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],937:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":936,"validate.io-nan-primitive":938}],938:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],939:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":963}],940:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":936,"validate.io-nonnegative-integer":941}],941:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":942}],942:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":943}],943:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],944:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],945:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],946:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":944,"./defaults.js":945,"./validate.js":948,"dup":60,"validate.io-array":949,"validate.io-string-primitive":966}],947:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":944,"./defaults.js":945,"./factory.js":946,"./validate.js":948,"dup":61,"validate.io-array":949,"validate.io-string-primitive":966}],948:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":964,"validate.io-string-primitive":966}],949:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],950:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],951:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],952:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":950,"./defaults.js":951,"./validate.js":954,"dup":66,"validate.io-array":955,"validate.io-string-primitive":966}],953:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":950,"./defaults.js":951,"./factory.js":952,"./validate.js":954,"dup":67,"validate.io-array":955,"validate.io-string-primitive":966}],954:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":959,"validate.io-object":964,"validate.io-string-primitive":966}],955:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],956:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":957,"dup":70,"validate.io-integer-primitive":958}],957:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],958:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":963}],959:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],960:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],961:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],962:[function(require,module,exports){
arguments[4][76][0].apply(exports,arguments)
},{"dup":76}],963:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],964:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":965}],965:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],966:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],967:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":968,"dup":81,"validate.io-integer-primitive":969}],968:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],969:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":963}],970:[function(require,module,exports){
/**
*
*	COMPUTE: stdev
*
*
*	DESCRIPTION:
*		- Computes the sample standard deviation over an array of values.
*
*
*	NOTES:
*		[1] 
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

(function() {
	'use strict';

	/**
	* FUNCTION: stdev( arr )
	*	Computes the sample standard deviation over an array of values.
	*
	* @param {Array} arr - array of values
	* @returns {Number} sample standard deviation
	*/
	function stdev( arr ) {
		if ( !Array.isArray( arr ) ) {
			throw new TypeError( 'stdev()::invalid input argument. Must provide an array.' );
		}
		var len = arr.length,
			N = 0,
			mean = 0,
			M2 = 0,
			delta = 0;

		if ( len < 2 ) {
			return 0;
		}
		for ( var i = 0; i < len; i++ ) {
			N += 1;
			delta = arr[ i ] - mean;
			mean += delta / N;
			M2 += delta * ( arr[i] - mean );
		}
		return Math.sqrt( M2 / ( N-1 ) );
	} // end FUNCTION stdev()


	// EXPORTS //

	module.exports = stdev;

})();
},{}],971:[function(require,module,exports){
'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isObject = require( 'validate.io-object' ),
	isFunction = require( 'validate.io-function' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isNumber = require( 'validate.io-number-primitive' );


// SUBTRACT //

/**
* FUNCTION: subtract( arr, x[, opts] )
*	Computes an element-wise subtraction.
*
* @param {Number[]|Array} arr - input array
* @param {Number[]|Array|Number} x - either an array of equal length or a scalar
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating whether to return a new array
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @returns {Number[]} output array
*/
function subtract( arr, x, opts ) {
	var isArr = isArray( x ),
		copy = true,
		arity,
		clbk,
		out,
		len,
		i;

	if ( !isArray( arr ) ) {
		throw new TypeError( 'subtract()::invalid input argument. Must provide an array. Value: `' + arr + '`.' );
	}
	if ( !isArr && !isNumber( x ) ) {
		throw new TypeError( 'subtract()::invalid input argument. Second argument must either be an array or number primitive. Value: `' + x + '`.' );
	}
	if ( arguments.length > 2 ) {
		if ( !isObject( opts ) ) {
			throw new TypeError( 'subtract()::invalid input argument. Options argument must be an object. Value: `' + opts + '`.' );
		}
		if ( opts.hasOwnProperty( 'copy' ) ) {
			copy = opts.copy;
			if ( !isBoolean( copy ) ) {
				throw new TypeError( 'subtract()::invalid option. Copy option must be a boolean primitive. Option: `' + copy + '`.' );
			}
		}
		if ( opts.hasOwnProperty( 'accessor' ) ) {
			clbk = opts.accessor;
			if ( !isFunction( clbk ) ) {
				throw new TypeError( 'subtract()::invalid option. Accessor must be a function. Option: `' + clbk + '`.' );
			}
			arity = clbk.length;
		}
	}
	len = arr.length;
	if ( copy ) {
		out = new Array( len );
	} else {
		out = arr;
	}
	// Case 1: x is an array
	if ( isArr ) {
		if ( len !== x.length ) {
			throw new Error( 'subtract()::invalid input argument. Array to be added must have a length equal to that of the input array.' );
		}
		if ( arity === 3 ) { // clbk implied
			for ( i = 0; i < len; i++ ) {
				out[ i ] = clbk( arr[i], i, 0 ) - clbk( x[i], i, 1 );
			}
		}
		else if ( clbk ) {
			for ( i = 0; i < len; i++ ) {
				out[ i ] = clbk( arr[i], i ) - x[ i ];
			}
		}
		else {
			for ( i = 0; i < len; i++ ) {
				out[ i ] = arr[ i ] - x[ i ];
			}
		}
	}
	// Case 2: accessor and scalar
	else if ( clbk ) {
		for ( i = 0; i < len; i++ ) {
			out[ i ] = clbk( arr[i], i ) - x;
		}
	}
	// Case 3: scalar
	else {
		for ( i = 0; i < len; i++ ) {
			out[ i ] = arr[ i ] - x;
		}
	}
	return out;
} // end FUNCTION subtract()


// EXPORTS //

module.exports = subtract;

},{"validate.io-array":972,"validate.io-boolean-primitive":973,"validate.io-function":974,"validate.io-number-primitive":975,"validate.io-object":976}],972:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],973:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],974:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],975:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],976:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":972}],977:[function(require,module,exports){
'use strict';

/**
* FUNCTION: sum( arr, clbk )
*	Computes a sum using an accessor.
*
* @param {Array} arr - input array
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number} sum
*/
function sum( arr, clbk ) {
	var len = arr.length,
		s = 0,
		i;
	for ( i = 0; i < len; i++ ) {
		s += clbk( arr[ i ], i );
	}
	return s;
} // end FUNCTION sum()


// EXPORTS //

module.exports = sum;

},{}],978:[function(require,module,exports){
'use strict';

/**
* FUNCTION: sum( arr )
*	Computes the sum.
*
* @param {Array} arr - input array
* @returns {Number} sum
*/
function sum( arr ) {
	var len = arr.length,
		s = 0,
		i;
	for ( i = 0; i < len; i++ ) {
		s += arr[ i ];
	}
	return s;
} // end FUNCTION sum()


// EXPORTS //

module.exports = sum;

},{}],979:[function(require,module,exports){
'use strict';

// MODULES //

var isArrayLike = require( 'validate.io-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ).raw,
	validate = require( './validate.js' );


// FUNCTIONS //

var sum1 = require( './array.js' ),
	sum2 = require( './accessor.js' ),
	sum3 = require( './matrix.js' );


// SUM //

/*
* FUNCTION: sum( x[, options] )
*	Computes the sum of elements in x.
*
* @param {Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} x - input value
* @param {Object} [options] - function options
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {Number} [options.dim=2] - dimension along which to compute the sum
* @param {String} [options.dtype="float64"] - output data type
* @returns {Number|Matrix} sum(s)
*/
function sum( x, options ) {
	/* jshint newcap:false */
	var opts = {},
		shape,
		ctor,
		err,
		len,
		dim,
		dt,
		d,
		m;

	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isMatrixLike( x ) ) {
		dt = opts.dtype || 'float64';
		dim = opts.dim;

		// Determine if provided a vector...
		if ( x.shape[ 0 ] === 1 || x.shape[ 1 ] === 1 ) {
			// Treat as an array-like object:
			return sum1( x.data );
		}
		if ( dim > 2 ) {
			throw new RangeError( 'sum()::invalid option. Dimension option exceeds number of matrix dimensions. Option: `' + dim + '`.' );
		}
		if ( dim === void 0 || dim === 2 ) {
			len = x.shape[ 0 ];
			shape = [ len, 1 ];
		} else {
			len = x.shape[ 1 ];
			shape = [ 1, len ];
		}
		ctor = ctors( dt );
		if ( ctor === null ) {
			throw new Error( 'sum()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
		}
		// Create an output matrix and calculate the sums:
		d = new ctor( len );
		m = matrix( d, shape, dt );
		return sum3( m, x, dim );
	}
	if ( isArrayLike( x ) ) {
		if ( opts.accessor ) {
			return sum2( x, opts.accessor );
		}
		return sum1( x );
	}
	throw new TypeError( 'sum()::invalid input argument. First argument must be either an array or a matrix. Value: `' + x + '`.' );
} // end FUNCTION sum()


// EXPORTS //

module.exports = sum;

},{"./accessor.js":977,"./array.js":978,"./matrix.js":980,"./validate.js":981,"compute-array-constructors":983,"dstructs-matrix":992,"validate.io-array-like":1030,"validate.io-matrix-like":1035}],980:[function(require,module,exports){
'use strict';

/**
* FUNCTION: sum( out, mat[, dim] )
*	Computes the sum along a matrix dimension.
*
* @param {Matrix} out - output matrix
* @param {Matrix} mat - input matrix
* @param {Number} [dim=2] - matrix dimension along which to compute the sum. If `dim=1`, compute along matrix rows. If `dim=2`, compute along matrix columns.
* @returns {Matrix|Number} sums or 0
*/
function sum( out, mat, dim ) {
	var s,
		M, N,
		s0, s1,
		o,
		i, j, k;

	if ( dim === 1 ) {
		// Compute along the rows...
		M = mat.shape[ 1 ];
		N = mat.shape[ 0 ];
		s0 = mat.strides[ 1 ];
		s1 = mat.strides[ 0 ];
	} else {
		// Compute along the columns...
		M = mat.shape[ 0 ];
		N = mat.shape[ 1 ];
		s0 = mat.strides[ 0 ];
		s1 = mat.strides[ 1 ];
	}
	if ( M === 0 || N === 0 ) {
		return 0;
	}
	o = mat.offset;
	for ( i = 0; i < M; i++ ) {
		k = o + i*s0;
		s = 0;
		for ( j = 0; j < N; j++ ) {
			s += mat.data[ k + j*s1 ];
		}
		out.data[ i ] = s;
	}
	return out;
} // end FUNCTION sum()


// EXPORTS //

module.exports = sum;

},{}],981:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' ),
	isPositiveInteger = require( 'validate.io-positive-integer' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {Number} [options.dim] - dimension
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'sum()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'sum()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dim' ) ) {
		opts.dim = options.dim;
		if ( !isPositiveInteger( opts.dim ) ) {
			return new TypeError( 'sum()::invalid option. Dimension option must be a positive integer. Option: `' + opts.dim + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'sum()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-function":1034,"validate.io-object":1036,"validate.io-positive-integer":1038,"validate.io-string-primitive":1041}],982:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],983:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":982,"dup":12}],984:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],985:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":988,"./iget.js":990,"./iset.js":993,"./mget.js":997,"./mset.js":999,"./set.js":1007,"./sget.js":1009,"./sset.js":1011,"./toString.js":1013,"dup":14}],986:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":989,"./iget.raw.js":991,"./iset.raw.js":994,"./mget.raw.js":998,"./mset.raw.js":1000,"./set.raw.js":1008,"./sget.raw.js":1010,"./sset.raw.js":1012,"./toString.js":1013,"dup":15}],987:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],988:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":1026}],989:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],990:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":1024}],991:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],992:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":995,"./matrix.raw.js":996,"dup":21}],993:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":1024,"validate.io-number-primitive":1029}],994:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],995:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":984,"./ctor.js":985,"./dtypes.js":987,"compute-cast-arrays":1014,"compute-dtype":1017,"dup":24,"validate.io-array":1021,"validate.io-contains":1022,"validate.io-nonnegative-integer-array":1025,"validate.io-string-primitive":1041}],996:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":984,"./ctor.raw.js":986,"./dtypes.js":987,"compute-dtype":1017,"dup":25,"validate.io-contains":1022,"validate.io-string-primitive":1041}],997:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":984,"dup":26,"validate.io-nonnegative-integer-array":1025}],998:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":984,"dup":27}],999:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":1001,"./mset2.js":1002,"./mset3.js":1003,"./mset4.js":1004,"./mset5.js":1005,"./mset6.js":1006,"dup":28,"validate.io-function":1034,"validate.io-nonnegative-integer-array":1025,"validate.io-number-primitive":1029}],1000:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":1001,"./mset2.js":1002,"./mset3.js":1003,"./mset4.js":1004,"./mset5.js":1005,"./mset6.js":1006,"dup":29}],1001:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],1002:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],1003:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],1004:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],1005:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],1006:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],1007:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":1026,"validate.io-number-primitive":1029}],1008:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],1009:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":984,"compute-indexspace":534,"dup":38,"validate.io-string-primitive":1041}],1010:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":984,"compute-indexspace":534,"dup":39}],1011:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":534,"dup":40,"validate.io-function":1034,"validate.io-number-primitive":1029,"validate.io-string-primitive":1041}],1012:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":534,"dup":41}],1013:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],1014:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":982,"compute-array-dtype/lib/dtypes":1015,"dup":43,"type-name":1016,"validate.io-array-like":1030}],1015:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1016:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1017:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":1019,"dup":46,"type-name":1020}],1018:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1019:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":1018,"dup":48}],1020:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1021:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1022:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":1021,"validate.io-nan-primitive":1023}],1023:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],1024:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1029}],1025:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":1021,"validate.io-nonnegative-integer":1026}],1026:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":1027}],1027:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":1028}],1028:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],1029:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],1030:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":1031,"dup":70,"validate.io-integer-primitive":1032}],1031:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],1032:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1033}],1033:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],1034:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],1035:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],1036:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":1037}],1037:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1038:[function(require,module,exports){
arguments[4][471][0].apply(exports,arguments)
},{"dup":471,"validate.io-integer":1039}],1039:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":1040}],1040:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],1041:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],1042:[function(require,module,exports){
/**
*
*	COMPUTE: trimean
*
*
*	DESCRIPTION:
*		- Computes the trimean of a numeric array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Rebekah Smith.
*
*
*	AUTHOR:
*		Rebekah Smith. rebekahjs17@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	median = require( 'compute-median' ),
	quantile = require( 'compute-quantile' );


// FUNCTIONS //

/**
* FUNCTION: ascending( a, b )
*	Function used to sort values in ascending order.
*
* @private
* @param {Number} a
* @param {Number} b
* @returns {Number} difference between `a` and `b`
*/
function ascending( a, b ) {
	return a - b;
} // end FUNCTION ascending()


// TRIMEAN //

/**
* FUNCTION: trimean( arr[, opts] )
*	Computes the trimean of a numeric array.
*
* @param {Array} arr - numeric array
* @param {Object} [opts] - quantile function options
* @returns {Number} trimean
*/
function trimean( arr, opts ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'trimean()::invalid input argument. Must provide an array.' );
	}
	if ( arguments.length > 1 ) {
		if ( !isObject( opts ) ) {
			throw new TypeError( 'trimean()::invalid input argument. Options should be an object.' );
		}
	} else {
		opts = {
			'sorted': false
		};
	}
	if ( !opts.sorted ) {
		arr = arr.slice();
		arr.sort( ascending );
		opts.sorted = true;
	}
	var q1 = quantile( arr, 0.25, opts ),
		q2 = median( arr, true ),
		q3 = quantile( arr, 0.75, opts );

	return ( q1 + 2*q2 + q3 ) / 4.0;
} // end FUNCTION trimean()


// EXPORTS //
module.exports = trimean;

},{"compute-median":1043,"compute-quantile":797,"validate.io-object":1044}],1043:[function(require,module,exports){
/**
*
*	COMPUTE: median
*
*
*	DESCRIPTION:
*		- Computes the median of a numeric array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// FUNCTIONS //

/**
* FUNCTION: ascending( a, b )
*	Comparator function used to sort values in ascending order.
*
* @private
* @param {Number} a
* @param {Number} b
* @returns {Number} difference between `a` and `b`
*/
function ascending( a, b ) {
	return a - b;
} // end FUNCTION ascending()


// MEDIAN //

/**
* FUNCTION: median( arr[, sorted] )
*	Computes the median of a numeric array.
*
* @param {Array} arr - numeric array
* @param {Boolean} [sorted] - boolean flag indicating if the array is sorted in ascending order
* @returns {Number} median value
*/
function median( arr, sorted ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'median()::invalid input argument. Must provide an array.' );
	}
	if ( arguments.length > 1 && typeof sorted !== 'boolean' ) {
		throw new TypeError( 'median()::invalid input argument. Second argument must be a boolean.' );
	}
	var len = arr.length,
		id;

	if ( !sorted ) {
		arr = arr.slice();
		arr.sort( ascending );
	}

	// Get the middle index:
	id = Math.floor( len / 2 );

	if ( len % 2 ) {
		// The number of elements is not evenly divisible by two, hence we have a middle index:
		return arr[ id ];
	}
	// Even number of elements, so must take the mean of the two middle values:
	return ( arr[ id-1 ] + arr[ id ] ) / 2.0;
} // end FUNCTION median()


// EXPORTS //

module.exports = median;

},{}],1044:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":1045}],1045:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1046:[function(require,module,exports){
/**
*
*	COMPUTE: truncmean
*
*
*	DESCRIPTION:
*		- Computes the truncated mean of an array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2015. Philipp Burckhardt.
*
*
*	AUTHOR:
*		Philipp Burckhardt. pburckhardt@outlook.com. 2015.
*
*/

'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
    isNumber = require( 'validate.io-number-primitive' ),
    isBoolean = require( 'validate.io-boolean-primitive' ),
    isObject = require( 'validate.io-object' ),
    isFunction = require( 'validate.io-function' ),
    isNonNegativeInteger = require( 'validate.io-nonnegative-integer' );


// FUNCTIONS //

/**
* FUNCTION: ascending( a, b )
*	Comparator function used to sort values in ascending order.
*
* @private
* @param {Number} a
* @param {Number} b
* @returns {Number} difference between `a` and `b`
*/
function ascending( a, b ) {
	return a - b;
} // end FUNCTION ascending()


// TRUNCATED MEAN //

/**
* FUNCTION: truncmean( arr, discard[, options] )
*	Computes the truncated mean of an array.
*
* @param {Array} arr - input array
* @param {Number} discard - either a percentage expressed on the interval [0,0.5] or a non-negative integer.
* @param {Object} [options] - function options
* @param {Boolean} [options.sorted=false] - boolean indicating if the input array is sorted in ascending order
* @param {Function} [options.accessor] - accessor function for accessing numeric array values
* @param {Boolean} [options.interpolate=false] - boolean indicating whether to interpolate between truncated means when a discard percentage does not yield an integer number of array values to discard
* @returns {Number|null} truncated mean
*/
function truncmean( arr, discard, options ) {
	var sorted,
		clbk,
		interp,
		delta,
		diff,
		len,
		mu,
		lo,
		hi,
		N,
		tmp,
		w1, w2,
		d, i;

	if ( !isArray( arr ) ) {
		throw new TypeError( 'truncmean()::invalid input argument. Must provide an array. Value: `' + arr + '`.' );
	}
	len = arr.length;
	if ( isNumber( discard ) ) {
		if ( !isNonNegativeInteger( discard ) ) {
			if ( discard < 0 || discard > 0.5 ) {
				throw new RangeError( 'truncmean()::invalid input argument. Discard must be either a number on the interval [0,0.5] or a nonnegative integer. Value: `' + discard + '`.' );
			}
		}
		else if ( discard > len/2 ) {
			throw new RangeError( 'truncmean()::invalid input argument. Number of discard values cannot exceed half the array length. Value: `' + discard + '`.' );
		}
	} else {
		throw new TypeError( 'truncmean()::invalid input argument. Discard must be either a number on the interval [0,0.5] or a nonnegative integer. Value: `' + discard + '`.' );
	}
	if ( arguments.length > 2  ) {
		if ( !isObject( options ) ) {
			throw new TypeError( 'truncmean()::invalid input argument. Options must be an object. Value: `' + options + '`.' );
		}
		if ( options.hasOwnProperty( 'sorted' ) ) {
			sorted = options.sorted;
			if ( !isBoolean( sorted ) ) {
				throw new TypeError( 'truncmean()::invalid option. Sorted option must be a boolean. Value: `' + sorted + '`.' );
			}
		}
		if ( options.hasOwnProperty( 'accessor' ) ) {
			clbk = options.accessor;
			if ( !isFunction( clbk ) ) {
				throw new TypeError( 'truncmean()::invalid option. Accessor must be a function. Value: `' + clbk + '`.' );
			}
		}
		if ( options.hasOwnProperty( 'interpolate' ) ) {
			interp = options.interpolate;
			if ( !isBoolean( interp ) ) {
				throw new TypeError( 'truncmean()::invalid option. Interpolate option must be a boolean. Value: `' + interp + '`.' );
			}
		}
	}
	if ( !len ) {
		return null;
	}
	if ( !clbk && sorted ) {
		d = arr;
	}
	else if ( !clbk ) {
		d = [];
		for ( i = 0; i < len; i++ ) {
			d.push( arr[ i ] );
		}
	} else {
		d = [];
		for ( i = 0; i < len; i++ ) {
			d.push( clbk( arr[ i ] ) );
		}
	}
	if ( !sorted ) {
		d.sort( ascending );
	}
	// Case 1: no interpolation...
	if ( discard >= 1 || discard === 0 || discard === 0.5 || !interp ) {
		if ( discard < 1 && discard !== 0 ) {
			// Convert the discard percentage to a number of values:
			discard = Math.floor( len * discard );
		}
		lo = discard;
		hi = len - discard - 1;

		// Handle case where discard = 0.5 and array length is even...
		if ( hi < lo ) {
			tmp = lo;
			lo = hi;
			hi = tmp;
		}
		mu = 0;
		N = 0;
		for ( i = lo; i <= hi; i++ ) {
			N += 1;
			delta = d[ i ] - mu;
			mu += delta / N;
		}
		return mu;
	}
	// Case 2: interpolation...
	N = Math.floor( len * discard );

	// Compute the discard percentages used for interpolation...
	w1 = N / len;
	w2 = (N+1) / len;

	lo = N;
	hi = len - N - 1;

	// Compute the first mean...
	mu = 0;
	N = 0;
	for ( i = lo; i <= hi; i++ ) {
		N += 1;
		delta = d[ i ] - mu;
		mu += delta / N;
	}
	// If w2 exceeds 50%, then we are interpolating from the median to the median, which, regardless of weights, is just the median...
	if ( w2*2 >= 1 ) {
		return mu;
	}
	tmp = mu;

	// Convert the discard percentages to weights...
	diff = w2 - w1;
	i = w1;
	w1 = (w2-discard) / diff;
	w2 = (discard-i) / diff;

	// Compute the second mean by removing the lowest and highest previously included elements (see TODO)...
	N -= 1;
	delta = d[ lo ] - mu;
	mu -= delta / N;

	N -= 1;
	delta = d[ hi ] - mu;
	mu -= delta / N;

	// Compute the weighted average:
	return w1*tmp + w2*mu;
} // end FUNCTION truncmean()


// EXPORTS //

module.exports = truncmean;

},{"validate.io-array":1047,"validate.io-boolean-primitive":1048,"validate.io-function":1049,"validate.io-nonnegative-integer":1050,"validate.io-number-primitive":1053,"validate.io-object":1054}],1047:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1048:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],1049:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],1050:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":1051}],1051:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":1052}],1052:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],1053:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],1054:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":1047}],1055:[function(require,module,exports){
/**
*
*	COMPUTE: tversky-index
*
*
*	DESCRIPTION:
*   -	Computes the Tversky index between two sequences.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2015. Philipp Burckhardt.
*
*
*	AUTHOR:
*		Philipp Burckhardt. pburckhardt@outlook.com. 2015.
*
*/

'use strict';

// MODULES //

var intersect = require( 'intersect' ),
	isArray = require( 'validate.io-array' ),
	isString = require( 'validate.io-string' ),
	isBoolean = require( 'validate.io-boolean' ),
	isObject = require( 'validate.io-object' ),
	isNonNegative = require( 'validate.io-nonnegative' );


// FUNCTIONS //

/**
* FUNCTION: unique( arr )
*	Computes the unique elements of an array.
*
* @private
* @param {Array} arr - input array
* @returns {Array} array of unique elements
*/
function unique( arr ) {
	var len = arr.length,
		hash = {},
		vals = [],
		val;

	for ( var i = 0; i < len; i++ ) {
		val = arr[ i ];
		if ( !hash.hasOwnProperty( val ) ) {
			hash[ val ] = true;
			vals.push( val );
		}
	}
	return vals;
} // end FUNCTION unique()


// TVERSKY INDEX //

/**
* FUNCTION: tversky( a, b[, options] )
*   Computes the Tversky index between two sequences. For alpha = beta = 0.5, the index is equal to Dice's coefficient. For alpha = beta = 1, the index is equal to the Tanimoto coefficient.
*
* @param {String|Array} a - array or string sequence
* @param {String|Array} b - array or string sequence
* @param {Object} [options] - method options
* @param {Number} [options.alpha=1] - weight of prototype
* @param {Number} [options.beta=1] - weight of variant
* @param {Boolean} [options.symmetric=false] - boolean specifying whether the index should be symmetric
* @returns {Number} Tversky index
*/
function tversky( a, b, options ) {
	var symmetric = false,
		alpha = 1,
		beta = 1,
		aType,
		bType,
		opts,
		anb,
		len,
		aCompl,
		bCompl,
		hash,
		min,
		max,
		i;

	aType = isString( a );
	if ( !isArray( a ) && !aType ) {
		throw new TypeError( 'tversky()::invalid input argument. Sequence must be either an array or a string. Value: `' + a + '`.' );
	}
	bType = isString( b );
	if ( !isArray( b )  && !bType ) {
		throw new TypeError( 'tversky()::invalid input argument. Sequence must be either an array or a string. Value: `' + b + '`.' );
	}
	if ( aType !== bType ) {
		throw new Error( 'tversky()::invalid input arguments. Sequences must be the same type; i.e., both strings or both arrays.' );
	}
	if ( arguments.length > 2 ) {
		opts = options;
		if ( !isObject( opts ) ) {
			throw new TypeError( 'tversky()::invalid input argument. Options must be an object. Value: `' + opts + '`.' );
		}
		if ( opts.hasOwnProperty( 'alpha' ) ) {
			alpha = opts.alpha;
			if ( !isNonNegative( alpha ) ) {
				throw new TypeError( 'tversky()::invalid option. Alpha must be a nonnegative number. Value: `' + alpha + '`.' );
			}
		}
		if ( opts.hasOwnProperty( 'beta' ) ) {
			beta = opts.beta;
			if ( !isNonNegative( beta ) ) {
				throw new TypeError( 'tversky()::invalid option. Beta must be a nonnegative number. Value: `' + beta + '`.' );
			}
		}
		if ( opts.hasOwnProperty( 'symmetric' ) ) {
			symmetric = opts.symmetric;
			if ( !isBoolean( symmetric ) ) {
				throw new TypeError( 'tversky()::invalid option. Symmetric flag must be a boolean. Value: `' + symmetric + '`.' );
			}
		}
	}
	if ( aType ){
		a = a.split( '' );
		b = b.split( '' );
	}
	// Determine the unique elements for each array:
	a = unique( a );
	b = unique( b );

	// Compute the intersection between the unique sets:
	anb = intersect( a, b );
	len = anb.length;

	// Create a hash...
	hash = {};
	for ( i = 0; i < len; i++ ) {
		hash[ anb[i] ] = true;
	}
	// Compute the relative complements...
	aCompl = 0;
	for ( i = 0; i < a.length; i++ ) {
		if ( !hash.hasOwnProperty( a[i] ) ) {
			aCompl++;
		}
	}
	bCompl = 0;
	for ( i = 0; i < b.length; i++ ) {
		if ( !hash.hasOwnProperty( b[i] ) ) {
			bCompl++;
		}
	}
	if ( symmetric ) {
		if ( aCompl > bCompl ) {
			min = bCompl;
			max = aCompl;
		} else {
			min = aCompl;
			max = bCompl;
		}
		return len / ( len + beta*(alpha*min + max*(1-alpha))  );
	}
	return len / ( len + alpha*aCompl + beta*bCompl );
} // end FUNCTION tversky()


// EXPORTS //

module.exports = tversky;

},{"intersect":1056,"validate.io-array":1057,"validate.io-boolean":1058,"validate.io-nonnegative":1059,"validate.io-object":1061,"validate.io-string":1062}],1056:[function(require,module,exports){
module.exports = intersect;

function intersect (a, b) {
  var res = [];
  for (var i = 0; i < a.length; i++) {
    if (indexOf(b, a[i]) > -1) res.push(a[i]);
  }
  return res;
}

intersect.big = function(a, b) {
  var ret = [];
  var temp = {};
  
  for (var i = 0; i < b.length; i++) {
    temp[b[i]] = true;
  }
  for (var i = 0; i < a.length; i++) {
    if (temp[a[i]]) ret.push(a[i]);
  }
  
  return ret;
}

function indexOf(arr, el) {
  for (var i = 0; i < arr.length; i++) {
    if (arr[i] === el) return i;
  }
  return -1;
}

},{}],1057:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1058:[function(require,module,exports){
arguments[4][652][0].apply(exports,arguments)
},{"dup":652}],1059:[function(require,module,exports){
/**
*
*	VALIDATE: nonnegative
*
*
*	DESCRIPTION:
*		- Validates if a value is a nonnegative number.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2015. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2015.
*
*/

'use strict';

// MODULES //

var isNumber = require( 'validate.io-number' );


// IS NONNEGATIVE //

/**
* FUNCTION: isNonNegative( value )
*	Validates if a value is a nonnegative number.
*
* @param {*} value - value to be validated
* @returns {Boolean} boolean indicating if a value is a nonnegative number
*/
function isNonNegative( value ) {
	return isNumber( value ) && value >= 0;
} // end FUNCTION isNonNegative()


// EXPORTS //

module.exports = isNonNegative;

},{"validate.io-number":1060}],1060:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],1061:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":1057}],1062:[function(require,module,exports){
arguments[4][500][0].apply(exports,arguments)
},{"dup":500}],1063:[function(require,module,exports){
/**
*
*	COMPUTE: umidmean
*
*
*	DESCRIPTION:
*		- Computes the interquartile mean of the values above the median for a numeric array (upper midmean).
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Rebekah Smith.
*
*
*	AUTHOR:
*		Rebekah Smith. rebekahjs17@gmail.com. 2014.
*
*/

'use strict';

// FUNCTIONS //

/**
* FUNCTION: ascending( a, b )
*	Comparator function used to sort values in ascending order.
*
* @private
* @param {Number} a
* @param {Number} b
* @returns {Number} difference between `a` and `b`
*/
function ascending( a, b ) {
	return a - b;
} // end FUNCTION ascending()


// UPPER MIDMEAN //

/**
* FUNCTION: umidmean( arr[, sorted] )
*	Computes the interquartile mean of the values above the median in a numeric array (upper midmean).
*
* @param {Array} arr - numeric array
* @param {Boolean} [sorted] - boolean flag indicating if the input array is sorted in ascending order
* @returns {Number} upper midmean
*/
function umidmean( arr, sorted ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'umidmean()::invalid input argument. Must provide an array.' );
	}
	if ( arguments.length > 1 && typeof sorted !== 'boolean' ) {
		throw new TypeError( 'umidmean()::invalid input argument. Second argument must be a boolean.' );
	}
	if ( arr.length < 6 ) {
		throw new Error( 'umidmean()::invalid input argument. Input array must have 6 or more elements.' );
	}
	if ( !sorted ) {
		arr = arr.slice();
		arr.sort( ascending );
	}
	var len = arr.length,
		mean = 0,
		N = 0,
		delta,
		low,
		high;

	// Quartiles sit between values
	if ( len%8 === 0 ) {
		low = len*0.625;
		high = len*0.875 - 1;
	}
	else {
		low = Math.ceil( len*0.625 );
		high = Math.floor( len*0.875 ) - 1;
	}

	// Compute an arithmetic mean...
	for ( var i = low; i <= high; i++ ) {
		N += 1;
		delta = arr[ i ] - mean;
		mean += delta / N;
	}

	return mean;
} // end FUNCTION umidmean()


// EXPORTS //

module.exports = umidmean;

},{}],1064:[function(require,module,exports){
/**
*
*	COMPUTE: unique
*
*
*	DESCRIPTION:
*		- Removes duplicate values from a numeric array.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// FUNCTIONS //

/**
* FUNCTION: ascending( a, b )
*	Comparator function to sort values in ascending order.
*
* @private
* @param {Number} a
* @param {Number} b
* @returns {Number} difference between `a` and `b`
*/
function ascending( a, b ) {
	return a - b;
} // end FUNCTION ascending()


// UNIQUE //

/**
* FUNCTION: unique( arr, sorted )
*	Removes duplicate values from a numeric array. Note: the input array is mutated.
*
* @param {Array} arr - array to be deduped
* @param {Boolean} sorted - boolean flag indicating if the input array is sorted
*/
function unique( arr, sorted ) {
	if ( !Array.isArray( arr ) ) {
		throw new TypeError( 'unique()::invalid input argument. First argument must be an array.' );
	}
	if ( arguments.length > 1 && typeof sorted !== 'boolean' ) {
		throw new TypeError( 'unique()::invalid input argument. Second argument must be an array.' );
	}
	var len = arr.length,
		i, j,
		val;

	if ( !len ) {
		return;
	}
	if ( !sorted ) {
		arr.sort( ascending );
	}
	// Loop through the array, only incrementing a pointer when successive values are different. When a succeeding value is different, move the pointer and set the next value. In the trivial case where all array elements are unique, we incur a slight penalty in resetting the element value for each unique value. In other cases, we simply move a unique value to a new position in the array. The end result is a sorted array with unique values.
	for ( i = 1, j = 0; i < len; i++ ) {
		val = arr[ i ];
		if ( arr[ j ] !== val ) {
			j++;
			arr[ j ] = val;
		}
	}
	// Truncate the array:
	arr.length = j+1;
} // end FUNCTION unique()


// EXPORTS //

module.exports = unique;

},{}],1065:[function(require,module,exports){
/**
*
*	COMPUTE: unzip
*
*
*	DESCRIPTION:
*		- Unzips a zipped array (i.e., a nested array of tuples).
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014-2015. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isArray = require( 'validate.io-array' ),
	isNonNegativeInteger = require( 'validate.io-nonnegative-integer' ),
	isArrayArray = require( 'validate.io-array-array' );


// UNZIP //

/**
* FUNCTION: unzip( arr[, idx] )
*	Unzips a zipped array (i.e., a nested array of tuples).
*
* @param {Array} arr - zipped array
* @param {Array} [idx] - array of indices specifying which tuple elements to unzip
* @returns {Array} array of unzipped arrays
*/
function unzip( arr, idx ) {
	var len,
		out,
		tmp,
		numVals,
		i, j, k;

	if ( !isArrayArray( arr ) ) {
		throw new TypeError( 'unzip()::invalid input argument. Must provide a zipped array (an array of arrays). Value: `' + arr + '`.' );
	}
	// Assume that the first tuple is representative of all tuples...
	numVals = arr[ 0 ].length;
	if ( arguments.length > 1 ) {
		if ( !isArray( idx ) ) {
			throw new TypeError( 'unzip()::invalid input argument. Indices must be specified as an array.' );
		}
		for ( i = 0; i < idx.length; i++ ) {
			k = idx[ i ];
			if ( !isNonNegativeInteger( k ) ) {
				throw new TypeError( 'unzip()::invalid input argument. All indices must be nonnegative integers. Value: `' + k + '`.' );
			}
			if ( k > numVals ) {
				throw new RangeError( 'unzip()::invalid input argument. Must provide valid indices; i.e., an index must be on the interval [0,' + numVals + ']. Value: `' + k + '`.' );
			}
		}
		numVals = idx.length;
	} else {
		idx = new Array( numVals );
		for ( i = 0; i < numVals; i++ ) {
			idx[ i ] = i;
		}
	}
	out = new Array( numVals );
	len = arr.length;
	for ( j = 0; j < numVals; j++ ) {
		tmp = new Array( len );
		k = idx[ j ];
		for ( i = 0; i < len; i++ ) {
			tmp[ i ] = arr[ i ][ k ];
		}
		out[ j ] = tmp;
	}
	return out;
} // end FUNCTION unzip()


// EXPORTS //

module.exports = unzip;

},{"validate.io-array":1067,"validate.io-array-array":1066,"validate.io-nonnegative-integer":1068}],1066:[function(require,module,exports){
/**
*
*	VALIDATE: array-array
*
*
*	DESCRIPTION:
*		- Validates if a value is an array of arrays.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2015. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2015.
*
*/

'use strict';

// MODULES //

var isArray = require( 'validate.io-array' );


// IS ARRAY OF ARRAYS //

/**
* FUNCTION: isArrayArray( value )
*	Validates if a value is an array of arrays.
*
* @param {*} value - value to be validated
* @returns {Boolean} boolean indicating if a value is an array of arrays
*/
function isArrayArray( value ) {
	var len;
	if ( !isArray( value ) ) {
		return false;
	}
	len = value.length;
	if ( !len ) {
		return false;
	}
	for ( var i = 0; i < len; i++ ) {
		if ( !isArray( value[i] ) ) {
			return false;
		}
	}
	return true;
} // end FUNCTION isArrayArray()


// EXPORTS //

module.exports = isArrayArray;

},{"validate.io-array":1067}],1067:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1068:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":1069}],1069:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":1070}],1070:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],1071:[function(require,module,exports){
'use strict';

/**
* FUNCTION: variance( arr, clbk[, bias] )
*	Computes the variance of an array using an accessor.
*
* @param {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} arr - input array
* @param {Function} accessor - accessor function for accessing array values
* @param {Boolean} [bias=false] - boolean indicating whether to calculate a biased or unbiased estimate of the variance
* @returns {Number|Null} variance or null
*/
function variance( arr, clbk, bias ) {
	var len = arr.length,
		delta = 0,
		mean = 0,
		M2 = 0,
		x, i;

	if ( !len ) {
		return null;
	}
	if ( len < 2 ) {
		return 0;
	}
	for ( i = 0; i < len; i++ ) {
		x = clbk( arr[ i ], i );
		delta = x - mean;
		mean += delta / (i+1);
		M2 += delta * ( x - mean );
	}
	if ( bias ) {
		return M2 / ( i );
	}
	return M2 / ( i - 1 );
} // end FUNCTION variance()


// EXPORTS //

module.exports = variance;

},{}],1072:[function(require,module,exports){
'use strict';

/**
* FUNCTION: variance( arr[, bias] )
*	Computes the variance of an array.
*
* @param {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} arr - input array
* @param {Boolean} [bias=false] - boolean indicating whether to calculate a biased or unbiased estimate of the variance
* @returns {Number|Null} variance or null
*/
function variance( arr, bias ) {
	var len = arr.length,
		delta = 0,
		mean = 0,
		M2 = 0,
		x, i;

	if ( !len ) {
		return null;
	}
	if ( len < 2 ) {
		return 0;
	}
	for ( i = 0; i < len; i++ ) {
		x = arr[ i ];
		delta = x - mean;
		mean += delta / (i+1);
		M2 += delta * ( x - mean );
	}
	if ( bias ) {
		return M2 / ( i );
	}
	return M2 / ( i - 1 );
} // end FUNCTION variance()


// EXPORTS //

module.exports = variance;

},{}],1073:[function(require,module,exports){
'use strict';

// MODULES //

var isArrayLike = require( 'validate.io-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ).raw,
	validate = require( './validate.js' );


// FUNCTIONS //

var variance1 = require( './array.js' ),
	variance2 = require( './accessor.js' ),
	variance3 = require( './matrix.js' );


// VARIANCE //

/*
* FUNCTION: variance( x[, options] )
*	Computes the variance of elements in x.
*
* @param {Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} x - input value
* @param {Object} [opts] - function options
* @param {Boolean} [opts.bias=false] - - boolean indicating whether to calculate a biased or unbiased estimate of the variance
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {Number} [opts.dim=2] - dimension along which to compute the variance
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Matrix|Null} variance value(s) or null
*/
function variance( x, options ) {
	/* jshint newcap:false */
	var opts = {},
		shape,
		ctor,
		err,
		len,
		dim,
		dt,
		d,
		m;

	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isMatrixLike( x ) ) {
		dt = opts.dtype || 'float64';
		dim = opts.dim;

		// Determine if provided a vector...
		if ( x.shape[ 0 ] === 1 || x.shape[ 1 ] === 1 ) {
			// Treat as an array-like object:
			return variance1( x.data );
		}
		if ( dim > 2 ) {
			throw new RangeError( 'variance()::invalid option. Dimension option exceeds number of matrix dimensions. Option: `' + dim + '`.' );
		}
		if ( dim === void 0 || dim === 2 ) {
			len = x.shape[ 0 ];
			shape = [ len, 1 ];
		} else {
			len = x.shape[ 1 ];
			shape = [ 1, len ];
		}
		ctor = ctors( dt );
		if ( ctor === null ) {
			throw new Error( 'variance()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
		}
		// Create an output matrix and calculate the variance(s):
		d = new ctor( len );
		m = matrix( d, shape, dt );
		return variance3( m, x, opts.bias, dim );
	}
	if ( isArrayLike( x ) ) {
		if ( opts.accessor ) {
			return variance2( x, opts.accessor, opts.bias );
		}
		return variance1( x, opts.bias );
	}
	throw new TypeError( 'variance()::invalid input argument. First argument must be either an array or a matrix. Value: `' + x + '`.' );
} // end FUNCTION variance()


// EXPORTS //

module.exports = variance;

},{"./accessor.js":1071,"./array.js":1072,"./matrix.js":1074,"./validate.js":1075,"compute-array-constructors":1077,"dstructs-matrix":1086,"validate.io-array-like":1124,"validate.io-matrix-like":1130}],1074:[function(require,module,exports){
'use strict';

/**
* FUNCTION: variance( out, mat[, bias[, dim] ] )
*	Computes the variance along a matrix dimension.
*
* @param {Matrix} out - output matrix
* @param {Matrix} mat - input matrix
* @param {Boolean} [bias=false] - boolean indicating whether to calculate a biased or unbiased estimate of the variance
* @param {Number} [dim=2] - matrix dimension along which to compute the maximum. If `dim=1`, compute along matrix rows. If `dim=2`, compute along matrix columns.
* @returns {Matrix|Null} variance or null
*/
function variance( out, mat, bias, dim ) {
	var delta,
		M2,
		mu,
		x,
		M, N,
		s0, s1,
		o,
		i, j, k;

	if ( dim === 1 ) {
		// Compute along the rows...
		M = mat.shape[ 1 ];
		N = mat.shape[ 0 ];
		s0 = mat.strides[ 1 ];
		s1 = mat.strides[ 0 ];
	} else {
		// Compute along the columns...
		M = mat.shape[ 0 ];
		N = mat.shape[ 1 ];
		s0 = mat.strides[ 0 ];
		s1 = mat.strides[ 1 ];
	}
	if ( M === 0 || N === 0 ) {
		return null;
	}
	o = mat.offset;
	for ( i = 0; i < M; i++ ) {
		k = o + i*s0;
		M2 = 0;
		mu = 0;
		delta = 0;
		for ( j = 0; j < N; j++ ) {
			x = mat.data[ k + j*s1 ];
			delta = x - mu;
			mu += delta / (j+1);
			M2 += delta * ( x - mu );
		}
		if ( bias ) {
			out.data[ i ] = M2 / ( N );
		} else {
			out.data[ i ] = M2 / ( N - 1 );
		}
	}
	return out;
} // end FUNCTION variance()


// EXPORTS //

module.exports = variance;

},{}],1075:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' ),
	isBoolean = require( 'validate.io-boolean-primitive'),
	isPositiveInteger = require( 'validate.io-positive-integer' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {Number} [options.dim] - dimension
* @param {String} [options.dtype] - output data type
* @param {Boolean} [options.bias=false] - boolean indicating whether to calculate a biased or unbiased estimate of the variance
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'variance()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'variance()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dim' ) ) {
		opts.dim = options.dim;
		if ( !isPositiveInteger( opts.dim ) ) {
			return new TypeError( 'variance()::invalid option. Dimension option must be a positive integer. Option: `' + opts.dim + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'variance()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'bias' ) ) {
		opts.bias = options.bias;
		if ( !isBoolean( opts.bias ) ) {
			return new TypeError( 'variance()::invalid option. Bias option must be a boolean primitive. Option: `' + opts.dim + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":1128,"validate.io-function":1129,"validate.io-object":1131,"validate.io-positive-integer":1133,"validate.io-string-primitive":1136}],1076:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],1077:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":1076,"dup":12}],1078:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],1079:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":1082,"./iget.js":1084,"./iset.js":1087,"./mget.js":1091,"./mset.js":1093,"./set.js":1101,"./sget.js":1103,"./sset.js":1105,"./toString.js":1107,"dup":14}],1080:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":1083,"./iget.raw.js":1085,"./iset.raw.js":1088,"./mget.raw.js":1092,"./mset.raw.js":1094,"./set.raw.js":1102,"./sget.raw.js":1104,"./sset.raw.js":1106,"./toString.js":1107,"dup":15}],1081:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],1082:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":1120}],1083:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],1084:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":1118}],1085:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],1086:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":1089,"./matrix.raw.js":1090,"dup":21}],1087:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":1118,"validate.io-number-primitive":1123}],1088:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],1089:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":1078,"./ctor.js":1079,"./dtypes.js":1081,"compute-cast-arrays":1108,"compute-dtype":1111,"dup":24,"validate.io-array":1115,"validate.io-contains":1116,"validate.io-nonnegative-integer-array":1119,"validate.io-string-primitive":1136}],1090:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":1078,"./ctor.raw.js":1080,"./dtypes.js":1081,"compute-dtype":1111,"dup":25,"validate.io-contains":1116,"validate.io-string-primitive":1136}],1091:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":1078,"dup":26,"validate.io-nonnegative-integer-array":1119}],1092:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":1078,"dup":27}],1093:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":1095,"./mset2.js":1096,"./mset3.js":1097,"./mset4.js":1098,"./mset5.js":1099,"./mset6.js":1100,"dup":28,"validate.io-function":1129,"validate.io-nonnegative-integer-array":1119,"validate.io-number-primitive":1123}],1094:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":1095,"./mset2.js":1096,"./mset3.js":1097,"./mset4.js":1098,"./mset5.js":1099,"./mset6.js":1100,"dup":29}],1095:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],1096:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],1097:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],1098:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],1099:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],1100:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],1101:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":1120,"validate.io-number-primitive":1123}],1102:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],1103:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":1078,"compute-indexspace":534,"dup":38,"validate.io-string-primitive":1136}],1104:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":1078,"compute-indexspace":534,"dup":39}],1105:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":534,"dup":40,"validate.io-function":1129,"validate.io-number-primitive":1123,"validate.io-string-primitive":1136}],1106:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":534,"dup":41}],1107:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],1108:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":1076,"compute-array-dtype/lib/dtypes":1109,"dup":43,"type-name":1110,"validate.io-array-like":1124}],1109:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1110:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1111:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":1113,"dup":46,"type-name":1114}],1112:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1113:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":1112,"dup":48}],1114:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1115:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1116:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":1115,"validate.io-nan-primitive":1117}],1117:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],1118:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1123}],1119:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":1115,"validate.io-nonnegative-integer":1120}],1120:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":1121}],1121:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":1122}],1122:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],1123:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],1124:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":1125,"dup":70,"validate.io-integer-primitive":1126}],1125:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],1126:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1127}],1127:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],1128:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],1129:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],1130:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],1131:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":1132}],1132:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1133:[function(require,module,exports){
arguments[4][471][0].apply(exports,arguments)
},{"dup":471,"validate.io-integer":1134}],1134:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":1135}],1135:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],1136:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],1137:[function(require,module,exports){
/**
*
*	COMPUTE: wmean
*
*
*	DESCRIPTION:
*		- Computes a weighted mean over an array of values.
*
*
*	NOTES:
*		[1] 
*
*
*	TODO:
*		[1] 
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

(function() {
	'use strict';

	/**
	* FUNCTION: wmean( arr, weights )
	*	Computes a weighted mean over an array of values.
	*
	* @param {Array} arr - array of values
	* @param {Array} weights - array of weights
	* @returns {Number} weighted mean value
	*/
	function wmean( arr, weights ) {
		if ( !Array.isArray( arr ) || !Array.isArray( weights ) ) {
			throw new TypeError( 'wmean()::invalid input argument. Must provide arrays.' );
		}
		if ( arr.length !== weights.length ) {
			throw new Error( 'wmean()::invalid input arguments. Value array and weights array must be the same length.' );
		}
		var len = arr.length,
			sum = 0,
			mu = 0,
			w;

		// Normalize the weights to sum to 1 and calculate the weighted mean...
		for ( var i = 0; i < len; i++ ) {
			sum += weights[ i ];
		}
		for ( var j = 0; j < len; j++ ) {
			w = weights[ j ] / sum;
			mu += arr[ j ] * w;
		}
		return mu;
	} // end FUNCTION wmean()


	// EXPORTS //

	module.exports = wmean;

})();
},{}],1138:[function(require,module,exports){
/**
*
*	COMPUTE: zip
*
*
*	DESCRIPTION:
*		- Generates array tuples from input arrays.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014-2015. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2014.
*
*/

'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isArray = require( 'validate.io-array' ),
	isBoolean = require( 'validate.io-boolean-primitive' );


// ZIP //

/**
* FUNCTION: zip( arr1, arr2,...[, opts] )
*	Generates array tuples from input arrays.
*
* @param {...Array} arr - input arrays to be zipped
* @param {Object} [opts] - function options
* @param {Boolean} [opts.trunc=true] - boolean indicating whether to truncate arrays longer than the shortest input array
* @param {*} [opts.fill=null] - fill value used for arrays of unequal length
* @param {Boolean} [opts.arrays=false] - boolean indicating whether an input array should be interpreted as an array of arrays to be zipped
*/
function zip() {
	var args = Array.prototype.slice.call( arguments ),
		numArgs = args.length,
		opts = {},
		fill = null,
		arg,
		flg,
		len,
		out,
		arr,
		val,
		i, j;

	for ( i = 0; i < numArgs-1; i++ ) {
		if ( !isArray( args[i] ) ) {
			throw new TypeError( 'zip()::invalid input argument. Must provide array arguments. Value: `' + args[i] + '`.' );
		}
	}
	arg = args[ numArgs-1 ];
	flg = isObject( arg );
	if ( !flg && !isArray( arg ) ) {
		throw new TypeError( 'zip()::invalid input argument. Last argument must be either an array or an options object. Value: `' + arg + '`.' );
	}
	if ( flg ) {
		opts = args.pop();
	}
	numArgs = args.length;
	if ( numArgs === 0 ) {
		throw new Error( 'zip()::insufficient input arguments. Must provide at least one array.' );
	}
	if ( opts.hasOwnProperty( 'trunc' ) ) {
		if ( !isBoolean( opts.trunc ) ) {
			throw new TypeError( 'zip()::invalid option. `trunc` option must be a boolean.  Value: `' + opts.trunc + '`.' );
		}
	} else {
		opts.trunc = true;
	}
	if ( opts.hasOwnProperty( 'fill' ) ) {
		fill = opts.fill;
	}
	if ( opts.hasOwnProperty( 'arrays' ) ) {
		if ( !isBoolean( opts.arrays ) ) {
			throw new TypeError( 'zip()::invalid option. `arrays` option must be a boolean. Value: `' + opts.arrays + '`.' );
		}
	} else {
		opts.arrays = false;
	}
	if ( numArgs === 1 && opts.arrays ) {
		// Treat the lone array argument as an array of arrays to be zipped...
		args = args[ 0 ];
		numArgs = args.length;
	}
	len = args[ 0 ].length;
	if ( opts.trunc ) {
		// Find the min array length...
		for ( i = 0; i < numArgs; i++ ) {
			val = args[ i ].length;
			if ( val < len ) {
				len = val;
			}
		}
	} else {
		// Find the max array length...
		for ( i = 0; i < numArgs; i++ ) {
			val = args[ i ].length;
			if ( val > len ) {
				len = val;
			}
		}
	}
	out = new Array( len );
	for ( j = 0; j < len; j++ ) {
		// Temporary array to store tuples...
		arr = new Array( numArgs );

		// Create the tuples...
		for ( i = 0; i < numArgs; i++ ) {
			arg = args[ i ];

			// If an array is too short, use a fill value...
			if ( arg.length <= j ) {
				arr[ i ] = fill;
				continue;
			}
			arr[ i ] = arg[ j ];
		}
		out[ j ] = arr;
	}
	return out;
} // end FUNCTION zip()


// EXPORTS //

module.exports = zip;

},{"validate.io-array":1139,"validate.io-boolean-primitive":1140,"validate.io-object":1141}],1139:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1140:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],1141:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":1139}],1142:[function(require,module,exports){
'use strict';

// DISTRIBUTIONS //

var dist = {};

// DISCRETE DISTRIBUTIONS //

/**
* Binomial distribution.
*/

dist.binom = {};

/*
dist.binom.pmf = require( 'distributions-binomial-pmf' );
dist.binom.cdf = require( 'distributions-binomial-cdf' );
dist.binom.quantile = require( 'distributions-binomial-quantile' );
dist.binom.mgf = require( 'distributions-binomial-mgf' );

dist.binom.random = require( 'distributions-binomial-random' );

dist.binom.mean = require( 'distributions-binomial-mean' );
dist.binom.median = require( 'distributions-binomial-median' );
dist.binom.variance = require( 'distributions-binomial-variance' );
dist.binom.skewness = require( 'distributions-binomial-skewness' );
dist.binom.ekurtosis = require( 'distributions-binomial-ekurtosis' );
dist.binom.entropy = require( 'distributions-binomial-entropy' );
*/

/**
* Geometric distribution.
*/

dist.geom = {};

/*
dist.geom.pmf = require( 'distributions-geometric-pmf' );
dist.geom.cdf = require( 'distributions-geometric-cdf' );
dist.geom.quantile = require( 'distributions-geometric-quantile' );
dist.geom.mgf = require( 'distributions-geometric-mgf' );

dist.geom.random = require( 'distributions-geometric-random' );

dist.geom.mean = require( 'distributions-geometric-mean' );
dist.geom.median = require( 'distributions-geometric-median' );
dist.geom.variance = require( 'distributions-geometric-variance' );
dist.geom.skewness = require( 'distributions-geometric-skewness' );
dist.geom.ekurtosis = require( 'distributions-geometric-ekurtosis' );
dist.geom.entropy = require( 'distributions-geometric-entropy' );
*/

/**
* Hypergeometric distribution.
*/

dist.hypergeom = {};

/*
dist.hypergeom.pmf = require( 'distributions-hypergeometric-pmf' );
dist.hypergeom.cdf = require( 'distributions-hypergeometric-cdf' );
dist.hypergeom.quantile = require( 'distributions-hypergeometric-quantile' );
dist.hypergeom.mgf = require( 'distributions-hypergeometric-mgf' );

dist.hypergeom.random = require( 'distributions-hypergeometric-random' );

dist.hypergeom.mean = require( 'distributions-hypergeometric-mean' );
dist.hypergeom.median = require( 'distributions-hypergeometric-median' );
dist.hypergeom.variance = require( 'distributions-hypergeometric-variance' );
dist.hypergeom.skewness = require( 'distributions-hypergeometric-skewness' );
dist.hypergeom.ekurtosis = require( 'distributions-hypergeometric-ekurtosis' );
dist.hypergeom.entropy = require( 'distributions-hypergeometric-entropy' );
*/

/**
* Negative binomial distribution.
*/

dist.nbinom = {};

/*
dist.nbinom.pmf = require( 'distributions-negative-binomial-pmf' );
dist.nbinom.cdf = require( 'distributions-negative-binomial-cdf' );
dist.nbinom.quantile = require( 'distributions-negative-binomial-quantile' );
dist.nbinom.mgf = require( 'distributions-negative-binomial-mgf' );

dist.nbinom.random = require( 'distributions-negative-binomial-random' );

dist.nbinom.mean = require( 'distributions-negative-binomial-mean' );
dist.nbinom.median = require( 'distributions-negative-binomial-median' );
dist.nbinom.variance = require( 'distributions-negative-binomial-variance' );
dist.nbinom.skewness = require( 'distributions-negative-binomial-skewness' );
dist.nbinom.ekurtosis = require( 'distributions-negative-binomial-ekurtosis' );
dist.nbinom.entropy = require( 'distributions-negative-binomial-entropy' );
*/

/**
* Poisson distribution.
*/

dist.poisson = {};

/*
dist.poisson.pmf = require( 'distributions-poisson-pmf' );
dist.poisson.cdf = require( 'distributions-poisson-cdf' );
dist.poisson.quantile = require( 'distributions-poisson-quantile' );
dist.poisson.mgf = require( 'distributions-poisson-mgf' );

dist.poisson.random = require( 'distributions-poisson-random' );
*/
dist.poisson.mean = require( 'distributions-poisson-mean' );
dist.poisson.median = require( 'distributions-poisson-median' );
dist.poisson.variance = require( 'distributions-poisson-variance' );
// dist.poisson.skewness = require( 'distributions-poisson-skewness' );
dist.poisson.ekurtosis = require( 'distributions-poisson-ekurtosis' );
// dist.poisson.entropy = require( 'distributions-poisson-entropy' );


// CONTINUOUS DISTRIBUTIONS //

/**
* Beta distribution.
*/

dist.beta = {};

/*
dist.beta.pdf = require( 'distributions-beta-pdf' );
dist.beta.cdf = require( 'distributions-beta-cdf' );
dist.beta.quantile = require( 'distributions-beta-quantile' );
dist.beta.mgf = require( 'distributions-beta-mgf' );

dist.beta.random = require( 'distributions-beta-random' );

dist.beta.mean = require( 'distributions-beta-mean' );
dist.beta.median = require( 'distributions-beta-median' );
dist.beta.variance = require( 'distributions-beta-variance' );
dist.beta.skewness = require( 'distributions-beta-skewness' );
dist.beta.ekurtosis = require( 'distributions-beta-ekurtosis' );
dist.beta.entropy = require( 'distributions-beta-entropy' );
*/

/**
* Cauchy distribution.
*/

dist.cauchy = {};

/*
dist.cauchy.pdf = require( 'distributions-cauchy-pdf' );
dist.cauchy.cdf = require( 'distributions-cauchy-cdf' );
dist.cauchy.quantile = require( 'distributions-cauchy-quantile' );

dist.cauchy.random = require( 'distributions-cauchy-random' );

dist.cauchy.median = require( 'distributions-cauchy-median' );
dist.cauchy.entropy = require( 'distributions-cauchy-entropy' );
*/

/**
* Chi-squared distribution.
*/

dist.chisq = {};

/*
dist.chisq.pdf = require( 'distributions-chisquare-pdf' );
dist.chisq.cdf = require( 'distributions-chisquare-cdf' );
dist.chisq.quantile = require( 'distributions-chisquare-quantile' );
dist.chisq.mgf = require( 'distributions-chisquare-mgf' );

dist.chisq.random = require( 'distributions-chisquare-random' );

dist.chisq.mean = require( 'distributions-chisquare-mean' );
dist.chisq.median = require( 'distributions-chisquare-median' );
dist.chisq.variance = require( 'distributions-chisquare-variance' );
dist.chisq.skewness = require( 'distributions-chisquare-skewness' );
dist.chisq.ekurtosis = require( 'distributions-chisquare-ekurtosis' );
dist.chisq.entropy = require( 'distributions-chisquare-entropy' );
*/

/**
* Erlang distribution.
*/

dist.erlang = {};

/*
dist.erlang.pdf = require( 'distributions-erlang-pdf' );
dist.erlang.cdf = require( 'distributions-erlang-cdf' );
dist.erlang.quantile = require( 'distributions-erlang-quantile' );
dist.erlang.mgf = require( 'distributions-erlang-mgf' );

dist.erlang.random = require( 'distributions-erlang-random' );

dist.erlang.mean = require( 'distributions-erlang-mean' );
dist.erlang.median = require( 'distributions-erlang-median' );
dist.erlang.variance = require( 'distributions-erlang-variance' );
dist.erlang.skewness = require( 'distributions-erlang-skewness' );
dist.erlang.ekurtosis = require( 'distributions-erlang-ekurtosis' );
dist.erlang.entropy = require( 'distributions-erlang-entropy' );
*/

/**
* Exponential distribution.
*/

dist.exp = {};

dist.exp.pdf = require( 'distributions-exponential-pdf' );
dist.exp.cdf = require( 'distributions-exponential-cdf' );
dist.exp.quantile = require( 'distributions-exponential-quantile' );
dist.exp.mgf = require( 'distributions-exponential-mgf' );

// dist.exp.random = require( 'distributions-exponential-random' );

dist.exp.mean = require( 'distributions-exponential-mean' );
dist.exp.median = require( 'distributions-exponential-median' );
dist.exp.variance = require( 'distributions-exponential-variance' );
dist.exp.skewness = require( 'distributions-exponential-skewness' );
dist.exp.ekurtosis = require( 'distributions-exponential-ekurtosis' );
// dist.exp.entropy = require( 'distributions-exponential-entropy' );

/**
* F distribution.
*/

dist.f = {};

/*
dist.f.pdf = require( 'distributions-f-pdf' );
dist.f.cdf = require( 'distributions-f-cdf' );
dist.f.quantile = require( 'distributions-f-quantile' );

dist.f.random = require( 'distributions-f-random' );

dist.f.mean = require( 'distributions-f-mean' );
dist.f.median = require( 'distributions-f-median' );
dist.f.variance = require( 'distributions-f-variance' );
dist.f.skewness = require( 'distributions-f-skewness' );
dist.f.ekurtosis = require( 'distributions-f-ekurtosis' );
dist.f.entropy = require( 'distributions-f-entropy' );
*/

/**
* Gamma distribution.
*/

dist.gamma = {};

/*
dist.gamma.pdf = require( 'distributions-gamma-pdf' );
dist.gamma.cdf = require( 'distributions-gamma-cdf' );
dist.gamma.quantile = require( 'distributions-gamma-quantile' );
dist.gamma.mgf = require( 'distributions-gamma-mgf' );

dist.gamma.random = require( 'distributions-gamma-random' );

dist.gamma.mean = require( 'distributions-gamma-mean' );
dist.gamma.median = require( 'distributions-gamma-median' );
dist.gamma.variance = require( 'distributions-gamma-variance' );
dist.gamma.skewness = require( 'distributions-gamma-skewness' );
dist.gamma.ekurtosis = require( 'distributions-gamma-ekurtosis' );
dist.gamma.entropy = require( 'distributions-gamma-entropy' );
*/

/**
* Gumbel distribution.
*/

dist.gumbel = {};

/*
dist.gumbel.pdf = require( 'distributions-gumbel-pdf' );
dist.gumbel.cdf = require( 'distributions-gumbel-cdf' );
dist.gumbel.quantile = require( 'distributions-gumbel-quantile' );
dist.gumbel.mgf = require( 'distributions-gumbel-mgf' );

dist.gumbel.random = require( 'distributions-gumbel-random' );

dist.gumbel.mean = require( 'distributions-gumbel-mean' );
dist.gumbel.median = require( 'distributions-gumbel-median' );
dist.gumbel.variance = require( 'distributions-gumbel-variance' );
dist.gumbel.skewness = require( 'distributions-gumbel-skewness' );
dist.gumbel.ekurtosis = require( 'distributions-gumbel-ekurtosis' );
dist.gumbel.entropy = require( 'distributions-gumbel-entropy' );
*/

/**
* Inverse-Gamma distribution.
*/

dist.invgamma = {};

/*
dist.invgamma.pdf = require( 'distributions-invgamma-pdf' );
dist.invgamma.cdf = require( 'distributions-invgamma-cdf' );
dist.invgamma.quantile = require( 'distributions-invgamma-quantile' );
dist.invgamma.mgf = require( 'distributions-invgamma-mgf' );

dist.invgamma.random = require( 'distributions-invgamma-random' );

dist.invgamma.mean = require( 'distributions-invgamma-mean' );
dist.invgamma.median = require( 'distributions-invgamma-median' );
dist.invgamma.variance = require( 'distributions-invgamma-variance' );
dist.invgamma.skewness = require( 'distributions-invgamma-skewness' );
dist.invgamma.ekurtosis = require( 'distributions-invgamma-ekurtosis' );
dist.invgamma.entropy = require( 'distributions-invgamma-entropy' );
*/

/**
* Laplace distribution.
*/

dist.laplace = {};

/*
dist.laplace.pdf = require( 'distributions-laplace-pdf' );
dist.laplace.cdf = require( 'distributions-laplace-cdf' );
dist.laplace.quantile = require( 'distributions-laplace-quantile' );
dist.laplace.mgf = require( 'distributions-laplace-mgf' );

dist.laplace.random = require( 'distributions-laplace-random' );

dist.laplace.mean = require( 'distributions-laplace-mean' );
dist.laplace.median = require( 'distributions-laplace-median' );
dist.laplace.variance = require( 'distributions-laplace-variance' );
dist.laplace.skewness = require( 'distributions-laplace-skewness' );
dist.laplace.ekurtosis = require( 'distributions-laplace-ekurtosis' );
dist.laplace.entropy = require( 'distributions-laplace-entropy' );
*/

/**
* Logistic distribution.
*/

dist.logistic = {};

/*
dist.logistic.pdf = require( 'distributions-logistic-pdf' );
dist.logistic.cdf = require( 'distributions-logistic-cdf' );
dist.logistic.quantile = require( 'distributions-logistic-quantile' );
dist.logistic.mgf = require( 'distributions-logistic-mgf' );

dist.logistic.random = require( 'distributions-logistic-random' );

dist.logistic.mean = require( 'distributions-logistic-mean' );
dist.logistic.median = require( 'distributions-logistic-median' );
dist.logistic.variance = require( 'distributions-logistic-variance' );
dist.logistic.skewness = require( 'distributions-logistic-skewness' );
dist.logistic.ekurtosis = require( 'distributions-logistic-ekurtosis' );
dist.logistic.entropy = require( 'distributions-logistic-entropy' );
*/

/**
* Lognormal distribution.
*/

dist.lnorm = {};

/*
dist.lnorm.pdf = require( 'distributions-lognormal-pdf' );
dist.lnorm.cdf = require( 'distributions-lognormal-cdf' );
dist.lnorm.quantile = require( 'distributions-lognormal-quantile' );
dist.lnorm.mgf = require( 'distributions-lognormal-mgf' );

dist.lnorm.random = require( 'distributions-lognormal-random' );

dist.lnorm.mean = require( 'distributions-lognormal-mean' );
dist.lnorm.median = require( 'distributions-lognormal-median' );
dist.lnorm.variance = require( 'distributions-lognormal-variance' );
dist.lnorm.skewness = require( 'distributions-lognormal-skewness' );
dist.lnorm.ekurtosis = require( 'distributions-lognormal-ekurtosis' );
dist.lnorm.entropy = require( 'distributions-lognormal-entropy' );
*/

/**
* Rayleigh distribution.
*/

dist.rayleigh = {};

dist.rayleigh.pdf = require( 'distributions-rayleigh-pdf' );
dist.rayleigh.cdf = require( 'distributions-rayleigh-cdf' );
dist.rayleigh.quantile = require( 'distributions-rayleigh-quantile' );
// dist.rayleigh.mgf = require( 'distributions-rayleigh-mgf' );

// dist.rayleigh.random = require( 'distributions-rayleigh-random' );

dist.rayleigh.mean = require( 'distributions-rayleigh-mean' );
dist.rayleigh.median = require( 'distributions-rayleigh-median' );
dist.rayleigh.variance = require( 'distributions-rayleigh-variance' );
dist.rayleigh.skewness = require( 'distributions-rayleigh-skewness' );
dist.rayleigh.ekurtosis = require( 'distributions-rayleigh-ekurtosis' );
dist.rayleigh.entropy = require( 'distributions-rayleigh-entropy' );

/**
* Student's t distribution.
*/

dist.t = {};

dist.t.pdf = require( 'distributions-t-pdf' );
/*
dist.t.cdf = require( 'distributions-t-cdf' );
dist.t.quantile = require( 'distributions-t-quantile' );

dist.t.random = require( 'distributions-t-random' );

dist.t.mean = require( 'distributions-t-mean' );
dist.t.median = require( 'distributions-t-median' );
dist.t.variance = require( 'distributions-t-variance' );
dist.t.skewness = require( 'distributions-t-skewness' );
dist.t.ekurtosis = require( 'distributions-t-ekurtosis' );
dist.t.entropy = require( 'distributions-t-entropy' );
*/

/**
* Uniform distribution.
*/

dist.unif = {};

/*
dist.unif.pdf = require( 'distributions-uniform-pdf' );
dist.unif.cdf = require( 'distributions-uniform-cdf' );
dist.unif.quantile = require( 'distributions-uniform-quantile' );
dist.unif.mgf = require( 'distributions-uniform-mgf' );

dist.unif.random = require( 'distributions-uniform-random' );

dist.unif.mean = require( 'distributions-uniform-mean' );
dist.unif.median = require( 'distributions-uniform-median' );
dist.unif.variance = require( 'distributions-uniform-variance' );
dist.unif.skewness = require( 'distributions-uniform-skewness' );
dist.unif.ekurtosis = require( 'distributions-uniform-ekurtosis' );
dist.unif.entropy = require( 'distributions-uniform-entropy' );
*/

/**
* Normal distribution.
*/

dist.norm = {};

dist.norm.pdf = require( 'distributions-normal-pdf' );
dist.norm.cdf = require( 'distributions-normal-cdf' );
dist.norm.quantile = require( 'distributions-normal-quantile' );
// dist.norm.mgf = require( 'distributions-normal-mgf' );

dist.norm.random = require( 'distributions-normal-random' );

/*
dist.norm.mean = require( 'distributions-normal-mean' );
dist.norm.median = require( 'distributions-normal-median' );
dist.norm.variance = require( 'distributions-normal-variance' );
dist.norm.skewness = require( 'distributions-normal-skewness' );
dist.norm.ekurtosis = require( 'distributions-normal-ekurtosis' );
dist.norm.entropy = require( 'distributions-normal-entropy' );
*/

/**
* Pareto (Type 1) distribution.
*/

dist.pareto = {};

/*
dist.pareto.pdf = require( 'distributions-pareto-type1-pdf' );
dist.pareto.cdf = require( 'distributions-pareto-type1-cdf' );
dist.pareto.quantile = require( 'distributions-pareto-type1-quantile' );
dist.pareto.mgf = require( 'distributions-pareto-type1-mgf' );

dist.pareto.random = require( 'distributions-pareto-type1-random' );

dist.pareto.mean = require( 'distributions-pareto-type1-mean' );
dist.pareto.median = require( 'distributions-pareto-type1-median' );
dist.pareto.variance = require( 'distributions-pareto-type1-variance' );
dist.pareto.skewness = require( 'distributions-pareto-type1-skewness' );
dist.pareto.ekurtosis = require( 'distributions-pareto-type1-ekurtosis' );
dist.pareto.entropy = require( 'distributions-pareto-type1-entropy' );
*/

/**
* Triangular distribution.
*/

dist.triangular = {};

/*
dist.triangular.pdf = require( 'distributions-triangular-pdf' );
dist.triangular.cdf = require( 'distributions-triangular-cdf' );
dist.triangular.quantile = require( 'distributions-triangular-quantile' );
dist.triangular.mgf = require( 'distributions-triangular-mgf' );

dist.triangular.random = require( 'distributions-triangular-random' );

dist.triangular.mean = require( 'distributions-triangular-mean' );
dist.triangular.median = require( 'distributions-triangular-median' );
dist.triangular.variance = require( 'distributions-triangular-variance' );
dist.triangular.skewness = require( 'distributions-triangular-skewness' );
dist.triangular.ekurtosis = require( 'distributions-triangular-ekurtosis' );
dist.triangular.entropy = require( 'distributions-triangular-entropy' );
*/

/**
* Weibull distribution.
*/

dist.weibull = {};

/*
dist.weibull.pdf = require( 'distributions-weibull-pdf' );
dist.weibull.cdf = require( 'distributions-weibull-cdf' );
dist.weibull.quantile = require( 'distributions-weibull-quantile' );
dist.weibull.mgf = require( 'distributions-weibull-mgf' );

dist.weibull.random = require( 'distributions-weibull-random' );

dist.weibull.mean = require( 'distributions-weibull-mean' );
dist.weibull.median = require( 'distributions-weibull-median' );
dist.weibull.variance = require( 'distributions-weibull-variance' );
dist.weibull.skewness = require( 'distributions-weibull-skewness' );
dist.weibull.ekurtosis = require( 'distributions-weibull-ekurtosis' );
dist.weibull.entropy = require( 'distributions-weibull-entropy' );
*/

// EXPORTS //

module.exports = dist;

},{"distributions-exponential-cdf":1146,"distributions-exponential-ekurtosis":1229,"distributions-exponential-mean":1312,"distributions-exponential-median":1395,"distributions-exponential-mgf":1478,"distributions-exponential-pdf":1561,"distributions-exponential-quantile":1645,"distributions-exponential-skewness":1728,"distributions-exponential-variance":1811,"distributions-normal-cdf":1894,"distributions-normal-pdf":1979,"distributions-normal-quantile":2063,"distributions-normal-random":2151,"distributions-poisson-ekurtosis":2224,"distributions-poisson-mean":2307,"distributions-poisson-median":2390,"distributions-poisson-variance":2473,"distributions-rayleigh-cdf":2556,"distributions-rayleigh-ekurtosis":2639,"distributions-rayleigh-entropy":2722,"distributions-rayleigh-mean":2806,"distributions-rayleigh-median":2889,"distributions-rayleigh-pdf":2972,"distributions-rayleigh-quantile":3055,"distributions-rayleigh-skewness":3138,"distributions-rayleigh-variance":3221,"distributions-t-pdf":3304}],1143:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// CDF //

/**
* FUNCTION: cdf( out, arr, lambda, accessor )
*	Evaluates the cumulative distribution function (CDF) for an exponential distribution with rate parameter `lambda` using an accessor function.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Number} lambda - rate parameter
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function cdf( y, x, lambda, clbk ) {
	var len = x.length,
		fcn,
		v, i;

	fcn = partial( lambda );
	for ( i = 0; i < len; i++ ) {
		v = clbk( x[ i ], i );
		if ( typeof v === 'number' ) {
			y[ i ] = fcn( v );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION cdf()


// EXPORTS //

module.exports = cdf;

},{"./partial.js":1149}],1144:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// CDF //

/**
* FUNCTION: cdf( out, arr, lambda )
*	Evaluates the cumulative distribution function (CDF) for an exponential distribution with rate parameter `lambda` for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Number} lambda - rate parameter
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function cdf( y, x, lambda ) {
	var len = x.length,
		fcn,
		i;

	fcn = partial( lambda );
	for ( i = 0; i < len; i++ ) {
		if ( typeof x[ i ] === 'number' ) {
			y[ i ] = fcn( x[ i ] );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION cdf()


// EXPORTS //

module.exports = cdf;

},{"./partial.js":1149}],1145:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory,
	partial = require( './partial.js' );


// CDF //

/**
* FUNCTION: cdf( arr, lambda, path[, sep] )
*	Evaluates the cumulative distribution function (CDF) for an exponential distribution with rate parameter `lambda` for each array element and sets the input array.
*
* @param {Array} arr - input array
* @param {Number} lambda - rate parameter
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function cdf( x, lambda, path, sep ) {
	var len = x.length,
		opts = {},
		dget,
		dset,
		fcn,
		v, i;
	if ( arguments.length > 3 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		fcn = partial( lambda );
		for ( i = 0; i < len; i++ ) {
			v = dget( x[ i ] );
			if ( typeof v === 'number' ) {
				dset( x[i], fcn( v ) );
			} else {
				dset( x[i], NaN );
			}
		}
	}
	return x;
} // end FUNCTION cdf()


// EXPORTS //

module.exports = cdf;

},{"./partial.js":1149,"utils-deep-get":1200,"utils-deep-set":1206}],1146:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var cdf1 = require( './number.js' ),
	cdf2 = require( './array.js' ),
	cdf3 = require( './accessor.js' ),
	cdf4 = require( './deepset.js' ),
	cdf5 = require( './matrix.js' ),
	cdf6 = require( './typedarray.js' );


// CDF //

/**
* FUNCTION: cdf( x[, opts] )
*	Evaluates the cumulative distribution function (CDF) for an exponential distribution.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} x - input value
* @param {Object} [opts] - function options
* @param {Number} [opts.lambda=1] - rate parameter
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} evaluated CDF
*/
function cdf( x, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	opts.lambda = typeof opts.lambda !== 'undefined' ? opts.lambda : 1;

	if ( isNumber( x ) ) {
		return cdf1( x, opts.lambda );
	}
	if ( isMatrixLike( x ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'cdf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( x.length );
			out = matrix( d, x.shape, dt );
		} else {
			out = x;
		}
		return cdf5( out, x, opts.lambda );
	}
	if ( isTypedArrayLike( x ) ) {
		if ( opts.copy === false ) {
			out = x;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'cdf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( x.length );
		}
		return cdf6( out, x, opts.lambda );
	}
	if ( isArrayLike( x ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return cdf4( x, opts.lambda, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = x;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'cdf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( x.length );
		}
		else {
			out = new Array( x.length );
		}
		if ( opts.accessor ) {
			return cdf3( out, x, opts.lambda, opts.accessor );
		}
		return cdf2( out, x, opts.lambda );
	}
	return NaN;
} // end FUNCTION cdf()


// EXPORTS //

module.exports = cdf;

},{"./accessor.js":1143,"./array.js":1144,"./deepset.js":1145,"./matrix.js":1147,"./number.js":1148,"./typedarray.js":1150,"./validate.js":1151,"compute-array-constructors":1153,"dstructs-matrix":1162,"validate.io-array-like":1209,"validate.io-matrix-like":1214,"validate.io-number-primitive":1218,"validate.io-typed-array-like":1223}],1147:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// CDF //

/**
* FUNCTION: cdf( out, matrix, lambda )
*	Evaluates the cumulative distribution function (CDF) for an exponential distribution with rate parameter `lambda` for each matrix element.
*
* @param {Matrix} out - output matrix
* @param {Matrix} arr - input matrix
* @param {Number} lambda - rate parameter
* @returns {Matrix} output matrix
*/
function cdf( y, x, lambda ) {
	var len = x.length,
		fcn,
		i;
	if ( y.length !== len ) {
		throw new Error( 'cdf()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	fcn = partial( lambda );
	for ( i = 0; i < len; i++ ) {
		y.data[ i ] = fcn( x.data[ i ] );
	}
	return y;
} // end FUNCTION cdf()


// EXPORTS //

module.exports = cdf;

},{"./partial.js":1149}],1148:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var exp = Math.exp;


// CDF //

/**
* FUNCTION: cdf( x, lambda )
*	Evaluates the cumulative distribution function (CDF) for an exponential distribution with rate parameter `lambda` at a value `x`.
*
* @param {Number} x - input value
* @param {Number} lambda - rate parameter
* @returns {Number} evaluated CDF
*/
function cdf( x, lambda ) {
	if ( x < 0 ) {
		return 0;
	} else {
		return 1 - exp( - lambda * x );
	}
} // end FUNCTION cdf()


// EXPORTS //

module.exports = cdf;

},{}],1149:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var exp = Math.exp;


// PARTIAL //

/**
* FUNCTION: partial( lambda )
*	Partially applies rate parameter `lambda` and returns a function for evaluating the cumulative distribution function (CDF) for an exponential distribution.
*
* @param {Number} lambda - rate parameter
* @returns {Function} CDF
*/
function partial( lambda ) {

	/**
	* FUNCTION: cdf( x )
	*	Evaluates the cumulative distribution function (CDF) for an exponential distribution.
	*
	* @private
	* @param {Number} x - input value
	* @returns {Number} evaluated CDF
	*/
	return function cdf( x ) {
		if ( x < 0 ) {
			return 0;
		} else {
			return 1 - exp( - lambda * x );
		}
	};
} // end FUNCTION partial()


// EXPORTS //

module.exports = partial;

},{}],1150:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// CDF //

/**
* FUNCTION: cdf( out, arr, lambda )
*	Evaluates the cumulative distribution function (CDF) for an exponential distribution with rate parameter `lambda` for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} arr - input array
* @param {Number} lambda - rate parameter
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function cdf( y, x, lambda ) {
	var len = x.length,
		fcn,
		i;

	fcn = partial ( lambda );
	for ( i = 0; i < len; i++ ) {
		y[ i ] = fcn( x[ i ] );
	}
	return y;
} // end FUNCTION cdf()


// EXPORTS //

module.exports = cdf;

},{"./partial.js":1149}],1151:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isPositive = require( 'validate.io-positive-primitive' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Number} [options.lambda] - rate parameter
* @param {Boolean} [options.copy] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'cdf()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'lambda' ) ) {
		opts.lambda = options.lambda;
		if ( !isPositive( opts.lambda ) ) {
			return new TypeError( 'cdf()::invalid option. Rate parameter `lambda` must be a positive number. Option: `' + opts.lambda + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'cdf()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'cdf()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'cdf()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'cdf()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'cdf()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":1212,"validate.io-function":1213,"validate.io-object":1219,"validate.io-positive-primitive":1221,"validate.io-string-primitive":1222}],1152:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],1153:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":1152,"dup":12}],1154:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],1155:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":1158,"./iget.js":1160,"./iset.js":1163,"./mget.js":1167,"./mset.js":1169,"./set.js":1177,"./sget.js":1179,"./sset.js":1181,"./toString.js":1183,"dup":14}],1156:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":1159,"./iget.raw.js":1161,"./iset.raw.js":1164,"./mget.raw.js":1168,"./mset.raw.js":1170,"./set.raw.js":1178,"./sget.raw.js":1180,"./sset.raw.js":1182,"./toString.js":1183,"dup":15}],1157:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],1158:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":1215}],1159:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],1160:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":1195}],1161:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],1162:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":1165,"./matrix.raw.js":1166,"dup":21}],1163:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":1195,"validate.io-number-primitive":1218}],1164:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],1165:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":1154,"./ctor.js":1155,"./dtypes.js":1157,"compute-cast-arrays":1184,"compute-dtype":1187,"dup":24,"validate.io-array":1192,"validate.io-contains":1193,"validate.io-nonnegative-integer-array":1196,"validate.io-string-primitive":1222}],1166:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":1154,"./ctor.raw.js":1156,"./dtypes.js":1157,"compute-dtype":1187,"dup":25,"validate.io-contains":1193,"validate.io-string-primitive":1222}],1167:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":1154,"dup":26,"validate.io-nonnegative-integer-array":1196}],1168:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":1154,"dup":27}],1169:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":1171,"./mset2.js":1172,"./mset3.js":1173,"./mset4.js":1174,"./mset5.js":1175,"./mset6.js":1176,"dup":28,"validate.io-function":1213,"validate.io-nonnegative-integer-array":1196,"validate.io-number-primitive":1218}],1170:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":1171,"./mset2.js":1172,"./mset3.js":1173,"./mset4.js":1174,"./mset5.js":1175,"./mset6.js":1176,"dup":29}],1171:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],1172:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],1173:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],1174:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],1175:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],1176:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],1177:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":1215,"validate.io-number-primitive":1218}],1178:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],1179:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":1154,"compute-indexspace":1191,"dup":38,"validate.io-string-primitive":1222}],1180:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":1154,"compute-indexspace":1191,"dup":39}],1181:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":1191,"dup":40,"validate.io-function":1213,"validate.io-number-primitive":1218,"validate.io-string-primitive":1222}],1182:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":1191,"dup":41}],1183:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],1184:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":1152,"compute-array-dtype/lib/dtypes":1185,"dup":43,"type-name":1186,"validate.io-array-like":1209}],1185:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1186:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1187:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":1189,"dup":46,"type-name":1190}],1188:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1189:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":1188,"dup":48}],1190:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1191:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":1215,"validate.io-string-primitive":1222}],1192:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1193:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":1192,"validate.io-nan-primitive":1194}],1194:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],1195:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1218}],1196:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":1192,"validate.io-nonnegative-integer":1215}],1197:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],1198:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],1199:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":1197,"./defaults.js":1198,"./validate.js":1201,"dup":60,"validate.io-array":1202,"validate.io-string-primitive":1222}],1200:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":1197,"./defaults.js":1198,"./factory.js":1199,"./validate.js":1201,"dup":61,"validate.io-array":1202,"validate.io-string-primitive":1222}],1201:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":1219,"validate.io-string-primitive":1222}],1202:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1203:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],1204:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],1205:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":1203,"./defaults.js":1204,"./validate.js":1207,"dup":66,"validate.io-array":1208,"validate.io-string-primitive":1222}],1206:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":1203,"./defaults.js":1204,"./factory.js":1205,"./validate.js":1207,"dup":67,"validate.io-array":1208,"validate.io-string-primitive":1222}],1207:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":1212,"validate.io-object":1219,"validate.io-string-primitive":1222}],1208:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1209:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":1210,"dup":70,"validate.io-integer-primitive":1211}],1210:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],1211:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1218}],1212:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],1213:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],1214:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],1215:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":1216}],1216:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":1217}],1217:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],1218:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],1219:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":1220}],1220:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1221:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' );


// IS POSITIVE //

/**
* FUNCTION: isPositive( value )
*	Validates if a value is a positive number primitive.
*
* @param {*} value - value to be validated
* @returns {Boolean} boolean indicating if a value is a positive number primitive
*/
function isPositive( value ) {
	return isNumber( value ) && value > 0;
} // end FUNCTION isPositive()


// EXPORTS //

module.exports = isPositive;

},{"validate.io-number-primitive":1218}],1222:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],1223:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":1224,"dup":81,"validate.io-integer-primitive":1225}],1224:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],1225:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1218}],1226:[function(require,module,exports){
'use strict';

// MODULES //

var EKURTOSIS = require( './number.js' );


// EKURTOSIS //

/**
* FUNCTION: ekurtosis( out, arr, accessor )
*	Computes the distribution ekurtosis for parameters stored in an array using an accessor function.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function ekurtosis( out, arr, clbk ) {
	var len = arr.length,
		v, i;
	for ( i = 0; i < len; i++ ) {
		v = clbk( arr[ i ], i );
		if ( typeof v === 'number' ) {
			out[ i ] = EKURTOSIS( v );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION ekurtosis()


// EXPORTS //

module.exports = ekurtosis;

},{"./number.js":1231}],1227:[function(require,module,exports){
'use strict';

// MODULES //

var EKURTOSIS = require( './number.js' );


// EKURTOSIS //

/**
* FUNCTION: ekurtosis( out, lambda )
*	Computes the distribution ekurtosis for parameters stored in an array.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} lambda - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function ekurtosis( out, arr ) {
	var len = arr.length,
		i;
	for ( i = 0; i < len; i++ ) {
		if ( typeof arr[ i ] === 'number' ) {
			out[ i ] = EKURTOSIS( arr[ i ] );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION ekurtosis()


// EXPORTS //

module.exports = ekurtosis;

},{"./number.js":1231}],1228:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory,
	EKURTOSIS  = require( './number.js' );


// EKURTOSIS //

/**
* FUNCTION: ekurtosis( arr, path[, sep] )
*	Computes the distribution ekurtosis and deep sets the input array.
*
* @param {Array} arrays - input array
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function ekurtosis( arr, path, sep ) {
	var len = arr.length,
		opts = {},
		dget,
		dset,
		v, i;
	if ( arguments.length > 2 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		for ( i = 0; i < len; i++ ) {
			v = dget( arr[ i ] );
			if ( typeof v === 'number' ) {
				dset( arr[i], EKURTOSIS ( v ) );
			} else {
				dset( arr[i], NaN );
			}
		}
	}
	return arr;
} // end FUNCTION ekurtosis()


// EXPORTS //

module.exports = ekurtosis;

},{"./number.js":1231,"utils-deep-get":1282,"utils-deep-set":1288}],1229:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isnan = require( 'validate.io-nan' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var ekurtosis1 = require( './number.js' ),
	ekurtosis2 = require( './array.js' ),
	ekurtosis3 = require( './accessor.js' ),
	ekurtosis4 = require( './deepset.js' ),
	ekurtosis5 = require( './matrix.js' ),
	ekurtosis6 = require( './typedarray.js' );


// EKURTOSIS //

/**
* FUNCTION: ekurtosis( lambda[, opts] )
*	Computes the distribution ekurtosis.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} lambda - input value
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} distribution ekurtosis(s)
*/
function ekurtosis( lambda, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( isNumber( lambda ) || isnan( lambda ) ) {
		return ekurtosis1( lambda );
	}
	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isMatrixLike( lambda ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'ekurtosis()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( lambda.length );
			out = matrix( d, lambda.shape, dt );
		} else {
			out = lambda;
		}
		return ekurtosis5( out, lambda );
	}
	if ( isTypedArrayLike( lambda ) ) {
		if ( opts.copy === false ) {
			out = lambda;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'ekurtosis()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( lambda.length );
		}
		return ekurtosis6( out, lambda );
	}
	if ( isArrayLike( lambda ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return ekurtosis4( lambda, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = lambda;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'ekurtosis()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( lambda.length );
		}
		else {
			out = new Array( lambda.length );
		}
		if ( opts.accessor ) {
			return ekurtosis3( out, lambda, opts.accessor );
		}
		return ekurtosis2( out, lambda );
	}
	return NaN;
} // end FUNCTION ekurtosis()


// EXPORTS //

module.exports = ekurtosis;

},{"./accessor.js":1226,"./array.js":1227,"./deepset.js":1228,"./matrix.js":1230,"./number.js":1231,"./typedarray.js":1232,"./validate.js":1233,"compute-array-constructors":1235,"dstructs-matrix":1244,"validate.io-array-like":1291,"validate.io-matrix-like":1296,"validate.io-nan":1297,"validate.io-number-primitive":1301,"validate.io-typed-array-like":1306}],1230:[function(require,module,exports){
'use strict';

// MODULES //

var EKURTOSIS = require( './number.js' );


// EKURTOSIS //

/**
* FUNCTION: ekurtosis( out, x )
*	Computes the distribution ekurtosis for each parameter stored in a matrix.
*
* @param {Matrix} out - output matrix
* @param {Matrix} x - input matrix
* @returns {Matrix} output matrix
*/
function ekurtosis( out, x ) {
	var len = x.length,
		i;
	if ( out.length !== len ) {
		throw new Error( 'ekurtosis()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	for ( i = 0; i < len; i++ ) {
		out.data[ i ] = EKURTOSIS( x.data[ i ] );
	}
	return out;
} // end FUNCTION ekurtosis()


// EXPORTS //

module.exports = ekurtosis;

},{"./number.js":1231}],1231:[function(require,module,exports){
'use strict';

// MODULES //

var isPositive = require( 'validate.io-positive-primitive' );


// EKURTOSIS //

/**
* FUNCTION ekurtosis( lambda )
*	Computes the distribution ekurtosis for a exponential distribution with parameter lambda.
*
* @param {Number} lambda - rate parameter
* @returns {Number} distribution ekurtosis
*/
function ekurtosis( lambda ) {
	if ( !isPositive( lambda ) ) {
		return NaN;
	}
	return 6;
} // end FUNCTION ekurtosis()


// EXPORTS

module.exports =  ekurtosis;

},{"validate.io-positive-primitive":1304}],1232:[function(require,module,exports){
'use strict';

// MODULES //

var EKURTOSIS = require( './number.js' );


// EKURTOSIS //

/**
* FUNCTION: ekurtosis( out, lambda )
*	Computes the distribution ekurtosis for parameters stored in a typed array.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} lambda - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function ekurtosis( out, lambda ) {
	var len = lambda.length,
		i;
	for ( i = 0; i < len; i++ ) {
		out[ i ] = EKURTOSIS( lambda[ i ] );
	}
	return out;
} // end FUNCTION ekurtosis()


// EXPORTS //

module.exports = ekurtosis;

},{"./number.js":1231}],1233:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Boolean} [options.copy] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'ekurtosis()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'ekurtosis()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'ekurtosis()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'ekurtosis()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'ekurtosis()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'ekurtosis()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":1294,"validate.io-function":1295,"validate.io-object":1302,"validate.io-string-primitive":1305}],1234:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],1235:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":1234,"dup":12}],1236:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],1237:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":1240,"./iget.js":1242,"./iset.js":1245,"./mget.js":1249,"./mset.js":1251,"./set.js":1259,"./sget.js":1261,"./sset.js":1263,"./toString.js":1265,"dup":14}],1238:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":1241,"./iget.raw.js":1243,"./iset.raw.js":1246,"./mget.raw.js":1250,"./mset.raw.js":1252,"./set.raw.js":1260,"./sget.raw.js":1262,"./sset.raw.js":1264,"./toString.js":1265,"dup":15}],1239:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],1240:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":1298}],1241:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],1242:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":1277}],1243:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],1244:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":1247,"./matrix.raw.js":1248,"dup":21}],1245:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":1277,"validate.io-number-primitive":1301}],1246:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],1247:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":1236,"./ctor.js":1237,"./dtypes.js":1239,"compute-cast-arrays":1266,"compute-dtype":1269,"dup":24,"validate.io-array":1274,"validate.io-contains":1275,"validate.io-nonnegative-integer-array":1278,"validate.io-string-primitive":1305}],1248:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":1236,"./ctor.raw.js":1238,"./dtypes.js":1239,"compute-dtype":1269,"dup":25,"validate.io-contains":1275,"validate.io-string-primitive":1305}],1249:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":1236,"dup":26,"validate.io-nonnegative-integer-array":1278}],1250:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":1236,"dup":27}],1251:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":1253,"./mset2.js":1254,"./mset3.js":1255,"./mset4.js":1256,"./mset5.js":1257,"./mset6.js":1258,"dup":28,"validate.io-function":1295,"validate.io-nonnegative-integer-array":1278,"validate.io-number-primitive":1301}],1252:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":1253,"./mset2.js":1254,"./mset3.js":1255,"./mset4.js":1256,"./mset5.js":1257,"./mset6.js":1258,"dup":29}],1253:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],1254:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],1255:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],1256:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],1257:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],1258:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],1259:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":1298,"validate.io-number-primitive":1301}],1260:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],1261:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":1236,"compute-indexspace":1273,"dup":38,"validate.io-string-primitive":1305}],1262:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":1236,"compute-indexspace":1273,"dup":39}],1263:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":1273,"dup":40,"validate.io-function":1295,"validate.io-number-primitive":1301,"validate.io-string-primitive":1305}],1264:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":1273,"dup":41}],1265:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],1266:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":1234,"compute-array-dtype/lib/dtypes":1267,"dup":43,"type-name":1268,"validate.io-array-like":1291}],1267:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1268:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1269:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":1271,"dup":46,"type-name":1272}],1270:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1271:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":1270,"dup":48}],1272:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1273:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":1298,"validate.io-string-primitive":1305}],1274:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1275:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":1274,"validate.io-nan-primitive":1276}],1276:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],1277:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1301}],1278:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":1274,"validate.io-nonnegative-integer":1298}],1279:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],1280:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],1281:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":1279,"./defaults.js":1280,"./validate.js":1283,"dup":60,"validate.io-array":1284,"validate.io-string-primitive":1305}],1282:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":1279,"./defaults.js":1280,"./factory.js":1281,"./validate.js":1283,"dup":61,"validate.io-array":1284,"validate.io-string-primitive":1305}],1283:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":1302,"validate.io-string-primitive":1305}],1284:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1285:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],1286:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],1287:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":1285,"./defaults.js":1286,"./validate.js":1289,"dup":66,"validate.io-array":1290,"validate.io-string-primitive":1305}],1288:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":1285,"./defaults.js":1286,"./factory.js":1287,"./validate.js":1289,"dup":67,"validate.io-array":1290,"validate.io-string-primitive":1305}],1289:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":1294,"validate.io-object":1302,"validate.io-string-primitive":1305}],1290:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1291:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":1292,"dup":70,"validate.io-integer-primitive":1293}],1292:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],1293:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1301}],1294:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],1295:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],1296:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],1297:[function(require,module,exports){
arguments[4][76][0].apply(exports,arguments)
},{"dup":76}],1298:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":1299}],1299:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":1300}],1300:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],1301:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],1302:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":1303}],1303:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1304:[function(require,module,exports){
arguments[4][1221][0].apply(exports,arguments)
},{"dup":1221,"validate.io-number-primitive":1301}],1305:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],1306:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":1307,"dup":81,"validate.io-integer-primitive":1308}],1307:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],1308:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1301}],1309:[function(require,module,exports){
'use strict';

// MODULES //

var MEAN = require( './number.js' );


// MEAN //

/**
* FUNCTION: mean( out, lambda, accessor )
*	Computes the distribution mean for parameters stored in an array using an accessor function.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} lambda - input array
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function mean( out, arr, clbk ) {
	var len = arr.length,
		v, i;
	for ( i = 0; i < len; i++ ) {
		v = clbk( arr[ i ], i );
		if ( typeof v === 'number' ) {
			out[ i ] = MEAN( v );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION mean()


// EXPORTS //

module.exports = mean;

},{"./number.js":1314}],1310:[function(require,module,exports){
'use strict';

// MODULES //

var MEAN = require( './number.js' );


// MEAN //

/**
* FUNCTION: mean( out, lambda )
*	Computes the distribution mean for parameters stored in an array.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} lambda - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function mean( out, arr ) {
	var len = arr.length,
		i;
	for ( i = 0; i < len; i++ ) {
		if ( typeof arr[ i ] === 'number' ) {
			out[ i ] = MEAN( arr[ i ] );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION mean()


// EXPORTS //

module.exports = mean;

},{"./number.js":1314}],1311:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory,
	MEAN  = require( './number.js' );


// MEAN //

/**
* FUNCTION: mean( lambda, path[, sep] )
*	Computes the distribution mean and deep sets the input array.
*
* @param {Array} lambda - input array
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function mean( lambda, path, sep ) {
	var len = lambda.length,
		opts = {},
		dget,
		dset,
		v, i;
	if ( arguments.length > 2 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		for ( i = 0; i < len; i++ ) {
			v = dget( lambda[ i ] );
			if ( typeof v === 'number' ) {
				dset( lambda[i], MEAN ( v ) );
			} else {
				dset( lambda[i], NaN );
			}
		}
	}
	return lambda;
} // end FUNCTION mean()


// EXPORTS //

module.exports = mean;

},{"./number.js":1314,"utils-deep-get":1365,"utils-deep-set":1371}],1312:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isnan = require( 'validate.io-nan' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var mean1 = require( './number.js' ),
	mean2 = require( './array.js' ),
	mean3 = require( './accessor.js' ),
	mean4 = require( './deepset.js' ),
	mean5 = require( './matrix.js' ),
	mean6 = require( './typedarray.js' );


// MEAN //

/**
* FUNCTION: mean( lambda[, opts] )
*	Computes the distribution mean.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} lambda - input value
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} distribution mean(s)
*/
function mean( lambda, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( isNumber( lambda ) || isnan( lambda ) ) {
		return mean1( lambda );
	}
	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isMatrixLike( lambda ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'mean()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( lambda.length );
			out = matrix( d, lambda.shape, dt );
		} else {
			out = lambda;
		}
		return mean5( out, lambda );
	}
	if ( isTypedArrayLike( lambda ) ) {
		if ( opts.copy === false ) {
			out = lambda;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'mean()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( lambda.length );
		}
		return mean6( out, lambda );
	}
	if ( isArrayLike( lambda ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return mean4( lambda, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = lambda;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'mean()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( lambda.length );
		}
		else {
			out = new Array( lambda.length );
		}
		if ( opts.accessor ) {
			return mean3( out, lambda, opts.accessor );
		}
		return mean2( out, lambda );
	}
	return NaN;
} // end FUNCTION mean()


// EXPORTS //

module.exports = mean;

},{"./accessor.js":1309,"./array.js":1310,"./deepset.js":1311,"./matrix.js":1313,"./number.js":1314,"./typedarray.js":1315,"./validate.js":1316,"compute-array-constructors":1318,"dstructs-matrix":1327,"validate.io-array-like":1374,"validate.io-matrix-like":1379,"validate.io-nan":1380,"validate.io-number-primitive":1384,"validate.io-typed-array-like":1389}],1313:[function(require,module,exports){
'use strict';

// MODULES //

var MEAN = require( './number.js' );


// MEAN //

/**
* FUNCTION: mean( out, x )
*	Computes the distribution mean for each parameter stored in a matrix.
*
* @param {Matrix} out - output matrix
* @param {Matrix} x - input matrix
* @returns {Matrix} output matrix
*/
function mean( out, x ) {
	var len = x.length,
		i;
	if ( out.length !== len ) {
		throw new Error( 'mean()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	for ( i = 0; i < len; i++ ) {
		out.data[ i ] = MEAN( x.data[ i ] );
	}
	return out;
} // end FUNCTION mean()


// EXPORTS //

module.exports = mean;

},{"./number.js":1314}],1314:[function(require,module,exports){
'use strict';

// MODULES //

var isPositive = require( 'validate.io-positive-primitive' );


// MEAN //

/**
* FUNCTION mean( lambda )
*	Computes the distribution mean for a Exponential with parameter lambda.
*
* @param {Number} lambda - rate parameter
* @returns {Number} distribution mean
*/
function mean( lambda ) {
	if ( !isPositive( lambda ) ) {
		return NaN;
	}
	return 1 / lambda;
} // end FUNCTION mean()


// EXPORTS

module.exports =  mean;

},{"validate.io-positive-primitive":1387}],1315:[function(require,module,exports){
'use strict';

// MODULES //

var MEAN = require( './number.js' );


// MEAN //

/**
* FUNCTION: mean( out, lambda )
*	Computes the distribution mean for parameters stored in a typed array.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} lambda - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function mean( out, lambda ) {
	var len = lambda.length,
		i;
	for ( i = 0; i < len; i++ ) {
		out[ i ] = MEAN( lambda[ i ] );
	}
	return out;
} // end FUNCTION mean()


// EXPORTS //

module.exports = mean;

},{"./number.js":1314}],1316:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Boolean} [options.copy] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'mean()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'mean()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'mean()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'mean()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'mean()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'mean()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":1377,"validate.io-function":1378,"validate.io-object":1385,"validate.io-string-primitive":1388}],1317:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],1318:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":1317,"dup":12}],1319:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],1320:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":1323,"./iget.js":1325,"./iset.js":1328,"./mget.js":1332,"./mset.js":1334,"./set.js":1342,"./sget.js":1344,"./sset.js":1346,"./toString.js":1348,"dup":14}],1321:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":1324,"./iget.raw.js":1326,"./iset.raw.js":1329,"./mget.raw.js":1333,"./mset.raw.js":1335,"./set.raw.js":1343,"./sget.raw.js":1345,"./sset.raw.js":1347,"./toString.js":1348,"dup":15}],1322:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],1323:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":1381}],1324:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],1325:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":1360}],1326:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],1327:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":1330,"./matrix.raw.js":1331,"dup":21}],1328:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":1360,"validate.io-number-primitive":1384}],1329:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],1330:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":1319,"./ctor.js":1320,"./dtypes.js":1322,"compute-cast-arrays":1349,"compute-dtype":1352,"dup":24,"validate.io-array":1357,"validate.io-contains":1358,"validate.io-nonnegative-integer-array":1361,"validate.io-string-primitive":1388}],1331:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":1319,"./ctor.raw.js":1321,"./dtypes.js":1322,"compute-dtype":1352,"dup":25,"validate.io-contains":1358,"validate.io-string-primitive":1388}],1332:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":1319,"dup":26,"validate.io-nonnegative-integer-array":1361}],1333:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":1319,"dup":27}],1334:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":1336,"./mset2.js":1337,"./mset3.js":1338,"./mset4.js":1339,"./mset5.js":1340,"./mset6.js":1341,"dup":28,"validate.io-function":1378,"validate.io-nonnegative-integer-array":1361,"validate.io-number-primitive":1384}],1335:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":1336,"./mset2.js":1337,"./mset3.js":1338,"./mset4.js":1339,"./mset5.js":1340,"./mset6.js":1341,"dup":29}],1336:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],1337:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],1338:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],1339:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],1340:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],1341:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],1342:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":1381,"validate.io-number-primitive":1384}],1343:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],1344:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":1319,"compute-indexspace":1356,"dup":38,"validate.io-string-primitive":1388}],1345:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":1319,"compute-indexspace":1356,"dup":39}],1346:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":1356,"dup":40,"validate.io-function":1378,"validate.io-number-primitive":1384,"validate.io-string-primitive":1388}],1347:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":1356,"dup":41}],1348:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],1349:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":1317,"compute-array-dtype/lib/dtypes":1350,"dup":43,"type-name":1351,"validate.io-array-like":1374}],1350:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1351:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1352:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":1354,"dup":46,"type-name":1355}],1353:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1354:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":1353,"dup":48}],1355:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1356:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":1381,"validate.io-string-primitive":1388}],1357:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1358:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":1357,"validate.io-nan-primitive":1359}],1359:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],1360:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1384}],1361:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":1357,"validate.io-nonnegative-integer":1381}],1362:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],1363:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],1364:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":1362,"./defaults.js":1363,"./validate.js":1366,"dup":60,"validate.io-array":1367,"validate.io-string-primitive":1388}],1365:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":1362,"./defaults.js":1363,"./factory.js":1364,"./validate.js":1366,"dup":61,"validate.io-array":1367,"validate.io-string-primitive":1388}],1366:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":1385,"validate.io-string-primitive":1388}],1367:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1368:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],1369:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],1370:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":1368,"./defaults.js":1369,"./validate.js":1372,"dup":66,"validate.io-array":1373,"validate.io-string-primitive":1388}],1371:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":1368,"./defaults.js":1369,"./factory.js":1370,"./validate.js":1372,"dup":67,"validate.io-array":1373,"validate.io-string-primitive":1388}],1372:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":1377,"validate.io-object":1385,"validate.io-string-primitive":1388}],1373:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1374:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":1375,"dup":70,"validate.io-integer-primitive":1376}],1375:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],1376:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1384}],1377:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],1378:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],1379:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],1380:[function(require,module,exports){
arguments[4][76][0].apply(exports,arguments)
},{"dup":76}],1381:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":1382}],1382:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":1383}],1383:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],1384:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],1385:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":1386}],1386:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1387:[function(require,module,exports){
arguments[4][1221][0].apply(exports,arguments)
},{"dup":1221,"validate.io-number-primitive":1384}],1388:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],1389:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":1390,"dup":81,"validate.io-integer-primitive":1391}],1390:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],1391:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1384}],1392:[function(require,module,exports){
'use strict';

// MODULES //

var MEDIAN = require( './number.js' );


// MEDIAN //

/**
* FUNCTION: median( out, arr, accessor )
*	Computes the distribution median for parameters stored in an array using an accessor function.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function median( out, arr, clbk ) {
	var len = arr.length,
		v, i;
	for ( i = 0; i < len; i++ ) {
		v = clbk( arr[ i ], i );
		if ( typeof v === 'number' ) {
			out[ i ] = MEDIAN( v );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION median()


// EXPORTS //

module.exports = median;

},{"./number.js":1397}],1393:[function(require,module,exports){
'use strict';

// MODULES //

var MEDIAN = require( './number.js' );


// MEDIAN //

/**
* FUNCTION: median( out, lambda )
*	Computes the distribution median for parameters stored in an array.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} lambda - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function median( out, arr ) {
	var len = arr.length,
		i;
	for ( i = 0; i < len; i++ ) {
		if ( typeof arr[ i ] === 'number' ) {
			out[ i ] = MEDIAN( arr[ i ] );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION median()


// EXPORTS //

module.exports = median;

},{"./number.js":1397}],1394:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory,
	MEDIAN  = require( './number.js' );


// MEDIAN //

/**
* FUNCTION: median( arr, path[, sep] )
*	Computes the distribution median and deep sets the input array.
*
* @param {Array} arrays - input array
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function median( arr, path, sep ) {
	var len = arr.length,
		opts = {},
		dget,
		dset,
		v, i;
	if ( arguments.length > 2 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		for ( i = 0; i < len; i++ ) {
			v = dget( arr[ i ] );
			if ( typeof v === 'number' ) {
				dset( arr[i], MEDIAN ( v ) );
			} else {
				dset( arr[i], NaN );
			}
		}
	}
	return arr;
} // end FUNCTION median()


// EXPORTS //

module.exports = median;

},{"./number.js":1397,"utils-deep-get":1448,"utils-deep-set":1454}],1395:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isnan = require( 'validate.io-nan' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var median1 = require( './number.js' ),
	median2 = require( './array.js' ),
	median3 = require( './accessor.js' ),
	median4 = require( './deepset.js' ),
	median5 = require( './matrix.js' ),
	median6 = require( './typedarray.js' );


// MEDIAN //

/**
* FUNCTION: median( lambda[, opts] )
*	Computes the distribution median.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} lambda - input value
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} distribution median(s)
*/
function median( lambda, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( isNumber( lambda ) || isnan( lambda ) ) {
		return median1( lambda );
	}
	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isMatrixLike( lambda ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'median()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( lambda.length );
			out = matrix( d, lambda.shape, dt );
		} else {
			out = lambda;
		}
		return median5( out, lambda );
	}
	if ( isTypedArrayLike( lambda ) ) {
		if ( opts.copy === false ) {
			out = lambda;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'median()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( lambda.length );
		}
		return median6( out, lambda );
	}
	if ( isArrayLike( lambda ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return median4( lambda, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = lambda;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'median()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( lambda.length );
		}
		else {
			out = new Array( lambda.length );
		}
		if ( opts.accessor ) {
			return median3( out, lambda, opts.accessor );
		}
		return median2( out, lambda );
	}
	return NaN;
} // end FUNCTION median()


// EXPORTS //

module.exports = median;

},{"./accessor.js":1392,"./array.js":1393,"./deepset.js":1394,"./matrix.js":1396,"./number.js":1397,"./typedarray.js":1398,"./validate.js":1399,"compute-array-constructors":1401,"dstructs-matrix":1410,"validate.io-array-like":1457,"validate.io-matrix-like":1462,"validate.io-nan":1463,"validate.io-number-primitive":1467,"validate.io-typed-array-like":1472}],1396:[function(require,module,exports){
'use strict';

// MODULES //

var MEDIAN = require( './number.js' );


// MEDIAN //

/**
* FUNCTION: median( out, x )
*	Computes the distribution median for each parameter stored in a matrix.
*
* @param {Matrix} out - output matrix
* @param {Matrix} x - input matrix
* @returns {Matrix} output matrix
*/
function median( out, x ) {
	var len = x.length,
		i;
	if ( out.length !== len ) {
		throw new Error( 'median()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	for ( i = 0; i < len; i++ ) {
		out.data[ i ] = MEDIAN( x.data[ i ] );
	}
	return out;
} // end FUNCTION median()


// EXPORTS //

module.exports = median;

},{"./number.js":1397}],1397:[function(require,module,exports){
'use strict';

// MODULES //

var isPositive = require( 'validate.io-positive-primitive' );


// FUNCTIONS //

var ln = Math.log;


// MEDIAN //

/**
* FUNCTION median( lambda )
*	Computes the distribution median for a exponential distribution with parameter lambda.
*
* @param {Number} lambda - rate parameter
* @returns {Number} distribution median
*/
function median( lambda ) {
	if ( !isPositive( lambda ) ) {
		return NaN;
	}
	return ( 1 / lambda ) * ln( 2 );
} // end FUNCTION median()


// EXPORTS

module.exports =  median;

},{"validate.io-positive-primitive":1470}],1398:[function(require,module,exports){
'use strict';

// MODULES //

var MEDIAN = require( './number.js' );


// MEDIAN //

/**
* FUNCTION: median( out, lambda )
*	Computes the distribution median for parameters stored in a typed array.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} lambda - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function median( out, lambda ) {
	var len = lambda.length,
		i;
	for ( i = 0; i < len; i++ ) {
		out[ i ] = MEDIAN( lambda[ i ] );
	}
	return out;
} // end FUNCTION median()


// EXPORTS //

module.exports = median;

},{"./number.js":1397}],1399:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Boolean} [options.copy] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'median()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'median()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'median()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'median()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'median()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'median()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":1460,"validate.io-function":1461,"validate.io-object":1468,"validate.io-string-primitive":1471}],1400:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],1401:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":1400,"dup":12}],1402:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],1403:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":1406,"./iget.js":1408,"./iset.js":1411,"./mget.js":1415,"./mset.js":1417,"./set.js":1425,"./sget.js":1427,"./sset.js":1429,"./toString.js":1431,"dup":14}],1404:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":1407,"./iget.raw.js":1409,"./iset.raw.js":1412,"./mget.raw.js":1416,"./mset.raw.js":1418,"./set.raw.js":1426,"./sget.raw.js":1428,"./sset.raw.js":1430,"./toString.js":1431,"dup":15}],1405:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],1406:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":1464}],1407:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],1408:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":1443}],1409:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],1410:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":1413,"./matrix.raw.js":1414,"dup":21}],1411:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":1443,"validate.io-number-primitive":1467}],1412:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],1413:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":1402,"./ctor.js":1403,"./dtypes.js":1405,"compute-cast-arrays":1432,"compute-dtype":1435,"dup":24,"validate.io-array":1440,"validate.io-contains":1441,"validate.io-nonnegative-integer-array":1444,"validate.io-string-primitive":1471}],1414:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":1402,"./ctor.raw.js":1404,"./dtypes.js":1405,"compute-dtype":1435,"dup":25,"validate.io-contains":1441,"validate.io-string-primitive":1471}],1415:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":1402,"dup":26,"validate.io-nonnegative-integer-array":1444}],1416:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":1402,"dup":27}],1417:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":1419,"./mset2.js":1420,"./mset3.js":1421,"./mset4.js":1422,"./mset5.js":1423,"./mset6.js":1424,"dup":28,"validate.io-function":1461,"validate.io-nonnegative-integer-array":1444,"validate.io-number-primitive":1467}],1418:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":1419,"./mset2.js":1420,"./mset3.js":1421,"./mset4.js":1422,"./mset5.js":1423,"./mset6.js":1424,"dup":29}],1419:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],1420:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],1421:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],1422:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],1423:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],1424:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],1425:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":1464,"validate.io-number-primitive":1467}],1426:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],1427:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":1402,"compute-indexspace":1439,"dup":38,"validate.io-string-primitive":1471}],1428:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":1402,"compute-indexspace":1439,"dup":39}],1429:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":1439,"dup":40,"validate.io-function":1461,"validate.io-number-primitive":1467,"validate.io-string-primitive":1471}],1430:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":1439,"dup":41}],1431:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],1432:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":1400,"compute-array-dtype/lib/dtypes":1433,"dup":43,"type-name":1434,"validate.io-array-like":1457}],1433:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1434:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1435:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":1437,"dup":46,"type-name":1438}],1436:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1437:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":1436,"dup":48}],1438:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1439:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":1464,"validate.io-string-primitive":1471}],1440:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1441:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":1440,"validate.io-nan-primitive":1442}],1442:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],1443:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1467}],1444:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":1440,"validate.io-nonnegative-integer":1464}],1445:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],1446:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],1447:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":1445,"./defaults.js":1446,"./validate.js":1449,"dup":60,"validate.io-array":1450,"validate.io-string-primitive":1471}],1448:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":1445,"./defaults.js":1446,"./factory.js":1447,"./validate.js":1449,"dup":61,"validate.io-array":1450,"validate.io-string-primitive":1471}],1449:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":1468,"validate.io-string-primitive":1471}],1450:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1451:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],1452:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],1453:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":1451,"./defaults.js":1452,"./validate.js":1455,"dup":66,"validate.io-array":1456,"validate.io-string-primitive":1471}],1454:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":1451,"./defaults.js":1452,"./factory.js":1453,"./validate.js":1455,"dup":67,"validate.io-array":1456,"validate.io-string-primitive":1471}],1455:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":1460,"validate.io-object":1468,"validate.io-string-primitive":1471}],1456:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1457:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":1458,"dup":70,"validate.io-integer-primitive":1459}],1458:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],1459:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1467}],1460:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],1461:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],1462:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],1463:[function(require,module,exports){
arguments[4][76][0].apply(exports,arguments)
},{"dup":76}],1464:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":1465}],1465:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":1466}],1466:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],1467:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],1468:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":1469}],1469:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1470:[function(require,module,exports){
arguments[4][1221][0].apply(exports,arguments)
},{"dup":1221,"validate.io-number-primitive":1467}],1471:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],1472:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":1473,"dup":81,"validate.io-integer-primitive":1474}],1473:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],1474:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1467}],1475:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// MGF //

/**
* FUNCTION: ( out, arr, lambda, accessor )
*	Evaluates the moment-generating function (MGF) for an exponential distribution with rate parameter `lambda` using an accessor function.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Number} lambda - rate parameter
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function mgf( y, x, lambda, clbk ) {
	var len = x.length,
		fcn,
		v, i;

	fcn = partial( lambda );
	for ( i = 0; i < len; i++ ) {
		v = clbk( x[ i ], i );
		if ( typeof v === 'number' ) {
			y[ i ] = fcn( v );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION mgf()


// EXPORTS //

module.exports = mgf;

},{"./partial.js":1481}],1476:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// MGF //

/**
* FUNCTION: mgf( out, arr, lambda )
*	Evaluates the moment-generating function (MGF) for an exponential distribution with rate parameter `lambda` for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Number} lambda - rate parameter
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function mgf( y, x, lambda ) {
	var len = x.length,
		fcn,
		i;

	fcn = partial( lambda );
	for ( i = 0; i < len; i++ ) {
		if ( typeof x[ i ] === 'number' ) {
			y[ i ] = fcn( x[ i ] );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION mgf()


// EXPORTS //

module.exports = mgf;

},{"./partial.js":1481}],1477:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory,
	partial = require( './partial.js' );


// MGF //

/**
* FUNCTION: mgf( arr, lambda, path[, sep] )
*	Evaluates the moment-generating function (MGF) for an exponential distribution with rate parameter `lambda` for each array element and sets the input array.
*
* @param {Array} arr - input array
* @param {Number} lambda - rate parameter
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function mgf( x, lambda, path, sep ) {
	var len = x.length,
		opts = {},
		dget,
		dset,
		fcn,
		v, i;
	if ( arguments.length > 3 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		fcn = partial( lambda );
		for ( i = 0; i < len; i++ ) {
			v = dget( x[ i ] );
			if ( typeof v === 'number' ) {
				dset( x[i], fcn( v ) );
			} else {
				dset( x[i], NaN );
			}
		}
	}
	return x;
} // end FUNCTION mgf()


// EXPORTS //

module.exports = mgf;

},{"./partial.js":1481,"utils-deep-get":1532,"utils-deep-set":1538}],1478:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var mgf1 = require( './number.js' ),
	mgf2 = require( './array.js' ),
	mgf3 = require( './accessor.js' ),
	mgf4 = require( './deepset.js' ),
	mgf5 = require( './matrix.js' ),
	mgf6 = require( './typedarray.js' );


// MGF //

/**
* FUNCTION: mgf( t[, opts] )
*	Evaluates the moment-generating function (MGF) for an exponential distribution.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} t - input value
* @param {Object} [opts] - function options
* @param {Number} [opts.lambda=1] - rate parameter
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} evaluated MGF
*/
function mgf( t, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	opts.lambda = typeof opts.lambda !== 'undefined' ? opts.lambda : 1;

	if ( isNumber( t ) ) {
		return mgf1( t, opts.lambda );
	}
	if ( isMatrixLike( t ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'mgf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( t.length );
			out = matrix( d, t.shape, dt );
		} else {
			out = t;
		}
		return mgf5( out, t, opts.lambda );
	}
	if ( isTypedArrayLike( t ) ) {
		if ( opts.copy === false ) {
			out = t;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'mgf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( t.length );
		}
		return mgf6( out, t, opts.lambda );
	}
	if ( isArrayLike( t ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return mgf4( t, opts.lambda, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = t;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'mgf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( t.length );
		}
		else {
			out = new Array( t.length );
		}
		if ( opts.accessor ) {
			return mgf3( out, t, opts.lambda, opts.accessor );
		}
		return mgf2( out, t, opts.lambda );
	}
	return NaN;
} // end FUNCTION mgf()


// EXPORTS //

module.exports = mgf;

},{"./accessor.js":1475,"./array.js":1476,"./deepset.js":1477,"./matrix.js":1479,"./number.js":1480,"./typedarray.js":1482,"./validate.js":1483,"compute-array-constructors":1485,"dstructs-matrix":1494,"validate.io-array-like":1541,"validate.io-matrix-like":1546,"validate.io-number-primitive":1550,"validate.io-typed-array-like":1555}],1479:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// MGF //

/**
* FUNCTION: mgf( out, matrix, lambda )
*	Evaluates the moment-generating function (MGF) for an exponential distribution with rate parameter `lambda` for each matrix element.
*
* @param {Matrix} out - output matrix
* @param {Matrix} arr - input matrix
* @param {Number} lambda - rate parameter
* @returns {Matrix} output matrix
*/
function mgf( y, x, lambda ) {
	var len = x.length,
		fcn,
		i;
	if ( y.length !== len ) {
		throw new Error( 'mgf()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	fcn = partial( lambda );
	for ( i = 0; i < len; i++ ) {
		y.data[ i ] = fcn( x.data[ i ] );
	}
	return y;
} // end FUNCTION mgf()


// EXPORTS //

module.exports = mgf;

},{"./partial.js":1481}],1480:[function(require,module,exports){
'use strict';

// FUNCTIONS //


// MGF //

/**
* FUNCTION: mgf( x, lambda )
*	Evaluates the moment-generating function (MGF) for an exponential distribution with rate parameter `lambda` at a value `t`.
*
* @param {Number} t - input value
* @param {Number} lambda - rate parameter
* @returns {Number} evaluated MGF
*/
function mgf( t, lambda ) {
	if ( t >= lambda ) {
		return NaN;
	}
	return lambda / ( lambda - t );
} // end FUNCTION mgf()


// EXPORTS //

module.exports = mgf;

},{}],1481:[function(require,module,exports){
'use strict';

// FUNCTIONS //


// PARTIAL //

/**
* FUNCTION: partial( lambda )
*	Partially applies rate parameter `lambda` and returns a function for evaluating the moment-generating function (MGF) for an exponential distribution.
*
* @param {Number} lambda - rate parameter
* @returns {Function} MGF
*/
function partial( lambda ) {

	/**
	* FUNCTION: mgf( t )
	*	Evaluates the moment-generating function (MGF) for an exponential distribution.
	*
	* @private
	* @param {Number} t - input value
	* @returns {Number} evaluated MGF
	*/
	return function mgf( t ) {
		if ( t >= lambda ) {
			return NaN;
		}
		return lambda / ( lambda - t );
	};
} // end FUNCTION partial()


// EXPORTS //

module.exports = partial;

},{}],1482:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// MGF //

/**
* FUNCTION: mgf( out, arr, lambda )
*	Evaluates the moment-generating function (MGF) for an exponential distribution with rate parameter `lambda` for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} arr - input array
* @param {Number} lambda - rate parameter
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function mgf( y, x, lambda ) {
	var len = x.length,
		fcn,
		i;

	fcn = partial ( lambda );
	for ( i = 0; i < len; i++ ) {
		y[ i ] = fcn( x[ i ] );
	}
	return y;
} // end FUNCTION mgf()


// EXPORTS //

module.exports = mgf;

},{"./partial.js":1481}],1483:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isPositive = require( 'validate.io-positive-primitive' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Number} [options.lambda] - rate parameter
* @param {Boolean} [options.copy] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'mgf()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'lambda' ) ) {
		opts.lambda = options.lambda;
		if ( !isPositive( opts.lambda ) ) {
			return new TypeError( 'mgf()::invalid option. `lambda` parameter must be a positive number. Option: `' + opts.lambda + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'mgf()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'mgf()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'mgf()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'mgf()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'mgf()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":1544,"validate.io-function":1545,"validate.io-object":1551,"validate.io-positive-primitive":1553,"validate.io-string-primitive":1554}],1484:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],1485:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":1484,"dup":12}],1486:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],1487:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":1490,"./iget.js":1492,"./iset.js":1495,"./mget.js":1499,"./mset.js":1501,"./set.js":1509,"./sget.js":1511,"./sset.js":1513,"./toString.js":1515,"dup":14}],1488:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":1491,"./iget.raw.js":1493,"./iset.raw.js":1496,"./mget.raw.js":1500,"./mset.raw.js":1502,"./set.raw.js":1510,"./sget.raw.js":1512,"./sset.raw.js":1514,"./toString.js":1515,"dup":15}],1489:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],1490:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":1547}],1491:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],1492:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":1527}],1493:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],1494:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":1497,"./matrix.raw.js":1498,"dup":21}],1495:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":1527,"validate.io-number-primitive":1550}],1496:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],1497:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":1486,"./ctor.js":1487,"./dtypes.js":1489,"compute-cast-arrays":1516,"compute-dtype":1519,"dup":24,"validate.io-array":1524,"validate.io-contains":1525,"validate.io-nonnegative-integer-array":1528,"validate.io-string-primitive":1554}],1498:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":1486,"./ctor.raw.js":1488,"./dtypes.js":1489,"compute-dtype":1519,"dup":25,"validate.io-contains":1525,"validate.io-string-primitive":1554}],1499:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":1486,"dup":26,"validate.io-nonnegative-integer-array":1528}],1500:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":1486,"dup":27}],1501:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":1503,"./mset2.js":1504,"./mset3.js":1505,"./mset4.js":1506,"./mset5.js":1507,"./mset6.js":1508,"dup":28,"validate.io-function":1545,"validate.io-nonnegative-integer-array":1528,"validate.io-number-primitive":1550}],1502:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":1503,"./mset2.js":1504,"./mset3.js":1505,"./mset4.js":1506,"./mset5.js":1507,"./mset6.js":1508,"dup":29}],1503:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],1504:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],1505:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],1506:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],1507:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],1508:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],1509:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":1547,"validate.io-number-primitive":1550}],1510:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],1511:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":1486,"compute-indexspace":1523,"dup":38,"validate.io-string-primitive":1554}],1512:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":1486,"compute-indexspace":1523,"dup":39}],1513:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":1523,"dup":40,"validate.io-function":1545,"validate.io-number-primitive":1550,"validate.io-string-primitive":1554}],1514:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":1523,"dup":41}],1515:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],1516:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":1484,"compute-array-dtype/lib/dtypes":1517,"dup":43,"type-name":1518,"validate.io-array-like":1541}],1517:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1518:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1519:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":1521,"dup":46,"type-name":1522}],1520:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1521:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":1520,"dup":48}],1522:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1523:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":1547,"validate.io-string-primitive":1554}],1524:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1525:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":1524,"validate.io-nan-primitive":1526}],1526:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],1527:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1550}],1528:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":1524,"validate.io-nonnegative-integer":1547}],1529:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],1530:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],1531:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":1529,"./defaults.js":1530,"./validate.js":1533,"dup":60,"validate.io-array":1534,"validate.io-string-primitive":1554}],1532:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":1529,"./defaults.js":1530,"./factory.js":1531,"./validate.js":1533,"dup":61,"validate.io-array":1534,"validate.io-string-primitive":1554}],1533:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":1551,"validate.io-string-primitive":1554}],1534:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1535:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],1536:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],1537:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":1535,"./defaults.js":1536,"./validate.js":1539,"dup":66,"validate.io-array":1540,"validate.io-string-primitive":1554}],1538:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":1535,"./defaults.js":1536,"./factory.js":1537,"./validate.js":1539,"dup":67,"validate.io-array":1540,"validate.io-string-primitive":1554}],1539:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":1544,"validate.io-object":1551,"validate.io-string-primitive":1554}],1540:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1541:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":1542,"dup":70,"validate.io-integer-primitive":1543}],1542:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],1543:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1550}],1544:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],1545:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],1546:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],1547:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":1548}],1548:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":1549}],1549:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],1550:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],1551:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":1552}],1552:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1553:[function(require,module,exports){
arguments[4][1221][0].apply(exports,arguments)
},{"dup":1221,"validate.io-number-primitive":1550}],1554:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],1555:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":1556,"dup":81,"validate.io-integer-primitive":1557}],1556:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],1557:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1550}],1558:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// PDF //

/**
* FUNCTION: pdf( out, arr, lambda, accessor )
*	Evaluates the probability density function (PDF) for an exponential distribution with rate parameter `lambda` using an accessor function.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Number} lambda - rate parameter
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function pdf( y, x, lambda, clbk ) {
	var len = x.length,
		fcn,
		v, i;

	fcn = partial( lambda );
	for ( i = 0; i < len; i++ ) {
		v = clbk( x[ i ], i );
		if ( typeof v === 'number' ) {
			y[ i ] = fcn( v );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./partial.js":1564}],1559:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// PDF //

/**
* FUNCTION: pdf( out, arr, lambda )
*	Evaluates the probability density function (PDF) for an exponential distribution with rate parameter `lambda` for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Number} lambda - rate parameter
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function pdf( y, x, lambda ) {
	var len = x.length,
		fcn,
		i;

	fcn = partial( lambda );
	for ( i = 0; i < len; i++ ) {
		if ( typeof x[ i ] === 'number' ) {
			y[ i ] = fcn( x[ i ] );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./partial.js":1564}],1560:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory,
	partial = require( './partial.js' );


// PDF //

/**
* FUNCTION: pdf( arr, lambda, path[, sep] )
*	Evaluates the probability density function (PDF) for an exponential distribution with rate parameter `lambda` for each array element and sets the input array.
*
* @param {Array} arr - input array
* @param {Number} lambda - rate parameter
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function pdf( x, lambda, path, sep ) {
	var len = x.length,
		opts = {},
		dget,
		dset,
		fcn,
		v, i;
	if ( arguments.length > 3 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		fcn = partial( lambda );
		for ( i = 0; i < len; i++ ) {
			v = dget( x[ i ] );
			if ( typeof v === 'number' ) {
				dset( x[i], fcn( v ) );
			} else {
				dset( x[i], NaN );
			}
		}
	}
	return x;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./partial.js":1564,"utils-deep-get":1615,"utils-deep-set":1621}],1561:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var pdf1 = require( './number.js' ),
	pdf2 = require( './array.js' ),
	pdf3 = require( './accessor.js' ),
	pdf4 = require( './deepset.js' ),
	pdf5 = require( './matrix.js' ),
	pdf6 = require( './typedarray.js' );


// PDF //

/**
* FUNCTION: pdf( x[, opts] )
*	Evaluates the probability density function (PDF) for an exponential distribution.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} x - input value
* @param {Object} [opts] - function options
* @param {Number} [opts.lambda=1] - rate parameter
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} evaluated PDF
*/
function pdf( x, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	opts.lambda = typeof opts.lambda !== 'undefined' ? opts.lambda : 1;

	if ( isNumber( x ) ) {
		return pdf1( x, opts.lambda );
	}
	if ( isMatrixLike( x ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'pdf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( x.length );
			out = matrix( d, x.shape, dt );
		} else {
			out = x;
		}
		return pdf5( out, x, opts.lambda );
	}
	if ( isTypedArrayLike( x ) ) {
		if ( opts.copy === false ) {
			out = x;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'pdf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( x.length );
		}
		return pdf6( out, x, opts.lambda );
	}
	if ( isArrayLike( x ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return pdf4( x, opts.lambda, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = x;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'pdf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( x.length );
		}
		else {
			out = new Array( x.length );
		}
		if ( opts.accessor ) {
			return pdf3( out, x, opts.lambda, opts.accessor );
		}
		return pdf2( out, x, opts.lambda );
	}
	return NaN;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./accessor.js":1558,"./array.js":1559,"./deepset.js":1560,"./matrix.js":1562,"./number.js":1563,"./typedarray.js":1565,"./validate.js":1566,"compute-array-constructors":1568,"dstructs-matrix":1577,"validate.io-array-like":1624,"validate.io-matrix-like":1629,"validate.io-number-primitive":1633,"validate.io-typed-array-like":1639}],1562:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// PDF //

/**
* FUNCTION: pdf( out, matrix, lambda )
*	Evaluates the probability density function (PDF) for an exponential distribution with rate parameter `lambda` for each matrix element.
*
* @param {Matrix} out - output matrix
* @param {Matrix} arr - input matrix
* @param {Number} lambda - rate parameter
* @returns {Matrix} output matrix
*/
function pdf( y, x, lambda ) {
	var len = x.length,
		fcn,
		i;
	if ( y.length !== len ) {
		throw new Error( 'pdf()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	fcn = partial( lambda );
	for ( i = 0; i < len; i++ ) {
		y.data[ i ] = fcn( x.data[ i ] );
	}
	return y;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./partial.js":1564}],1563:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var exp = Math.exp;


// PDF //

/**
* FUNCTION: pdf( x, lambda )
*	Evaluates the probability density function (PDF) for an exponential distribution with rate parameter `lambda` at a value `x`.
*
* @param {Number} x - input value
* @param {Number} lambda - rate parameter
* @returns {Number} evaluated PDF
*/
function pdf( x, lambda ) {
	if ( x >= 0 ) {
		return lambda * exp( -lambda*x );
	} else {
		return 0;
	}
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{}],1564:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var exp = Math.exp;


// PARTIAL //

/**
* FUNCTION: partial( lambda )
*	Partially applies rate parameter `lambda` and returns a function for evaluating the probability density function (PDF) for an exponential distribution.
*
* @param {Number} lambda - rate parameter
* @returns {Function} PDF
*/
function partial( lambda ) {

	/**
	* FUNCTION: pdf( x )
	*	Evaluates the probability density function (PDF) for an exponential distribution.
	*
	* @private
	* @param {Number} x - input value
	* @returns {Number} evaluated PDF
	*/
	return function pdf( x ) {
		if ( x >= 0 ) {
			return lambda * exp( -lambda*x );
		} else {
			return 0;
		}
	};
} // end FUNCTION partial()


// EXPORTS //

module.exports = partial;

},{}],1565:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// PDF //

/**
* FUNCTION: pdf( out, arr, lambda )
*	Evaluates the probability density function (PDF) for an exponential distribution with rate parameter `lambda` for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} arr - input array
* @param {Number} lambda - rate parameter
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function pdf( y, x, lambda ) {
	var len = x.length,
		fcn,
		i;

	fcn = partial ( lambda );
	for ( i = 0; i < len; i++ ) {
		y[ i ] = fcn( x[ i ] );
	}
	return y;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./partial.js":1564}],1566:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isPositive = require( 'validate.io-positive' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Number} [options.lambda] - rate parameter
* @param {Boolean} [options.copy] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'pdf()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'lambda' ) ) {
		opts.lambda = options.lambda;
		if ( !isPositive( opts.lambda ) ) {
			return new TypeError( 'pdf()::invalid option. `lambda` parameter must be a positive number. Option: `' + opts.lambda + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'pdf()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'pdf()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'pdf()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'pdf()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'pdf()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":1627,"validate.io-function":1628,"validate.io-object":1634,"validate.io-positive":1636,"validate.io-string-primitive":1638}],1567:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],1568:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":1567,"dup":12}],1569:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],1570:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":1573,"./iget.js":1575,"./iset.js":1578,"./mget.js":1582,"./mset.js":1584,"./set.js":1592,"./sget.js":1594,"./sset.js":1596,"./toString.js":1598,"dup":14}],1571:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":1574,"./iget.raw.js":1576,"./iset.raw.js":1579,"./mget.raw.js":1583,"./mset.raw.js":1585,"./set.raw.js":1593,"./sget.raw.js":1595,"./sset.raw.js":1597,"./toString.js":1598,"dup":15}],1572:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],1573:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":1630}],1574:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],1575:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":1610}],1576:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],1577:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":1580,"./matrix.raw.js":1581,"dup":21}],1578:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":1610,"validate.io-number-primitive":1633}],1579:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],1580:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":1569,"./ctor.js":1570,"./dtypes.js":1572,"compute-cast-arrays":1599,"compute-dtype":1602,"dup":24,"validate.io-array":1607,"validate.io-contains":1608,"validate.io-nonnegative-integer-array":1611,"validate.io-string-primitive":1638}],1581:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":1569,"./ctor.raw.js":1571,"./dtypes.js":1572,"compute-dtype":1602,"dup":25,"validate.io-contains":1608,"validate.io-string-primitive":1638}],1582:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":1569,"dup":26,"validate.io-nonnegative-integer-array":1611}],1583:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":1569,"dup":27}],1584:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":1586,"./mset2.js":1587,"./mset3.js":1588,"./mset4.js":1589,"./mset5.js":1590,"./mset6.js":1591,"dup":28,"validate.io-function":1628,"validate.io-nonnegative-integer-array":1611,"validate.io-number-primitive":1633}],1585:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":1586,"./mset2.js":1587,"./mset3.js":1588,"./mset4.js":1589,"./mset5.js":1590,"./mset6.js":1591,"dup":29}],1586:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],1587:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],1588:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],1589:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],1590:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],1591:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],1592:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":1630,"validate.io-number-primitive":1633}],1593:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],1594:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":1569,"compute-indexspace":1606,"dup":38,"validate.io-string-primitive":1638}],1595:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":1569,"compute-indexspace":1606,"dup":39}],1596:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":1606,"dup":40,"validate.io-function":1628,"validate.io-number-primitive":1633,"validate.io-string-primitive":1638}],1597:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":1606,"dup":41}],1598:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],1599:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":1567,"compute-array-dtype/lib/dtypes":1600,"dup":43,"type-name":1601,"validate.io-array-like":1624}],1600:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1601:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1602:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":1604,"dup":46,"type-name":1605}],1603:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1604:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":1603,"dup":48}],1605:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1606:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":1630,"validate.io-string-primitive":1638}],1607:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1608:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":1607,"validate.io-nan-primitive":1609}],1609:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],1610:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1633}],1611:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":1607,"validate.io-nonnegative-integer":1630}],1612:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],1613:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],1614:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":1612,"./defaults.js":1613,"./validate.js":1616,"dup":60,"validate.io-array":1617,"validate.io-string-primitive":1638}],1615:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":1612,"./defaults.js":1613,"./factory.js":1614,"./validate.js":1616,"dup":61,"validate.io-array":1617,"validate.io-string-primitive":1638}],1616:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":1634,"validate.io-string-primitive":1638}],1617:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1618:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],1619:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],1620:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":1618,"./defaults.js":1619,"./validate.js":1622,"dup":66,"validate.io-array":1623,"validate.io-string-primitive":1638}],1621:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":1618,"./defaults.js":1619,"./factory.js":1620,"./validate.js":1622,"dup":67,"validate.io-array":1623,"validate.io-string-primitive":1638}],1622:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":1627,"validate.io-object":1634,"validate.io-string-primitive":1638}],1623:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1624:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":1625,"dup":70,"validate.io-integer-primitive":1626}],1625:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],1626:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1633}],1627:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],1628:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],1629:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],1630:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":1631}],1631:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":1632}],1632:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],1633:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],1634:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":1635}],1635:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1636:[function(require,module,exports){
/**
*
*	VALIDATE: positive
*
*
*	DESCRIPTION:
*		- Validates if a value is a positive number.
*
*
*	NOTES:
*		[1]
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2015. Athan Reines.
*
*
*	AUTHOR:
*		Athan Reines. kgryte@gmail.com. 2015.
*
*/

'use strict';

// MODULES //

var isNumber = require( 'validate.io-number' );


// ISPOSITIVE //

/**
* FUNCTION: isPositive( value )
*	Validates if a value is a positive number.
*
* @param {*} value - value to be validated
* @returns {Boolean} boolean indicating if a value is a positive number
*/
function isPositive( value ) {
	return isNumber( value ) && value > 0;
} // end FUNCTION isPositive()


// EXPORTS //

module.exports = isPositive;

},{"validate.io-number":1637}],1637:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],1638:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],1639:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":1640,"dup":81,"validate.io-integer-primitive":1641}],1640:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],1641:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1633}],1642:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// QUANTILE //

/**
* FUNCTION: quantile( out, arr, lambda, accessor )
*	Evaluates the quantile function for an exponential distribution with rate parameter `lambda` using an accessor function.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Number} lambda - rate parameter
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function quantile( y, x, lambda, clbk ) {
	var len = x.length,
		fcn,
		v, i;

	fcn = partial( lambda );
	for ( i = 0; i < len; i++ ) {
		v = clbk( x[ i ], i );
		if ( typeof v === 'number' ) {
			y[ i ] = fcn( v );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION quantile()


// EXPORTS //

module.exports = quantile;

},{"./partial.js":1648}],1643:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// QUANTILE //

/**
* FUNCTION: quantile( out, arr, lambda )
*	Evaluates the quantile function for an exponential distribution with rate parameter `lambda` for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Number} lambda - rate parameter
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function quantile( y, x, lambda ) {
	var len = x.length,
		fcn,
		i;

	fcn = partial( lambda );
	for ( i = 0; i < len; i++ ) {
		if ( typeof x[ i ] === 'number' ) {
			y[ i ] = fcn( x[ i ] );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION quantile()


// EXPORTS //

module.exports = quantile;

},{"./partial.js":1648}],1644:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory,
	partial = require( './partial.js' );


// QUANTILE //

/**
* FUNCTION: quantile( arr, lambda, path[, sep] )
*	Evaluates the quantile function for an exponential distribution with rate parameter `lambda` for each array element and sets the input array.
*
* @param {Array} arr - input array
* @param {Number} lambda - rate parameter
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function quantile( x, lambda, path, sep ) {
	var len = x.length,
		opts = {},
		dget,
		dset,
		fcn,
		v, i;
	if ( arguments.length > 3 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		fcn = partial( lambda );
		for ( i = 0; i < len; i++ ) {
			v = dget( x[ i ] );
			if ( typeof v === 'number' ) {
				dset( x[i], fcn( v ) );
			} else {
				dset( x[i], NaN );
			}
		}
	}
	return x;
} // end FUNCTION quantile()


// EXPORTS //

module.exports = quantile;

},{"./partial.js":1648,"utils-deep-get":1699,"utils-deep-set":1705}],1645:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var quantile1 = require( './number.js' ),
	quantile2 = require( './array.js' ),
	quantile3 = require( './accessor.js' ),
	quantile4 = require( './deepset.js' ),
	quantile5 = require( './matrix.js' ),
	quantile6 = require( './typedarray.js' );


// PDF //

/**
* FUNCTION: quantile( p[, opts] )
*	Evaluates the quantile function for an exponential distribution.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} p - input value
* @param {Object} [opts] - function options
* @param {Number} [opts.lambda=1] - rate parameter
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} quantile function value(s)
*/
function quantile( p, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	opts.lambda = typeof opts.lambda !== 'undefined' ? opts.lambda : 1;

	if ( isNumber( p ) ) {
		return quantile1( p, opts.lambda );
	}
	if ( isMatrixLike( p ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'quantile()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( p.length );
			out = matrix( d, p.shape, dt );
		} else {
			out = p;
		}
		return quantile5( out, p, opts.lambda );
	}
	if ( isTypedArrayLike( p ) ) {
		if ( opts.copy === false ) {
			out = p;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'quantile()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( p.length );
		}
		return quantile6( out, p, opts.lambda );
	}
	if ( isArrayLike( p ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return quantile4( p, opts.lambda, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = p;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'quantile()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( p.length );
		}
		else {
			out = new Array( p.length );
		}
		if ( opts.accessor ) {
			return quantile3( out, p, opts.lambda, opts.accessor );
		}
		return quantile2( out, p, opts.lambda );
	}
	return NaN;
} // end FUNCTION quantile()


// EXPORTS //

module.exports = quantile;

},{"./accessor.js":1642,"./array.js":1643,"./deepset.js":1644,"./matrix.js":1646,"./number.js":1647,"./typedarray.js":1649,"./validate.js":1650,"compute-array-constructors":1652,"dstructs-matrix":1661,"validate.io-array-like":1708,"validate.io-matrix-like":1713,"validate.io-number-primitive":1717,"validate.io-typed-array-like":1722}],1646:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// QUANTILE //

/**
* FUNCTION: quantile( out, matrix, lambda )
*	Evaluates the quantile function for an exponential distribution with rate parameter `lambda` for each matrix element.
*
* @param {Matrix} out - output matrix
* @param {Matrix} arr - input matrix
* @param {Number} lambda - rate parameter
* @returns {Matrix} output matrix
*/
function quantile( y, x, lambda ) {
	var len = x.length,
		fcn,
		i;
	if ( y.length !== len ) {
		throw new Error( 'quantile()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	fcn = partial( lambda );
	for ( i = 0; i < len; i++ ) {
		y.data[ i ] = fcn( x.data[ i ] );
	}
	return y;
} // end FUNCTION quantile()


// EXPORTS //

module.exports = quantile;

},{"./partial.js":1648}],1647:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var ln = Math.log;


// QUANTILE //

/**
* FUNCTION: quantile( p, lambda )
*	Evaluates the quantile function for an exponential distribution with rate parameter `lambda` at a probability `p`.
*
* @param {Number} p - input value
* @param {Number} lambda - rate parameter
* @returns {Number} evaluated quantile function
*/
function quantile( p, lambda ) {
	if ( p !== p || p < 0 || p > 1 ) {
		return NaN;
	}
	return - ln( 1 - p ) / lambda;
} // end FUNCTION quantile()


// EXPORTS //

module.exports = quantile;

},{}],1648:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var ln = Math.log;


// PARTIAL //

/**
* FUNCTION: partial( lambda )
*	Partially applies rate parameter `lambda` and returns a function for evaluating the quantile function for an exponential distribution.
*
* @param {Number} lambda - rate parameter
* @returns {Function} quantile function
*/
function partial( lambda ) {

	/**
	* FUNCTION: quantile( p )
	*	Evaluates the quantile function for an exponential distribution.
	*
	* @private
	* @param {Number} p - input value
	* @returns {Number} evaluated quantile function
	*/
	return function quantile( p ) {
		if ( p !== p || p < 0 || p > 1 ) {
			return NaN;
		}
		return - ln( 1 - p ) / lambda;
	};
} // end FUNCTION partial()


// EXPORTS //

module.exports = partial;

},{}],1649:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// QUANTILE //

/**
* FUNCTION: quantile( out, arr, lambda )
*	Evaluates the quantile function for an exponential distribution with rate parameter `lambda` for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} arr - input array
* @param {Number} lambda - rate parameter
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function quantile( y, x, lambda ) {
	var len = x.length,
		fcn,
		i;

	fcn = partial ( lambda );
	for ( i = 0; i < len; i++ ) {
		y[ i ] = fcn( x[ i ] );
	}
	return y;
} // end FUNCTION quantile()


// EXPORTS //

module.exports = quantile;

},{"./partial.js":1648}],1650:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isPositive = require( 'validate.io-positive-primitive' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Number} [options.lambda] - rate parameter
* @param {Boolean} [options.copy] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'quantile()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'lambda' ) ) {
		opts.lambda = options.lambda;
		if ( !isPositive( opts.lambda ) ) {
			return new TypeError( 'quantile()::invalid option. Rate parameter `lambda` must be a positive number. Option: `' + opts.lambda + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'quantile()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'quantile()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'quantile()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'quantile()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'quantile()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":1711,"validate.io-function":1712,"validate.io-object":1718,"validate.io-positive-primitive":1720,"validate.io-string-primitive":1721}],1651:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],1652:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":1651,"dup":12}],1653:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],1654:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":1657,"./iget.js":1659,"./iset.js":1662,"./mget.js":1666,"./mset.js":1668,"./set.js":1676,"./sget.js":1678,"./sset.js":1680,"./toString.js":1682,"dup":14}],1655:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":1658,"./iget.raw.js":1660,"./iset.raw.js":1663,"./mget.raw.js":1667,"./mset.raw.js":1669,"./set.raw.js":1677,"./sget.raw.js":1679,"./sset.raw.js":1681,"./toString.js":1682,"dup":15}],1656:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],1657:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":1714}],1658:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],1659:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":1694}],1660:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],1661:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":1664,"./matrix.raw.js":1665,"dup":21}],1662:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":1694,"validate.io-number-primitive":1717}],1663:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],1664:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":1653,"./ctor.js":1654,"./dtypes.js":1656,"compute-cast-arrays":1683,"compute-dtype":1686,"dup":24,"validate.io-array":1691,"validate.io-contains":1692,"validate.io-nonnegative-integer-array":1695,"validate.io-string-primitive":1721}],1665:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":1653,"./ctor.raw.js":1655,"./dtypes.js":1656,"compute-dtype":1686,"dup":25,"validate.io-contains":1692,"validate.io-string-primitive":1721}],1666:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":1653,"dup":26,"validate.io-nonnegative-integer-array":1695}],1667:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":1653,"dup":27}],1668:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":1670,"./mset2.js":1671,"./mset3.js":1672,"./mset4.js":1673,"./mset5.js":1674,"./mset6.js":1675,"dup":28,"validate.io-function":1712,"validate.io-nonnegative-integer-array":1695,"validate.io-number-primitive":1717}],1669:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":1670,"./mset2.js":1671,"./mset3.js":1672,"./mset4.js":1673,"./mset5.js":1674,"./mset6.js":1675,"dup":29}],1670:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],1671:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],1672:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],1673:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],1674:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],1675:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],1676:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":1714,"validate.io-number-primitive":1717}],1677:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],1678:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":1653,"compute-indexspace":1690,"dup":38,"validate.io-string-primitive":1721}],1679:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":1653,"compute-indexspace":1690,"dup":39}],1680:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":1690,"dup":40,"validate.io-function":1712,"validate.io-number-primitive":1717,"validate.io-string-primitive":1721}],1681:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":1690,"dup":41}],1682:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],1683:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":1651,"compute-array-dtype/lib/dtypes":1684,"dup":43,"type-name":1685,"validate.io-array-like":1708}],1684:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1685:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1686:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":1688,"dup":46,"type-name":1689}],1687:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1688:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":1687,"dup":48}],1689:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1690:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":1714,"validate.io-string-primitive":1721}],1691:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1692:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":1691,"validate.io-nan-primitive":1693}],1693:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],1694:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1717}],1695:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":1691,"validate.io-nonnegative-integer":1714}],1696:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],1697:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],1698:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":1696,"./defaults.js":1697,"./validate.js":1700,"dup":60,"validate.io-array":1701,"validate.io-string-primitive":1721}],1699:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":1696,"./defaults.js":1697,"./factory.js":1698,"./validate.js":1700,"dup":61,"validate.io-array":1701,"validate.io-string-primitive":1721}],1700:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":1718,"validate.io-string-primitive":1721}],1701:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1702:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],1703:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],1704:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":1702,"./defaults.js":1703,"./validate.js":1706,"dup":66,"validate.io-array":1707,"validate.io-string-primitive":1721}],1705:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":1702,"./defaults.js":1703,"./factory.js":1704,"./validate.js":1706,"dup":67,"validate.io-array":1707,"validate.io-string-primitive":1721}],1706:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":1711,"validate.io-object":1718,"validate.io-string-primitive":1721}],1707:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1708:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":1709,"dup":70,"validate.io-integer-primitive":1710}],1709:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],1710:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1717}],1711:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],1712:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],1713:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],1714:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":1715}],1715:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":1716}],1716:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],1717:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],1718:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":1719}],1719:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1720:[function(require,module,exports){
arguments[4][1221][0].apply(exports,arguments)
},{"dup":1221,"validate.io-number-primitive":1717}],1721:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],1722:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":1723,"dup":81,"validate.io-integer-primitive":1724}],1723:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],1724:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1717}],1725:[function(require,module,exports){
'use strict';

// MODULES //

var SKEWNESS = require( './number.js' );


// SKEWNESS //

/**
* FUNCTION: skewness( out, arr, accessor )
*	Computes the distribution skewness for parameters stored in an array using an accessor function.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function skewness( out, arr, clbk ) {
	var len = arr.length,
		v, i;
	for ( i = 0; i < len; i++ ) {
		v = clbk( arr[ i ], i );
		if ( typeof v === 'number' ) {
			out[ i ] = SKEWNESS( v );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION skewness()


// EXPORTS //

module.exports = skewness;

},{"./number.js":1730}],1726:[function(require,module,exports){
'use strict';

// MODULES //

var SKEWNESS = require( './number.js' );


// SKEWNESS //

/**
* FUNCTION: skewness( out, lambda )
*	Computes the distribution skewness for parameters stored in an array.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} lambda - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function skewness( out, arr ) {
	var len = arr.length,
		i;
	for ( i = 0; i < len; i++ ) {
		if ( typeof arr[ i ] === 'number' ) {
			out[ i ] = SKEWNESS( arr[ i ] );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION skewness()


// EXPORTS //

module.exports = skewness;

},{"./number.js":1730}],1727:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory,
	SKEWNESS  = require( './number.js' );


// SKEWNESS //

/**
* FUNCTION: skewness( arr, path[, sep] )
*	Computes the distribution skewness and deep sets the input array.
*
* @param {Array} arrays - input array
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function skewness( arr, path, sep ) {
	var len = arr.length,
		opts = {},
		dget,
		dset,
		v, i;
	if ( arguments.length > 2 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		for ( i = 0; i < len; i++ ) {
			v = dget( arr[ i ] );
			if ( typeof v === 'number' ) {
				dset( arr[i], SKEWNESS ( v ) );
			} else {
				dset( arr[i], NaN );
			}
		}
	}
	return arr;
} // end FUNCTION skewness()


// EXPORTS //

module.exports = skewness;

},{"./number.js":1730,"utils-deep-get":1781,"utils-deep-set":1787}],1728:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isnan = require( 'validate.io-nan' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var skewness1 = require( './number.js' ),
	skewness2 = require( './array.js' ),
	skewness3 = require( './accessor.js' ),
	skewness4 = require( './deepset.js' ),
	skewness5 = require( './matrix.js' ),
	skewness6 = require( './typedarray.js' );


// SKEWNESS //

/**
* FUNCTION: skewness( lambda[, opts] )
*	Computes the distribution skewness.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} lambda - input value
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} distribution skewness(s)
*/
function skewness( lambda, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( isNumber( lambda ) || isnan( lambda ) ) {
		return skewness1( lambda );
	}
	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isMatrixLike( lambda ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'skewness()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( lambda.length );
			out = matrix( d, lambda.shape, dt );
		} else {
			out = lambda;
		}
		return skewness5( out, lambda );
	}
	if ( isTypedArrayLike( lambda ) ) {
		if ( opts.copy === false ) {
			out = lambda;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'skewness()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( lambda.length );
		}
		return skewness6( out, lambda );
	}
	if ( isArrayLike( lambda ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return skewness4( lambda, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = lambda;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'skewness()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( lambda.length );
		}
		else {
			out = new Array( lambda.length );
		}
		if ( opts.accessor ) {
			return skewness3( out, lambda, opts.accessor );
		}
		return skewness2( out, lambda );
	}
	return NaN;
} // end FUNCTION skewness()


// EXPORTS //

module.exports = skewness;

},{"./accessor.js":1725,"./array.js":1726,"./deepset.js":1727,"./matrix.js":1729,"./number.js":1730,"./typedarray.js":1731,"./validate.js":1732,"compute-array-constructors":1734,"dstructs-matrix":1743,"validate.io-array-like":1790,"validate.io-matrix-like":1795,"validate.io-nan":1796,"validate.io-number-primitive":1800,"validate.io-typed-array-like":1805}],1729:[function(require,module,exports){
'use strict';

// MODULES //

var SKEWNESS = require( './number.js' );


// SKEWNESS //

/**
* FUNCTION: skewness( out, x )
*	Computes the distribution skewness for each parameter stored in a matrix.
*
* @param {Matrix} out - output matrix
* @param {Matrix} x - input matrix
* @returns {Matrix} output matrix
*/
function skewness( out, x ) {
	var len = x.length,
		i;
	if ( out.length !== len ) {
		throw new Error( 'skewness()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	for ( i = 0; i < len; i++ ) {
		out.data[ i ] = SKEWNESS( x.data[ i ] );
	}
	return out;
} // end FUNCTION skewness()


// EXPORTS //

module.exports = skewness;

},{"./number.js":1730}],1730:[function(require,module,exports){
'use strict';

// MODULES //

var isPositive = require( 'validate.io-positive-primitive' );


// SKEWNESS //

/**
* FUNCTION skewness( lambda )
*	Computes the distribution skewness for a exponential distribution with parameter lambda.
*
* @param {Number} lambda - rate parameter
* @returns {Number} distribution skewness
*/
function skewness( lambda ) {
	if ( !isPositive( lambda ) ) {
		return NaN;
	}
	return 2;
} // end FUNCTION skewness()


// EXPORTS

module.exports =  skewness;

},{"validate.io-positive-primitive":1803}],1731:[function(require,module,exports){
'use strict';

// MODULES //

var SKEWNESS = require( './number.js' );


// SKEWNESS //

/**
* FUNCTION: skewness( out, lambda )
*	Computes the distribution skewness for parameters stored in a typed array.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} lambda - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function skewness( out, lambda ) {
	var len = lambda.length,
		i;
	for ( i = 0; i < len; i++ ) {
		out[ i ] = SKEWNESS( lambda[ i ] );
	}
	return out;
} // end FUNCTION skewness()


// EXPORTS //

module.exports = skewness;

},{"./number.js":1730}],1732:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Boolean} [options.copy] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'skewness()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'skewness()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'skewness()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'skewness()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'skewness()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'skewness()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":1793,"validate.io-function":1794,"validate.io-object":1801,"validate.io-string-primitive":1804}],1733:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],1734:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":1733,"dup":12}],1735:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],1736:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":1739,"./iget.js":1741,"./iset.js":1744,"./mget.js":1748,"./mset.js":1750,"./set.js":1758,"./sget.js":1760,"./sset.js":1762,"./toString.js":1764,"dup":14}],1737:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":1740,"./iget.raw.js":1742,"./iset.raw.js":1745,"./mget.raw.js":1749,"./mset.raw.js":1751,"./set.raw.js":1759,"./sget.raw.js":1761,"./sset.raw.js":1763,"./toString.js":1764,"dup":15}],1738:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],1739:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":1797}],1740:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],1741:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":1776}],1742:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],1743:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":1746,"./matrix.raw.js":1747,"dup":21}],1744:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":1776,"validate.io-number-primitive":1800}],1745:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],1746:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":1735,"./ctor.js":1736,"./dtypes.js":1738,"compute-cast-arrays":1765,"compute-dtype":1768,"dup":24,"validate.io-array":1773,"validate.io-contains":1774,"validate.io-nonnegative-integer-array":1777,"validate.io-string-primitive":1804}],1747:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":1735,"./ctor.raw.js":1737,"./dtypes.js":1738,"compute-dtype":1768,"dup":25,"validate.io-contains":1774,"validate.io-string-primitive":1804}],1748:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":1735,"dup":26,"validate.io-nonnegative-integer-array":1777}],1749:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":1735,"dup":27}],1750:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":1752,"./mset2.js":1753,"./mset3.js":1754,"./mset4.js":1755,"./mset5.js":1756,"./mset6.js":1757,"dup":28,"validate.io-function":1794,"validate.io-nonnegative-integer-array":1777,"validate.io-number-primitive":1800}],1751:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":1752,"./mset2.js":1753,"./mset3.js":1754,"./mset4.js":1755,"./mset5.js":1756,"./mset6.js":1757,"dup":29}],1752:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],1753:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],1754:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],1755:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],1756:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],1757:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],1758:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":1797,"validate.io-number-primitive":1800}],1759:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],1760:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":1735,"compute-indexspace":1772,"dup":38,"validate.io-string-primitive":1804}],1761:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":1735,"compute-indexspace":1772,"dup":39}],1762:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":1772,"dup":40,"validate.io-function":1794,"validate.io-number-primitive":1800,"validate.io-string-primitive":1804}],1763:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":1772,"dup":41}],1764:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],1765:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":1733,"compute-array-dtype/lib/dtypes":1766,"dup":43,"type-name":1767,"validate.io-array-like":1790}],1766:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1767:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1768:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":1770,"dup":46,"type-name":1771}],1769:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1770:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":1769,"dup":48}],1771:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1772:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":1797,"validate.io-string-primitive":1804}],1773:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1774:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":1773,"validate.io-nan-primitive":1775}],1775:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],1776:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1800}],1777:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":1773,"validate.io-nonnegative-integer":1797}],1778:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],1779:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],1780:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":1778,"./defaults.js":1779,"./validate.js":1782,"dup":60,"validate.io-array":1783,"validate.io-string-primitive":1804}],1781:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":1778,"./defaults.js":1779,"./factory.js":1780,"./validate.js":1782,"dup":61,"validate.io-array":1783,"validate.io-string-primitive":1804}],1782:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":1801,"validate.io-string-primitive":1804}],1783:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1784:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],1785:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],1786:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":1784,"./defaults.js":1785,"./validate.js":1788,"dup":66,"validate.io-array":1789,"validate.io-string-primitive":1804}],1787:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":1784,"./defaults.js":1785,"./factory.js":1786,"./validate.js":1788,"dup":67,"validate.io-array":1789,"validate.io-string-primitive":1804}],1788:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":1793,"validate.io-object":1801,"validate.io-string-primitive":1804}],1789:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1790:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":1791,"dup":70,"validate.io-integer-primitive":1792}],1791:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],1792:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1800}],1793:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],1794:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],1795:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],1796:[function(require,module,exports){
arguments[4][76][0].apply(exports,arguments)
},{"dup":76}],1797:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":1798}],1798:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":1799}],1799:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],1800:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],1801:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":1802}],1802:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1803:[function(require,module,exports){
arguments[4][1221][0].apply(exports,arguments)
},{"dup":1221,"validate.io-number-primitive":1800}],1804:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],1805:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":1806,"dup":81,"validate.io-integer-primitive":1807}],1806:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],1807:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1800}],1808:[function(require,module,exports){
'use strict';

// MODULES //

var VARIANCE = require( './number.js' );


// VARIANCE //

/**
* FUNCTION: variance( out, arr, accessor )
*	Computes the distribution variance for parameters stored in an array using an accessor function.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function variance( out, arr, clbk ) {
	var len = arr.length,
		v, i;
	for ( i = 0; i < len; i++ ) {
		v = clbk( arr[ i ], i );
		if ( typeof v === 'number' ) {
			out[ i ] = VARIANCE( v );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION variance()


// EXPORTS //

module.exports = variance;

},{"./number.js":1813}],1809:[function(require,module,exports){
'use strict';

// MODULES //

var VARIANCE = require( './number.js' );


// VARIANCE //

/**
* FUNCTION: variance( out, lambda )
*	Computes the distribution variance for parameters stored in an array.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} lambda - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function variance( out, arr ) {
	var len = arr.length,
		i;
	for ( i = 0; i < len; i++ ) {
		if ( typeof arr[ i ] === 'number' ) {
			out[ i ] = VARIANCE( arr[ i ] );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION variance()


// EXPORTS //

module.exports = variance;

},{"./number.js":1813}],1810:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory,
	VARIANCE  = require( './number.js' );


// VARIANCE //

/**
* FUNCTION: variance( arr, path[, sep] )
*	Computes the distribution variance and deep sets the input array.
*
* @param {Array} arrays - input array
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function variance( arr, path, sep ) {
	var len = arr.length,
		opts = {},
		dget,
		dset,
		v, i;
	if ( arguments.length > 2 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		for ( i = 0; i < len; i++ ) {
			v = dget( arr[ i ] );
			if ( typeof v === 'number' ) {
				dset( arr[i], VARIANCE ( v ) );
			} else {
				dset( arr[i], NaN );
			}
		}
	}
	return arr;
} // end FUNCTION variance()


// EXPORTS //

module.exports = variance;

},{"./number.js":1813,"utils-deep-get":1864,"utils-deep-set":1870}],1811:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isnan = require( 'validate.io-nan' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var variance1 = require( './number.js' ),
	variance2 = require( './array.js' ),
	variance3 = require( './accessor.js' ),
	variance4 = require( './deepset.js' ),
	variance5 = require( './matrix.js' ),
	variance6 = require( './typedarray.js' );


// VARIANCE //

/**
* FUNCTION: variance( lambda[, opts] )
*	Computes the distribution variance.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} lambda - input value
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} distribution variance(s)
*/
function variance( lambda, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( isNumber( lambda ) || isnan( lambda ) ) {
		return variance1( lambda );
	}
	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isMatrixLike( lambda ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'variance()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( lambda.length );
			out = matrix( d, lambda.shape, dt );
		} else {
			out = lambda;
		}
		return variance5( out, lambda );
	}
	if ( isTypedArrayLike( lambda ) ) {
		if ( opts.copy === false ) {
			out = lambda;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'variance()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( lambda.length );
		}
		return variance6( out, lambda );
	}
	if ( isArrayLike( lambda ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return variance4( lambda, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = lambda;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'variance()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( lambda.length );
		}
		else {
			out = new Array( lambda.length );
		}
		if ( opts.accessor ) {
			return variance3( out, lambda, opts.accessor );
		}
		return variance2( out, lambda );
	}
	return NaN;
} // end FUNCTION variance()


// EXPORTS //

module.exports = variance;

},{"./accessor.js":1808,"./array.js":1809,"./deepset.js":1810,"./matrix.js":1812,"./number.js":1813,"./typedarray.js":1814,"./validate.js":1815,"compute-array-constructors":1817,"dstructs-matrix":1826,"validate.io-array-like":1873,"validate.io-matrix-like":1878,"validate.io-nan":1879,"validate.io-number-primitive":1883,"validate.io-typed-array-like":1888}],1812:[function(require,module,exports){
'use strict';

// MODULES //

var VARIANCE = require( './number.js' );


// VARIANCE //

/**
* FUNCTION: variance( out, x )
*	Computes the distribution variance for each parameter stored in a matrix.
*
* @param {Matrix} out - output matrix
* @param {Matrix} x - input matrix
* @returns {Matrix} output matrix
*/
function variance( out, x ) {
	var len = x.length,
		i;
	if ( out.length !== len ) {
		throw new Error( 'variance()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	for ( i = 0; i < len; i++ ) {
		out.data[ i ] = VARIANCE( x.data[ i ] );
	}
	return out;
} // end FUNCTION variance()


// EXPORTS //

module.exports = variance;

},{"./number.js":1813}],1813:[function(require,module,exports){
'use strict';

// MODULES //

var isPositive = require( 'validate.io-positive-primitive' );


// FUNCTIONS //

var pow = Math.pow;


// VARIANCE //

/**
* FUNCTION variance( lambda )
*	Computes the distribution variance for an exponential distribution with parameter lambda.
*
* @param {Number} lambda - rate parameter
* @returns {Number} distribution variance
*/
function variance( lambda ) {
	if ( !isPositive( lambda ) ) {
		return NaN;
	}
	return pow( lambda, -2 );
} // end FUNCTION variance()


// EXPORTS

module.exports =  variance;

},{"validate.io-positive-primitive":1886}],1814:[function(require,module,exports){
'use strict';

// MODULES //

var VARIANCE = require( './number.js' );


// VARIANCE //

/**
* FUNCTION: variance( out, lambda )
*	Computes the distribution variance for parameters stored in a typed array.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} lambda - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function variance( out, lambda ) {
	var len = lambda.length,
		i;
	for ( i = 0; i < len; i++ ) {
		out[ i ] = VARIANCE( lambda[ i ] );
	}
	return out;
} // end FUNCTION variance()


// EXPORTS //

module.exports = variance;

},{"./number.js":1813}],1815:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Boolean} [options.copy] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'variance()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'variance()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'variance()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'variance()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'variance()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'variance()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":1876,"validate.io-function":1877,"validate.io-object":1884,"validate.io-string-primitive":1887}],1816:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],1817:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":1816,"dup":12}],1818:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],1819:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":1822,"./iget.js":1824,"./iset.js":1827,"./mget.js":1831,"./mset.js":1833,"./set.js":1841,"./sget.js":1843,"./sset.js":1845,"./toString.js":1847,"dup":14}],1820:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":1823,"./iget.raw.js":1825,"./iset.raw.js":1828,"./mget.raw.js":1832,"./mset.raw.js":1834,"./set.raw.js":1842,"./sget.raw.js":1844,"./sset.raw.js":1846,"./toString.js":1847,"dup":15}],1821:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],1822:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":1880}],1823:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],1824:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":1859}],1825:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],1826:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":1829,"./matrix.raw.js":1830,"dup":21}],1827:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":1859,"validate.io-number-primitive":1883}],1828:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],1829:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":1818,"./ctor.js":1819,"./dtypes.js":1821,"compute-cast-arrays":1848,"compute-dtype":1851,"dup":24,"validate.io-array":1856,"validate.io-contains":1857,"validate.io-nonnegative-integer-array":1860,"validate.io-string-primitive":1887}],1830:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":1818,"./ctor.raw.js":1820,"./dtypes.js":1821,"compute-dtype":1851,"dup":25,"validate.io-contains":1857,"validate.io-string-primitive":1887}],1831:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":1818,"dup":26,"validate.io-nonnegative-integer-array":1860}],1832:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":1818,"dup":27}],1833:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":1835,"./mset2.js":1836,"./mset3.js":1837,"./mset4.js":1838,"./mset5.js":1839,"./mset6.js":1840,"dup":28,"validate.io-function":1877,"validate.io-nonnegative-integer-array":1860,"validate.io-number-primitive":1883}],1834:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":1835,"./mset2.js":1836,"./mset3.js":1837,"./mset4.js":1838,"./mset5.js":1839,"./mset6.js":1840,"dup":29}],1835:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],1836:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],1837:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],1838:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],1839:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],1840:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],1841:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":1880,"validate.io-number-primitive":1883}],1842:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],1843:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":1818,"compute-indexspace":1855,"dup":38,"validate.io-string-primitive":1887}],1844:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":1818,"compute-indexspace":1855,"dup":39}],1845:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":1855,"dup":40,"validate.io-function":1877,"validate.io-number-primitive":1883,"validate.io-string-primitive":1887}],1846:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":1855,"dup":41}],1847:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],1848:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":1816,"compute-array-dtype/lib/dtypes":1849,"dup":43,"type-name":1850,"validate.io-array-like":1873}],1849:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1850:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1851:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":1853,"dup":46,"type-name":1854}],1852:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1853:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":1852,"dup":48}],1854:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1855:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":1880,"validate.io-string-primitive":1887}],1856:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1857:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":1856,"validate.io-nan-primitive":1858}],1858:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],1859:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1883}],1860:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":1856,"validate.io-nonnegative-integer":1880}],1861:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],1862:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],1863:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":1861,"./defaults.js":1862,"./validate.js":1865,"dup":60,"validate.io-array":1866,"validate.io-string-primitive":1887}],1864:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":1861,"./defaults.js":1862,"./factory.js":1863,"./validate.js":1865,"dup":61,"validate.io-array":1866,"validate.io-string-primitive":1887}],1865:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":1884,"validate.io-string-primitive":1887}],1866:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1867:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],1868:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],1869:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":1867,"./defaults.js":1868,"./validate.js":1871,"dup":66,"validate.io-array":1872,"validate.io-string-primitive":1887}],1870:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":1867,"./defaults.js":1868,"./factory.js":1869,"./validate.js":1871,"dup":67,"validate.io-array":1872,"validate.io-string-primitive":1887}],1871:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":1876,"validate.io-object":1884,"validate.io-string-primitive":1887}],1872:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1873:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":1874,"dup":70,"validate.io-integer-primitive":1875}],1874:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],1875:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1883}],1876:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],1877:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],1878:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],1879:[function(require,module,exports){
arguments[4][76][0].apply(exports,arguments)
},{"dup":76}],1880:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":1881}],1881:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":1882}],1882:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],1883:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],1884:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":1885}],1885:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1886:[function(require,module,exports){
arguments[4][1221][0].apply(exports,arguments)
},{"dup":1221,"validate.io-number-primitive":1883}],1887:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],1888:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":1889,"dup":81,"validate.io-integer-primitive":1890}],1889:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],1890:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1883}],1891:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// CDF //

/**
* FUNCTION: cdf( out, arr, mu, sigma, accessor )
*	Evaluates the cumulative distribution function (CDF) for a Normal distribution with mean `mu` and standard deviation `sigma` using an accessor function.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Number} mu - mean
* @param {Number} sigma - standard deviation
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function cdf( y, x, mu, sigma, clbk ) {
	var len = x.length,
		fcn,
		v, i;

	fcn = partial( mu, sigma );
	for ( i = 0; i < len; i++ ) {
		v = clbk( x[ i ], i );
		if ( typeof v === 'number' ) {
			y[ i ] = fcn( v );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION cdf()


// EXPORTS //

module.exports = cdf;

},{"./partial.js":1897}],1892:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// CDF //

/**
* FUNCTION: cdf( out, arr, mu, sigma )
*	Evaluates the cumulative distribution function (CDF) for a Normal distribution with mean `mu` and standard deviation `sigma` for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Number} mu - mean
* @param {Number} sigma - standard deviation
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function cdf( y, x, mu, sigma ) {
	var len = x.length,
		fcn,
		i;

	fcn = partial( mu, sigma );
	for ( i = 0; i < len; i++ ) {
		if ( typeof x[ i ] === 'number' ) {
			y[ i ] = fcn( x[ i ] );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION cdf()


// EXPORTS //

module.exports = cdf;

},{"./partial.js":1897}],1893:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory,
	partial = require( './partial.js' );


// CDF //

/**
* FUNCTION: cdf( arr, mu, sigma, path[, sep] )
*	Evaluates the cumulative distribution function (CDF) for a Normal distribution with mean `mu` and standard deviation `sigma` for each array element and sets the input array.
*
* @param {Array} arr - input array
* @param {Number} mu - mean
* @param {Number} sigma - standard deviation
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function cdf( x, mu, sigma, path, sep ) {
	var len = x.length,
		opts = {},
		dget,
		dset,
		fcn,
		v, i;
	if ( arguments.length > 4 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		fcn = partial( mu, sigma );
		for ( i = 0; i < len; i++ ) {
			v = dget( x[ i ] );
			if ( typeof v === 'number' ) {
				dset( x[i], fcn( v ) );
			} else {
				dset( x[i], NaN );
			}
		}
	}
	return x;
} // end FUNCTION cdf()


// EXPORTS //

module.exports = cdf;

},{"./partial.js":1897,"utils-deep-get":1949,"utils-deep-set":1955}],1894:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var cdf1 = require( './number.js' ),
	cdf2 = require( './array.js' ),
	cdf3 = require( './accessor.js' ),
	cdf4 = require( './deepset.js' ),
	cdf5 = require( './matrix.js' ),
	cdf6 = require( './typedarray.js' );


// CDF //

/**
* FUNCTION: cdf( x[, opts] )
*	Evaluates the cumulative distribution function (CDF) for a Normal distribution.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} x - input value
* @param {Object} [opts] - function options
* @param {Number} [opts.mu=0] - mean
* @param {Number} [opts.sigma=1] - standard deviation
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} evaluated CDF
*/
function cdf( x, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	opts.mu = typeof opts.mu !== 'undefined' ? opts.mu : 0;
	opts.sigma = typeof opts.sigma !== 'undefined' ? opts.sigma : 1;

	if ( isNumber( x ) ) {
		return cdf1( x, opts.mu, opts.sigma );
	}
	if ( isMatrixLike( x ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'cdf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( x.length );
			out = matrix( d, x.shape, dt );
		} else {
			out = x;
		}
		return cdf5( out, x, opts.mu, opts.sigma );
	}
	if ( isTypedArrayLike( x ) ) {
		if ( opts.copy === false ) {
			out = x;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'cdf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( x.length );
		}
		return cdf6( out, x, opts.mu, opts.sigma );
	}
	if ( isArrayLike( x ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return cdf4( x, opts.mu, opts.sigma, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = x;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'cdf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( x.length );
		}
		else {
			out = new Array( x.length );
		}
		if ( opts.accessor ) {
			return cdf3( out, x, opts.mu, opts.sigma, opts.accessor );
		}
		return cdf2( out, x, opts.mu, opts.sigma );
	}
	return NaN;
} // end FUNCTION cdf()


// EXPORTS //

module.exports = cdf;

},{"./accessor.js":1891,"./array.js":1892,"./deepset.js":1893,"./matrix.js":1895,"./number.js":1896,"./typedarray.js":1898,"./validate.js":1899,"compute-array-constructors":1901,"dstructs-matrix":1911,"validate.io-array-like":1958,"validate.io-matrix-like":1963,"validate.io-number-primitive":1969,"validate.io-typed-array-like":1973}],1895:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// CDF //

/**
* FUNCTION: cdf( out, matrix, mu, sigma )
*	Evaluates the cumulative distribution function (CDF) for a Normal distribution with mean `mu` and standard deviation `sigma` for each matrix element.
*
* @param {Matrix} out - output matrix
* @param {Matrix} arr - input matrix
* @param {Number} mu - mean
* @param {Number} sigma - standard deviation
* @returns {Matrix} output matrix
*/
function cdf( y, x, mu, sigma ) {
	var len = x.length,
		fcn,
		i;
	if ( y.length !== len ) {
		throw new Error( 'cdf()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	fcn = partial( mu, sigma );
	for ( i = 0; i < len; i++ ) {
		y.data[ i ] = fcn( x.data[ i ] );
	}
	return y;
} // end FUNCTION cdf()


// EXPORTS //

module.exports = cdf;

},{"./partial.js":1897}],1896:[function(require,module,exports){
'use strict';

// MODULES //

var erf = require( 'compute-erf/lib/number.js' );


// FUNCTIONS //

var sqrt = Math.sqrt;


// CDF //

/**
* FUNCTION: cdf( x, mu, sigma )
*	Evaluates the cumulative distribution function (CDF) for a Normal distribution with mean `mu` and standard deviation `sigma` at a value `x`.
*
* @param {Number} x - input value
* @param {Number} mu - mean
* @param {Number} sigma - standard deviation
* @returns {Number} evaluated CDF
*/
function cdf( x, mu, sigma ) {
	if( sigma === 0 ) {
		return (x < mu) ? 0 : 1;
	}
	var A = 1 / 2,
		B = sigma * sqrt( 2 ),
		C = x - mu;
	return A * ( 1 + erf( C / B ) );
} // end FUNCTION cdf()


// EXPORTS //

module.exports = cdf;

},{"compute-erf/lib/number.js":1902}],1897:[function(require,module,exports){
'use strict';

// MODULES //

var erf = require( 'compute-erf/lib/number.js' );


// FUNCTIONS //

var sqrt = Math.sqrt;


// PARTIAL //

/**
* FUNCTION: partial( mu, sigma )
*	Partially applies mean `mu` and standard deviation `sigma` and returns a function for evaluating the cumulative distribution function (CDF) for a Normal distribution.
*
* @param {Number} mu - mean
* @param {Number} sigma - standard deviation
* @returns {Function} CDF
*/
function partial( mu, sigma ) {
	var A = 1 / 2,
		B = sigma * sqrt( 2 );
	/**
	* FUNCTION: cdf( x )
	*	Evaluates the cumulative distribution function (CDF) for a Normal distribution.
	*
	* @private
	* @param {Number} x - input value
	* @returns {Number} evaluated CDF
	*/
	if( sigma === 0 ) {
		return function cdf( x ) {
			return (x < mu) ? 0 : 1;
		};
	}
	return function cdf( x ) {
		var C = x - mu;
		return A * ( 1 + erf( C / B ) );
	};
} // end FUNCTION partial()


// EXPORTS //

module.exports = partial;

},{"compute-erf/lib/number.js":1902}],1898:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// CDF //

/**
* FUNCTION: cdf( out, arr, mu, sigma )
*	Evaluates the cumulative distribution function (CDF) for a Normal distribution with mean `mu` and standard deviation `sigma` for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} arr - input array
* @param {Number} mu - mean
* @param {Number} sigma - standard deviation
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function cdf( y, x, mu, sigma ) {
	var len = x.length,
		fcn,
		i;

	fcn = partial ( mu, sigma );
	for ( i = 0; i < len; i++ ) {
		y[ i ] = fcn( x[ i ] );
	}
	return y;
} // end FUNCTION cdf()


// EXPORTS //

module.exports = cdf;

},{"./partial.js":1897}],1899:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isNumber = require( 'validate.io-number-primitive' ),
	isNonNegative = require( 'validate.io-nonnegative' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Number} [options.mu] - mean
* @param {Number} [options.sigma] - standard deviation
* @param {Boolean} [options.copy] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'cdf()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'mu' ) ) {
		opts.mu = options.mu;
		if ( !isNumber( opts.mu ) ) {
			return new TypeError( 'cdf()::invalid option. `mu` parameter must be a number primitive. Option: `' + opts.mu + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sigma' ) ) {
		opts.sigma = options.sigma;
		if ( !isNonNegative( opts.sigma ) ) {
			return new TypeError( 'cdf()::invalid option. `sigma` parameter must be a non-negative number. Option: `' + opts.sigma + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'cdf()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'cdf()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'cdf()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'cdf()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'cdf()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":1961,"validate.io-function":1962,"validate.io-nonnegative":1967,"validate.io-number-primitive":1969,"validate.io-object":1970,"validate.io-string-primitive":1972}],1900:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],1901:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":1900,"dup":12}],1902:[function(require,module,exports){
arguments[4][136][0].apply(exports,arguments)
},{"dup":136}],1903:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],1904:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":1907,"./iget.js":1909,"./iset.js":1912,"./mget.js":1916,"./mset.js":1918,"./set.js":1926,"./sget.js":1928,"./sset.js":1930,"./toString.js":1932,"dup":14}],1905:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":1908,"./iget.raw.js":1910,"./iset.raw.js":1913,"./mget.raw.js":1917,"./mset.raw.js":1919,"./set.raw.js":1927,"./sget.raw.js":1929,"./sset.raw.js":1931,"./toString.js":1932,"dup":15}],1906:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],1907:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":1964}],1908:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],1909:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":1944}],1910:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],1911:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":1914,"./matrix.raw.js":1915,"dup":21}],1912:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":1944,"validate.io-number-primitive":1969}],1913:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],1914:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":1903,"./ctor.js":1904,"./dtypes.js":1906,"compute-cast-arrays":1933,"compute-dtype":1936,"dup":24,"validate.io-array":1941,"validate.io-contains":1942,"validate.io-nonnegative-integer-array":1945,"validate.io-string-primitive":1972}],1915:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":1903,"./ctor.raw.js":1905,"./dtypes.js":1906,"compute-dtype":1936,"dup":25,"validate.io-contains":1942,"validate.io-string-primitive":1972}],1916:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":1903,"dup":26,"validate.io-nonnegative-integer-array":1945}],1917:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":1903,"dup":27}],1918:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":1920,"./mset2.js":1921,"./mset3.js":1922,"./mset4.js":1923,"./mset5.js":1924,"./mset6.js":1925,"dup":28,"validate.io-function":1962,"validate.io-nonnegative-integer-array":1945,"validate.io-number-primitive":1969}],1919:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":1920,"./mset2.js":1921,"./mset3.js":1922,"./mset4.js":1923,"./mset5.js":1924,"./mset6.js":1925,"dup":29}],1920:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],1921:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],1922:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],1923:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],1924:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],1925:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],1926:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":1964,"validate.io-number-primitive":1969}],1927:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],1928:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":1903,"compute-indexspace":1940,"dup":38,"validate.io-string-primitive":1972}],1929:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":1903,"compute-indexspace":1940,"dup":39}],1930:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":1940,"dup":40,"validate.io-function":1962,"validate.io-number-primitive":1969,"validate.io-string-primitive":1972}],1931:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":1940,"dup":41}],1932:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],1933:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":1900,"compute-array-dtype/lib/dtypes":1934,"dup":43,"type-name":1935,"validate.io-array-like":1958}],1934:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1935:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1936:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":1938,"dup":46,"type-name":1939}],1937:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],1938:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":1937,"dup":48}],1939:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],1940:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":1964,"validate.io-string-primitive":1972}],1941:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1942:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":1941,"validate.io-nan-primitive":1943}],1943:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],1944:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1969}],1945:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":1941,"validate.io-nonnegative-integer":1964}],1946:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],1947:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],1948:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":1946,"./defaults.js":1947,"./validate.js":1950,"dup":60,"validate.io-array":1951,"validate.io-string-primitive":1972}],1949:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":1946,"./defaults.js":1947,"./factory.js":1948,"./validate.js":1950,"dup":61,"validate.io-array":1951,"validate.io-string-primitive":1972}],1950:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":1970,"validate.io-string-primitive":1972}],1951:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1952:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],1953:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],1954:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":1952,"./defaults.js":1953,"./validate.js":1956,"dup":66,"validate.io-array":1957,"validate.io-string-primitive":1972}],1955:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":1952,"./defaults.js":1953,"./factory.js":1954,"./validate.js":1956,"dup":67,"validate.io-array":1957,"validate.io-string-primitive":1972}],1956:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":1961,"validate.io-object":1970,"validate.io-string-primitive":1972}],1957:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1958:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":1959,"dup":70,"validate.io-integer-primitive":1960}],1959:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],1960:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1969}],1961:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],1962:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],1963:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],1964:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":1965}],1965:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":1966}],1966:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],1967:[function(require,module,exports){
arguments[4][1059][0].apply(exports,arguments)
},{"dup":1059,"validate.io-number":1968}],1968:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],1969:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],1970:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":1971}],1971:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],1972:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],1973:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":1974,"dup":81,"validate.io-integer-primitive":1975}],1974:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],1975:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":1969}],1976:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// PDF //

/**
* FUNCTION: pdf( out, arr, mu, sigma, accessor )
*	Evaluates the probability density function (PDF) for a Normal distribution with mean  `mu` and standard deviation `sigma` using an accessor function.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Number} mu - mean 
* @param {Number} sigma - standard deviation
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function pdf( y, x, mu, sigma, clbk ) {
	var len = x.length,
		fcn,
		v, i;

	fcn = partial( mu, sigma );
	for ( i = 0; i < len; i++ ) {
		v = clbk( x[ i ], i );
		if ( typeof v === 'number' ) {
			y[ i ] = fcn( v );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./partial.js":1982}],1977:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// PDF //

/**
* FUNCTION: pdf( out, arr, mu, sigma )
*	Evaluates the probability density function (PDF) for a Normal distribution with mean  `mu` and standard deviation `sigma` for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Number} mu - mean 
* @param {Number} sigma - standard deviation
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function pdf( y, x, mu, sigma ) {
	var len = x.length,
		fcn,
		i;

	fcn = partial( mu, sigma );
	for ( i = 0; i < len; i++ ) {
		if ( typeof x[ i ] === 'number' ) {
			y[ i ] = fcn( x[ i ] );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./partial.js":1982}],1978:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory,
	partial = require( './partial.js' );


// PDF //

/**
* FUNCTION: pdf( arr, mu, sigma, path[, sep] )
*	Evaluates the probability density function (PDF) for a Normal distribution with mean  `mu` and standard deviation `sigma` for each array element and sets the input array.
*
* @param {Array} arr - input array
* @param {Number} mu - mean 
* @param {Number} sigma - standard deviation
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function pdf( x, mu, sigma, path, sep ) {
	var len = x.length,
		opts = {},
		dget,
		dset,
		fcn,
		v, i;
	if ( arguments.length > 4 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		fcn = partial( mu, sigma );
		for ( i = 0; i < len; i++ ) {
			v = dget( x[ i ] );
			if ( typeof v === 'number' ) {
				dset( x[i], fcn( v ) );
			} else {
				dset( x[i], NaN );
			}
		}
	}
	return x;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./partial.js":1982,"utils-deep-get":2033,"utils-deep-set":2039}],1979:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var pdf1 = require( './number.js' ),
	pdf2 = require( './array.js' ),
	pdf3 = require( './accessor.js' ),
	pdf4 = require( './deepset.js' ),
	pdf5 = require( './matrix.js' ),
	pdf6 = require( './typedarray.js' );


// PDF //

/**
* FUNCTION: pdf( x[, opts] )
*	Evaluates the probability density function (PDF) for a Normal distribution.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} x - input value
* @param {Object} [opts] - function options
* @param {Number} [opts.mu=0] - mean 
* @param {Number} [opts.sigma=1] - standard deviation
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} evaluated PDF
*/
function pdf( x, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	opts.mu = typeof opts.mu !== 'undefined' ? opts.mu : 0;
	opts.sigma = typeof opts.sigma !== 'undefined' ? opts.sigma : 1;

	if ( isNumber( x ) ) {
		return pdf1( x, opts.mu, opts.sigma );
	}
	if ( isMatrixLike( x ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'pdf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( x.length );
			out = matrix( d, x.shape, dt );
		} else {
			out = x;
		}
		return pdf5( out, x, opts.mu, opts.sigma );
	}
	if ( isTypedArrayLike( x ) ) {
		if ( opts.copy === false ) {
			out = x;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'pdf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( x.length );
		}
		return pdf6( out, x, opts.mu, opts.sigma );
	}
	if ( isArrayLike( x ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return pdf4( x, opts.mu, opts.sigma, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = x;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'pdf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( x.length );
		}
		else {
			out = new Array( x.length );
		}
		if ( opts.accessor ) {
			return pdf3( out, x, opts.mu, opts.sigma, opts.accessor );
		}
		return pdf2( out, x, opts.mu, opts.sigma );
	}
	return NaN;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./accessor.js":1976,"./array.js":1977,"./deepset.js":1978,"./matrix.js":1980,"./number.js":1981,"./typedarray.js":1983,"./validate.js":1984,"compute-array-constructors":1986,"dstructs-matrix":1995,"validate.io-array-like":2042,"validate.io-matrix-like":2047,"validate.io-number-primitive":2053,"validate.io-typed-array-like":2057}],1980:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// PDF //

/**
* FUNCTION: pdf( out, matrix, mu, sigma )
*	Evaluates the probability density function (PDF) for a Normal distribution with mean  `mu` and standard deviation `sigma` for each matrix element.
*
* @param {Matrix} out - output matrix
* @param {Matrix} arr - input matrix
* @param {Number} mu - mean 
* @param {Number} sigma - standard deviation
* @returns {Matrix} output matrix
*/
function pdf( y, x, mu, sigma ) {
	var len = x.length,
		fcn,
		i;
	if ( y.length !== len ) {
		throw new Error( 'pdf()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	fcn = partial( mu, sigma );
	for ( i = 0; i < len; i++ ) {
		y.data[ i ] = fcn( x.data[ i ] );
	}
	return y;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./partial.js":1982}],1981:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var exp = Math.exp,
	pow = Math.pow,
	sqrt = Math.sqrt;


// VARIABLES //

var PI = Math.PI;


// PDF //

/**
* FUNCTION: pdf( x, mu, sigma )
*	Evaluates the probability density function (PDF) for a Normal distribution with mean  `mu` and standard deviation `sigma` at a value `x`.
*
* @param {Number} x - input value
* @param {Number} mu - mean
* @param {Number} sigma - standard deviation
* @returns {Number} evaluated PDF
*/
function pdf( x, mu, sigma ) {
	if ( sigma === 0 ) {
		return x === mu ? Number.POSITIVE_INFINITY : 0;
	}
	var s2 = pow( sigma, 2 ),
		A = 1 / ( sqrt( 2 * s2 * PI ) ),
		B = -1 / ( 2 * s2 );
	return A * exp( B * pow( x - mu, 2 ) );
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{}],1982:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var exp = Math.exp,
	pow = Math.pow,
	sqrt = Math.sqrt;


// VARIABLES //

var PI = Math.PI;


// PARTIAL //

/**
* FUNCTION: partial( mu, sigma )
*	Partially applies mean  `mu` and standard deviation `sigma` and returns a function for evaluating the probability density function (PDF) for a Normal distribution.
*
* @param {Number} mu - mean
* @param {Number} sigma - standard deviation
* @returns {Function} PDF
*/
function partial( mu, sigma ) {
	var s2 = pow( sigma, 2 ),
		A = 1 / ( sqrt( 2 * s2 * PI ) ),
		B = -1 / ( 2 * s2 );
	/**
	* FUNCTION: pdf( x )
	*	Evaluates the probability density function (PDF) for a Normal distribution.
	*
	* @private
	* @param {Number} x - input value
	* @returns {Number} evaluated PDF
	*/
	if ( sigma === 0 ) {
		return function pdf( x ) {
			return x === mu ? Number.POSITIVE_INFINITY : 0;
		};
	}
	return function pdf( x ) {
		return A * exp( B * pow( x - mu, 2 ) );
	};
} // end FUNCTION partial()


// EXPORTS //

module.exports = partial;

},{}],1983:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// PDF //

/**
* FUNCTION: pdf( out, arr, mu, sigma )
*	Evaluates the probability density function (PDF) for a Normal distribution with mean  `mu` and standard deviation `sigma` for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} arr - input array
* @param {Number} mu - mean 
* @param {Number} sigma - standard deviation
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function pdf( y, x, mu, sigma ) {
	var len = x.length,
		fcn,
		i;

	fcn = partial ( mu, sigma );
	for ( i = 0; i < len; i++ ) {
		y[ i ] = fcn( x[ i ] );
	}
	return y;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./partial.js":1982}],1984:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isNumber = require( 'validate.io-number-primitive' ),
	isNonNegative = require( 'validate.io-nonnegative' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Number} [options.mu] - mean
* @param {Number} [options.sigma] - standard deviation
* @param {Boolean} [options.copy] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'pdf()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'mu' ) ) {
		opts.mu = options.mu;
		if ( !isNumber( opts.mu ) ) {
			return new TypeError( 'pdf()::invalid option. `mu` parameter must be a number primitive. Option: `' + opts.mu + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sigma' ) ) {
		opts.sigma = options.sigma;
		if ( !isNonNegative( opts.sigma ) ) {
			return new TypeError( 'pdf()::invalid option. `sigma` parameter must be a non-negative number. Option: `' + opts.sigma + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'pdf()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'pdf()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'pdf()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'pdf()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'pdf()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":2045,"validate.io-function":2046,"validate.io-nonnegative":2051,"validate.io-number-primitive":2053,"validate.io-object":2054,"validate.io-string-primitive":2056}],1985:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],1986:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":1985,"dup":12}],1987:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],1988:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":1991,"./iget.js":1993,"./iset.js":1996,"./mget.js":2000,"./mset.js":2002,"./set.js":2010,"./sget.js":2012,"./sset.js":2014,"./toString.js":2016,"dup":14}],1989:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":1992,"./iget.raw.js":1994,"./iset.raw.js":1997,"./mget.raw.js":2001,"./mset.raw.js":2003,"./set.raw.js":2011,"./sget.raw.js":2013,"./sset.raw.js":2015,"./toString.js":2016,"dup":15}],1990:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],1991:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":2048}],1992:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],1993:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":2028}],1994:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],1995:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":1998,"./matrix.raw.js":1999,"dup":21}],1996:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":2028,"validate.io-number-primitive":2053}],1997:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],1998:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":1987,"./ctor.js":1988,"./dtypes.js":1990,"compute-cast-arrays":2017,"compute-dtype":2020,"dup":24,"validate.io-array":2025,"validate.io-contains":2026,"validate.io-nonnegative-integer-array":2029,"validate.io-string-primitive":2056}],1999:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":1987,"./ctor.raw.js":1989,"./dtypes.js":1990,"compute-dtype":2020,"dup":25,"validate.io-contains":2026,"validate.io-string-primitive":2056}],2000:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":1987,"dup":26,"validate.io-nonnegative-integer-array":2029}],2001:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":1987,"dup":27}],2002:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":2004,"./mset2.js":2005,"./mset3.js":2006,"./mset4.js":2007,"./mset5.js":2008,"./mset6.js":2009,"dup":28,"validate.io-function":2046,"validate.io-nonnegative-integer-array":2029,"validate.io-number-primitive":2053}],2003:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":2004,"./mset2.js":2005,"./mset3.js":2006,"./mset4.js":2007,"./mset5.js":2008,"./mset6.js":2009,"dup":29}],2004:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],2005:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],2006:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],2007:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],2008:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],2009:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],2010:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":2048,"validate.io-number-primitive":2053}],2011:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],2012:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":1987,"compute-indexspace":2024,"dup":38,"validate.io-string-primitive":2056}],2013:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":1987,"compute-indexspace":2024,"dup":39}],2014:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":2024,"dup":40,"validate.io-function":2046,"validate.io-number-primitive":2053,"validate.io-string-primitive":2056}],2015:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":2024,"dup":41}],2016:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],2017:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":1985,"compute-array-dtype/lib/dtypes":2018,"dup":43,"type-name":2019,"validate.io-array-like":2042}],2018:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2019:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2020:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":2022,"dup":46,"type-name":2023}],2021:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2022:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":2021,"dup":48}],2023:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2024:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":2048,"validate.io-string-primitive":2056}],2025:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2026:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":2025,"validate.io-nan-primitive":2027}],2027:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],2028:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2053}],2029:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":2025,"validate.io-nonnegative-integer":2048}],2030:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],2031:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],2032:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":2030,"./defaults.js":2031,"./validate.js":2034,"dup":60,"validate.io-array":2035,"validate.io-string-primitive":2056}],2033:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":2030,"./defaults.js":2031,"./factory.js":2032,"./validate.js":2034,"dup":61,"validate.io-array":2035,"validate.io-string-primitive":2056}],2034:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":2054,"validate.io-string-primitive":2056}],2035:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2036:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],2037:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],2038:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":2036,"./defaults.js":2037,"./validate.js":2040,"dup":66,"validate.io-array":2041,"validate.io-string-primitive":2056}],2039:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":2036,"./defaults.js":2037,"./factory.js":2038,"./validate.js":2040,"dup":67,"validate.io-array":2041,"validate.io-string-primitive":2056}],2040:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":2045,"validate.io-object":2054,"validate.io-string-primitive":2056}],2041:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2042:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":2043,"dup":70,"validate.io-integer-primitive":2044}],2043:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],2044:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2053}],2045:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],2046:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],2047:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],2048:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":2049}],2049:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":2050}],2050:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],2051:[function(require,module,exports){
arguments[4][1059][0].apply(exports,arguments)
},{"dup":1059,"validate.io-number":2052}],2052:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],2053:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],2054:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":2055}],2055:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2056:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],2057:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":2058,"dup":81,"validate.io-integer-primitive":2059}],2058:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],2059:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2053}],2060:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// QUANTILE //

/**
* FUNCTION: quantile( out, arr, mu, sigma, accessor )
*	Evaluates the quantile function for a Normal distribution with mean `mu` and standard deviation `sigma` using an accessor function.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Number} mu - mean
* @param {Number} sigma - standard deviation
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function quantile( y, x, mu, sigma, clbk ) {
	var len = x.length,
		fcn,
		v, i;

	fcn = partial( mu, sigma );
	for ( i = 0; i < len; i++ ) {
		v = clbk( x[ i ], i );
		if ( typeof v === 'number' ) {
			y[ i ] = fcn( v );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION quantile()


// EXPORTS //

module.exports = quantile;

},{"./partial.js":2066}],2061:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// QUANTILE //

/**
* FUNCTION: quantile( out, arr, mu, sigma )
*	Evaluates the quantile function for a Normal distribution with mean `mu` and standard deviation `sigma` for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Number} mu - mean
* @param {Number} sigma - standard deviation
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function quantile( y, x, mu, sigma ) {
	var len = x.length,
		fcn,
		i;

	fcn = partial( mu, sigma );
	for ( i = 0; i < len; i++ ) {
		if ( typeof x[ i ] === 'number' ) {
			y[ i ] = fcn( x[ i ] );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION quantile()


// EXPORTS //

module.exports = quantile;

},{"./partial.js":2066}],2062:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory,
	partial = require( './partial.js' );


// QUANTILE //

/**
* FUNCTION: quantile( arr, mu, sigma, path[, sep] )
*	Evaluates the quantile function for a Normal distribution with mean `mu` and standard deviation `sigma` for each array element and sets the input array.
*
* @param {Array} arr - input array
* @param {Number} mu - mean
* @param {Number} sigma - standard deviation
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function quantile( x, mu, sigma, path, sep ) {
	var len = x.length,
		opts = {},
		dget,
		dset,
		fcn,
		v, i;
	if ( arguments.length > 4 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		fcn = partial( mu, sigma );
		for ( i = 0; i < len; i++ ) {
			v = dget( x[ i ] );
			if ( typeof v === 'number' ) {
				dset( x[i], fcn( v ) );
			} else {
				dset( x[i], NaN );
			}
		}
	}
	return x;
} // end FUNCTION quantile()


// EXPORTS //

module.exports = quantile;

},{"./partial.js":2066,"utils-deep-get":2121,"utils-deep-set":2127}],2063:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var quantile1 = require( './number.js' ),
	quantile2 = require( './array.js' ),
	quantile3 = require( './accessor.js' ),
	quantile4 = require( './deepset.js' ),
	quantile5 = require( './matrix.js' ),
	quantile6 = require( './typedarray.js' );


// PDF //

/**
* FUNCTION: quantile( p[, opts] )
*	Evaluates the quantile function for a Normal distribution.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} p - input value
* @param {Object} [opts] - function options
* @param {Number} [opts.mu=0] - mean
* @param {Number} [opts.sigma=1] - standard deviation
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} quantile function value(s)
*/
function quantile( p, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	opts.mu = typeof opts.mu !== 'undefined' ? opts.mu : 0;
	opts.sigma = typeof opts.sigma !== 'undefined' ? opts.sigma : 1;

	if ( isNumber( p ) ) {
		return quantile1( p, opts.mu, opts.sigma );
	}
	if ( isMatrixLike( p ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'quantile()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( p.length );
			out = matrix( d, p.shape, dt );
		} else {
			out = p;
		}
		return quantile5( out, p, opts.mu, opts.sigma );
	}
	if ( isTypedArrayLike( p ) ) {
		if ( opts.copy === false ) {
			out = p;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'quantile()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( p.length );
		}
		return quantile6( out, p, opts.mu, opts.sigma );
	}
	if ( isArrayLike( p ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return quantile4( p, opts.mu, opts.sigma, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = p;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'quantile()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( p.length );
		}
		else {
			out = new Array( p.length );
		}
		if ( opts.accessor ) {
			return quantile3( out, p, opts.mu, opts.sigma, opts.accessor );
		}
		return quantile2( out, p, opts.mu, opts.sigma );
	}
	return NaN;
} // end FUNCTION quantile()


// EXPORTS //

module.exports = quantile;

},{"./accessor.js":2060,"./array.js":2061,"./deepset.js":2062,"./matrix.js":2064,"./number.js":2065,"./typedarray.js":2067,"./validate.js":2068,"compute-array-constructors":2070,"dstructs-matrix":2083,"validate.io-array-like":2130,"validate.io-matrix-like":2135,"validate.io-number-primitive":2141,"validate.io-typed-array-like":2145}],2064:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// QUANTILE //

/**
* FUNCTION: quantile( out, matrix, mu, sigma )
*	Evaluates the quantile function for a Normal distribution with mean `mu` and standard deviation `sigma` for each matrix element.
*
* @param {Matrix} out - output matrix
* @param {Matrix} arr - input matrix
* @param {Number} mu - mean
* @param {Number} sigma - standard deviation
* @returns {Matrix} output matrix
*/
function quantile( y, x, mu, sigma ) {
	var len = x.length,
		fcn,
		i;
	if ( y.length !== len ) {
		throw new Error( 'quantile()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	fcn = partial( mu, sigma );
	for ( i = 0; i < len; i++ ) {
		y.data[ i ] = fcn( x.data[ i ] );
	}
	return y;
} // end FUNCTION quantile()


// EXPORTS //

module.exports = quantile;

},{"./partial.js":2066}],2065:[function(require,module,exports){
'use strict';

// MODULES //

var erfinv = require( 'compute-erfinv/lib/number.js' );


// FUNCTIONS //

var sqrt = Math.sqrt;


// QUANTILE //

/**
* FUNCTION: quantile( p, mu, sigma )
*	Evaluates the quantile function for a Normal distribution with mean `mu` and standard deviation `sigma` at a probability `p`.
*
* @param {Number} p - input value
* @param {Number} mu - mean
* @param {Number} sigma - standard deviation
* @returns {Number} evaluated quantile function
*/
function quantile( p, mu, sigma ) {
	if ( p !== p || p < 0 || p > 1 ) {
		return NaN;
	}
	if ( sigma === 0 ) {
		return mu;
	}

	var A = mu,
		B = sigma * sqrt( 2 );

	return A + B * erfinv( 2 * p - 1 );
} // end FUNCTION quantile()


// EXPORTS //

module.exports = quantile;

},{"compute-erfinv/lib/number.js":2071}],2066:[function(require,module,exports){
'use strict';

// MODULES //

var erfinv = require( 'compute-erfinv/lib/number.js' );


// FUNCTIONS //

var sqrt = Math.sqrt;


// PARTIAL //

/**
* FUNCTION: partial( mu, sigma )
*	Partially applies mean `mu` and standard deviation `sigma` and returns a function for evaluating the quantile function for a Normal distribution.
*
* @param {Number} mu - mean
* @param {Number} sigma - standard deviation
* @returns {Function} quantile function
*/
function partial( mu, sigma ) {
	var A = mu,
		B = sigma * sqrt( 2 );
	/**
	* FUNCTION: quantile( p )
	*	Evaluates the quantile function for a Normal distribution.
	*
	* @private
	* @param {Number} p - input value
	* @returns {Number} evaluated quantile function
	*/
	if ( sigma === 0 ) {
		return function quantile( p ) {
			if ( p !== p || p < 0 || p > 1 ) {
				return NaN;
			}
			return mu;
		};
	}
	return function quantile( p ) {
		if ( p !== p || p < 0 || p > 1 ) {
			return NaN;
		}
		return A + B * erfinv( 2 * p - 1 );
	};
} // end FUNCTION partial()


// EXPORTS //

module.exports = partial;

},{"compute-erfinv/lib/number.js":2071}],2067:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// QUANTILE //

/**
* FUNCTION: quantile( out, arr, mu, sigma )
*	Evaluates the quantile function for a Normal distribution with mean `mu` and standard deviation `sigma` for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} arr - input array
* @param {Number} mu - mean
* @param {Number} sigma - standard deviation
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function quantile( y, x, mu, sigma ) {
	var len = x.length,
		fcn,
		i;

	fcn = partial ( mu, sigma );
	for ( i = 0; i < len; i++ ) {
		y[ i ] = fcn( x[ i ] );
	}
	return y;
} // end FUNCTION quantile()


// EXPORTS //

module.exports = quantile;

},{"./partial.js":2066}],2068:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isNumber = require( 'validate.io-number-primitive' ),
	isNonNegative = require( 'validate.io-nonnegative' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Number} [options.mu] - mean
* @param {Number} [options.sigma] - standard deviation
* @param {Boolean} [options.copy] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'quantile()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'mu' ) ) {
		opts.mu = options.mu;
		if ( !isNumber( opts.mu ) ) {
			return new TypeError( 'quantile()::invalid option. `mu` parameter must be a number primitive. Option: `' + opts.mu + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sigma' ) ) {
		opts.sigma = options.sigma;
		if ( !isNonNegative( opts.sigma ) ) {
			return new TypeError( 'quantile()::invalid option. `sigma` parameter must be a non-negative number. Option: `' + opts.sigma + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'quantile()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'quantile()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'quantile()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'quantile()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'quantile()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":2133,"validate.io-function":2134,"validate.io-nonnegative":2139,"validate.io-number-primitive":2141,"validate.io-object":2142,"validate.io-string-primitive":2144}],2069:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],2070:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":2069,"dup":12}],2071:[function(require,module,exports){
arguments[4][379][0].apply(exports,arguments)
},{"compute-polynomial":2072,"dup":379}],2072:[function(require,module,exports){
arguments[4][724][0].apply(exports,arguments)
},{"dup":724,"validate.io-array":2073,"validate.io-boolean-primitive":2133,"validate.io-function":2134,"validate.io-number-primitive":2141,"validate.io-number-primitive-array":2074,"validate.io-object":2142}],2073:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2074:[function(require,module,exports){
arguments[4][728][0].apply(exports,arguments)
},{"dup":728,"validate.io-array":2073}],2075:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],2076:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":2079,"./iget.js":2081,"./iset.js":2084,"./mget.js":2088,"./mset.js":2090,"./set.js":2098,"./sget.js":2100,"./sset.js":2102,"./toString.js":2104,"dup":14}],2077:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":2080,"./iget.raw.js":2082,"./iset.raw.js":2085,"./mget.raw.js":2089,"./mset.raw.js":2091,"./set.raw.js":2099,"./sget.raw.js":2101,"./sset.raw.js":2103,"./toString.js":2104,"dup":15}],2078:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],2079:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":2136}],2080:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],2081:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":2116}],2082:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],2083:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":2086,"./matrix.raw.js":2087,"dup":21}],2084:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":2116,"validate.io-number-primitive":2141}],2085:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],2086:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":2075,"./ctor.js":2076,"./dtypes.js":2078,"compute-cast-arrays":2105,"compute-dtype":2108,"dup":24,"validate.io-array":2113,"validate.io-contains":2114,"validate.io-nonnegative-integer-array":2117,"validate.io-string-primitive":2144}],2087:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":2075,"./ctor.raw.js":2077,"./dtypes.js":2078,"compute-dtype":2108,"dup":25,"validate.io-contains":2114,"validate.io-string-primitive":2144}],2088:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":2075,"dup":26,"validate.io-nonnegative-integer-array":2117}],2089:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":2075,"dup":27}],2090:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":2092,"./mset2.js":2093,"./mset3.js":2094,"./mset4.js":2095,"./mset5.js":2096,"./mset6.js":2097,"dup":28,"validate.io-function":2134,"validate.io-nonnegative-integer-array":2117,"validate.io-number-primitive":2141}],2091:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":2092,"./mset2.js":2093,"./mset3.js":2094,"./mset4.js":2095,"./mset5.js":2096,"./mset6.js":2097,"dup":29}],2092:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],2093:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],2094:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],2095:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],2096:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],2097:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],2098:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":2136,"validate.io-number-primitive":2141}],2099:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],2100:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":2075,"compute-indexspace":2112,"dup":38,"validate.io-string-primitive":2144}],2101:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":2075,"compute-indexspace":2112,"dup":39}],2102:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":2112,"dup":40,"validate.io-function":2134,"validate.io-number-primitive":2141,"validate.io-string-primitive":2144}],2103:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":2112,"dup":41}],2104:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],2105:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":2069,"compute-array-dtype/lib/dtypes":2106,"dup":43,"type-name":2107,"validate.io-array-like":2130}],2106:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2107:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2108:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":2110,"dup":46,"type-name":2111}],2109:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2110:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":2109,"dup":48}],2111:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2112:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":2136,"validate.io-string-primitive":2144}],2113:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2114:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":2113,"validate.io-nan-primitive":2115}],2115:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],2116:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2141}],2117:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":2113,"validate.io-nonnegative-integer":2136}],2118:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],2119:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],2120:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":2118,"./defaults.js":2119,"./validate.js":2122,"dup":60,"validate.io-array":2123,"validate.io-string-primitive":2144}],2121:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":2118,"./defaults.js":2119,"./factory.js":2120,"./validate.js":2122,"dup":61,"validate.io-array":2123,"validate.io-string-primitive":2144}],2122:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":2142,"validate.io-string-primitive":2144}],2123:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2124:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],2125:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],2126:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":2124,"./defaults.js":2125,"./validate.js":2128,"dup":66,"validate.io-array":2129,"validate.io-string-primitive":2144}],2127:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":2124,"./defaults.js":2125,"./factory.js":2126,"./validate.js":2128,"dup":67,"validate.io-array":2129,"validate.io-string-primitive":2144}],2128:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":2133,"validate.io-object":2142,"validate.io-string-primitive":2144}],2129:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2130:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":2131,"dup":70,"validate.io-integer-primitive":2132}],2131:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],2132:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2141}],2133:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],2134:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],2135:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],2136:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":2137}],2137:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":2138}],2138:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],2139:[function(require,module,exports){
arguments[4][1059][0].apply(exports,arguments)
},{"dup":1059,"validate.io-number":2140}],2140:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],2141:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],2142:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":2143}],2143:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2144:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],2145:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":2146,"dup":81,"validate.io-integer-primitive":2147}],2146:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],2147:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2141}],2148:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// RANDOM //

/**
* FUNCTION: random( len, mu, sigma[, rand] )
*	Creates an array of normally distributed random numbers.
*
* @param {Number} len - array length
* @param {Number} mu - mean parameter
* @param {Number} sigma - standard deviation
* @param {Function} [rand=Math.random] - random number generator
* @returns {Number[]} array filled with normal random numbers
*/
function random( len, mu, sigma, rand ) {
	var out,
		draw,
		i;

	draw = partial( mu, sigma, rand );
	// Ensure fast elements...
	if ( len < 64000 ) {
		out = new Array( len );
		for ( i = 0; i < len; i++ ) {
			out[ i ] = draw();
		}
	} else {
		out = [];
		for ( i = 0; i < len; i++ ) {
			out.push( draw() );
		}
	}
	return out;
} // end FUNCTION random()


// EXPORTS //

module.exports = random;

},{"./partial.js":2154}],2149:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' ),
	recurse = require( './recurse.js' );


// RANDOM //

/**
* FUNCTION: random( dims, mu, sigma[, rand] )
*	Creates a multidimensional array of normally distributed random numbers.
*
* @param {Number[]} dims - dimensions
* @param {Number} mu - mean parameter
* @param {Number} sigma - standard deviation
* @param {Function} [rand=Math.random] - random number generator
* @returns {Array} multidimensional array filled with normal random numbers
*/
function random( dims, mu, sigma, rand ) {
	var draw = partial( mu, sigma, rand );
	return recurse( dims, 0, draw );
} // end FUNCTION random()


// EXPORTS //

module.exports = random;

},{"./partial.js":2154,"./recurse.js":2155}],2150:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var ln = Math.log;


// NORMAL TAIL //

/**
* FUNCTION dRanNormalTail( dMin, iNegative, rand )
*	Transform the tail of the normal distribution to
*	the unit interval and then use rejection technique
*	to generate standar normal variable.
*	Reference:
*		Marsaclia, G. (1964). Generating a Variable from the Tail
*		of the Normal Distribution. Technometrics, 6(1),
*		101–102. doi:10.1080/00401706.1964.10490150
*
* @param {Number} dMin - start value of the right tail
* @param {Boolean} iNegative - boolean indicating which side to evaluate
* @returns {Number} standard normal variable
*/
function dRanNormalTail( dMin, iNegative, rand ) {
	var x, y;
	do {
		x = ln( rand() ) / dMin;
		y = ln( rand() );
	} while ( -2 * y < x * x );
	return iNegative ? x - dMin : dMin - x;
} // end FUNCTION dRanNormalTail()


// EXPORTS //

module.exports = dRanNormalTail;

},{}],2151:[function(require,module,exports){
'use strict';

// MODULES //

var isPositiveIntegerArray = require( 'validate.io-positive-integer-array' ),
	isPositiveInteger = require( 'validate.io-positive-integer' ),
	lcg = require( 'compute-lcg' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var array = require( './array.js' ),
	typedarray = require( './typedarray.js' ),
	arrayarray = require( './arrayarray.js' ),
	matrix = require( './matrix.js' ),
	number = require( './number.js' );


// UNIFORM GENERATOR //

var RAND = lcg();


// NORMAL RANDOM VARIATES //

/**
* FUNCTION: random( [dims][, opts] )
*	Creates a matrix or array filled with normal random numbers.
*
* @param {Number|Number[]} [dims] - dimensions
* @param {Object} [opts] - function options
* @param {Number} [opts.mu=0] - mean parameter
* @param {Number} [opts.sigma=1] - standard deviation
* @param {String} [opts.dtype="generic"] - output data type
* @param {Number} [opts.seed] - integer-valued seed
* @returns {Array|Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} random numbers
*/
function random( dims, options ) {
	var opts = {},
		isArray,
		ndims,
		err,
		len,
		mu,
		sigma,
		rand,
		dt;

	if ( arguments.length > 0 ) {
		isArray = isPositiveIntegerArray( dims );
		if ( !isArray && !isPositiveInteger( dims ) ) {
			throw new TypeError( 'random()::invalid input argument. Dimensions argument must be either a positive integer or a positive integer array. Value: `' + dims + '`.' );
		}
	}
	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}

	if ( opts.seed ) {
		rand = lcg( opts.seed );
	} else {
		rand = RAND;
	}
	dt = opts.dtype || 'generic';

	mu = typeof opts.mu !== 'undefined' ? opts.mu : 0;
	sigma = typeof opts.sigma !== 'undefined' ? opts.sigma : 1;

	if ( arguments.length === 0 ) {
		return number( mu, sigma, rand );
	}
	if ( isArray ) {
		ndims = dims.length;
		if ( ndims < 2 ) {
			len = dims[ 0 ];
		}
	} else {
		ndims = 1;
		len = dims;
	}
	// 1-dimensional data structures...
	if ( ndims === 1 ) {
		if ( len === 1 ) {
			return number( mu, sigma, rand );
		}
		if ( dt === 'generic' ) {
			return array( len, mu, sigma, rand );
		}
		return typedarray( len, dt, mu, sigma, rand );
	}
	// Multidimensional data structures...
	if ( dt !== 'generic' ) {
		if ( ndims === 2 ) {
			return matrix( dims, dt, mu, sigma, rand );
		}
		// TODO: dstructs-ndarray support goes here. Until then, fall through to plain arrays...
	}
	return arrayarray( dims, mu, sigma, rand );
} // end FUNCTION random()


// EXPORTS //

module.exports = random;

Object.defineProperty( module.exports, 'seed', {
	set: function ( newVal ) {
		if ( !isPositiveInteger( newVal ) ) {
			throw new TypeError( 'random()::invalid value. Seed property must be a positive integer. Option: `' + newVal + '`.' );
		}
		RAND = lcg( newVal );
	}
});

},{"./array.js":2148,"./arrayarray.js":2149,"./matrix.js":2152,"./number.js":2153,"./typedarray.js":2156,"./validate.js":2157,"compute-lcg":2160,"validate.io-positive-integer":2217,"validate.io-positive-integer-array":2215}],2152:[function(require,module,exports){
'use strict';

// MODULES //

var matrix = require( 'dstructs-matrix' ),
	partial = require( './partial.js' );


// RANDOM //

/**
* FUNCTION: random( dims, dt, mu, sigma[, rand] )
*	Creates a matrix of normally distributed random numbers.
*
* @param {Number[]} dims - dimensions
* @param {String} dt - data type
* @param {Number} mu - mean parameter
* @param {Number} sigma - standard deviation
* @param {Function} [rand=Math.random] - random number generator
* @returns {Matrix} matrix filled with normal random numbers
*/
function random( dims, dt, mu, sigma, rand ) {
	var out,
		draw,
		i;

	draw = partial( mu, sigma, rand );
	out = matrix( dims, dt );
	for ( i = 0; i < out.length; i++ ) {
		out.data[ i ] = draw();
	}
	return out;
} // end FUNCTION random()


// EXPORTS //

module.exports = random;

},{"./partial.js":2154,"dstructs-matrix":2169}],2153:[function(require,module,exports){
'use strict';

// MODULES //

var dRanNormalTail = require( './dRanNormalTail.js' );


// FUNCTIONS //

var abs = Math.abs,
	exp = Math.exp,
	log = Math.log,
	pow = Math.pow,
	sqrt = Math.sqrt;


// CONSTANTS //

var TWO_P_32 = pow( 2, 32);


// GENERATE NORMAL RANDOM NUMBERS //

/**
* FUNCTION random( mu, sigma[, rand] )
*	Generates a random draw from a normal distribution
*	with parameters `mu` and `sigma`. Implementation
*	of the "Improved Ziggurat Method" by J. Doornik.
*	Reference:
*		Doornik, J. a. (2005).
*		An Improved Ziggurat Method to Generate Normal Random Samples.
*
* @param {Number} mu - mean parameter
* @param {Number} sigma - standard deviation
* @param {Function} [rand=Math.random] - random number generator
* @returns {Number} random draw from the specified distribution
*/
function random( mu, sigma, rand ) {

	if ( !rand ) {
		rand = Math.random;
	}

	var ZIGNOR_C = 128,/* number of blocks */
 		ZIGNOR_R = 3.442619855899, /* start of the right tail *
		/* (R * phi(R) + Pr(X>=R)) * sqrt(2\pi) */
		ZIGNOR_V = 9.91256303526217e-3,
		/* s_adZigX holds coordinates, such that each rectangle has
			same area; s_adZigR holds s_adZigX[i + 1] / s_adZigX[i] */
		s_adZigX = new Array( ZIGNOR_C + 1 ),
		s_adZigR = new Array( ZIGNOR_C ),
		i, f;

	f = exp( -0.5 * ZIGNOR_R * ZIGNOR_R );
	s_adZigX[0] = ZIGNOR_V / f; /* [0] is bottom block: V / f(R) */
	s_adZigX[1] = ZIGNOR_R;
	s_adZigX[ZIGNOR_C] = 0;
	for ( i = 2; i < ZIGNOR_C; i++ ) {
		s_adZigX[i] = sqrt( -2 * log( ZIGNOR_V / s_adZigX[i - 1] + f ) );
		f = exp( -0.5 * s_adZigX[i] * s_adZigX[i] );
	}
	for ( i = 0; i < ZIGNOR_C; i++ ) {
		s_adZigR[i] = s_adZigX[i + 1] / s_adZigX[i];
	}
	var x, u, f0, f1;
	for (;;) {
		u = 2 * rand() - 1;
		i = TWO_P_32 * rand() & 0x7F;
		/* first try the rectangular boxes */
		if ( abs(u) < s_adZigR[i] ) {
			return mu + sigma * u * s_adZigX[i];
		}
		/* bottom box: sample from the tail */
		if ( i === 0 ) {
			return mu + sigma * dRanNormalTail( ZIGNOR_R, u < 0, rand );
		}
		/* is this a sample from the wedges? */
		x = u * s_adZigX[i];
		f0 = exp( -0.5 * ( s_adZigX[i] * s_adZigX[i] - x * x ) );
		f1 = exp( -0.5 * ( s_adZigX[i+1] * s_adZigX[i+1] - x * x ) );
		if ( f1 + rand() * (f0 - f1) < 1.0 ) {
			return mu + sigma * x;
		}
	}
} // end FUNCTION random()


// EXPORTS //

module.exports = random;

},{"./dRanNormalTail.js":2150}],2154:[function(require,module,exports){
'use strict';

// MODULES //

var dRanNormalTail = require( './dRanNormalTail.js' );


// FUNCTIONS  //

var abs = Math.abs,
	exp = Math.exp,
	log = Math.log,
	pow = Math.pow,
	sqrt = Math.sqrt;


// PARTIAL //

/**
* FUNCTION: partial( mu, sigma[, rand] )
*	Partially applies `mu` and `sigma` and returns a function
*	to generate random variables from the normal distribution. Implementation
*	of the "Improved Ziggurat Method" by J. Doornik.
*	Reference:
*		Doornik, J. a. (2005).
*		An Improved Ziggurat Method to Generate Normal Random Samples.
*
* @param {Number} mu - mean parameter
* @param {Number} sigma - standard deviation
* @param {Function} [rand=Math.random] - random number generator
* @returns {Function} function which generates random draws from the specified distribution
*/
function partial( mu, sigma, rand ) {
	var random,
		ZIGNOR_C = 128,/* number of blocks */
		ZIGNOR_R = 3.442619855899, /* start of the right tail *
		/* (R * phi(R) + Pr(X>=R)) * sqrt(2\pi) */
		ZIGNOR_V = 9.91256303526217e-3,
		/* s_adZigX holds coordinates, such that each rectangle has
			same area; s_adZigR holds s_adZigX[i + 1] / s_adZigX[i] */
		s_adZigX = new Array( ZIGNOR_C + 1 ),
		s_adZigR = new Array( ZIGNOR_C ),
		i, f,
		TWO_P_32 = pow( 2, 32 );

	if ( rand ) {
		random = rand;
	} else {
		random = Math.random;
	}

	f = exp( -0.5 * ZIGNOR_R * ZIGNOR_R );
	s_adZigX[0] = ZIGNOR_V / f; /* [0] is bottom block: V / f(R) */
	s_adZigX[1] = ZIGNOR_R;
	s_adZigX[ZIGNOR_C] = 0;
	for ( i = 2; i < ZIGNOR_C; i++ ) {
		s_adZigX[i] = sqrt( -2 * log( ZIGNOR_V / s_adZigX[i - 1] + f ) );
		f = exp( -0.5 * s_adZigX[i] * s_adZigX[i] );
	}
	for ( i = 0; i < ZIGNOR_C; i++ ) {
		s_adZigR[i] = s_adZigX[i + 1] / s_adZigX[i];
	}

	/**
	* FUNCTION: draw( x )
	*	Generates a random draw for a normal distribution with parameters `mu` and `sigma`.
	*
	* @private
	* @returns {Number} random draw from the specified distribution
	*/
	return function draw() {
		var x, u, f0, f1;
		for (;;) {
			u = 2 * random() - 1;
			i = TWO_P_32 * random() & 0x7F;
			/* first try the rectangular boxes */
			if ( abs(u) < s_adZigR[i] ) {
				return mu + sigma * u * s_adZigX[i];
			}
			/* bottom box: sample from the tail */
			if ( i === 0 ) {
				return mu + sigma * dRanNormalTail( ZIGNOR_R, u < 0, rand );
			}
			/* is this a sample from the wedges? */
			x = u * s_adZigX[i];
			f0 = exp( -0.5 * ( s_adZigX[i] * s_adZigX[i] - x * x ) );
			f1 = exp( -0.5 * ( s_adZigX[i+1] * s_adZigX[i+1] - x * x ) );
			if ( f1 + random() * (f0 - f1) < 1.0 ) {
				return mu + sigma * x;
			}
		}
	}; // end FUNCTION draw()
} // end FUNCTION partial()


// EXPORTS //

module.exports = partial;

},{"./dRanNormalTail.js":2150}],2155:[function(require,module,exports){
'use strict';

/**
* FUNCTION: recurse( dims, d, draw )
*	Recursively create a multidimensional array of normally distributed random numbers.
*
* @param {Number[]} dims - dimensions
* @param {Number} d - current recursion depth
* @param {Function} draw - function to generate normal random numbers with given `mu` and `sigma`
* @returns {Array} output array
*/
function recurse( dims, d, draw ) {
	var out = [],
		len,
		i;

	len = dims[ d ];
	d += 1;
	if ( d < dims.length ) {
		for ( i = 0; i < len; i++ ) {
			out.push( recurse( dims, d, draw ) );
		}
	} else {
		for ( i = 0; i < len; i++ ) {
			out.push( draw() );
		}
	}
	return out;
} // end FUNCTION recurse()


// EXPORTS //

module.exports = recurse;

},{}],2156:[function(require,module,exports){
'use strict';

// MODULES //

var ctors = require( 'compute-array-constructors' ),
	partial = require( './partial.js' );


// RANDOM //

/**
* FUNCTION: random( len, dt, mu, sigma[, rand] )
*	Creates a typed array of normally distributed random numbers.
*
* @param {Number} len - array length
* @param {String} dt - data type
* @param {Number} mu - mean parameter
* @param {Number} sigma - standard deviation
* @param {Function} [rand=Math.random] - random number generator
* @returns {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} typed array filled with normal random numbers
*/
function random( len, dt, mu, sigma, rand ) {
	/* jshint newcap:false */
	var ctor,
		out,
		draw,
		i;

	draw = partial( mu, sigma, rand );
	ctor = ctors( dt );
	if ( ctor === null ) {
		throw new Error( 'random()::invalid value. Data type does not have a corresponding array constructor. Value: `' + dt + '`.' );
	}
	out = new ctor( len );
	for ( i = 0; i < len; i++ ) {
		out[ i ] = draw();
	}
	return out;
} // end FUNCTION random()


// EXPORTS //

module.exports = random;

},{"./partial.js":2154,"compute-array-constructors":2159}],2157:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isNonNegative = require( 'validate.io-nonnegative' ),
	isNumber = require( 'validate.io-number-primitive' ),
	isPositiveInteger = require( 'validate.io-positive-integer' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Number} [options.mu] - mean parameter
* @param {Number} [options.sigma] - standard deviation
* @param {String} [options.dtype] - output data type
* @param {Number} [options.seed] - integer-valued seed
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'random()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'mu' ) ) {
		opts.mu = options.mu;
		if ( !isNumber( opts.mu ) ) {
			return new TypeError( 'random()::invalid option. `mu` parameter must be a number primitive. Option: `' + opts.mu + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sigma' ) ) {
		opts.sigma = options.sigma;
		if ( !isNonNegative( opts.sigma ) ) {
			return new TypeError( 'random()::invalid option. `sigma` parameter must be a non-negative number. Option: `' + opts.sigma + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'random()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'seed' ) ) {
		opts.seed = options.seed;
		if ( !isPositiveInteger( opts.seed ) ) {
			return new TypeError( 'random()::invalid option. Seed option must be a positive integer. Option: `' + opts.seed + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-nonnegative":2211,"validate.io-number-primitive":3388,"validate.io-object":2213,"validate.io-positive-integer":2217,"validate.io-string-primitive":2220}],2158:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],2159:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":2158,"dup":12}],2160:[function(require,module,exports){
/**
*
*	COMPUTE: lcg
*
*
*	DESCRIPTION:
*		- A linear congruential pseudorandom number generator (lcg).
*
*
*	NOTES:
*		[1] Based on W. Press, et al., Numerical Recipes in C (2d ed. 1992)
*
*
*	TODO:
*		[1]
*
*
*	LICENSE:
*		MIT
*
*	Copyright (c) 2014. rgizz.
*
*
*	AUTHOR:
*		rgizz. gztown2216@yahoo.com. 2014.
*
*/

'use strict';

// VARIABLES //

var MASK = 123459876,
	M = 2147483647,
	A = 16807;


// LCG //

/**
* FUNCTION: lcg( [seed] )
*	Returns a linear congruential pseudorandom number generator. If not provided a seed, a seed is generated based on the current time.
*
* @param {Number} [seed] - random number generator seed
* @returns {Function} generator
*/
function lcg( val ) {
	var seed;
	if ( arguments.length ) {
		if ( typeof val !== 'number' || val !== val || val % 1 !== 0 || val < 1 ) {
			throw new TypeError( 'lcg()::invalid input argument. Seed must be a positive integer.' );
		}
		seed = val;
	} else {
		seed = Date.now() % 100000000;
	}
	/**
	* FUNCTION: lcg( [N] )
	*	Linear congruential pseudorandom number generator.
	*
	* @param {Number} [N] - number of pseudorandom numbers to return
	* @returns {Number|Array} pseudorandom floating-point number(s) between 0 and 1
	*/
	return function lcg( N ) {
		var arr,
			rand;
		if ( !arguments.length ) {
			seed = seed ^ MASK;
			seed = ( A * seed ) % M;
			rand = seed / M;
			seed = seed ^ MASK;
			return rand;
		}
		if ( typeof N !== 'number' || N !== N || N%1 !== 0 || N < 1 ) {
			throw new TypeError( 'lcg()::invalid input argument. Array length must be a positive integer.' );
		}
		arr = new Array( N );
		for ( var i = 0; i < N; i++ ) {
			seed = seed ^ MASK;
			seed = ( A * seed ) % M;
			arr[ i ] = seed / M;
			seed = seed ^ MASK;
		}
		return arr;
	};
} // end FUNCTION lcg()


// EXPORTS //

module.exports = lcg;



},{}],2161:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],2162:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":2165,"./iget.js":2167,"./iset.js":2170,"./mget.js":2174,"./mset.js":2176,"./set.js":2184,"./sget.js":2186,"./sset.js":2188,"./toString.js":2190,"dup":14}],2163:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":2166,"./iget.raw.js":2168,"./iset.raw.js":2171,"./mget.raw.js":2175,"./mset.raw.js":2177,"./set.raw.js":2185,"./sget.raw.js":2187,"./sset.raw.js":2189,"./toString.js":2190,"dup":15}],2164:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],2165:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":2207}],2166:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],2167:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":2205}],2168:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],2169:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":2172,"./matrix.raw.js":2173,"dup":21}],2170:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":2205,"validate.io-number-primitive":2210}],2171:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],2172:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":2161,"./ctor.js":2162,"./dtypes.js":2164,"compute-cast-arrays":2191,"compute-dtype":2196,"dup":24,"validate.io-array":2201,"validate.io-contains":2202,"validate.io-nonnegative-integer-array":2206,"validate.io-string-primitive":2220}],2173:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":2161,"./ctor.raw.js":2163,"./dtypes.js":2164,"compute-dtype":2196,"dup":25,"validate.io-contains":2202,"validate.io-string-primitive":2220}],2174:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":2161,"dup":26,"validate.io-nonnegative-integer-array":2206}],2175:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":2161,"dup":27}],2176:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":2178,"./mset2.js":2179,"./mset3.js":2180,"./mset4.js":2181,"./mset5.js":2182,"./mset6.js":2183,"dup":28,"validate.io-function":2204,"validate.io-nonnegative-integer-array":2206,"validate.io-number-primitive":2210}],2177:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":2178,"./mset2.js":2179,"./mset3.js":2180,"./mset4.js":2181,"./mset5.js":2182,"./mset6.js":2183,"dup":29}],2178:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],2179:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],2180:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],2181:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],2182:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],2183:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],2184:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":2207,"validate.io-number-primitive":2210}],2185:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],2186:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":2161,"compute-indexspace":2200,"dup":38,"validate.io-string-primitive":2220}],2187:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":2161,"compute-indexspace":2200,"dup":39}],2188:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":2200,"dup":40,"validate.io-function":2204,"validate.io-number-primitive":2210,"validate.io-string-primitive":2220}],2189:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":2200,"dup":41}],2190:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],2191:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":2158,"compute-array-dtype/lib/dtypes":2192,"dup":43,"type-name":2193,"validate.io-array-like":2194}],2192:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2193:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2194:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":2195,"dup":70,"validate.io-integer-primitive":2205}],2195:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],2196:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":2198,"dup":46,"type-name":2199}],2197:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2198:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":2197,"dup":48}],2199:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2200:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":2207,"validate.io-string-primitive":2220}],2201:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2202:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":2201,"validate.io-nan-primitive":2203}],2203:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],2204:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],2205:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2210}],2206:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":2201,"validate.io-nonnegative-integer":2207}],2207:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":2208}],2208:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":2209}],2209:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],2210:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],2211:[function(require,module,exports){
arguments[4][1059][0].apply(exports,arguments)
},{"dup":1059,"validate.io-number":2212}],2212:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],2213:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":2214}],2214:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2215:[function(require,module,exports){
arguments[4][470][0].apply(exports,arguments)
},{"dup":470,"validate.io-array":2216,"validate.io-positive-integer":2217}],2216:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2217:[function(require,module,exports){
arguments[4][471][0].apply(exports,arguments)
},{"dup":471,"validate.io-integer":2218}],2218:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":2219}],2219:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],2220:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],2221:[function(require,module,exports){
arguments[4][1226][0].apply(exports,arguments)
},{"./number.js":2226,"dup":1226}],2222:[function(require,module,exports){
arguments[4][1227][0].apply(exports,arguments)
},{"./number.js":2226,"dup":1227}],2223:[function(require,module,exports){
arguments[4][1228][0].apply(exports,arguments)
},{"./number.js":2226,"dup":1228,"utils-deep-get":2277,"utils-deep-set":2283}],2224:[function(require,module,exports){
arguments[4][1229][0].apply(exports,arguments)
},{"./accessor.js":2221,"./array.js":2222,"./deepset.js":2223,"./matrix.js":2225,"./number.js":2226,"./typedarray.js":2227,"./validate.js":2228,"compute-array-constructors":2230,"dstructs-matrix":2239,"dup":1229,"validate.io-array-like":2286,"validate.io-matrix-like":2291,"validate.io-nan":2292,"validate.io-number-primitive":2296,"validate.io-typed-array-like":2301}],2225:[function(require,module,exports){
arguments[4][1230][0].apply(exports,arguments)
},{"./number.js":2226,"dup":1230}],2226:[function(require,module,exports){
'use strict';

// MODULES //

var isPositive = require( 'validate.io-positive-primitive' );


// EKURTOSIS //

/**
* FUNCTION ekurtosis( lambda )
*	Computes the distribution ekurtosis for a Poisson distribution with parameter lambda.
*
* @param {Number} lambda - mean parameter
* @returns {Number} distribution ekurtosis
*/
function ekurtosis( lambda ) {
	if ( !isPositive( lambda ) ) {
		return NaN;
	}
	return 1 / lambda;
} // end FUNCTION ekurtosis()


// EXPORTS

module.exports =  ekurtosis;

},{"validate.io-positive-primitive":2299}],2227:[function(require,module,exports){
arguments[4][1232][0].apply(exports,arguments)
},{"./number.js":2226,"dup":1232}],2228:[function(require,module,exports){
arguments[4][1233][0].apply(exports,arguments)
},{"dup":1233,"validate.io-boolean-primitive":2289,"validate.io-function":2290,"validate.io-object":2297,"validate.io-string-primitive":2300}],2229:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],2230:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":2229,"dup":12}],2231:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],2232:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":2235,"./iget.js":2237,"./iset.js":2240,"./mget.js":2244,"./mset.js":2246,"./set.js":2254,"./sget.js":2256,"./sset.js":2258,"./toString.js":2260,"dup":14}],2233:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":2236,"./iget.raw.js":2238,"./iset.raw.js":2241,"./mget.raw.js":2245,"./mset.raw.js":2247,"./set.raw.js":2255,"./sget.raw.js":2257,"./sset.raw.js":2259,"./toString.js":2260,"dup":15}],2234:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],2235:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":2293}],2236:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],2237:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":2272}],2238:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],2239:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":2242,"./matrix.raw.js":2243,"dup":21}],2240:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":2272,"validate.io-number-primitive":2296}],2241:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],2242:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":2231,"./ctor.js":2232,"./dtypes.js":2234,"compute-cast-arrays":2261,"compute-dtype":2264,"dup":24,"validate.io-array":2269,"validate.io-contains":2270,"validate.io-nonnegative-integer-array":2273,"validate.io-string-primitive":2300}],2243:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":2231,"./ctor.raw.js":2233,"./dtypes.js":2234,"compute-dtype":2264,"dup":25,"validate.io-contains":2270,"validate.io-string-primitive":2300}],2244:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":2231,"dup":26,"validate.io-nonnegative-integer-array":2273}],2245:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":2231,"dup":27}],2246:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":2248,"./mset2.js":2249,"./mset3.js":2250,"./mset4.js":2251,"./mset5.js":2252,"./mset6.js":2253,"dup":28,"validate.io-function":2290,"validate.io-nonnegative-integer-array":2273,"validate.io-number-primitive":2296}],2247:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":2248,"./mset2.js":2249,"./mset3.js":2250,"./mset4.js":2251,"./mset5.js":2252,"./mset6.js":2253,"dup":29}],2248:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],2249:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],2250:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],2251:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],2252:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],2253:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],2254:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":2293,"validate.io-number-primitive":2296}],2255:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],2256:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":2231,"compute-indexspace":2268,"dup":38,"validate.io-string-primitive":2300}],2257:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":2231,"compute-indexspace":2268,"dup":39}],2258:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":2268,"dup":40,"validate.io-function":2290,"validate.io-number-primitive":2296,"validate.io-string-primitive":2300}],2259:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":2268,"dup":41}],2260:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],2261:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":2229,"compute-array-dtype/lib/dtypes":2262,"dup":43,"type-name":2263,"validate.io-array-like":2286}],2262:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2263:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2264:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":2266,"dup":46,"type-name":2267}],2265:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2266:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":2265,"dup":48}],2267:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2268:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":2293,"validate.io-string-primitive":2300}],2269:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2270:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":2269,"validate.io-nan-primitive":2271}],2271:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],2272:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2296}],2273:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":2269,"validate.io-nonnegative-integer":2293}],2274:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],2275:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],2276:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":2274,"./defaults.js":2275,"./validate.js":2278,"dup":60,"validate.io-array":2279,"validate.io-string-primitive":2300}],2277:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":2274,"./defaults.js":2275,"./factory.js":2276,"./validate.js":2278,"dup":61,"validate.io-array":2279,"validate.io-string-primitive":2300}],2278:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":2297,"validate.io-string-primitive":2300}],2279:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2280:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],2281:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],2282:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":2280,"./defaults.js":2281,"./validate.js":2284,"dup":66,"validate.io-array":2285,"validate.io-string-primitive":2300}],2283:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":2280,"./defaults.js":2281,"./factory.js":2282,"./validate.js":2284,"dup":67,"validate.io-array":2285,"validate.io-string-primitive":2300}],2284:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":2289,"validate.io-object":2297,"validate.io-string-primitive":2300}],2285:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2286:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":2287,"dup":70,"validate.io-integer-primitive":2288}],2287:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],2288:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2296}],2289:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],2290:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],2291:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],2292:[function(require,module,exports){
arguments[4][76][0].apply(exports,arguments)
},{"dup":76}],2293:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":2294}],2294:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":2295}],2295:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],2296:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],2297:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":2298}],2298:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2299:[function(require,module,exports){
arguments[4][1221][0].apply(exports,arguments)
},{"dup":1221,"validate.io-number-primitive":2296}],2300:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],2301:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":2302,"dup":81,"validate.io-integer-primitive":2303}],2302:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],2303:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2296}],2304:[function(require,module,exports){
arguments[4][1309][0].apply(exports,arguments)
},{"./number.js":2309,"dup":1309}],2305:[function(require,module,exports){
arguments[4][1310][0].apply(exports,arguments)
},{"./number.js":2309,"dup":1310}],2306:[function(require,module,exports){
arguments[4][1311][0].apply(exports,arguments)
},{"./number.js":2309,"dup":1311,"utils-deep-get":2360,"utils-deep-set":2366}],2307:[function(require,module,exports){
arguments[4][1312][0].apply(exports,arguments)
},{"./accessor.js":2304,"./array.js":2305,"./deepset.js":2306,"./matrix.js":2308,"./number.js":2309,"./typedarray.js":2310,"./validate.js":2311,"compute-array-constructors":2313,"dstructs-matrix":2322,"dup":1312,"validate.io-array-like":2369,"validate.io-matrix-like":2374,"validate.io-nan":2375,"validate.io-number-primitive":2379,"validate.io-typed-array-like":2384}],2308:[function(require,module,exports){
arguments[4][1313][0].apply(exports,arguments)
},{"./number.js":2309,"dup":1313}],2309:[function(require,module,exports){
'use strict';

// MODULES //

var isPositive = require( 'validate.io-positive-primitive' );


// MEAN //

/**
* FUNCTION mean( lambda )
*	Computes the distribution mean for a Poisson with parameter lambda.
*
* @param {Number} lambda - mean parameter
* @returns {Number} distribution mean
*/
function mean( lambda ) {
	if ( !isPositive( lambda ) ) {
		return NaN;
	}
	return lambda;
} // end FUNCTION mean()


// EXPORTS

module.exports =  mean;

},{"validate.io-positive-primitive":2382}],2310:[function(require,module,exports){
arguments[4][1315][0].apply(exports,arguments)
},{"./number.js":2309,"dup":1315}],2311:[function(require,module,exports){
arguments[4][1316][0].apply(exports,arguments)
},{"dup":1316,"validate.io-boolean-primitive":2372,"validate.io-function":2373,"validate.io-object":2380,"validate.io-string-primitive":2383}],2312:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],2313:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":2312,"dup":12}],2314:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],2315:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":2318,"./iget.js":2320,"./iset.js":2323,"./mget.js":2327,"./mset.js":2329,"./set.js":2337,"./sget.js":2339,"./sset.js":2341,"./toString.js":2343,"dup":14}],2316:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":2319,"./iget.raw.js":2321,"./iset.raw.js":2324,"./mget.raw.js":2328,"./mset.raw.js":2330,"./set.raw.js":2338,"./sget.raw.js":2340,"./sset.raw.js":2342,"./toString.js":2343,"dup":15}],2317:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],2318:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":2376}],2319:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],2320:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":2355}],2321:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],2322:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":2325,"./matrix.raw.js":2326,"dup":21}],2323:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":2355,"validate.io-number-primitive":2379}],2324:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],2325:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":2314,"./ctor.js":2315,"./dtypes.js":2317,"compute-cast-arrays":2344,"compute-dtype":2347,"dup":24,"validate.io-array":2352,"validate.io-contains":2353,"validate.io-nonnegative-integer-array":2356,"validate.io-string-primitive":2383}],2326:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":2314,"./ctor.raw.js":2316,"./dtypes.js":2317,"compute-dtype":2347,"dup":25,"validate.io-contains":2353,"validate.io-string-primitive":2383}],2327:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":2314,"dup":26,"validate.io-nonnegative-integer-array":2356}],2328:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":2314,"dup":27}],2329:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":2331,"./mset2.js":2332,"./mset3.js":2333,"./mset4.js":2334,"./mset5.js":2335,"./mset6.js":2336,"dup":28,"validate.io-function":2373,"validate.io-nonnegative-integer-array":2356,"validate.io-number-primitive":2379}],2330:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":2331,"./mset2.js":2332,"./mset3.js":2333,"./mset4.js":2334,"./mset5.js":2335,"./mset6.js":2336,"dup":29}],2331:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],2332:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],2333:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],2334:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],2335:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],2336:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],2337:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":2376,"validate.io-number-primitive":2379}],2338:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],2339:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":2314,"compute-indexspace":2351,"dup":38,"validate.io-string-primitive":2383}],2340:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":2314,"compute-indexspace":2351,"dup":39}],2341:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":2351,"dup":40,"validate.io-function":2373,"validate.io-number-primitive":2379,"validate.io-string-primitive":2383}],2342:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":2351,"dup":41}],2343:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],2344:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":2312,"compute-array-dtype/lib/dtypes":2345,"dup":43,"type-name":2346,"validate.io-array-like":2369}],2345:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2346:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2347:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":2349,"dup":46,"type-name":2350}],2348:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2349:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":2348,"dup":48}],2350:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2351:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":2376,"validate.io-string-primitive":2383}],2352:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2353:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":2352,"validate.io-nan-primitive":2354}],2354:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],2355:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2379}],2356:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":2352,"validate.io-nonnegative-integer":2376}],2357:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],2358:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],2359:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":2357,"./defaults.js":2358,"./validate.js":2361,"dup":60,"validate.io-array":2362,"validate.io-string-primitive":2383}],2360:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":2357,"./defaults.js":2358,"./factory.js":2359,"./validate.js":2361,"dup":61,"validate.io-array":2362,"validate.io-string-primitive":2383}],2361:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":2380,"validate.io-string-primitive":2383}],2362:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2363:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],2364:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],2365:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":2363,"./defaults.js":2364,"./validate.js":2367,"dup":66,"validate.io-array":2368,"validate.io-string-primitive":2383}],2366:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":2363,"./defaults.js":2364,"./factory.js":2365,"./validate.js":2367,"dup":67,"validate.io-array":2368,"validate.io-string-primitive":2383}],2367:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":2372,"validate.io-object":2380,"validate.io-string-primitive":2383}],2368:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2369:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":2370,"dup":70,"validate.io-integer-primitive":2371}],2370:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],2371:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2379}],2372:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],2373:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],2374:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],2375:[function(require,module,exports){
arguments[4][76][0].apply(exports,arguments)
},{"dup":76}],2376:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":2377}],2377:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":2378}],2378:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],2379:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],2380:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":2381}],2381:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2382:[function(require,module,exports){
arguments[4][1221][0].apply(exports,arguments)
},{"dup":1221,"validate.io-number-primitive":2379}],2383:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],2384:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":2385,"dup":81,"validate.io-integer-primitive":2386}],2385:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],2386:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2379}],2387:[function(require,module,exports){
arguments[4][1392][0].apply(exports,arguments)
},{"./number.js":2392,"dup":1392}],2388:[function(require,module,exports){
arguments[4][1393][0].apply(exports,arguments)
},{"./number.js":2392,"dup":1393}],2389:[function(require,module,exports){
arguments[4][1394][0].apply(exports,arguments)
},{"./number.js":2392,"dup":1394,"utils-deep-get":2443,"utils-deep-set":2449}],2390:[function(require,module,exports){
arguments[4][1395][0].apply(exports,arguments)
},{"./accessor.js":2387,"./array.js":2388,"./deepset.js":2389,"./matrix.js":2391,"./number.js":2392,"./typedarray.js":2393,"./validate.js":2394,"compute-array-constructors":2396,"dstructs-matrix":2405,"dup":1395,"validate.io-array-like":2452,"validate.io-matrix-like":2457,"validate.io-nan":2458,"validate.io-number-primitive":2462,"validate.io-typed-array-like":2467}],2391:[function(require,module,exports){
arguments[4][1396][0].apply(exports,arguments)
},{"./number.js":2392,"dup":1396}],2392:[function(require,module,exports){
'use strict';

// MODULES //

var isPositive = require( 'validate.io-positive-primitive' );


// FUNCTIONS //

var floor = Math.floor;


// MEDIAN //

/**
* FUNCTION median( lambda )
*	Computes the distribution median for a Poisson distribution with parameter lambda.
*
* @param {Number} lambda - mean parameter
* @returns {Number} distribution median
*/
function median( lambda ) {
	if ( !isPositive( lambda ) ) {
		return NaN;
	}
	return floor( lambda + 1/3 - 0.02/lambda );
} // end FUNCTION median()


// EXPORTS

module.exports =  median;

},{"validate.io-positive-primitive":2465}],2393:[function(require,module,exports){
arguments[4][1398][0].apply(exports,arguments)
},{"./number.js":2392,"dup":1398}],2394:[function(require,module,exports){
arguments[4][1399][0].apply(exports,arguments)
},{"dup":1399,"validate.io-boolean-primitive":2455,"validate.io-function":2456,"validate.io-object":2463,"validate.io-string-primitive":2466}],2395:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],2396:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":2395,"dup":12}],2397:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],2398:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":2401,"./iget.js":2403,"./iset.js":2406,"./mget.js":2410,"./mset.js":2412,"./set.js":2420,"./sget.js":2422,"./sset.js":2424,"./toString.js":2426,"dup":14}],2399:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":2402,"./iget.raw.js":2404,"./iset.raw.js":2407,"./mget.raw.js":2411,"./mset.raw.js":2413,"./set.raw.js":2421,"./sget.raw.js":2423,"./sset.raw.js":2425,"./toString.js":2426,"dup":15}],2400:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],2401:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":2459}],2402:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],2403:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":2438}],2404:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],2405:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":2408,"./matrix.raw.js":2409,"dup":21}],2406:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":2438,"validate.io-number-primitive":2462}],2407:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],2408:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":2397,"./ctor.js":2398,"./dtypes.js":2400,"compute-cast-arrays":2427,"compute-dtype":2430,"dup":24,"validate.io-array":2435,"validate.io-contains":2436,"validate.io-nonnegative-integer-array":2439,"validate.io-string-primitive":2466}],2409:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":2397,"./ctor.raw.js":2399,"./dtypes.js":2400,"compute-dtype":2430,"dup":25,"validate.io-contains":2436,"validate.io-string-primitive":2466}],2410:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":2397,"dup":26,"validate.io-nonnegative-integer-array":2439}],2411:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":2397,"dup":27}],2412:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":2414,"./mset2.js":2415,"./mset3.js":2416,"./mset4.js":2417,"./mset5.js":2418,"./mset6.js":2419,"dup":28,"validate.io-function":2456,"validate.io-nonnegative-integer-array":2439,"validate.io-number-primitive":2462}],2413:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":2414,"./mset2.js":2415,"./mset3.js":2416,"./mset4.js":2417,"./mset5.js":2418,"./mset6.js":2419,"dup":29}],2414:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],2415:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],2416:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],2417:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],2418:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],2419:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],2420:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":2459,"validate.io-number-primitive":2462}],2421:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],2422:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":2397,"compute-indexspace":2434,"dup":38,"validate.io-string-primitive":2466}],2423:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":2397,"compute-indexspace":2434,"dup":39}],2424:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":2434,"dup":40,"validate.io-function":2456,"validate.io-number-primitive":2462,"validate.io-string-primitive":2466}],2425:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":2434,"dup":41}],2426:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],2427:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":2395,"compute-array-dtype/lib/dtypes":2428,"dup":43,"type-name":2429,"validate.io-array-like":2452}],2428:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2429:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2430:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":2432,"dup":46,"type-name":2433}],2431:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2432:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":2431,"dup":48}],2433:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2434:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":2459,"validate.io-string-primitive":2466}],2435:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2436:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":2435,"validate.io-nan-primitive":2437}],2437:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],2438:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2462}],2439:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":2435,"validate.io-nonnegative-integer":2459}],2440:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],2441:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],2442:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":2440,"./defaults.js":2441,"./validate.js":2444,"dup":60,"validate.io-array":2445,"validate.io-string-primitive":2466}],2443:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":2440,"./defaults.js":2441,"./factory.js":2442,"./validate.js":2444,"dup":61,"validate.io-array":2445,"validate.io-string-primitive":2466}],2444:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":2463,"validate.io-string-primitive":2466}],2445:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2446:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],2447:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],2448:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":2446,"./defaults.js":2447,"./validate.js":2450,"dup":66,"validate.io-array":2451,"validate.io-string-primitive":2466}],2449:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":2446,"./defaults.js":2447,"./factory.js":2448,"./validate.js":2450,"dup":67,"validate.io-array":2451,"validate.io-string-primitive":2466}],2450:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":2455,"validate.io-object":2463,"validate.io-string-primitive":2466}],2451:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2452:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":2453,"dup":70,"validate.io-integer-primitive":2454}],2453:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],2454:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2462}],2455:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],2456:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],2457:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],2458:[function(require,module,exports){
arguments[4][76][0].apply(exports,arguments)
},{"dup":76}],2459:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":2460}],2460:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":2461}],2461:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],2462:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],2463:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":2464}],2464:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2465:[function(require,module,exports){
arguments[4][1221][0].apply(exports,arguments)
},{"dup":1221,"validate.io-number-primitive":2462}],2466:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],2467:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":2468,"dup":81,"validate.io-integer-primitive":2469}],2468:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],2469:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2462}],2470:[function(require,module,exports){
arguments[4][1808][0].apply(exports,arguments)
},{"./number.js":2475,"dup":1808}],2471:[function(require,module,exports){
arguments[4][1809][0].apply(exports,arguments)
},{"./number.js":2475,"dup":1809}],2472:[function(require,module,exports){
arguments[4][1810][0].apply(exports,arguments)
},{"./number.js":2475,"dup":1810,"utils-deep-get":2526,"utils-deep-set":2532}],2473:[function(require,module,exports){
arguments[4][1811][0].apply(exports,arguments)
},{"./accessor.js":2470,"./array.js":2471,"./deepset.js":2472,"./matrix.js":2474,"./number.js":2475,"./typedarray.js":2476,"./validate.js":2477,"compute-array-constructors":2479,"dstructs-matrix":2488,"dup":1811,"validate.io-array-like":2535,"validate.io-matrix-like":2540,"validate.io-nan":2541,"validate.io-number-primitive":2545,"validate.io-typed-array-like":2550}],2474:[function(require,module,exports){
arguments[4][1812][0].apply(exports,arguments)
},{"./number.js":2475,"dup":1812}],2475:[function(require,module,exports){
'use strict';

// MODULES //

var isPositive = require( 'validate.io-positive-primitive' );


// VARIANCE //

/**
* FUNCTION variance( lambda )
*	Computes the distribution variance for a Poisson distribution with parameter lambda.
*
* @param {Number} lambda - mean parameter
* @returns {Number} distribution variance
*/
function variance( lambda ) {
	if ( !isPositive( lambda ) ) {
		return NaN;
	}
	return lambda;
} // end FUNCTION variance()


// EXPORTS

module.exports =  variance;

},{"validate.io-positive-primitive":2548}],2476:[function(require,module,exports){
arguments[4][1814][0].apply(exports,arguments)
},{"./number.js":2475,"dup":1814}],2477:[function(require,module,exports){
arguments[4][1815][0].apply(exports,arguments)
},{"dup":1815,"validate.io-boolean-primitive":2538,"validate.io-function":2539,"validate.io-object":2546,"validate.io-string-primitive":2549}],2478:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],2479:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":2478,"dup":12}],2480:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],2481:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":2484,"./iget.js":2486,"./iset.js":2489,"./mget.js":2493,"./mset.js":2495,"./set.js":2503,"./sget.js":2505,"./sset.js":2507,"./toString.js":2509,"dup":14}],2482:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":2485,"./iget.raw.js":2487,"./iset.raw.js":2490,"./mget.raw.js":2494,"./mset.raw.js":2496,"./set.raw.js":2504,"./sget.raw.js":2506,"./sset.raw.js":2508,"./toString.js":2509,"dup":15}],2483:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],2484:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":2542}],2485:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],2486:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":2521}],2487:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],2488:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":2491,"./matrix.raw.js":2492,"dup":21}],2489:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":2521,"validate.io-number-primitive":2545}],2490:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],2491:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":2480,"./ctor.js":2481,"./dtypes.js":2483,"compute-cast-arrays":2510,"compute-dtype":2513,"dup":24,"validate.io-array":2518,"validate.io-contains":2519,"validate.io-nonnegative-integer-array":2522,"validate.io-string-primitive":2549}],2492:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":2480,"./ctor.raw.js":2482,"./dtypes.js":2483,"compute-dtype":2513,"dup":25,"validate.io-contains":2519,"validate.io-string-primitive":2549}],2493:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":2480,"dup":26,"validate.io-nonnegative-integer-array":2522}],2494:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":2480,"dup":27}],2495:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":2497,"./mset2.js":2498,"./mset3.js":2499,"./mset4.js":2500,"./mset5.js":2501,"./mset6.js":2502,"dup":28,"validate.io-function":2539,"validate.io-nonnegative-integer-array":2522,"validate.io-number-primitive":2545}],2496:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":2497,"./mset2.js":2498,"./mset3.js":2499,"./mset4.js":2500,"./mset5.js":2501,"./mset6.js":2502,"dup":29}],2497:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],2498:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],2499:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],2500:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],2501:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],2502:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],2503:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":2542,"validate.io-number-primitive":2545}],2504:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],2505:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":2480,"compute-indexspace":2517,"dup":38,"validate.io-string-primitive":2549}],2506:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":2480,"compute-indexspace":2517,"dup":39}],2507:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":2517,"dup":40,"validate.io-function":2539,"validate.io-number-primitive":2545,"validate.io-string-primitive":2549}],2508:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":2517,"dup":41}],2509:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],2510:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":2478,"compute-array-dtype/lib/dtypes":2511,"dup":43,"type-name":2512,"validate.io-array-like":2535}],2511:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2512:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2513:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":2515,"dup":46,"type-name":2516}],2514:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2515:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":2514,"dup":48}],2516:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2517:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":2542,"validate.io-string-primitive":2549}],2518:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2519:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":2518,"validate.io-nan-primitive":2520}],2520:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],2521:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2545}],2522:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":2518,"validate.io-nonnegative-integer":2542}],2523:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],2524:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],2525:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":2523,"./defaults.js":2524,"./validate.js":2527,"dup":60,"validate.io-array":2528,"validate.io-string-primitive":2549}],2526:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":2523,"./defaults.js":2524,"./factory.js":2525,"./validate.js":2527,"dup":61,"validate.io-array":2528,"validate.io-string-primitive":2549}],2527:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":2546,"validate.io-string-primitive":2549}],2528:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2529:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],2530:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],2531:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":2529,"./defaults.js":2530,"./validate.js":2533,"dup":66,"validate.io-array":2534,"validate.io-string-primitive":2549}],2532:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":2529,"./defaults.js":2530,"./factory.js":2531,"./validate.js":2533,"dup":67,"validate.io-array":2534,"validate.io-string-primitive":2549}],2533:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":2538,"validate.io-object":2546,"validate.io-string-primitive":2549}],2534:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2535:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":2536,"dup":70,"validate.io-integer-primitive":2537}],2536:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],2537:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2545}],2538:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],2539:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],2540:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],2541:[function(require,module,exports){
arguments[4][76][0].apply(exports,arguments)
},{"dup":76}],2542:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":2543}],2543:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":2544}],2544:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],2545:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],2546:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":2547}],2547:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2548:[function(require,module,exports){
arguments[4][1221][0].apply(exports,arguments)
},{"dup":1221,"validate.io-number-primitive":2545}],2549:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],2550:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":2551,"dup":81,"validate.io-integer-primitive":2552}],2551:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],2552:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2545}],2553:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// CDF //

/**
* FUNCTION: cdf( out, arr, sigma, accessor )
*	Evaluates the cumulative distribution function (CDF) for a Rayleigh distribution with scale parameter `sigma` using an accessor function.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Number} sigma - scale parameter
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function cdf( y, x, sigma, clbk ) {
	var len = x.length,
		fcn,
		v, i;

	fcn = partial( sigma );
	for ( i = 0; i < len; i++ ) {
		v = clbk( x[ i ], i );
		if ( typeof v === 'number' ) {
			y[ i ] = fcn( v );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION cdf()


// EXPORTS //

module.exports = cdf;

},{"./partial.js":2559}],2554:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// CDF //

/**
* FUNCTION: cdf( out, arr, sigma )
*	Evaluates the cumulative distribution function (CDF) for a Rayleigh distribution with scale parameter `sigma` for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Number} sigma - scale parameter
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function cdf( y, x, sigma ) {
	var len = x.length,
		fcn,
		i;

	fcn = partial( sigma );
	for ( i = 0; i < len; i++ ) {
		if ( typeof x[ i ] === 'number' ) {
			y[ i ] = fcn( x[ i ] );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION cdf()


// EXPORTS //

module.exports = cdf;

},{"./partial.js":2559}],2555:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory,
	partial = require( './partial.js' );


// CDF //

/**
* FUNCTION: cdf( arr, sigma, path[, sep] )
*	Evaluates the cumulative distribution function (CDF) for a Rayleigh distribution with scale parameter `sigma` for each array element and sets the input array.
*
* @param {Array} arr - input array
* @param {Number} sigma - scale parameter
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function cdf( x, sigma, path, sep ) {
	var len = x.length,
		opts = {},
		dget,
		dset,
		fcn,
		v, i;
	if ( arguments.length > 3 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		fcn = partial( sigma );
		for ( i = 0; i < len; i++ ) {
			v = dget( x[ i ] );
			if ( typeof v === 'number' ) {
				dset( x[i], fcn( v ) );
			} else {
				dset( x[i], NaN );
			}
		}
	}
	return x;
} // end FUNCTION cdf()


// EXPORTS //

module.exports = cdf;

},{"./partial.js":2559,"utils-deep-get":2610,"utils-deep-set":2616}],2556:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var cdf1 = require( './number.js' ),
	cdf2 = require( './array.js' ),
	cdf3 = require( './accessor.js' ),
	cdf4 = require( './deepset.js' ),
	cdf5 = require( './matrix.js' ),
	cdf6 = require( './typedarray.js' );


// CDF //

/**
* FUNCTION: cdf( x[, opts] )
*	Evaluates the cumulative distribution function (CDF) for a Rayleigh distribution.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} x - input value
* @param {Object} [opts] - function options
* @param {Number} [opts.sigma=1] - scale parameter
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} evaluated CDF
*/
function cdf( x, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	opts.sigma = typeof opts.sigma !== 'undefined' ? opts.sigma : 1;

	if ( isNumber( x ) ) {
		return cdf1( x, opts.sigma );
	}
	if ( isMatrixLike( x ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'cdf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( x.length );
			out = matrix( d, x.shape, dt );
		} else {
			out = x;
		}
		return cdf5( out, x, opts.sigma );
	}
	if ( isTypedArrayLike( x ) ) {
		if ( opts.copy === false ) {
			out = x;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'cdf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( x.length );
		}
		return cdf6( out, x, opts.sigma );
	}
	if ( isArrayLike( x ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return cdf4( x, opts.sigma, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = x;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'cdf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( x.length );
		}
		else {
			out = new Array( x.length );
		}
		if ( opts.accessor ) {
			return cdf3( out, x, opts.sigma, opts.accessor );
		}
		return cdf2( out, x, opts.sigma );
	}
	return NaN;
} // end FUNCTION cdf()


// EXPORTS //

module.exports = cdf;

},{"./accessor.js":2553,"./array.js":2554,"./deepset.js":2555,"./matrix.js":2557,"./number.js":2558,"./typedarray.js":2560,"./validate.js":2561,"compute-array-constructors":2563,"dstructs-matrix":2572,"validate.io-array-like":2619,"validate.io-matrix-like":2624,"validate.io-number-primitive":2628,"validate.io-typed-array-like":2633}],2557:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// CDF //

/**
* FUNCTION: cdf( out, matrix, sigma )
*	Evaluates the cumulative distribution function (CDF) for a Rayleigh distribution with scale parameter `sigma` for each matrix element.
*
* @param {Matrix} out - output matrix
* @param {Matrix} arr - input matrix
* @param {Number} sigma - scale parameter
* @returns {Matrix} output matrix
*/
function cdf( y, x, sigma ) {
	var len = x.length,
		fcn,
		i;
	if ( y.length !== len ) {
		throw new Error( 'cdf()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	fcn = partial( sigma );
	for ( i = 0; i < len; i++ ) {
		y.data[ i ] = fcn( x.data[ i ] );
	}
	return y;
} // end FUNCTION cdf()


// EXPORTS //

module.exports = cdf;

},{"./partial.js":2559}],2558:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var expm1 = Math.expm1 || function(x) {
  return Math.exp(x) - 1;
};
var pow = Math.pow;



// CDF //

/**
* FUNCTION: cdf( x, sigma )
*	Evaluates the cumulative distribution function (CDF) for a Rayleigh distribution with scale parameter `sigma` at a value `x`.
*
* @param {Number} x - input value
* @param {Number} sigma - scale parameter
* @returns {Number} evaluated CDF
*/
function cdf( x, sigma ) {
	if ( x < 0 ) {
		return 0;
	}
	var s2 = pow( sigma, 2 );
	return -expm1( - pow( x, 2) / ( 2 * s2 ) );
} // end FUNCTION cdf()


// EXPORTS //

module.exports = cdf;

},{}],2559:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var expm1 = Math.expm1 || function(x) {
  return Math.exp(x) - 1;
};
var pow = Math.pow;


// PARTIAL //

/**
* FUNCTION: partial( sigma )
*	Partially applies scale parameter `sigma` and returns a function for evaluating the cumulative distribution function (CDF) for a Rayleigh distribution.
*
* @param {Number} sigma - scale parameter
* @returns {Function} CDF
*/
function partial( sigma ) {
	var s2 = pow( sigma, 2 );
	/**
	* FUNCTION: cdf( x )
	*	Evaluates the cumulative distribution function (CDF) for a Rayleigh distribution.
	*
	* @private
	* @param {Number} x - input value
	* @returns {Number} evaluated CDF
	*/
	return function cdf( x ) {
		if ( x < 0 ) {
			return 0;
		}
		return -expm1( - pow( x, 2) / ( 2 * s2 ) ) ;
	};
} // end FUNCTION partial()


// EXPORTS //

module.exports = partial;

},{}],2560:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// CDF //

/**
* FUNCTION: cdf( out, arr, sigma )
*	Evaluates the cumulative distribution function (CDF) for a Rayleigh distribution with scale parameter `sigma` for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} arr - input array
* @param {Number} sigma - scale parameter
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function cdf( y, x, sigma ) {
	var len = x.length,
		fcn,
		i;

	fcn = partial ( sigma );
	for ( i = 0; i < len; i++ ) {
		y[ i ] = fcn( x[ i ] );
	}
	return y;
} // end FUNCTION cdf()


// EXPORTS //

module.exports = cdf;

},{"./partial.js":2559}],2561:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isPositive = require( 'validate.io-positive-primitive' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Number} [options.sigma=1] - scale parameter
* @param {Boolean} [options.copy] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'cdf()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'sigma' ) ) {
		opts.sigma = options.sigma;
		if ( !isPositive( opts.sigma ) ) {
			return new TypeError( 'cdf()::invalid option. `sigma` parameter must be a positive number. Option: `' + opts.sigma + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'cdf()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'cdf()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'cdf()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'cdf()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'cdf()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":2622,"validate.io-function":2623,"validate.io-object":2629,"validate.io-positive-primitive":2631,"validate.io-string-primitive":2632}],2562:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],2563:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":2562,"dup":12}],2564:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],2565:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":2568,"./iget.js":2570,"./iset.js":2573,"./mget.js":2577,"./mset.js":2579,"./set.js":2587,"./sget.js":2589,"./sset.js":2591,"./toString.js":2593,"dup":14}],2566:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":2569,"./iget.raw.js":2571,"./iset.raw.js":2574,"./mget.raw.js":2578,"./mset.raw.js":2580,"./set.raw.js":2588,"./sget.raw.js":2590,"./sset.raw.js":2592,"./toString.js":2593,"dup":15}],2567:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],2568:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":2625}],2569:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],2570:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":2605}],2571:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],2572:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":2575,"./matrix.raw.js":2576,"dup":21}],2573:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":2605,"validate.io-number-primitive":2628}],2574:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],2575:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":2564,"./ctor.js":2565,"./dtypes.js":2567,"compute-cast-arrays":2594,"compute-dtype":2597,"dup":24,"validate.io-array":2602,"validate.io-contains":2603,"validate.io-nonnegative-integer-array":2606,"validate.io-string-primitive":2632}],2576:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":2564,"./ctor.raw.js":2566,"./dtypes.js":2567,"compute-dtype":2597,"dup":25,"validate.io-contains":2603,"validate.io-string-primitive":2632}],2577:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":2564,"dup":26,"validate.io-nonnegative-integer-array":2606}],2578:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":2564,"dup":27}],2579:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":2581,"./mset2.js":2582,"./mset3.js":2583,"./mset4.js":2584,"./mset5.js":2585,"./mset6.js":2586,"dup":28,"validate.io-function":2623,"validate.io-nonnegative-integer-array":2606,"validate.io-number-primitive":2628}],2580:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":2581,"./mset2.js":2582,"./mset3.js":2583,"./mset4.js":2584,"./mset5.js":2585,"./mset6.js":2586,"dup":29}],2581:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],2582:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],2583:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],2584:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],2585:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],2586:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],2587:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":2625,"validate.io-number-primitive":2628}],2588:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],2589:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":2564,"compute-indexspace":2601,"dup":38,"validate.io-string-primitive":2632}],2590:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":2564,"compute-indexspace":2601,"dup":39}],2591:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":2601,"dup":40,"validate.io-function":2623,"validate.io-number-primitive":2628,"validate.io-string-primitive":2632}],2592:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":2601,"dup":41}],2593:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],2594:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":2562,"compute-array-dtype/lib/dtypes":2595,"dup":43,"type-name":2596,"validate.io-array-like":2619}],2595:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2596:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2597:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":2599,"dup":46,"type-name":2600}],2598:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2599:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":2598,"dup":48}],2600:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2601:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":2625,"validate.io-string-primitive":2632}],2602:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2603:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":2602,"validate.io-nan-primitive":2604}],2604:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],2605:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2628}],2606:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":2602,"validate.io-nonnegative-integer":2625}],2607:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],2608:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],2609:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":2607,"./defaults.js":2608,"./validate.js":2611,"dup":60,"validate.io-array":2612,"validate.io-string-primitive":2632}],2610:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":2607,"./defaults.js":2608,"./factory.js":2609,"./validate.js":2611,"dup":61,"validate.io-array":2612,"validate.io-string-primitive":2632}],2611:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":2629,"validate.io-string-primitive":2632}],2612:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2613:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],2614:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],2615:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":2613,"./defaults.js":2614,"./validate.js":2617,"dup":66,"validate.io-array":2618,"validate.io-string-primitive":2632}],2616:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":2613,"./defaults.js":2614,"./factory.js":2615,"./validate.js":2617,"dup":67,"validate.io-array":2618,"validate.io-string-primitive":2632}],2617:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":2622,"validate.io-object":2629,"validate.io-string-primitive":2632}],2618:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2619:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":2620,"dup":70,"validate.io-integer-primitive":2621}],2620:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],2621:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2628}],2622:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],2623:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],2624:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],2625:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":2626}],2626:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":2627}],2627:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],2628:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],2629:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":2630}],2630:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2631:[function(require,module,exports){
arguments[4][1221][0].apply(exports,arguments)
},{"dup":1221,"validate.io-number-primitive":2628}],2632:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],2633:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":2634,"dup":81,"validate.io-integer-primitive":2635}],2634:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],2635:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2628}],2636:[function(require,module,exports){
arguments[4][1226][0].apply(exports,arguments)
},{"./number.js":2641,"dup":1226}],2637:[function(require,module,exports){
'use strict';

// MODULES //

var EKURTOSIS = require( './number.js' );


// EKURTOSIS //

/**
* FUNCTION: ekurtosis( out, sigma )
*	Computes the distribution ekurtosis for parameters stored in an array.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} sigma - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function ekurtosis( out, arr ) {
	var len = arr.length,
		i;
	for ( i = 0; i < len; i++ ) {
		if ( typeof arr[ i ] === 'number' ) {
			out[ i ] = EKURTOSIS( arr[ i ] );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION ekurtosis()


// EXPORTS //

module.exports = ekurtosis;

},{"./number.js":2641}],2638:[function(require,module,exports){
arguments[4][1228][0].apply(exports,arguments)
},{"./number.js":2641,"dup":1228,"utils-deep-get":2692,"utils-deep-set":2698}],2639:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isnan = require( 'validate.io-nan' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var ekurtosis1 = require( './number.js' ),
	ekurtosis2 = require( './array.js' ),
	ekurtosis3 = require( './accessor.js' ),
	ekurtosis4 = require( './deepset.js' ),
	ekurtosis5 = require( './matrix.js' ),
	ekurtosis6 = require( './typedarray.js' );


// EKURTOSIS //

/**
* FUNCTION: ekurtosis( sigma[, opts] )
*	Computes the distribution ekurtosis.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} sigma - input value
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} distribution ekurtosis(s)
*/
function ekurtosis( sigma, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( isNumber( sigma ) || isnan( sigma ) ) {
		return ekurtosis1( sigma );
	}
	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isMatrixLike( sigma ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'ekurtosis()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( sigma.length );
			out = matrix( d, sigma.shape, dt );
		} else {
			out = sigma;
		}
		return ekurtosis5( out, sigma );
	}
	if ( isTypedArrayLike( sigma ) ) {
		if ( opts.copy === false ) {
			out = sigma;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'ekurtosis()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( sigma.length );
		}
		return ekurtosis6( out, sigma );
	}
	if ( isArrayLike( sigma ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return ekurtosis4( sigma, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = sigma;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'ekurtosis()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( sigma.length );
		}
		else {
			out = new Array( sigma.length );
		}
		if ( opts.accessor ) {
			return ekurtosis3( out, sigma, opts.accessor );
		}
		return ekurtosis2( out, sigma );
	}
	return NaN;
} // end FUNCTION ekurtosis()


// EXPORTS //

module.exports = ekurtosis;

},{"./accessor.js":2636,"./array.js":2637,"./deepset.js":2638,"./matrix.js":2640,"./number.js":2641,"./typedarray.js":2642,"./validate.js":2643,"compute-array-constructors":2645,"dstructs-matrix":2654,"validate.io-array-like":2701,"validate.io-matrix-like":2706,"validate.io-nan":2707,"validate.io-number-primitive":2711,"validate.io-typed-array-like":2716}],2640:[function(require,module,exports){
arguments[4][1230][0].apply(exports,arguments)
},{"./number.js":2641,"dup":1230}],2641:[function(require,module,exports){
'use strict';

// MODULES //

var isPositive = require( 'validate.io-positive-primitive' );

// FUNCTIONS //

var pow = Math.pow;


// CONSTANTS //

var PI = Math.PI,
	PI2 = pow( PI, 2 ),
	EKURTOSIS_RAYLEIGH = - ( 6 * PI2 - 24 * PI + 16 ) / pow( 4 - PI, 2 );


// EKURTOSIS //

/**
* FUNCTION ekurtosis( sigma )
*	Computes the distribution ekurtosis for a Rayleigh distribution with parameter sigma.
*
* @param {Number} sigma - scale parameter
* @returns {Number} distribution ekurtosis
*/
function ekurtosis( sigma ) {
	if ( !isPositive( sigma ) ) {
		return NaN;
	}
	return EKURTOSIS_RAYLEIGH;
} // end FUNCTION ekurtosis()


// EXPORTS

module.exports =  ekurtosis;

},{"validate.io-positive-primitive":2714}],2642:[function(require,module,exports){
'use strict';

// MODULES //

var EKURTOSIS = require( './number.js' );


// EKURTOSIS //

/**
* FUNCTION: ekurtosis( out, sigma )
*	Computes the distribution ekurtosis for parameters stored in a typed array.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} sigma - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function ekurtosis( out, sigma ) {
	var len = sigma.length,
		i;
	for ( i = 0; i < len; i++ ) {
		out[ i ] = EKURTOSIS( sigma[ i ] );
	}
	return out;
} // end FUNCTION ekurtosis()


// EXPORTS //

module.exports = ekurtosis;

},{"./number.js":2641}],2643:[function(require,module,exports){
arguments[4][1233][0].apply(exports,arguments)
},{"dup":1233,"validate.io-boolean-primitive":2704,"validate.io-function":2705,"validate.io-object":2712,"validate.io-string-primitive":2715}],2644:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],2645:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":2644,"dup":12}],2646:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],2647:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":2650,"./iget.js":2652,"./iset.js":2655,"./mget.js":2659,"./mset.js":2661,"./set.js":2669,"./sget.js":2671,"./sset.js":2673,"./toString.js":2675,"dup":14}],2648:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":2651,"./iget.raw.js":2653,"./iset.raw.js":2656,"./mget.raw.js":2660,"./mset.raw.js":2662,"./set.raw.js":2670,"./sget.raw.js":2672,"./sset.raw.js":2674,"./toString.js":2675,"dup":15}],2649:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],2650:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":2708}],2651:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],2652:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":2687}],2653:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],2654:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":2657,"./matrix.raw.js":2658,"dup":21}],2655:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":2687,"validate.io-number-primitive":2711}],2656:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],2657:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":2646,"./ctor.js":2647,"./dtypes.js":2649,"compute-cast-arrays":2676,"compute-dtype":2679,"dup":24,"validate.io-array":2684,"validate.io-contains":2685,"validate.io-nonnegative-integer-array":2688,"validate.io-string-primitive":2715}],2658:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":2646,"./ctor.raw.js":2648,"./dtypes.js":2649,"compute-dtype":2679,"dup":25,"validate.io-contains":2685,"validate.io-string-primitive":2715}],2659:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":2646,"dup":26,"validate.io-nonnegative-integer-array":2688}],2660:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":2646,"dup":27}],2661:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":2663,"./mset2.js":2664,"./mset3.js":2665,"./mset4.js":2666,"./mset5.js":2667,"./mset6.js":2668,"dup":28,"validate.io-function":2705,"validate.io-nonnegative-integer-array":2688,"validate.io-number-primitive":2711}],2662:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":2663,"./mset2.js":2664,"./mset3.js":2665,"./mset4.js":2666,"./mset5.js":2667,"./mset6.js":2668,"dup":29}],2663:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],2664:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],2665:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],2666:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],2667:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],2668:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],2669:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":2708,"validate.io-number-primitive":2711}],2670:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],2671:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":2646,"compute-indexspace":2683,"dup":38,"validate.io-string-primitive":2715}],2672:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":2646,"compute-indexspace":2683,"dup":39}],2673:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":2683,"dup":40,"validate.io-function":2705,"validate.io-number-primitive":2711,"validate.io-string-primitive":2715}],2674:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":2683,"dup":41}],2675:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],2676:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":2644,"compute-array-dtype/lib/dtypes":2677,"dup":43,"type-name":2678,"validate.io-array-like":2701}],2677:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2678:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2679:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":2681,"dup":46,"type-name":2682}],2680:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2681:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":2680,"dup":48}],2682:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2683:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":2708,"validate.io-string-primitive":2715}],2684:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2685:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":2684,"validate.io-nan-primitive":2686}],2686:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],2687:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2711}],2688:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":2684,"validate.io-nonnegative-integer":2708}],2689:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],2690:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],2691:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":2689,"./defaults.js":2690,"./validate.js":2693,"dup":60,"validate.io-array":2694,"validate.io-string-primitive":2715}],2692:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":2689,"./defaults.js":2690,"./factory.js":2691,"./validate.js":2693,"dup":61,"validate.io-array":2694,"validate.io-string-primitive":2715}],2693:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":2712,"validate.io-string-primitive":2715}],2694:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2695:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],2696:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],2697:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":2695,"./defaults.js":2696,"./validate.js":2699,"dup":66,"validate.io-array":2700,"validate.io-string-primitive":2715}],2698:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":2695,"./defaults.js":2696,"./factory.js":2697,"./validate.js":2699,"dup":67,"validate.io-array":2700,"validate.io-string-primitive":2715}],2699:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":2704,"validate.io-object":2712,"validate.io-string-primitive":2715}],2700:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2701:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":2702,"dup":70,"validate.io-integer-primitive":2703}],2702:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],2703:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2711}],2704:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],2705:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],2706:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],2707:[function(require,module,exports){
arguments[4][76][0].apply(exports,arguments)
},{"dup":76}],2708:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":2709}],2709:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":2710}],2710:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],2711:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],2712:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":2713}],2713:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2714:[function(require,module,exports){
arguments[4][1221][0].apply(exports,arguments)
},{"dup":1221,"validate.io-number-primitive":2711}],2715:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],2716:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":2717,"dup":81,"validate.io-integer-primitive":2718}],2717:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],2718:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2711}],2719:[function(require,module,exports){
'use strict';

// MODULES //

var ENTROPY = require( './number.js' );


// ENTROPY //

/**
* FUNCTION: entropy( out, arr, accessor )
*	Computes the distribution entropy for parameters stored in an array using an accessor function.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function entropy( out, arr, clbk ) {
	var len = arr.length,
		v, i;
	for ( i = 0; i < len; i++ ) {
		v = clbk( arr[ i ], i );
		if ( typeof v === 'number' ) {
			out[ i ] = ENTROPY( v );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION entropy()


// EXPORTS //

module.exports = entropy;

},{"./number.js":2724}],2720:[function(require,module,exports){
'use strict';

// MODULES //

var ENTROPY = require( './number.js' );


// ENTROPY //

/**
* FUNCTION: entropy( out, sigma )
*	Computes the distribution entropy for parameters stored in an array.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} sigma - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function entropy( out, arr ) {
	var len = arr.length,
		i;
	for ( i = 0; i < len; i++ ) {
		if ( typeof arr[ i ] === 'number' ) {
			out[ i ] = ENTROPY( arr[ i ] );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION entropy()


// EXPORTS //

module.exports = entropy;

},{"./number.js":2724}],2721:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory,
	ENTROPY  = require( './number.js' );


// ENTROPY //

/**
* FUNCTION: entropy( arr, path[, sep] )
*	Computes the distribution entropy and deep sets the input array.
*
* @param {Array} arrays - input array
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function entropy( arr, path, sep ) {
	var len = arr.length,
		opts = {},
		dget,
		dset,
		v, i;
	if ( arguments.length > 2 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		for ( i = 0; i < len; i++ ) {
			v = dget( arr[ i ] );
			if ( typeof v === 'number' ) {
				dset( arr[i], ENTROPY ( v ) );
			} else {
				dset( arr[i], NaN );
			}
		}
	}
	return arr;
} // end FUNCTION entropy()


// EXPORTS //

module.exports = entropy;

},{"./number.js":2724,"utils-deep-get":2776,"utils-deep-set":2782}],2722:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isnan = require( 'validate.io-nan' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var entropy1 = require( './number.js' ),
	entropy2 = require( './array.js' ),
	entropy3 = require( './accessor.js' ),
	entropy4 = require( './deepset.js' ),
	entropy5 = require( './matrix.js' ),
	entropy6 = require( './typedarray.js' );


// ENTROPY //

/**
* FUNCTION: entropy( sigma[, opts] )
*	Computes the distribution entropy.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} sigma - input value
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} distribution entropy(s)
*/
function entropy( sigma, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( isNumber( sigma ) || isnan( sigma ) ) {
		return entropy1( sigma );
	}
	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isMatrixLike( sigma ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'entropy()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( sigma.length );
			out = matrix( d, sigma.shape, dt );
		} else {
			out = sigma;
		}
		return entropy5( out, sigma );
	}
	if ( isTypedArrayLike( sigma ) ) {
		if ( opts.copy === false ) {
			out = sigma;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'entropy()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( sigma.length );
		}
		return entropy6( out, sigma );
	}
	if ( isArrayLike( sigma ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return entropy4( sigma, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = sigma;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'entropy()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( sigma.length );
		}
		else {
			out = new Array( sigma.length );
		}
		if ( opts.accessor ) {
			return entropy3( out, sigma, opts.accessor );
		}
		return entropy2( out, sigma );
	}
	return NaN;
} // end FUNCTION entropy()


// EXPORTS //

module.exports = entropy;

},{"./accessor.js":2719,"./array.js":2720,"./deepset.js":2721,"./matrix.js":2723,"./number.js":2724,"./typedarray.js":2725,"./validate.js":2726,"compute-array-constructors":2728,"dstructs-matrix":2738,"validate.io-array-like":2785,"validate.io-matrix-like":2790,"validate.io-nan":2791,"validate.io-number-primitive":2795,"validate.io-typed-array-like":2800}],2723:[function(require,module,exports){
'use strict';

// MODULES //

var ENTROPY = require( './number.js' );


// ENTROPY //

/**
* FUNCTION: entropy( out, x )
*	Computes the distribution entropy for each parameter stored in a matrix.
*
* @param {Matrix} out - output matrix
* @param {Matrix} x - input matrix
* @returns {Matrix} output matrix
*/
function entropy( out, x ) {
	var len = x.length,
		i;
	if ( out.length !== len ) {
		throw new Error( 'entropy()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	for ( i = 0; i < len; i++ ) {
		out.data[ i ] = ENTROPY( x.data[ i ] );
	}
	return out;
} // end FUNCTION entropy()


// EXPORTS //

module.exports = entropy;

},{"./number.js":2724}],2724:[function(require,module,exports){
'use strict';

// MODULES //

var isPositive = require( 'validate.io-positive-primitive' ),
	gamma = require( 'compute-const-eulergamma' );


// FUNCTIONS //

var ln = Math.log,
	sqrt = Math.sqrt;


// CONSTANTS //

var CONST_PART = 1 + gamma / 2,
	SQRT_TWO = sqrt( 2 );


// ENTROPY //

/**
* FUNCTION entropy( sigma )
*	Computes the distribution entropy for a Rayleigh distribution with parameter sigma.
*
* @param {Number} sigma - scale parameter
* @returns {Number} distribution entropy
*/
function entropy( sigma ) {
	if ( !isPositive( sigma ) ) {
		return NaN;
	}
	return CONST_PART + ln( sigma / SQRT_TWO );
} // end FUNCTION entropy()


// EXPORTS

module.exports =  entropy;

},{"compute-const-eulergamma":2729,"validate.io-positive-primitive":2798}],2725:[function(require,module,exports){
'use strict';

// MODULES //

var ENTROPY = require( './number.js' );


// ENTROPY //

/**
* FUNCTION: entropy( out, sigma )
*	Computes the distribution entropy for parameters stored in a typed array.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} sigma - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function entropy( out, sigma ) {
	var len = sigma.length,
		i;
	for ( i = 0; i < len; i++ ) {
		out[ i ] = ENTROPY( sigma[ i ] );
	}
	return out;
} // end FUNCTION entropy()


// EXPORTS //

module.exports = entropy;

},{"./number.js":2724}],2726:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Boolean} [options.copy] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'entropy()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'entropy()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'entropy()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'entropy()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'entropy()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'entropy()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":2788,"validate.io-function":2789,"validate.io-object":2796,"validate.io-string-primitive":2799}],2727:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],2728:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":2727,"dup":12}],2729:[function(require,module,exports){
'use strict';

// EXPORTS //

// http://oeis.org/A001620
module.exports = 0.577215664901532860606512090082402431042;

},{}],2730:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],2731:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":2734,"./iget.js":2736,"./iset.js":2739,"./mget.js":2743,"./mset.js":2745,"./set.js":2753,"./sget.js":2755,"./sset.js":2757,"./toString.js":2759,"dup":14}],2732:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":2735,"./iget.raw.js":2737,"./iset.raw.js":2740,"./mget.raw.js":2744,"./mset.raw.js":2746,"./set.raw.js":2754,"./sget.raw.js":2756,"./sset.raw.js":2758,"./toString.js":2759,"dup":15}],2733:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],2734:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":2792}],2735:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],2736:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":2771}],2737:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],2738:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":2741,"./matrix.raw.js":2742,"dup":21}],2739:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":2771,"validate.io-number-primitive":2795}],2740:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],2741:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":2730,"./ctor.js":2731,"./dtypes.js":2733,"compute-cast-arrays":2760,"compute-dtype":2763,"dup":24,"validate.io-array":2768,"validate.io-contains":2769,"validate.io-nonnegative-integer-array":2772,"validate.io-string-primitive":2799}],2742:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":2730,"./ctor.raw.js":2732,"./dtypes.js":2733,"compute-dtype":2763,"dup":25,"validate.io-contains":2769,"validate.io-string-primitive":2799}],2743:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":2730,"dup":26,"validate.io-nonnegative-integer-array":2772}],2744:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":2730,"dup":27}],2745:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":2747,"./mset2.js":2748,"./mset3.js":2749,"./mset4.js":2750,"./mset5.js":2751,"./mset6.js":2752,"dup":28,"validate.io-function":2789,"validate.io-nonnegative-integer-array":2772,"validate.io-number-primitive":2795}],2746:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":2747,"./mset2.js":2748,"./mset3.js":2749,"./mset4.js":2750,"./mset5.js":2751,"./mset6.js":2752,"dup":29}],2747:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],2748:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],2749:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],2750:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],2751:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],2752:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],2753:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":2792,"validate.io-number-primitive":2795}],2754:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],2755:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":2730,"compute-indexspace":2767,"dup":38,"validate.io-string-primitive":2799}],2756:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":2730,"compute-indexspace":2767,"dup":39}],2757:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":2767,"dup":40,"validate.io-function":2789,"validate.io-number-primitive":2795,"validate.io-string-primitive":2799}],2758:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":2767,"dup":41}],2759:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],2760:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":2727,"compute-array-dtype/lib/dtypes":2761,"dup":43,"type-name":2762,"validate.io-array-like":2785}],2761:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2762:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2763:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":2765,"dup":46,"type-name":2766}],2764:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2765:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":2764,"dup":48}],2766:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2767:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":2792,"validate.io-string-primitive":2799}],2768:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2769:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":2768,"validate.io-nan-primitive":2770}],2770:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],2771:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2795}],2772:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":2768,"validate.io-nonnegative-integer":2792}],2773:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],2774:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],2775:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":2773,"./defaults.js":2774,"./validate.js":2777,"dup":60,"validate.io-array":2778,"validate.io-string-primitive":2799}],2776:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":2773,"./defaults.js":2774,"./factory.js":2775,"./validate.js":2777,"dup":61,"validate.io-array":2778,"validate.io-string-primitive":2799}],2777:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":2796,"validate.io-string-primitive":2799}],2778:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2779:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],2780:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],2781:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":2779,"./defaults.js":2780,"./validate.js":2783,"dup":66,"validate.io-array":2784,"validate.io-string-primitive":2799}],2782:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":2779,"./defaults.js":2780,"./factory.js":2781,"./validate.js":2783,"dup":67,"validate.io-array":2784,"validate.io-string-primitive":2799}],2783:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":2788,"validate.io-object":2796,"validate.io-string-primitive":2799}],2784:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2785:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":2786,"dup":70,"validate.io-integer-primitive":2787}],2786:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],2787:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2795}],2788:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],2789:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],2790:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],2791:[function(require,module,exports){
arguments[4][76][0].apply(exports,arguments)
},{"dup":76}],2792:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":2793}],2793:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":2794}],2794:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],2795:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],2796:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":2797}],2797:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2798:[function(require,module,exports){
arguments[4][1221][0].apply(exports,arguments)
},{"dup":1221,"validate.io-number-primitive":2795}],2799:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],2800:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":2801,"dup":81,"validate.io-integer-primitive":2802}],2801:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],2802:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2795}],2803:[function(require,module,exports){
'use strict';

// MODULES //

var MEAN = require( './number.js' );


// MEAN //

/**
* FUNCTION: mean( out, sigma, accessor )
*	Computes the distribution mean for parameters stored in an array using an accessor function.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} sigma - input array
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function mean( out, arr, clbk ) {
	var len = arr.length,
		v, i;
	for ( i = 0; i < len; i++ ) {
		v = clbk( arr[ i ], i );
		if ( typeof v === 'number' ) {
			out[ i ] = MEAN( v );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION mean()


// EXPORTS //

module.exports = mean;

},{"./number.js":2808}],2804:[function(require,module,exports){
'use strict';

// MODULES //

var MEAN = require( './number.js' );


// MEAN //

/**
* FUNCTION: mean( out, sigma )
*	Computes the distribution mean for parameters stored in an array.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} sigma - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function mean( out, arr ) {
	var len = arr.length,
		i;
	for ( i = 0; i < len; i++ ) {
		if ( typeof arr[ i ] === 'number' ) {
			out[ i ] = MEAN( arr[ i ] );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION mean()


// EXPORTS //

module.exports = mean;

},{"./number.js":2808}],2805:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory,
	MEAN  = require( './number.js' );


// MEAN //

/**
* FUNCTION: mean( sigma, path[, sep] )
*	Computes the distribution mean and deep sets the input array.
*
* @param {Array} sigma - input array
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function mean( sigma, path, sep ) {
	var len = sigma.length,
		opts = {},
		dget,
		dset,
		v, i;
	if ( arguments.length > 2 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		for ( i = 0; i < len; i++ ) {
			v = dget( sigma[ i ] );
			if ( typeof v === 'number' ) {
				dset( sigma[i], MEAN ( v ) );
			} else {
				dset( sigma[i], NaN );
			}
		}
	}
	return sigma;
} // end FUNCTION mean()


// EXPORTS //

module.exports = mean;

},{"./number.js":2808,"utils-deep-get":2859,"utils-deep-set":2865}],2806:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isnan = require( 'validate.io-nan' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var mean1 = require( './number.js' ),
	mean2 = require( './array.js' ),
	mean3 = require( './accessor.js' ),
	mean4 = require( './deepset.js' ),
	mean5 = require( './matrix.js' ),
	mean6 = require( './typedarray.js' );


// MEAN //

/**
* FUNCTION: mean( sigma[, opts] )
*	Computes the distribution mean.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} sigma - input value
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} distribution mean(s)
*/
function mean( sigma, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( isNumber( sigma ) || isnan( sigma ) ) {
		return mean1( sigma );
	}
	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isMatrixLike( sigma ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'mean()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( sigma.length );
			out = matrix( d, sigma.shape, dt );
		} else {
			out = sigma;
		}
		return mean5( out, sigma );
	}
	if ( isTypedArrayLike( sigma ) ) {
		if ( opts.copy === false ) {
			out = sigma;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'mean()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( sigma.length );
		}
		return mean6( out, sigma );
	}
	if ( isArrayLike( sigma ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return mean4( sigma, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = sigma;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'mean()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( sigma.length );
		}
		else {
			out = new Array( sigma.length );
		}
		if ( opts.accessor ) {
			return mean3( out, sigma, opts.accessor );
		}
		return mean2( out, sigma );
	}
	return NaN;
} // end FUNCTION mean()


// EXPORTS //

module.exports = mean;

},{"./accessor.js":2803,"./array.js":2804,"./deepset.js":2805,"./matrix.js":2807,"./number.js":2808,"./typedarray.js":2809,"./validate.js":2810,"compute-array-constructors":2812,"dstructs-matrix":2821,"validate.io-array-like":2868,"validate.io-matrix-like":2873,"validate.io-nan":2874,"validate.io-number-primitive":2878,"validate.io-typed-array-like":2883}],2807:[function(require,module,exports){
arguments[4][1313][0].apply(exports,arguments)
},{"./number.js":2808,"dup":1313}],2808:[function(require,module,exports){
'use strict';

// MODULES //

var isPositive = require( 'validate.io-positive-primitive' );


// FUNCTIONS //

var sqrt = Math.sqrt;


// CONSTANTS //

var PI = Math.PI;


// MEAN //

/**
* FUNCTION mean( sigma )
*	Computes the distribution mean for a Rayleigh with parameter sigma.
*
* @param {Number} sigma - scale parameter
* @returns {Number} distribution mean
*/
function mean( sigma ) {
	if ( !isPositive( sigma ) ) {
		return NaN;
	}
	return sigma * sqrt( PI / 2 );
} // end FUNCTION mean()


// EXPORTS

module.exports =  mean;

},{"validate.io-positive-primitive":2881}],2809:[function(require,module,exports){
'use strict';

// MODULES //

var MEAN = require( './number.js' );


// MEAN //

/**
* FUNCTION: mean( out, sigma )
*	Computes the distribution mean for parameters stored in a typed array.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} sigma - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function mean( out, sigma ) {
	var len = sigma.length,
		i;
	for ( i = 0; i < len; i++ ) {
		out[ i ] = MEAN( sigma[ i ] );
	}
	return out;
} // end FUNCTION mean()


// EXPORTS //

module.exports = mean;

},{"./number.js":2808}],2810:[function(require,module,exports){
arguments[4][1316][0].apply(exports,arguments)
},{"dup":1316,"validate.io-boolean-primitive":2871,"validate.io-function":2872,"validate.io-object":2879,"validate.io-string-primitive":2882}],2811:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],2812:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":2811,"dup":12}],2813:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],2814:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":2817,"./iget.js":2819,"./iset.js":2822,"./mget.js":2826,"./mset.js":2828,"./set.js":2836,"./sget.js":2838,"./sset.js":2840,"./toString.js":2842,"dup":14}],2815:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":2818,"./iget.raw.js":2820,"./iset.raw.js":2823,"./mget.raw.js":2827,"./mset.raw.js":2829,"./set.raw.js":2837,"./sget.raw.js":2839,"./sset.raw.js":2841,"./toString.js":2842,"dup":15}],2816:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],2817:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":2875}],2818:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],2819:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":2854}],2820:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],2821:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":2824,"./matrix.raw.js":2825,"dup":21}],2822:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":2854,"validate.io-number-primitive":2878}],2823:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],2824:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":2813,"./ctor.js":2814,"./dtypes.js":2816,"compute-cast-arrays":2843,"compute-dtype":2846,"dup":24,"validate.io-array":2851,"validate.io-contains":2852,"validate.io-nonnegative-integer-array":2855,"validate.io-string-primitive":2882}],2825:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":2813,"./ctor.raw.js":2815,"./dtypes.js":2816,"compute-dtype":2846,"dup":25,"validate.io-contains":2852,"validate.io-string-primitive":2882}],2826:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":2813,"dup":26,"validate.io-nonnegative-integer-array":2855}],2827:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":2813,"dup":27}],2828:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":2830,"./mset2.js":2831,"./mset3.js":2832,"./mset4.js":2833,"./mset5.js":2834,"./mset6.js":2835,"dup":28,"validate.io-function":2872,"validate.io-nonnegative-integer-array":2855,"validate.io-number-primitive":2878}],2829:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":2830,"./mset2.js":2831,"./mset3.js":2832,"./mset4.js":2833,"./mset5.js":2834,"./mset6.js":2835,"dup":29}],2830:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],2831:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],2832:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],2833:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],2834:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],2835:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],2836:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":2875,"validate.io-number-primitive":2878}],2837:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],2838:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":2813,"compute-indexspace":2850,"dup":38,"validate.io-string-primitive":2882}],2839:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":2813,"compute-indexspace":2850,"dup":39}],2840:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":2850,"dup":40,"validate.io-function":2872,"validate.io-number-primitive":2878,"validate.io-string-primitive":2882}],2841:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":2850,"dup":41}],2842:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],2843:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":2811,"compute-array-dtype/lib/dtypes":2844,"dup":43,"type-name":2845,"validate.io-array-like":2868}],2844:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2845:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2846:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":2848,"dup":46,"type-name":2849}],2847:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2848:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":2847,"dup":48}],2849:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2850:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":2875,"validate.io-string-primitive":2882}],2851:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2852:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":2851,"validate.io-nan-primitive":2853}],2853:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],2854:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2878}],2855:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":2851,"validate.io-nonnegative-integer":2875}],2856:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],2857:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],2858:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":2856,"./defaults.js":2857,"./validate.js":2860,"dup":60,"validate.io-array":2861,"validate.io-string-primitive":2882}],2859:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":2856,"./defaults.js":2857,"./factory.js":2858,"./validate.js":2860,"dup":61,"validate.io-array":2861,"validate.io-string-primitive":2882}],2860:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":2879,"validate.io-string-primitive":2882}],2861:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2862:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],2863:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],2864:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":2862,"./defaults.js":2863,"./validate.js":2866,"dup":66,"validate.io-array":2867,"validate.io-string-primitive":2882}],2865:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":2862,"./defaults.js":2863,"./factory.js":2864,"./validate.js":2866,"dup":67,"validate.io-array":2867,"validate.io-string-primitive":2882}],2866:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":2871,"validate.io-object":2879,"validate.io-string-primitive":2882}],2867:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2868:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":2869,"dup":70,"validate.io-integer-primitive":2870}],2869:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],2870:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2878}],2871:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],2872:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],2873:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],2874:[function(require,module,exports){
arguments[4][76][0].apply(exports,arguments)
},{"dup":76}],2875:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":2876}],2876:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":2877}],2877:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],2878:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],2879:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":2880}],2880:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2881:[function(require,module,exports){
arguments[4][1221][0].apply(exports,arguments)
},{"dup":1221,"validate.io-number-primitive":2878}],2882:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],2883:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":2884,"dup":81,"validate.io-integer-primitive":2885}],2884:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],2885:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2878}],2886:[function(require,module,exports){
arguments[4][1392][0].apply(exports,arguments)
},{"./number.js":2891,"dup":1392}],2887:[function(require,module,exports){
'use strict';

// MODULES //

var MEDIAN = require( './number.js' );


// MEDIAN //

/**
* FUNCTION: median( out, sigma )
*	Computes the distribution median for parameters stored in an array.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} sigma - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function median( out, arr ) {
	var len = arr.length,
		i;
	for ( i = 0; i < len; i++ ) {
		if ( typeof arr[ i ] === 'number' ) {
			out[ i ] = MEDIAN( arr[ i ] );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION median()


// EXPORTS //

module.exports = median;

},{"./number.js":2891}],2888:[function(require,module,exports){
arguments[4][1394][0].apply(exports,arguments)
},{"./number.js":2891,"dup":1394,"utils-deep-get":2942,"utils-deep-set":2948}],2889:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isnan = require( 'validate.io-nan' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var median1 = require( './number.js' ),
	median2 = require( './array.js' ),
	median3 = require( './accessor.js' ),
	median4 = require( './deepset.js' ),
	median5 = require( './matrix.js' ),
	median6 = require( './typedarray.js' );


// MEDIAN //

/**
* FUNCTION: median( sigma[, opts] )
*	Computes the distribution median.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} sigma - input value
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} distribution median(s)
*/
function median( sigma, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( isNumber( sigma ) || isnan( sigma ) ) {
		return median1( sigma );
	}
	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isMatrixLike( sigma ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'median()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( sigma.length );
			out = matrix( d, sigma.shape, dt );
		} else {
			out = sigma;
		}
		return median5( out, sigma );
	}
	if ( isTypedArrayLike( sigma ) ) {
		if ( opts.copy === false ) {
			out = sigma;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'median()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( sigma.length );
		}
		return median6( out, sigma );
	}
	if ( isArrayLike( sigma ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return median4( sigma, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = sigma;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'median()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( sigma.length );
		}
		else {
			out = new Array( sigma.length );
		}
		if ( opts.accessor ) {
			return median3( out, sigma, opts.accessor );
		}
		return median2( out, sigma );
	}
	return NaN;
} // end FUNCTION median()


// EXPORTS //

module.exports = median;

},{"./accessor.js":2886,"./array.js":2887,"./deepset.js":2888,"./matrix.js":2890,"./number.js":2891,"./typedarray.js":2892,"./validate.js":2893,"compute-array-constructors":2895,"dstructs-matrix":2904,"validate.io-array-like":2951,"validate.io-matrix-like":2956,"validate.io-nan":2957,"validate.io-number-primitive":2961,"validate.io-typed-array-like":2966}],2890:[function(require,module,exports){
arguments[4][1396][0].apply(exports,arguments)
},{"./number.js":2891,"dup":1396}],2891:[function(require,module,exports){
'use strict';

// MODULES //

var isPositive = require( 'validate.io-positive-primitive' );


// FUNCTIONS //

var ln = Math.log,
	sqrt = Math.sqrt;


// MEDIAN //

/**
* FUNCTION median( sigma )
*	Computes the distribution median for a Rayleigh distribution with parameter sigma.
*
* @param {Number} sigma - scale parameter
* @returns {Number} distribution median
*/
function median( sigma ) {
	if ( !isPositive( sigma ) ) {
		return NaN;
	}
	return sigma * sqrt( 2 * ln( 2 ) );
} // end FUNCTION median()


// EXPORTS

module.exports =  median;

},{"validate.io-positive-primitive":2964}],2892:[function(require,module,exports){
'use strict';

// MODULES //

var MEDIAN = require( './number.js' );


// MEDIAN //

/**
* FUNCTION: median( out, sigma )
*	Computes the distribution median for parameters stored in a typed array.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} sigma - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function median( out, sigma ) {
	var len = sigma.length,
		i;
	for ( i = 0; i < len; i++ ) {
		out[ i ] = MEDIAN( sigma[ i ] );
	}
	return out;
} // end FUNCTION median()


// EXPORTS //

module.exports = median;

},{"./number.js":2891}],2893:[function(require,module,exports){
arguments[4][1399][0].apply(exports,arguments)
},{"dup":1399,"validate.io-boolean-primitive":2954,"validate.io-function":2955,"validate.io-object":2962,"validate.io-string-primitive":2965}],2894:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],2895:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":2894,"dup":12}],2896:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],2897:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":2900,"./iget.js":2902,"./iset.js":2905,"./mget.js":2909,"./mset.js":2911,"./set.js":2919,"./sget.js":2921,"./sset.js":2923,"./toString.js":2925,"dup":14}],2898:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":2901,"./iget.raw.js":2903,"./iset.raw.js":2906,"./mget.raw.js":2910,"./mset.raw.js":2912,"./set.raw.js":2920,"./sget.raw.js":2922,"./sset.raw.js":2924,"./toString.js":2925,"dup":15}],2899:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],2900:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":2958}],2901:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],2902:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":2937}],2903:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],2904:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":2907,"./matrix.raw.js":2908,"dup":21}],2905:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":2937,"validate.io-number-primitive":2961}],2906:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],2907:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":2896,"./ctor.js":2897,"./dtypes.js":2899,"compute-cast-arrays":2926,"compute-dtype":2929,"dup":24,"validate.io-array":2934,"validate.io-contains":2935,"validate.io-nonnegative-integer-array":2938,"validate.io-string-primitive":2965}],2908:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":2896,"./ctor.raw.js":2898,"./dtypes.js":2899,"compute-dtype":2929,"dup":25,"validate.io-contains":2935,"validate.io-string-primitive":2965}],2909:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":2896,"dup":26,"validate.io-nonnegative-integer-array":2938}],2910:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":2896,"dup":27}],2911:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":2913,"./mset2.js":2914,"./mset3.js":2915,"./mset4.js":2916,"./mset5.js":2917,"./mset6.js":2918,"dup":28,"validate.io-function":2955,"validate.io-nonnegative-integer-array":2938,"validate.io-number-primitive":2961}],2912:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":2913,"./mset2.js":2914,"./mset3.js":2915,"./mset4.js":2916,"./mset5.js":2917,"./mset6.js":2918,"dup":29}],2913:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],2914:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],2915:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],2916:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],2917:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],2918:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],2919:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":2958,"validate.io-number-primitive":2961}],2920:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],2921:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":2896,"compute-indexspace":2933,"dup":38,"validate.io-string-primitive":2965}],2922:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":2896,"compute-indexspace":2933,"dup":39}],2923:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":2933,"dup":40,"validate.io-function":2955,"validate.io-number-primitive":2961,"validate.io-string-primitive":2965}],2924:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":2933,"dup":41}],2925:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],2926:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":2894,"compute-array-dtype/lib/dtypes":2927,"dup":43,"type-name":2928,"validate.io-array-like":2951}],2927:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2928:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2929:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":2931,"dup":46,"type-name":2932}],2930:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],2931:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":2930,"dup":48}],2932:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],2933:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":2958,"validate.io-string-primitive":2965}],2934:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2935:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":2934,"validate.io-nan-primitive":2936}],2936:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],2937:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2961}],2938:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":2934,"validate.io-nonnegative-integer":2958}],2939:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],2940:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],2941:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":2939,"./defaults.js":2940,"./validate.js":2943,"dup":60,"validate.io-array":2944,"validate.io-string-primitive":2965}],2942:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":2939,"./defaults.js":2940,"./factory.js":2941,"./validate.js":2943,"dup":61,"validate.io-array":2944,"validate.io-string-primitive":2965}],2943:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":2962,"validate.io-string-primitive":2965}],2944:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2945:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],2946:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],2947:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":2945,"./defaults.js":2946,"./validate.js":2949,"dup":66,"validate.io-array":2950,"validate.io-string-primitive":2965}],2948:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":2945,"./defaults.js":2946,"./factory.js":2947,"./validate.js":2949,"dup":67,"validate.io-array":2950,"validate.io-string-primitive":2965}],2949:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":2954,"validate.io-object":2962,"validate.io-string-primitive":2965}],2950:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2951:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":2952,"dup":70,"validate.io-integer-primitive":2953}],2952:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],2953:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2961}],2954:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],2955:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],2956:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],2957:[function(require,module,exports){
arguments[4][76][0].apply(exports,arguments)
},{"dup":76}],2958:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":2959}],2959:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":2960}],2960:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],2961:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],2962:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":2963}],2963:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],2964:[function(require,module,exports){
arguments[4][1221][0].apply(exports,arguments)
},{"dup":1221,"validate.io-number-primitive":2961}],2965:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],2966:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":2967,"dup":81,"validate.io-integer-primitive":2968}],2967:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],2968:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":2961}],2969:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// PDF //

/**
* FUNCTION: pdf( out, arr, sigma, accessor )
*	Evaluates the probability density function (PDF) for a Rayleigh distribution with scale parameter `sigma` using an accessor function.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Number} sigma - scale parameter
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function pdf( y, x, sigma, clbk ) {
	var len = x.length,
		fcn,
		v, i;

	fcn = partial( sigma );
	for ( i = 0; i < len; i++ ) {
		v = clbk( x[ i ], i );
		if ( typeof v === 'number' ) {
			y[ i ] = fcn( v );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./partial.js":2975}],2970:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// PDF //

/**
* FUNCTION: pdf( out, arr, sigma )
*	Evaluates the probability density function (PDF) for a Rayleigh distribution with scale parameter `sigma` for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Number} sigma - scale parameter
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function pdf( y, x, sigma ) {
	var len = x.length,
		fcn,
		i;

	fcn = partial( sigma );
	for ( i = 0; i < len; i++ ) {
		if ( typeof x[ i ] === 'number' ) {
			y[ i ] = fcn( x[ i ] );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./partial.js":2975}],2971:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory,
	partial = require( './partial.js' );


// PDF //

/**
* FUNCTION: pdf( arr, sigma, path[, sep] )
*	Evaluates the probability density function (PDF) for a Rayleigh distribution with scale parameter `sigma` for each array element and sets the input array.
*
* @param {Array} arr - input array
* @param {Number} sigma - scale parameter
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function pdf( x, sigma, path, sep ) {
	var len = x.length,
		opts = {},
		dget,
		dset,
		fcn,
		v, i;
	if ( arguments.length > 3 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		fcn = partial( sigma );
		for ( i = 0; i < len; i++ ) {
			v = dget( x[ i ] );
			if ( typeof v === 'number' ) {
				dset( x[i], fcn( v ) );
			} else {
				dset( x[i], NaN );
			}
		}
	}
	return x;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./partial.js":2975,"utils-deep-get":3026,"utils-deep-set":3032}],2972:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var pdf1 = require( './number.js' ),
	pdf2 = require( './array.js' ),
	pdf3 = require( './accessor.js' ),
	pdf4 = require( './deepset.js' ),
	pdf5 = require( './matrix.js' ),
	pdf6 = require( './typedarray.js' );


// PDF //

/**
* FUNCTION: pdf( x[, opts] )
*	Evaluates the probability density function (PDF) for a Rayleigh distribution.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} x - input value
* @param {Object} [opts] - function options
* @param {Number} [opts.sigma=1] - scale parameter
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} evaluated PDF
*/
function pdf( x, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	opts.sigma = typeof opts.sigma !== 'undefined' ? opts.sigma : 1;

	if ( isNumber( x ) ) {
		return pdf1( x, opts.sigma );
	}
	if ( isMatrixLike( x ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'pdf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( x.length );
			out = matrix( d, x.shape, dt );
		} else {
			out = x;
		}
		return pdf5( out, x, opts.sigma );
	}
	if ( isTypedArrayLike( x ) ) {
		if ( opts.copy === false ) {
			out = x;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'pdf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( x.length );
		}
		return pdf6( out, x, opts.sigma );
	}
	if ( isArrayLike( x ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return pdf4( x, opts.sigma, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = x;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'pdf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( x.length );
		}
		else {
			out = new Array( x.length );
		}
		if ( opts.accessor ) {
			return pdf3( out, x, opts.sigma, opts.accessor );
		}
		return pdf2( out, x, opts.sigma );
	}
	return NaN;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./accessor.js":2969,"./array.js":2970,"./deepset.js":2971,"./matrix.js":2973,"./number.js":2974,"./typedarray.js":2976,"./validate.js":2977,"compute-array-constructors":2979,"dstructs-matrix":2988,"validate.io-array-like":3035,"validate.io-matrix-like":3040,"validate.io-number-primitive":3044,"validate.io-typed-array-like":3049}],2973:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// PDF //

/**
* FUNCTION: pdf( out, matrix, sigma )
*	Evaluates the probability density function (PDF) for a Rayleigh distribution with scale parameter `sigma` for each matrix element.
*
* @param {Matrix} out - output matrix
* @param {Matrix} arr - input matrix
* @param {Number} sigma - scale parameter
* @returns {Matrix} output matrix
*/
function pdf( y, x, sigma ) {
	var len = x.length,
		fcn,
		i;
	if ( y.length !== len ) {
		throw new Error( 'pdf()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	fcn = partial( sigma );
	for ( i = 0; i < len; i++ ) {
		y.data[ i ] = fcn( x.data[ i ] );
	}
	return y;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./partial.js":2975}],2974:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var exp = Math.exp,
	pow = Math.pow;


// PDF //

/**
* FUNCTION: pdf( x, sigma )
*	Evaluates the probability density function (PDF) for a Rayleigh distribution with scale parameter `sigma` at a value `x`.
*
* @param {Number} x - input value
* @param {Number} sigma - scale parameter
* @returns {Number} evaluated PDF
*/
function pdf( x, sigma ) {
	var s2 = pow( sigma, 2 );
	if ( x < 0 ) {
		return 0;
	}
	return (1/s2) * x * exp( -pow( x, 2 ) / ( 2 * s2 ) );
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{}],2975:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var exp = Math.exp,
	pow = Math.pow;


// PARTIAL //

/**
* FUNCTION: partial( sigma )
*	Partially applies scale parameter `sigma` and returns a function for evaluating the probability density function (PDF) for a Rayleigh distribution.
*
* @param {Number} sigma - scale parameter
* @returns {Function} PDF
*/
function partial( sigma ) {
	var s2 = pow( sigma, 2 );
	/**
	* FUNCTION: pdf( x )
	*	Evaluates the probability density function (PDF) for a Rayleigh distribution.
	*
	* @private
	* @param {Number} x - input value
	* @returns {Number} evaluated PDF
	*/
	return function pdf( x ) {
		if ( x < 0 ) {
			return 0;
		}
		return (1/s2) * x * exp( -pow( x, 2 ) / ( 2 * s2 ) );
	};
} // end FUNCTION partial()


// EXPORTS //

module.exports = partial;

},{}],2976:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// PDF //

/**
* FUNCTION: pdf( out, arr, sigma )
*	Evaluates the probability density function (PDF) for a Rayleigh distribution with scale parameter `sigma` for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} arr - input array
* @param {Number} sigma - scale parameter
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function pdf( y, x, sigma ) {
	var len = x.length,
		fcn,
		i;

	fcn = partial ( sigma );
	for ( i = 0; i < len; i++ ) {
		y[ i ] = fcn( x[ i ] );
	}
	return y;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./partial.js":2975}],2977:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isPositive = require( 'validate.io-positive-primitive' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Number} [options.sigma=1] - scale parameter
* @param {Boolean} [options.copy] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'pdf()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'sigma' ) ) {
		opts.sigma = options.sigma;
		if ( !isPositive( opts.sigma ) ) {
			return new TypeError( 'pdf()::invalid option. `sigma` parameter must be a positive number. Option: `' + opts.sigma + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'pdf()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'pdf()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'pdf()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'pdf()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'pdf()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":3038,"validate.io-function":3039,"validate.io-object":3045,"validate.io-positive-primitive":3047,"validate.io-string-primitive":3048}],2978:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],2979:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":2978,"dup":12}],2980:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],2981:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":2984,"./iget.js":2986,"./iset.js":2989,"./mget.js":2993,"./mset.js":2995,"./set.js":3003,"./sget.js":3005,"./sset.js":3007,"./toString.js":3009,"dup":14}],2982:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":2985,"./iget.raw.js":2987,"./iset.raw.js":2990,"./mget.raw.js":2994,"./mset.raw.js":2996,"./set.raw.js":3004,"./sget.raw.js":3006,"./sset.raw.js":3008,"./toString.js":3009,"dup":15}],2983:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],2984:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":3041}],2985:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],2986:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":3021}],2987:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],2988:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":2991,"./matrix.raw.js":2992,"dup":21}],2989:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":3021,"validate.io-number-primitive":3044}],2990:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],2991:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":2980,"./ctor.js":2981,"./dtypes.js":2983,"compute-cast-arrays":3010,"compute-dtype":3013,"dup":24,"validate.io-array":3018,"validate.io-contains":3019,"validate.io-nonnegative-integer-array":3022,"validate.io-string-primitive":3048}],2992:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":2980,"./ctor.raw.js":2982,"./dtypes.js":2983,"compute-dtype":3013,"dup":25,"validate.io-contains":3019,"validate.io-string-primitive":3048}],2993:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":2980,"dup":26,"validate.io-nonnegative-integer-array":3022}],2994:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":2980,"dup":27}],2995:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":2997,"./mset2.js":2998,"./mset3.js":2999,"./mset4.js":3000,"./mset5.js":3001,"./mset6.js":3002,"dup":28,"validate.io-function":3039,"validate.io-nonnegative-integer-array":3022,"validate.io-number-primitive":3044}],2996:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":2997,"./mset2.js":2998,"./mset3.js":2999,"./mset4.js":3000,"./mset5.js":3001,"./mset6.js":3002,"dup":29}],2997:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],2998:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],2999:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],3000:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],3001:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],3002:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],3003:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":3041,"validate.io-number-primitive":3044}],3004:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],3005:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":2980,"compute-indexspace":3017,"dup":38,"validate.io-string-primitive":3048}],3006:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":2980,"compute-indexspace":3017,"dup":39}],3007:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":3017,"dup":40,"validate.io-function":3039,"validate.io-number-primitive":3044,"validate.io-string-primitive":3048}],3008:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":3017,"dup":41}],3009:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],3010:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":2978,"compute-array-dtype/lib/dtypes":3011,"dup":43,"type-name":3012,"validate.io-array-like":3035}],3011:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],3012:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],3013:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":3015,"dup":46,"type-name":3016}],3014:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],3015:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":3014,"dup":48}],3016:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],3017:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":3041,"validate.io-string-primitive":3048}],3018:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],3019:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":3018,"validate.io-nan-primitive":3020}],3020:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],3021:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":3044}],3022:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":3018,"validate.io-nonnegative-integer":3041}],3023:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],3024:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],3025:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":3023,"./defaults.js":3024,"./validate.js":3027,"dup":60,"validate.io-array":3028,"validate.io-string-primitive":3048}],3026:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":3023,"./defaults.js":3024,"./factory.js":3025,"./validate.js":3027,"dup":61,"validate.io-array":3028,"validate.io-string-primitive":3048}],3027:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":3045,"validate.io-string-primitive":3048}],3028:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],3029:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],3030:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],3031:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":3029,"./defaults.js":3030,"./validate.js":3033,"dup":66,"validate.io-array":3034,"validate.io-string-primitive":3048}],3032:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":3029,"./defaults.js":3030,"./factory.js":3031,"./validate.js":3033,"dup":67,"validate.io-array":3034,"validate.io-string-primitive":3048}],3033:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":3038,"validate.io-object":3045,"validate.io-string-primitive":3048}],3034:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],3035:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":3036,"dup":70,"validate.io-integer-primitive":3037}],3036:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],3037:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":3044}],3038:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],3039:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],3040:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],3041:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":3042}],3042:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":3043}],3043:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],3044:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],3045:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":3046}],3046:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],3047:[function(require,module,exports){
arguments[4][1221][0].apply(exports,arguments)
},{"dup":1221,"validate.io-number-primitive":3044}],3048:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],3049:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":3050,"dup":81,"validate.io-integer-primitive":3051}],3050:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],3051:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":3044}],3052:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// QUANTILE //

/**
* FUNCTION: quantile( out, arr, sigma, accessor )
*	Evaluates the quantile function for a Rayleigh distribution with scale parameter `sigma` using an accessor function.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Number} sigma - scale parameter
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function quantile( y, x, sigma, clbk ) {
	var len = x.length,
		fcn,
		v, i;

	fcn = partial( sigma );
	for ( i = 0; i < len; i++ ) {
		v = clbk( x[ i ], i );
		if ( typeof v === 'number' ) {
			y[ i ] = fcn( v );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION quantile()


// EXPORTS //

module.exports = quantile;

},{"./partial.js":3058}],3053:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// QUANTILE //

/**
* FUNCTION: quantile( out, arr, sigma )
*	Evaluates the quantile function for a Rayleigh distribution with scale parameter `sigma` for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Number} sigma - scale parameter
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function quantile( y, x, sigma ) {
	var len = x.length,
		fcn,
		i;

	fcn = partial( sigma );
	for ( i = 0; i < len; i++ ) {
		if ( typeof x[ i ] === 'number' ) {
			y[ i ] = fcn( x[ i ] );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION quantile()


// EXPORTS //

module.exports = quantile;

},{"./partial.js":3058}],3054:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory,
	partial = require( './partial.js' );


// QUANTILE //

/**
* FUNCTION: quantile( arr, sigma, path[, sep] )
*	Evaluates the quantile function for a Rayleigh distribution with scale parameter `sigma` for each array element and sets the input array.
*
* @param {Array} arr - input array
* @param {Number} sigma - scale parameter
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function quantile( x, sigma, path, sep ) {
	var len = x.length,
		opts = {},
		dget,
		dset,
		fcn,
		v, i;
	if ( arguments.length > 3 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		fcn = partial( sigma );
		for ( i = 0; i < len; i++ ) {
			v = dget( x[ i ] );
			if ( typeof v === 'number' ) {
				dset( x[i], fcn( v ) );
			} else {
				dset( x[i], NaN );
			}
		}
	}
	return x;
} // end FUNCTION quantile()


// EXPORTS //

module.exports = quantile;

},{"./partial.js":3058,"utils-deep-get":3109,"utils-deep-set":3115}],3055:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var quantile1 = require( './number.js' ),
	quantile2 = require( './array.js' ),
	quantile3 = require( './accessor.js' ),
	quantile4 = require( './deepset.js' ),
	quantile5 = require( './matrix.js' ),
	quantile6 = require( './typedarray.js' );


// PDF //

/**
* FUNCTION: quantile( p[, opts] )
*	Evaluates the quantile function for a Rayleigh distribution.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} p - input value
* @param {Object} [opts] - function options
* @param {Number} [opts.sigma=1] - scale parameter
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} quantile function value(s)
*/
function quantile( p, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	opts.sigma = typeof opts.sigma !== 'undefined' ? opts.sigma : 1;

	if ( isNumber( p ) ) {
		return quantile1( p, opts.sigma );
	}
	if ( isMatrixLike( p ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'quantile()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( p.length );
			out = matrix( d, p.shape, dt );
		} else {
			out = p;
		}
		return quantile5( out, p, opts.sigma );
	}
	if ( isTypedArrayLike( p ) ) {
		if ( opts.copy === false ) {
			out = p;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'quantile()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( p.length );
		}
		return quantile6( out, p, opts.sigma );
	}
	if ( isArrayLike( p ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return quantile4( p, opts.sigma, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = p;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'quantile()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( p.length );
		}
		else {
			out = new Array( p.length );
		}
		if ( opts.accessor ) {
			return quantile3( out, p, opts.sigma, opts.accessor );
		}
		return quantile2( out, p, opts.sigma );
	}
	return NaN;
} // end FUNCTION quantile()


// EXPORTS //

module.exports = quantile;

},{"./accessor.js":3052,"./array.js":3053,"./deepset.js":3054,"./matrix.js":3056,"./number.js":3057,"./typedarray.js":3059,"./validate.js":3060,"compute-array-constructors":3062,"dstructs-matrix":3071,"validate.io-array-like":3118,"validate.io-matrix-like":3123,"validate.io-number-primitive":3127,"validate.io-typed-array-like":3132}],3056:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// QUANTILE //

/**
* FUNCTION: quantile( out, matrix, sigma )
*	Evaluates the quantile function for a Rayleigh distribution with scale parameter `sigma` for each matrix element.
*
* @param {Matrix} out - output matrix
* @param {Matrix} arr - input matrix
* @param {Number} sigma - scale parameter
* @returns {Matrix} output matrix
*/
function quantile( y, x, sigma ) {
	var len = x.length,
		fcn,
		i;
	if ( y.length !== len ) {
		throw new Error( 'quantile()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	fcn = partial( sigma );
	for ( i = 0; i < len; i++ ) {
		y.data[ i ] = fcn( x.data[ i ] );
	}
	return y;
} // end FUNCTION quantile()


// EXPORTS //

module.exports = quantile;

},{"./partial.js":3058}],3057:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var ln = Math.log,
	pow = Math.pow,
	sqrt = Math.sqrt;
	

// QUANTILE //

/**
* FUNCTION: quantile( p, sigma )
*	Evaluates the quantile function for a Rayleigh distribution with scale parameter `sigma` at a probability `p`.
*
* @param {Number} p - input value
* @param {Number} sigma - scale parameter
* @returns {Number} evaluated quantile function
*/
function quantile( p, sigma ) {
	if ( p !== p || p < 0 || p > 1 ) {
		return NaN;
	}
	return sigma * sqrt( -ln( pow( 1 - p, 2 ) ) );
} // end FUNCTION quantile()


// EXPORTS //

module.exports = quantile;

},{}],3058:[function(require,module,exports){
'use strict';

// FUNCTIONS //

var ln = Math.log,
	pow = Math.pow,
	sqrt = Math.sqrt;


// PARTIAL //

/**
* FUNCTION: partial( sigma )
*	Partially applies scale parameter `sigma` and returns a function for evaluating the quantile function for a Rayleigh distribution.
*
* @param {Number} sigma - scale parameter
* @returns {Function} quantile function
*/
function partial( sigma ) {

	/**
	* FUNCTION: quantile( p )
	*	Evaluates the quantile function for a Rayleigh distribution.
	*
	* @private
	* @param {Number} p - input value
	* @returns {Number} evaluated quantile function
	*/
	return function quantile( p ) {
		if ( p !== p || p < 0 || p > 1 ) {
			return NaN;
		}
		return sigma * sqrt( -ln( pow( 1 - p, 2 ) ) );
	};
} // end FUNCTION partial()


// EXPORTS //

module.exports = partial;

},{}],3059:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// QUANTILE //

/**
* FUNCTION: quantile( out, arr, sigma )
*	Evaluates the quantile function for a Rayleigh distribution with scale parameter `sigma` for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} arr - input array
* @param {Number} sigma - scale parameter
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function quantile( y, x, sigma ) {
	var len = x.length,
		fcn,
		i;

	fcn = partial ( sigma );
	for ( i = 0; i < len; i++ ) {
		y[ i ] = fcn( x[ i ] );
	}
	return y;
} // end FUNCTION quantile()


// EXPORTS //

module.exports = quantile;

},{"./partial.js":3058}],3060:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isPositive = require( 'validate.io-positive-primitive' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Number} [options.sigma=1] - scale parameter
* @param {Boolean} [options.copy] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'quantile()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'sigma' ) ) {
		opts.sigma = options.sigma;
		if ( !isPositive( opts.sigma ) ) {
			return new TypeError( 'quantile()::invalid option. `sigma` parameter must be a positive number. Option: `' + opts.sigma + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'quantile()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'quantile()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'quantile()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'quantile()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'quantile()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":3121,"validate.io-function":3122,"validate.io-object":3128,"validate.io-positive-primitive":3130,"validate.io-string-primitive":3131}],3061:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],3062:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":3061,"dup":12}],3063:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],3064:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":3067,"./iget.js":3069,"./iset.js":3072,"./mget.js":3076,"./mset.js":3078,"./set.js":3086,"./sget.js":3088,"./sset.js":3090,"./toString.js":3092,"dup":14}],3065:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":3068,"./iget.raw.js":3070,"./iset.raw.js":3073,"./mget.raw.js":3077,"./mset.raw.js":3079,"./set.raw.js":3087,"./sget.raw.js":3089,"./sset.raw.js":3091,"./toString.js":3092,"dup":15}],3066:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],3067:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":3124}],3068:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],3069:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":3104}],3070:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],3071:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":3074,"./matrix.raw.js":3075,"dup":21}],3072:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":3104,"validate.io-number-primitive":3127}],3073:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],3074:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":3063,"./ctor.js":3064,"./dtypes.js":3066,"compute-cast-arrays":3093,"compute-dtype":3096,"dup":24,"validate.io-array":3101,"validate.io-contains":3102,"validate.io-nonnegative-integer-array":3105,"validate.io-string-primitive":3131}],3075:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":3063,"./ctor.raw.js":3065,"./dtypes.js":3066,"compute-dtype":3096,"dup":25,"validate.io-contains":3102,"validate.io-string-primitive":3131}],3076:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":3063,"dup":26,"validate.io-nonnegative-integer-array":3105}],3077:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":3063,"dup":27}],3078:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":3080,"./mset2.js":3081,"./mset3.js":3082,"./mset4.js":3083,"./mset5.js":3084,"./mset6.js":3085,"dup":28,"validate.io-function":3122,"validate.io-nonnegative-integer-array":3105,"validate.io-number-primitive":3127}],3079:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":3080,"./mset2.js":3081,"./mset3.js":3082,"./mset4.js":3083,"./mset5.js":3084,"./mset6.js":3085,"dup":29}],3080:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],3081:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],3082:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],3083:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],3084:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],3085:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],3086:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":3124,"validate.io-number-primitive":3127}],3087:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],3088:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":3063,"compute-indexspace":3100,"dup":38,"validate.io-string-primitive":3131}],3089:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":3063,"compute-indexspace":3100,"dup":39}],3090:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":3100,"dup":40,"validate.io-function":3122,"validate.io-number-primitive":3127,"validate.io-string-primitive":3131}],3091:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":3100,"dup":41}],3092:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],3093:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":3061,"compute-array-dtype/lib/dtypes":3094,"dup":43,"type-name":3095,"validate.io-array-like":3118}],3094:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],3095:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],3096:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":3098,"dup":46,"type-name":3099}],3097:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],3098:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":3097,"dup":48}],3099:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],3100:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":3124,"validate.io-string-primitive":3131}],3101:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],3102:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":3101,"validate.io-nan-primitive":3103}],3103:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],3104:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":3127}],3105:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":3101,"validate.io-nonnegative-integer":3124}],3106:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],3107:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],3108:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":3106,"./defaults.js":3107,"./validate.js":3110,"dup":60,"validate.io-array":3111,"validate.io-string-primitive":3131}],3109:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":3106,"./defaults.js":3107,"./factory.js":3108,"./validate.js":3110,"dup":61,"validate.io-array":3111,"validate.io-string-primitive":3131}],3110:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":3128,"validate.io-string-primitive":3131}],3111:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],3112:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],3113:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],3114:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":3112,"./defaults.js":3113,"./validate.js":3116,"dup":66,"validate.io-array":3117,"validate.io-string-primitive":3131}],3115:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":3112,"./defaults.js":3113,"./factory.js":3114,"./validate.js":3116,"dup":67,"validate.io-array":3117,"validate.io-string-primitive":3131}],3116:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":3121,"validate.io-object":3128,"validate.io-string-primitive":3131}],3117:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],3118:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":3119,"dup":70,"validate.io-integer-primitive":3120}],3119:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],3120:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":3127}],3121:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],3122:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],3123:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],3124:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":3125}],3125:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":3126}],3126:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],3127:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],3128:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":3129}],3129:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],3130:[function(require,module,exports){
arguments[4][1221][0].apply(exports,arguments)
},{"dup":1221,"validate.io-number-primitive":3127}],3131:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],3132:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":3133,"dup":81,"validate.io-integer-primitive":3134}],3133:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],3134:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":3127}],3135:[function(require,module,exports){
arguments[4][1725][0].apply(exports,arguments)
},{"./number.js":3140,"dup":1725}],3136:[function(require,module,exports){
'use strict';

// MODULES //

var SKEWNESS = require( './number.js' );


// SKEWNESS //

/**
* FUNCTION: skewness( out, sigma )
*	Computes the distribution skewness for parameters stored in an array.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} sigma - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function skewness( out, arr ) {
	var len = arr.length,
		i;
	for ( i = 0; i < len; i++ ) {
		if ( typeof arr[ i ] === 'number' ) {
			out[ i ] = SKEWNESS( arr[ i ] );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION skewness()


// EXPORTS //

module.exports = skewness;

},{"./number.js":3140}],3137:[function(require,module,exports){
arguments[4][1727][0].apply(exports,arguments)
},{"./number.js":3140,"dup":1727,"utils-deep-get":3191,"utils-deep-set":3197}],3138:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isnan = require( 'validate.io-nan' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var skewness1 = require( './number.js' ),
	skewness2 = require( './array.js' ),
	skewness3 = require( './accessor.js' ),
	skewness4 = require( './deepset.js' ),
	skewness5 = require( './matrix.js' ),
	skewness6 = require( './typedarray.js' );


// SKEWNESS //

/**
* FUNCTION: skewness( sigma[, opts] )
*	Computes the distribution skewness.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} sigma - input value
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} distribution skewness(s)
*/
function skewness( sigma, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( isNumber( sigma ) || isnan( sigma ) ) {
		return skewness1( sigma );
	}
	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isMatrixLike( sigma ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'skewness()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( sigma.length );
			out = matrix( d, sigma.shape, dt );
		} else {
			out = sigma;
		}
		return skewness5( out, sigma );
	}
	if ( isTypedArrayLike( sigma ) ) {
		if ( opts.copy === false ) {
			out = sigma;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'skewness()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( sigma.length );
		}
		return skewness6( out, sigma );
	}
	if ( isArrayLike( sigma ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return skewness4( sigma, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = sigma;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'skewness()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( sigma.length );
		}
		else {
			out = new Array( sigma.length );
		}
		if ( opts.accessor ) {
			return skewness3( out, sigma, opts.accessor );
		}
		return skewness2( out, sigma );
	}
	return NaN;
} // end FUNCTION skewness()


// EXPORTS //

module.exports = skewness;

},{"./accessor.js":3135,"./array.js":3136,"./deepset.js":3137,"./matrix.js":3139,"./number.js":3140,"./typedarray.js":3141,"./validate.js":3142,"compute-array-constructors":3144,"dstructs-matrix":3153,"validate.io-array-like":3200,"validate.io-matrix-like":3205,"validate.io-nan":3206,"validate.io-number-primitive":3210,"validate.io-typed-array-like":3215}],3139:[function(require,module,exports){
arguments[4][1729][0].apply(exports,arguments)
},{"./number.js":3140,"dup":1729}],3140:[function(require,module,exports){
'use strict';

// MODULES //

var isPositive = require( 'validate.io-positive-primitive' );

// FUNCTIONS //

var pow = Math.pow,
	sqrt = Math.sqrt;


// CONSTANTS //

var PI = Math.PI,
	SKEWNESS = 2 * sqrt(PI) * ( PI - 3 ) / pow( 4 - PI, 3/2 );


// SKEWNESS //

/**
* FUNCTION skewness( sigma )
*	Computes the distribution skewness for a Rayleigh distribution with parameter sigma.
*
* @param {Number} sigma - scale parameter
* @returns {Number} distribution skewness
*/
function skewness( sigma ) {
	if ( !isPositive( sigma ) ) {
		return NaN;
	}
	return SKEWNESS;
} // end FUNCTION skewness()


// EXPORTS

module.exports =  skewness;

},{"validate.io-positive-primitive":3213}],3141:[function(require,module,exports){
'use strict';

// MODULES //

var SKEWNESS = require( './number.js' );


// SKEWNESS //

/**
* FUNCTION: skewness( out, sigma )
*	Computes the distribution skewness for parameters stored in a typed array.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} sigma - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function skewness( out, sigma ) {
	var len = sigma.length,
		i;
	for ( i = 0; i < len; i++ ) {
		out[ i ] = SKEWNESS( sigma[ i ] );
	}
	return out;
} // end FUNCTION skewness()


// EXPORTS //

module.exports = skewness;

},{"./number.js":3140}],3142:[function(require,module,exports){
arguments[4][1732][0].apply(exports,arguments)
},{"dup":1732,"validate.io-boolean-primitive":3203,"validate.io-function":3204,"validate.io-object":3211,"validate.io-string-primitive":3214}],3143:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],3144:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":3143,"dup":12}],3145:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],3146:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":3149,"./iget.js":3151,"./iset.js":3154,"./mget.js":3158,"./mset.js":3160,"./set.js":3168,"./sget.js":3170,"./sset.js":3172,"./toString.js":3174,"dup":14}],3147:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":3150,"./iget.raw.js":3152,"./iset.raw.js":3155,"./mget.raw.js":3159,"./mset.raw.js":3161,"./set.raw.js":3169,"./sget.raw.js":3171,"./sset.raw.js":3173,"./toString.js":3174,"dup":15}],3148:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],3149:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":3207}],3150:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],3151:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":3186}],3152:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],3153:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":3156,"./matrix.raw.js":3157,"dup":21}],3154:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":3186,"validate.io-number-primitive":3210}],3155:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],3156:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":3145,"./ctor.js":3146,"./dtypes.js":3148,"compute-cast-arrays":3175,"compute-dtype":3178,"dup":24,"validate.io-array":3183,"validate.io-contains":3184,"validate.io-nonnegative-integer-array":3187,"validate.io-string-primitive":3214}],3157:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":3145,"./ctor.raw.js":3147,"./dtypes.js":3148,"compute-dtype":3178,"dup":25,"validate.io-contains":3184,"validate.io-string-primitive":3214}],3158:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":3145,"dup":26,"validate.io-nonnegative-integer-array":3187}],3159:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":3145,"dup":27}],3160:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":3162,"./mset2.js":3163,"./mset3.js":3164,"./mset4.js":3165,"./mset5.js":3166,"./mset6.js":3167,"dup":28,"validate.io-function":3204,"validate.io-nonnegative-integer-array":3187,"validate.io-number-primitive":3210}],3161:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":3162,"./mset2.js":3163,"./mset3.js":3164,"./mset4.js":3165,"./mset5.js":3166,"./mset6.js":3167,"dup":29}],3162:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],3163:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],3164:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],3165:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],3166:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],3167:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],3168:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":3207,"validate.io-number-primitive":3210}],3169:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],3170:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":3145,"compute-indexspace":3182,"dup":38,"validate.io-string-primitive":3214}],3171:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":3145,"compute-indexspace":3182,"dup":39}],3172:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":3182,"dup":40,"validate.io-function":3204,"validate.io-number-primitive":3210,"validate.io-string-primitive":3214}],3173:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":3182,"dup":41}],3174:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],3175:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":3143,"compute-array-dtype/lib/dtypes":3176,"dup":43,"type-name":3177,"validate.io-array-like":3200}],3176:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],3177:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],3178:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":3180,"dup":46,"type-name":3181}],3179:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],3180:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":3179,"dup":48}],3181:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],3182:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":3207,"validate.io-string-primitive":3214}],3183:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],3184:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":3183,"validate.io-nan-primitive":3185}],3185:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],3186:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":3210}],3187:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":3183,"validate.io-nonnegative-integer":3207}],3188:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],3189:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],3190:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":3188,"./defaults.js":3189,"./validate.js":3192,"dup":60,"validate.io-array":3193,"validate.io-string-primitive":3214}],3191:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":3188,"./defaults.js":3189,"./factory.js":3190,"./validate.js":3192,"dup":61,"validate.io-array":3193,"validate.io-string-primitive":3214}],3192:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":3211,"validate.io-string-primitive":3214}],3193:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],3194:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],3195:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],3196:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":3194,"./defaults.js":3195,"./validate.js":3198,"dup":66,"validate.io-array":3199,"validate.io-string-primitive":3214}],3197:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":3194,"./defaults.js":3195,"./factory.js":3196,"./validate.js":3198,"dup":67,"validate.io-array":3199,"validate.io-string-primitive":3214}],3198:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":3203,"validate.io-object":3211,"validate.io-string-primitive":3214}],3199:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],3200:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":3201,"dup":70,"validate.io-integer-primitive":3202}],3201:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],3202:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":3210}],3203:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],3204:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],3205:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],3206:[function(require,module,exports){
arguments[4][76][0].apply(exports,arguments)
},{"dup":76}],3207:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":3208}],3208:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":3209}],3209:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],3210:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],3211:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":3212}],3212:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],3213:[function(require,module,exports){
arguments[4][1221][0].apply(exports,arguments)
},{"dup":1221,"validate.io-number-primitive":3210}],3214:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],3215:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":3216,"dup":81,"validate.io-integer-primitive":3217}],3216:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],3217:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":3210}],3218:[function(require,module,exports){
arguments[4][1808][0].apply(exports,arguments)
},{"./number.js":3223,"dup":1808}],3219:[function(require,module,exports){
'use strict';

// MODULES //

var VARIANCE = require( './number.js' );


// VARIANCE //

/**
* FUNCTION: variance( out, sigma )
*	Computes the distribution variance for parameters stored in an array.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} sigma - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function variance( out, arr ) {
	var len = arr.length,
		i;
	for ( i = 0; i < len; i++ ) {
		if ( typeof arr[ i ] === 'number' ) {
			out[ i ] = VARIANCE( arr[ i ] );
		} else {
			out[ i ] = NaN;
		}
	}
	return out;
} // end FUNCTION variance()


// EXPORTS //

module.exports = variance;

},{"./number.js":3223}],3220:[function(require,module,exports){
arguments[4][1810][0].apply(exports,arguments)
},{"./number.js":3223,"dup":1810,"utils-deep-get":3274,"utils-deep-set":3280}],3221:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isnan = require( 'validate.io-nan' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var variance1 = require( './number.js' ),
	variance2 = require( './array.js' ),
	variance3 = require( './accessor.js' ),
	variance4 = require( './deepset.js' ),
	variance5 = require( './matrix.js' ),
	variance6 = require( './typedarray.js' );


// VARIANCE //

/**
* FUNCTION: variance( sigma[, opts] )
*	Computes the distribution variance.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} sigma - input value
* @param {Object} [opts] - function options
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} distribution variance(s)
*/
function variance( sigma, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( isNumber( sigma ) || isnan( sigma ) ) {
		return variance1( sigma );
	}
	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	if ( isMatrixLike( sigma ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'variance()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( sigma.length );
			out = matrix( d, sigma.shape, dt );
		} else {
			out = sigma;
		}
		return variance5( out, sigma );
	}
	if ( isTypedArrayLike( sigma ) ) {
		if ( opts.copy === false ) {
			out = sigma;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'variance()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( sigma.length );
		}
		return variance6( out, sigma );
	}
	if ( isArrayLike( sigma ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return variance4( sigma, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = sigma;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'variance()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( sigma.length );
		}
		else {
			out = new Array( sigma.length );
		}
		if ( opts.accessor ) {
			return variance3( out, sigma, opts.accessor );
		}
		return variance2( out, sigma );
	}
	return NaN;
} // end FUNCTION variance()


// EXPORTS //

module.exports = variance;

},{"./accessor.js":3218,"./array.js":3219,"./deepset.js":3220,"./matrix.js":3222,"./number.js":3223,"./typedarray.js":3224,"./validate.js":3225,"compute-array-constructors":3227,"dstructs-matrix":3236,"validate.io-array-like":3283,"validate.io-matrix-like":3288,"validate.io-nan":3289,"validate.io-number-primitive":3293,"validate.io-typed-array-like":3298}],3222:[function(require,module,exports){
arguments[4][1812][0].apply(exports,arguments)
},{"./number.js":3223,"dup":1812}],3223:[function(require,module,exports){
'use strict';

// MODULES //

var isPositive = require( 'validate.io-positive-primitive' );

// FUNCTIONS //

var pow = Math.pow;


// CONSTANTS //

var PI = Math.PI;


// VARIANCE //

/**
* FUNCTION variance( sigma )
*	Computes the distribution variance for a Rayleigh distribution with parameter sigma.
*
* @param {Number} sigma - scale parameter
* @returns {Number} distribution variance
*/
function variance( sigma ) {
	var s2;
	if ( !isPositive( sigma ) ) {
		return NaN;
	}
	s2 = pow( sigma, 2 );
	return ( ( 4 - PI ) / 2 ) * s2;
} // end FUNCTION variance()


// EXPORTS

module.exports =  variance;

},{"validate.io-positive-primitive":3296}],3224:[function(require,module,exports){
'use strict';

// MODULES //

var VARIANCE = require( './number.js' );


// VARIANCE //

/**
* FUNCTION: variance( out, sigma )
*	Computes the distribution variance for parameters stored in a typed array.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} sigma - input array
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function variance( out, sigma ) {
	var len = sigma.length,
		i;
	for ( i = 0; i < len; i++ ) {
		out[ i ] = VARIANCE( sigma[ i ] );
	}
	return out;
} // end FUNCTION variance()


// EXPORTS //

module.exports = variance;

},{"./number.js":3223}],3225:[function(require,module,exports){
arguments[4][1815][0].apply(exports,arguments)
},{"dup":1815,"validate.io-boolean-primitive":3286,"validate.io-function":3287,"validate.io-object":3294,"validate.io-string-primitive":3297}],3226:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],3227:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":3226,"dup":12}],3228:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],3229:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":3232,"./iget.js":3234,"./iset.js":3237,"./mget.js":3241,"./mset.js":3243,"./set.js":3251,"./sget.js":3253,"./sset.js":3255,"./toString.js":3257,"dup":14}],3230:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":3233,"./iget.raw.js":3235,"./iset.raw.js":3238,"./mget.raw.js":3242,"./mset.raw.js":3244,"./set.raw.js":3252,"./sget.raw.js":3254,"./sset.raw.js":3256,"./toString.js":3257,"dup":15}],3231:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],3232:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":3290}],3233:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],3234:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":3269}],3235:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],3236:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":3239,"./matrix.raw.js":3240,"dup":21}],3237:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":3269,"validate.io-number-primitive":3293}],3238:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],3239:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":3228,"./ctor.js":3229,"./dtypes.js":3231,"compute-cast-arrays":3258,"compute-dtype":3261,"dup":24,"validate.io-array":3266,"validate.io-contains":3267,"validate.io-nonnegative-integer-array":3270,"validate.io-string-primitive":3297}],3240:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":3228,"./ctor.raw.js":3230,"./dtypes.js":3231,"compute-dtype":3261,"dup":25,"validate.io-contains":3267,"validate.io-string-primitive":3297}],3241:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":3228,"dup":26,"validate.io-nonnegative-integer-array":3270}],3242:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":3228,"dup":27}],3243:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":3245,"./mset2.js":3246,"./mset3.js":3247,"./mset4.js":3248,"./mset5.js":3249,"./mset6.js":3250,"dup":28,"validate.io-function":3287,"validate.io-nonnegative-integer-array":3270,"validate.io-number-primitive":3293}],3244:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":3245,"./mset2.js":3246,"./mset3.js":3247,"./mset4.js":3248,"./mset5.js":3249,"./mset6.js":3250,"dup":29}],3245:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],3246:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],3247:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],3248:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],3249:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],3250:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],3251:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":3290,"validate.io-number-primitive":3293}],3252:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],3253:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":3228,"compute-indexspace":3265,"dup":38,"validate.io-string-primitive":3297}],3254:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":3228,"compute-indexspace":3265,"dup":39}],3255:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":3265,"dup":40,"validate.io-function":3287,"validate.io-number-primitive":3293,"validate.io-string-primitive":3297}],3256:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":3265,"dup":41}],3257:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],3258:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":3226,"compute-array-dtype/lib/dtypes":3259,"dup":43,"type-name":3260,"validate.io-array-like":3283}],3259:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],3260:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],3261:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":3263,"dup":46,"type-name":3264}],3262:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],3263:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":3262,"dup":48}],3264:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],3265:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":3290,"validate.io-string-primitive":3297}],3266:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],3267:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":3266,"validate.io-nan-primitive":3268}],3268:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],3269:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":3293}],3270:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":3266,"validate.io-nonnegative-integer":3290}],3271:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],3272:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],3273:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":3271,"./defaults.js":3272,"./validate.js":3275,"dup":60,"validate.io-array":3276,"validate.io-string-primitive":3297}],3274:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":3271,"./defaults.js":3272,"./factory.js":3273,"./validate.js":3275,"dup":61,"validate.io-array":3276,"validate.io-string-primitive":3297}],3275:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":3294,"validate.io-string-primitive":3297}],3276:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],3277:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],3278:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],3279:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":3277,"./defaults.js":3278,"./validate.js":3281,"dup":66,"validate.io-array":3282,"validate.io-string-primitive":3297}],3280:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":3277,"./defaults.js":3278,"./factory.js":3279,"./validate.js":3281,"dup":67,"validate.io-array":3282,"validate.io-string-primitive":3297}],3281:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":3286,"validate.io-object":3294,"validate.io-string-primitive":3297}],3282:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],3283:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":3284,"dup":70,"validate.io-integer-primitive":3285}],3284:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],3285:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":3293}],3286:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],3287:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],3288:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],3289:[function(require,module,exports){
arguments[4][76][0].apply(exports,arguments)
},{"dup":76}],3290:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":3291}],3291:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":3292}],3292:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],3293:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],3294:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":3295}],3295:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],3296:[function(require,module,exports){
arguments[4][1221][0].apply(exports,arguments)
},{"dup":1221,"validate.io-number-primitive":3293}],3297:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],3298:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":3299,"dup":81,"validate.io-integer-primitive":3300}],3299:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],3300:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":3293}],3301:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// PDF //

/**
* FUNCTION: pdf( out, arr, v, accessor )
*	Evaluates the probability density function (PDF) for a Student t distribution with degrees of freedom `v` using an accessor function.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Number} v - degrees of freedom
* @param {Function} accessor - accessor function for accessing array values
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function pdf( y, x, v, clbk ) {
	var len = x.length,
		fcn,
		v, i;

	fcn = partial( v );
	for ( i = 0; i < len; i++ ) {
		v = clbk( x[ i ], i );
		if ( typeof v === 'number' ) {
			y[ i ] = fcn( v );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./partial.js":3307}],3302:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// PDF //

/**
* FUNCTION: pdf( out, arr, v )
*	Evaluates the probability density function (PDF) for a Student t distribution with degrees of freedom `v` for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Array} arr - input array
* @param {Number} v - degrees of freedom
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function pdf( y, x, v ) {
	var len = x.length,
		fcn,
		i;

	fcn = partial( v );
	for ( i = 0; i < len; i++ ) {
		if ( typeof x[ i ] === 'number' ) {
			y[ i ] = fcn( x[ i ] );
		} else {
			y[ i ] = NaN;
		}
	}
	return y;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./partial.js":3307}],3303:[function(require,module,exports){
'use strict';

// MODULES //

var deepSet = require( 'utils-deep-set' ).factory,
	deepGet = require( 'utils-deep-get' ).factory,
	partial = require( './partial.js' );


// PDF //

/**
* FUNCTION: pdf( arr, v, path[, sep] )
*	Evaluates the probability density function (PDF) for a Student t distribution with degrees of freedom `v` for each array element and sets the input array.
*
* @param {Array} arr - input array
* @param {Number} v - degrees of freedom
* @param {String} path - key path used when deep getting and setting
* @param {String} [sep] - key path separator
* @returns {Array} input array
*/
function pdf( x, v, path, sep ) {
	var len = x.length,
		opts = {},
		dget,
		dset,
		fcn,
		v, i;
	if ( arguments.length > 3 ) {
		opts.sep = sep;
	}
	if ( len ) {
		dget = deepGet( path, opts );
		dset = deepSet( path, opts );
		fcn = partial( v );
		for ( i = 0; i < len; i++ ) {
			v = dget( x[ i ] );
			if ( typeof v === 'number' ) {
				dset( x[i], fcn( v ) );
			} else {
				dset( x[i], NaN );
			}
		}
	}
	return x;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./partial.js":3307,"utils-deep-get":3361,"utils-deep-set":3367}],3304:[function(require,module,exports){
'use strict';

// MODULES //

var isNumber = require( 'validate.io-number-primitive' ),
	isArrayLike = require( 'validate.io-array-like' ),
	isTypedArrayLike = require( 'validate.io-typed-array-like' ),
	isMatrixLike = require( 'validate.io-matrix-like' ),
	ctors = require( 'compute-array-constructors' ),
	matrix = require( 'dstructs-matrix' ),
	validate = require( './validate.js' );


// FUNCTIONS //

var pdf1 = require( './number.js' ),
	pdf2 = require( './array.js' ),
	pdf3 = require( './accessor.js' ),
	pdf4 = require( './deepset.js' ),
	pdf5 = require( './matrix.js' ),
	pdf6 = require( './typedarray.js' );


// PDF //

/**
* FUNCTION: pdf( x[, opts] )
*	Evaluates the probability density function (PDF) for a Student t distribution.
*
* @param {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} x - input value
* @param {Object} [opts] - function options
* @param {Number} [opts.v=1] - degrees of freedom
* @param {Boolean} [opts.copy=true] - boolean indicating if the function should return a new data structure
* @param {Function} [opts.accessor] - accessor function for accessing array values
* @param {String} [opts.path] - deep get/set key path
* @param {String} [opts.sep="."] - deep get/set key path separator
* @param {String} [opts.dtype="float64"] - output data type
* @returns {Number|Number[]|Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array|Matrix} evaluated PDF
*/
function pdf( x, options ) {
	/* jshint newcap:false */
	var opts = {},
		ctor,
		err,
		out,
		dt,
		d;

	if ( arguments.length > 1 ) {
		err = validate( opts, options );
		if ( err ) {
			throw err;
		}
	}
	opts.v = typeof opts.v !== 'undefined' ? opts.v : 1;

	if ( isNumber( x ) ) {
		return pdf1( x, opts.v );
	}
	if ( isMatrixLike( x ) ) {
		if ( opts.copy !== false ) {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'pdf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			// Create an output matrix:
			d = new ctor( x.length );
			out = matrix( d, x.shape, dt );
		} else {
			out = x;
		}
		return pdf5( out, x, opts.v );
	}
	if ( isTypedArrayLike( x ) ) {
		if ( opts.copy === false ) {
			out = x;
		} else {
			dt = opts.dtype || 'float64';
			ctor = ctors( dt );
			if ( ctor === null ) {
				throw new Error( 'pdf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + dt + '`.' );
			}
			out = new ctor( x.length );
		}
		return pdf6( out, x, opts.v );
	}
	if ( isArrayLike( x ) ) {
		// Handle deepset first...
		if ( opts.path ) {
			opts.sep = opts.sep || '.';
			return pdf4( x, opts.v, opts.path, opts.sep );
		}
		// Handle regular and accessor arrays next...
		if ( opts.copy === false ) {
			out = x;
		}
		else if ( opts.dtype ) {
			ctor = ctors( opts.dtype );
			if ( ctor === null ) {
				throw new TypeError( 'pdf()::invalid option. Data type option does not have a corresponding array constructor. Option: `' + opts.dtype + '`.' );
			}
			out = new ctor( x.length );
		}
		else {
			out = new Array( x.length );
		}
		if ( opts.accessor ) {
			return pdf3( out, x, opts.v, opts.accessor );
		}
		return pdf2( out, x, opts.v );
	}
	return NaN;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./accessor.js":3301,"./array.js":3302,"./deepset.js":3303,"./matrix.js":3305,"./number.js":3306,"./typedarray.js":3308,"./validate.js":3309,"compute-array-constructors":3311,"dstructs-matrix":3323,"validate.io-array-like":3370,"validate.io-matrix-like":3375,"validate.io-number-primitive":3379,"validate.io-typed-array-like":3384}],3305:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// PDF //

/**
* FUNCTION: pdf( out, matrix, v )
*	Evaluates the probability density function (PDF) for a Student t distribution with degrees of freedom `v` for each matrix element.
*
* @param {Matrix} out - output matrix
* @param {Matrix} arr - input matrix
* @param {Number} v - degrees of freedom
* @returns {Matrix} output matrix
*/
function pdf( y, x, v ) {
	var len = x.length,
		fcn,
		i;
	if ( y.length !== len ) {
		throw new Error( 'pdf()::invalid input arguments. Input and output matrices must be the same length.' );
	}
	fcn = partial( v );
	for ( i = 0; i < len; i++ ) {
		y.data[ i ] = fcn( x.data[ i ] );
	}
	return y;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./partial.js":3307}],3306:[function(require,module,exports){
'use strict';

// MODULES //

var beta = require( 'compute-beta/lib/number-number.js' );


// FUNCTIONS //

var pow = Math.pow,
	sqrt = Math.sqrt;


// PDF //

/**
* FUNCTION: pdf( x, v )
*	Evaluates the probability density function (PDF) for a Student t distribution with degrees of freedom `v` at a value `x`.
*
* @param {Number} x - input value
* @param {Number} v - degrees of freedom
* @returns {Number} evaluated PDF
*/
function pdf( x, v ) {
	return pow( v / ( v + pow( x, 2 ) ), ( 1 + v ) / 2 ) / ( sqrt(v) * beta( v/2, 0.5 ) );
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"compute-beta/lib/number-number.js":3313}],3307:[function(require,module,exports){
'use strict';

// MODULES //

var beta = require( 'compute-beta/lib/number-number.js' );


// FUNCTIONS //

var pow = Math.pow,
	sqrt = Math.sqrt;


// PARTIAL //

/**
* FUNCTION: partial( v )
*	Partially applies degrees of freedom `v` and returns a function for evaluating the probability density function (PDF) for a Student t distribution.
*
* @param {Number} v - degrees of freedom
* @returns {Function} PDF
*/
function partial( v ) {
	var betaTerm =  sqrt(v) * beta( v/2, 0.5 ),
		exponent =  ( 1 + v ) / 2;
	/**
	* FUNCTION: pdf( x )
	*	Evaluates the probability density function (PDF) for a Student's t distribution.
	*
	* @private
	* @param {Number} x - input value
	* @returns {Number} evaluated PDF
	*/
	return function pdf( x ) {
		return pow( v / ( v + pow( x, 2 ) ), exponent ) / betaTerm;
	};
} // end FUNCTION partial()


// EXPORTS //

module.exports = partial;

},{"compute-beta/lib/number-number.js":3313}],3308:[function(require,module,exports){
'use strict';

// MODULES //

var partial = require( './partial.js' );


// PDF //

/**
* FUNCTION: pdf( out, arr, v )
*	Evaluates the probability density function (PDF) for a Student t distribution with degrees of freedom `v` for each array element.
*
* @param {Array|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} out - output array
* @param {Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} arr - input array
* @param {Number} v - degrees of freedom
* @returns {Number[]|Int8Array|Uint8Array|Uint8ClampedArray|Int16Array|Uint16Array|Int32Array|Uint32Array|Float32Array|Float64Array} output array
*/
function pdf( y, x, v ) {
	var len = x.length,
		fcn,
		i;

	fcn = partial ( v );
	for ( i = 0; i < len; i++ ) {
		y[ i ] = fcn( x[ i ] );
	}
	return y;
} // end FUNCTION pdf()


// EXPORTS //

module.exports = pdf;

},{"./partial.js":3307}],3309:[function(require,module,exports){
'use strict';

// MODULES //

var isObject = require( 'validate.io-object' ),
	isPositive = require( 'validate.io-positive-primitive' ),
	isBoolean = require( 'validate.io-boolean-primitive' ),
	isFunction = require( 'validate.io-function' ),
	isString = require( 'validate.io-string-primitive' );


// VALIDATE //

/**
* FUNCTION: validate( opts, options )
*	Validates function options.
*
* @param {Object} opts - destination for validated options
* @param {Object} options - function options
* @param {Number} [options.v] - degrees of freedom
* @param {Boolean} [options.copy] - boolean indicating if the function should return a new data structure
* @param {Function} [options.accessor] - accessor function for accessing array values
* @param {String} [options.sep] - deep get/set key path separator
* @param {String} [options.path] - deep get/set key path
* @param {String} [options.dtype] - output data type
* @returns {Null|Error} null or an error
*/
function validate( opts, options ) {
	if ( !isObject( options ) ) {
		return new TypeError( 'pdf()::invalid input argument. Options argument must be an object. Value: `' + options + '`.' );
	}
	if ( options.hasOwnProperty( 'v' ) ) {
		opts.v = options.v;
		if ( !isPositive( opts.v ) ) {
			return new TypeError( 'pdf()::invalid option. `v` parameter must be a positive number. Option: `' + opts.v + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'copy' ) ) {
		opts.copy = options.copy;
		if ( !isBoolean( opts.copy ) ) {
			return new TypeError( 'pdf()::invalid option. Copy option must be a boolean primitive. Option: `' + opts.copy + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'accessor' ) ) {
		opts.accessor = options.accessor;
		if ( !isFunction( opts.accessor ) ) {
			return new TypeError( 'pdf()::invalid option. Accessor must be a function. Option: `' + opts.accessor + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'path' ) ) {
		opts.path = options.path;
		if ( !isString( opts.path ) ) {
			return new TypeError( 'pdf()::invalid option. Key path option must be a string primitive. Option: `' + opts.path + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'sep' ) ) {
		opts.sep = options.sep;
		if ( !isString( opts.sep ) ) {
			return new TypeError( 'pdf()::invalid option. Separator option must be a string primitive. Option: `' + opts.sep + '`.' );
		}
	}
	if ( options.hasOwnProperty( 'dtype' ) ) {
		opts.dtype = options.dtype;
		if ( !isString( opts.dtype ) ) {
			return new TypeError( 'pdf()::invalid option. Data type option must be a string primitive. Option: `' + opts.dtype + '`.' );
		}
	}
	return null;
} // end FUNCTION validate()


// EXPORTS //

module.exports = validate;

},{"validate.io-boolean-primitive":3373,"validate.io-function":3374,"validate.io-object":3380,"validate.io-positive-primitive":3382,"validate.io-string-primitive":3383}],3310:[function(require,module,exports){
arguments[4][11][0].apply(exports,arguments)
},{"dup":11}],3311:[function(require,module,exports){
arguments[4][12][0].apply(exports,arguments)
},{"./ctors.js":3310,"dup":12}],3312:[function(require,module,exports){
'use strict';

function betaln() {
	return 1;
}


// EXPORTS //

module.exports = betaln;

},{}],3313:[function(require,module,exports){
'use strict';

// MODULES //

var gamma = require( 'gamma' ),
	betaln = require( './betaln.js' ); //require( 'compute-betaln/lib/number.js' );


// FUNCTIONS //

var EXP = Math.exp;


// VARIABLES //

var GAMMA_XMAX = 171.61447887182298;


// BETA //

/**
* FUNCTION: beta( x, y )
*	Evaluates the beta function.
*
* @param {Number} x - input value
* @param {Number} y - input value
* @returns {Number} evaluated beta function
*/
function beta( x, y  ) {
	if ( x !== x || y !== y || x < 0 || y < 0 ) {
		return NaN;
	}
	if ( x === 0 || y === 0 ) {
		return Number.POSITIVE_INFINITY;
	}
	if ( x + y > GAMMA_XMAX ) {
		return EXP( betaln( x, y ) );
	}
	return gamma( x ) * gamma( y ) / gamma( x + y );
} // end FUNCTION beta()


// EXPORTS //

module.exports = beta;

},{"./betaln.js":3312,"gamma":3314}],3314:[function(require,module,exports){
// transliterated from the python snippet here:
// http://en.wikipedia.org/wiki/Lanczos_approximation

var g = 7;
var p = [
    0.99999999999980993,
    676.5203681218851,
    -1259.1392167224028,
    771.32342877765313,
    -176.61502916214059,
    12.507343278686905,
    -0.13857109526572012,
    9.9843695780195716e-6,
    1.5056327351493116e-7
];

var g_ln = 607/128;
var p_ln = [
    0.99999999999999709182,
    57.156235665862923517,
    -59.597960355475491248,
    14.136097974741747174,
    -0.49191381609762019978,
    0.33994649984811888699e-4,
    0.46523628927048575665e-4,
    -0.98374475304879564677e-4,
    0.15808870322491248884e-3,
    -0.21026444172410488319e-3,
    0.21743961811521264320e-3,
    -0.16431810653676389022e-3,
    0.84418223983852743293e-4,
    -0.26190838401581408670e-4,
    0.36899182659531622704e-5
];

// Spouge approximation (suitable for large arguments)
function lngamma(z) {

    if(z < 0) return Number('0/0');
    var x = p_ln[0];
    for(var i = p_ln.length - 1; i > 0; --i) x += p_ln[i] / (z + i);
    var t = z + g_ln + 0.5;
    return .5*Math.log(2*Math.PI)+(z+.5)*Math.log(t)-t+Math.log(x)-Math.log(z);
}

module.exports = function gamma (z) {
    if (z < 0.5) {
        return Math.PI / (Math.sin(Math.PI * z) * gamma(1 - z));
    }
    else if(z > 100) return Math.exp(lngamma(z));
    else {
        z -= 1;
        var x = p[0];
        for (var i = 1; i < g + 2; i++) {
            x += p[i] / (z + i);
        }
        var t = z + g + 0.5;

        return Math.sqrt(2 * Math.PI)
            * Math.pow(t, z + 0.5)
            * Math.exp(-t)
            * x
        ;
    }
};

module.exports.log = lngamma;

},{}],3315:[function(require,module,exports){
arguments[4][13][0].apply(exports,arguments)
},{"dup":13}],3316:[function(require,module,exports){
arguments[4][14][0].apply(exports,arguments)
},{"./get.js":3319,"./iget.js":3321,"./iset.js":3324,"./mget.js":3328,"./mset.js":3330,"./set.js":3338,"./sget.js":3340,"./sset.js":3342,"./toString.js":3344,"dup":14}],3317:[function(require,module,exports){
arguments[4][15][0].apply(exports,arguments)
},{"./get.raw.js":3320,"./iget.raw.js":3322,"./iset.raw.js":3325,"./mget.raw.js":3329,"./mset.raw.js":3331,"./set.raw.js":3339,"./sget.raw.js":3341,"./sset.raw.js":3343,"./toString.js":3344,"dup":15}],3318:[function(require,module,exports){
arguments[4][16][0].apply(exports,arguments)
},{"dup":16}],3319:[function(require,module,exports){
arguments[4][17][0].apply(exports,arguments)
},{"dup":17,"validate.io-nonnegative-integer":3376}],3320:[function(require,module,exports){
arguments[4][18][0].apply(exports,arguments)
},{"dup":18}],3321:[function(require,module,exports){
arguments[4][19][0].apply(exports,arguments)
},{"dup":19,"validate.io-integer-primitive":3356}],3322:[function(require,module,exports){
arguments[4][20][0].apply(exports,arguments)
},{"dup":20}],3323:[function(require,module,exports){
arguments[4][21][0].apply(exports,arguments)
},{"./matrix.js":3326,"./matrix.raw.js":3327,"dup":21}],3324:[function(require,module,exports){
arguments[4][22][0].apply(exports,arguments)
},{"dup":22,"validate.io-integer-primitive":3356,"validate.io-number-primitive":3379}],3325:[function(require,module,exports){
arguments[4][23][0].apply(exports,arguments)
},{"dup":23}],3326:[function(require,module,exports){
arguments[4][24][0].apply(exports,arguments)
},{"./btypes.js":3315,"./ctor.js":3316,"./dtypes.js":3318,"compute-cast-arrays":3345,"compute-dtype":3348,"dup":24,"validate.io-array":3353,"validate.io-contains":3354,"validate.io-nonnegative-integer-array":3357,"validate.io-string-primitive":3383}],3327:[function(require,module,exports){
arguments[4][25][0].apply(exports,arguments)
},{"./btypes.js":3315,"./ctor.raw.js":3317,"./dtypes.js":3318,"compute-dtype":3348,"dup":25,"validate.io-contains":3354,"validate.io-string-primitive":3383}],3328:[function(require,module,exports){
arguments[4][26][0].apply(exports,arguments)
},{"./btypes.js":3315,"dup":26,"validate.io-nonnegative-integer-array":3357}],3329:[function(require,module,exports){
arguments[4][27][0].apply(exports,arguments)
},{"./btypes.js":3315,"dup":27}],3330:[function(require,module,exports){
arguments[4][28][0].apply(exports,arguments)
},{"./mset1.js":3332,"./mset2.js":3333,"./mset3.js":3334,"./mset4.js":3335,"./mset5.js":3336,"./mset6.js":3337,"dup":28,"validate.io-function":3374,"validate.io-nonnegative-integer-array":3357,"validate.io-number-primitive":3379}],3331:[function(require,module,exports){
arguments[4][29][0].apply(exports,arguments)
},{"./mset1.js":3332,"./mset2.js":3333,"./mset3.js":3334,"./mset4.js":3335,"./mset5.js":3336,"./mset6.js":3337,"dup":29}],3332:[function(require,module,exports){
arguments[4][30][0].apply(exports,arguments)
},{"dup":30}],3333:[function(require,module,exports){
arguments[4][31][0].apply(exports,arguments)
},{"dup":31}],3334:[function(require,module,exports){
arguments[4][32][0].apply(exports,arguments)
},{"dup":32}],3335:[function(require,module,exports){
arguments[4][33][0].apply(exports,arguments)
},{"dup":33}],3336:[function(require,module,exports){
arguments[4][34][0].apply(exports,arguments)
},{"dup":34}],3337:[function(require,module,exports){
arguments[4][35][0].apply(exports,arguments)
},{"dup":35}],3338:[function(require,module,exports){
arguments[4][36][0].apply(exports,arguments)
},{"dup":36,"validate.io-nonnegative-integer":3376,"validate.io-number-primitive":3379}],3339:[function(require,module,exports){
arguments[4][37][0].apply(exports,arguments)
},{"dup":37}],3340:[function(require,module,exports){
arguments[4][38][0].apply(exports,arguments)
},{"./btypes.js":3315,"compute-indexspace":3352,"dup":38,"validate.io-string-primitive":3383}],3341:[function(require,module,exports){
arguments[4][39][0].apply(exports,arguments)
},{"./btypes.js":3315,"compute-indexspace":3352,"dup":39}],3342:[function(require,module,exports){
arguments[4][40][0].apply(exports,arguments)
},{"compute-indexspace":3352,"dup":40,"validate.io-function":3374,"validate.io-number-primitive":3379,"validate.io-string-primitive":3383}],3343:[function(require,module,exports){
arguments[4][41][0].apply(exports,arguments)
},{"compute-indexspace":3352,"dup":41}],3344:[function(require,module,exports){
arguments[4][42][0].apply(exports,arguments)
},{"dup":42}],3345:[function(require,module,exports){
arguments[4][43][0].apply(exports,arguments)
},{"compute-array-constructors/lib/ctors":3310,"compute-array-dtype/lib/dtypes":3346,"dup":43,"type-name":3347,"validate.io-array-like":3370}],3346:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],3347:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],3348:[function(require,module,exports){
arguments[4][46][0].apply(exports,arguments)
},{"compute-array-dtype":3350,"dup":46,"type-name":3351}],3349:[function(require,module,exports){
arguments[4][44][0].apply(exports,arguments)
},{"dup":44}],3350:[function(require,module,exports){
arguments[4][48][0].apply(exports,arguments)
},{"./dtypes.js":3349,"dup":48}],3351:[function(require,module,exports){
arguments[4][45][0].apply(exports,arguments)
},{"dup":45}],3352:[function(require,module,exports){
arguments[4][534][0].apply(exports,arguments)
},{"dup":534,"validate.io-nonnegative-integer":3376,"validate.io-string-primitive":3383}],3353:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],3354:[function(require,module,exports){
arguments[4][51][0].apply(exports,arguments)
},{"dup":51,"validate.io-array":3353,"validate.io-nan-primitive":3355}],3355:[function(require,module,exports){
arguments[4][52][0].apply(exports,arguments)
},{"dup":52}],3356:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":3379}],3357:[function(require,module,exports){
arguments[4][54][0].apply(exports,arguments)
},{"dup":54,"validate.io-array":3353,"validate.io-nonnegative-integer":3376}],3358:[function(require,module,exports){
arguments[4][58][0].apply(exports,arguments)
},{"dup":58}],3359:[function(require,module,exports){
arguments[4][59][0].apply(exports,arguments)
},{"dup":59}],3360:[function(require,module,exports){
arguments[4][60][0].apply(exports,arguments)
},{"./deepget.js":3358,"./defaults.js":3359,"./validate.js":3362,"dup":60,"validate.io-array":3363,"validate.io-string-primitive":3383}],3361:[function(require,module,exports){
arguments[4][61][0].apply(exports,arguments)
},{"./deepget.js":3358,"./defaults.js":3359,"./factory.js":3360,"./validate.js":3362,"dup":61,"validate.io-array":3363,"validate.io-string-primitive":3383}],3362:[function(require,module,exports){
arguments[4][62][0].apply(exports,arguments)
},{"dup":62,"validate.io-object":3380,"validate.io-string-primitive":3383}],3363:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],3364:[function(require,module,exports){
arguments[4][64][0].apply(exports,arguments)
},{"dup":64}],3365:[function(require,module,exports){
arguments[4][65][0].apply(exports,arguments)
},{"dup":65}],3366:[function(require,module,exports){
arguments[4][66][0].apply(exports,arguments)
},{"./deepset.js":3364,"./defaults.js":3365,"./validate.js":3368,"dup":66,"validate.io-array":3369,"validate.io-string-primitive":3383}],3367:[function(require,module,exports){
arguments[4][67][0].apply(exports,arguments)
},{"./deepset.js":3364,"./defaults.js":3365,"./factory.js":3366,"./validate.js":3368,"dup":67,"validate.io-array":3369,"validate.io-string-primitive":3383}],3368:[function(require,module,exports){
arguments[4][68][0].apply(exports,arguments)
},{"dup":68,"validate.io-boolean-primitive":3373,"validate.io-object":3380,"validate.io-string-primitive":3383}],3369:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],3370:[function(require,module,exports){
arguments[4][70][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":3371,"dup":70,"validate.io-integer-primitive":3372}],3371:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],3372:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":3379}],3373:[function(require,module,exports){
arguments[4][73][0].apply(exports,arguments)
},{"dup":73}],3374:[function(require,module,exports){
arguments[4][74][0].apply(exports,arguments)
},{"dup":74}],3375:[function(require,module,exports){
arguments[4][75][0].apply(exports,arguments)
},{"dup":75}],3376:[function(require,module,exports){
arguments[4][55][0].apply(exports,arguments)
},{"dup":55,"validate.io-integer":3377}],3377:[function(require,module,exports){
arguments[4][56][0].apply(exports,arguments)
},{"dup":56,"validate.io-number":3378}],3378:[function(require,module,exports){
arguments[4][57][0].apply(exports,arguments)
},{"dup":57}],3379:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}],3380:[function(require,module,exports){
arguments[4][78][0].apply(exports,arguments)
},{"dup":78,"validate.io-array":3381}],3381:[function(require,module,exports){
arguments[4][50][0].apply(exports,arguments)
},{"dup":50}],3382:[function(require,module,exports){
arguments[4][1221][0].apply(exports,arguments)
},{"dup":1221,"validate.io-number-primitive":3379}],3383:[function(require,module,exports){
arguments[4][80][0].apply(exports,arguments)
},{"dup":80}],3384:[function(require,module,exports){
arguments[4][81][0].apply(exports,arguments)
},{"compute-const-max-safe-integer":3385,"dup":81,"validate.io-integer-primitive":3386}],3385:[function(require,module,exports){
arguments[4][71][0].apply(exports,arguments)
},{"dup":71}],3386:[function(require,module,exports){
arguments[4][53][0].apply(exports,arguments)
},{"dup":53,"validate.io-number-primitive":3379}],3387:[function(require,module,exports){
(function (global){
global.distributions = require( 'distributions.io' );
global.compute = require( 'compute.io' );

}).call(this,typeof global !== "undefined" ? global : typeof self !== "undefined" ? self : typeof window !== "undefined" ? window : {})
},{"compute.io":2,"distributions.io":1142}],3388:[function(require,module,exports){
arguments[4][77][0].apply(exports,arguments)
},{"dup":77}]},{},[3387]);
